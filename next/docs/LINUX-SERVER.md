# Máy chủ Linux `D:\ServerLinux` — mổ nhị phân (M10)

Tài liệu này là kết quả **mổ nhị phân** bản server Linux (JX2/Kiếm Thế bản VNG) mà chủ dự án giao,
để lấy **toàn bộ hệ thống settings và bộ hàm script** cho JX NEXT làm được như bản Linux. Mọi con
số ở đây đọc thẳng từ tệp nhị phân bằng bộ công cụ `next/tools/re/` (§2), không đoán; chỗ nào có
thể là suy luận đều ghi rõ "suy luận" và ghi cách kiểm.

> Đây là **bản đồ để làm M11–M13** (vật phẩm, chiến đấu/kỹ năng, nhiệm vụ + bộ hàm script):
> mỗi hàm script có chữ ký (§5), mỗi tệp settings có danh sách cột/khoá mà mã đọc (§4, §6).

## 1. Ba tệp thực thi

| Tệp | Vai trò | Kích thước | Ghi chú |
|---|---|---:|---|
| `server1/jx_linux_y` | **GameServer** — mô phỏng thế giới, chạy script | 8,9 MB | ELF 32-bit i386, **không có section header** |
| `gateway/s3relay/s3relay_y` | Relay/gateway | | ELF 32-bit i386 |
| `server1/libheaven.so`, `librainbow.so` | Thư viện engine (KGLua, mạng) | 183 KB, 84 KB | **có symbol**, dễ đối chiếu |
| `server1/KG_Angel.so`, `GameExtConnect.so`, `GameExtContent.so` | Chống sửa / mở rộng | 4 MB | |

`jx_linux_y` **không dùng UPX**. Nhưng có **một segment `rwx` 5,7 MB entropy ~8.0** (mã hoá, giải
mã lúc chạy — lớp bảo vệ của `KG_Angel`), và `.fini` trỏ vào đó. **Code thật của game, các bảng đăng
ký Lua và mọi chuỗi nằm trong segment `r-x` đầu (`0x08048000`–`0x082DA338`), ở dạng rõ** — đó là
chỗ mổ. Lưu ý: `.rodata` **nằm chung segment với `.text`**, nên "một địa chỉ trong segment code" chưa
chắc là code; chuỗi là "cái gì đọc được như chuỗi". Bảng chương trình:

```
LOAD va 08048000..082DA338 file 0+292338 r-x   <- code + .rodata (chuỗi, bảng Lua) — RÕ
LOAD va 082DB000..097B5ED4                rw-   <- .got (082DB45C..) + dữ liệu ghi
LOAD va 09B6C190..0A0E720E                rwx   <- 5,7 MB entropy 8.0 (lớp bảo vệ, bỏ qua)
entry 0804B7C0 -> __libc_start_main(main=0x0804B880)
```

Cần: `libdl libuuid libpthread libstdc++ libm libgcc_s libc`. Đoạn `DYNAMIC` đã bị lớp bảo vệ **viết
lại** (SYMTAB/STRTAB/JMPREL trỏ vào segment `rwx`): 312 symbol, 248 stub PLT của lớp bảo vệ ở
`0x09B6C290`. Nhưng **`.rel.plt` gốc vẫn còn ở `0x0804A654` (183 mục `R_386_JMP_SLOT`)** và chỉ số
symbol trong đó **vẫn khớp** bảng symbol hiện tại — kiểm bằng 5 stub đã biết chức năng từ thân hàm
gọi chúng (`strtol` sau `GetKeyValue`, `sprintf`, `strncpy`, `strtod` trước `fstp`, `__cxa_atexit` ngay
sau constructor của đối tượng toàn cục): cả 5 đúng. Vậy **183 stub PLT gốc `0x0804Axxx` đều có tên**
(`re_elf.py imports`, tổng 431 stub) và `dis` chú thích `call 0x804b30c ; strtol@plt`.

## 2. Bộ công cụ (`next/tools/re/`)

| Công cụ | Việc | Lệnh |
|---|---|---|
| `re_elf.py` | Đọc ELF không section header qua program header + DYNAMIC (như loader); segment, import (cả PLT gốc), chuỗi GBK, xref, disassemble có chú thích, bảng đăng ký Lua | `info`, `imports`, `strings <regex>`, `xrefstr <text>`, `dis <va> [n]`, `func <va>`, `luamap` |
| `re_calls.py` | **Đồ thị gọi hàm** theo từng hàm (không quét tuyến tính — quét tuyến tính lạc nhịp ở dữ liệu giữa các hàm). Theo dõi hằng trong thanh ghi/ô stack/`[esp+N]` để biết **mỗi lời gọi truyền chuỗi, đối tượng nào**; hiểu `this` = đối số 1, thành viên `this+off`, đối tượng trên stack, tail-call (`jmp` tới prologue), khối sau `ret` sớm (trạng thái lấy từ nhánh nhảy tới đó), thanh ghi cất/khôi phục qua `[ebp-N]`. Cache `<elf>.calls.json` (~10 s, 6027 hàm / 35 074 lời gọi) | `build`, `callers <va>`, `strargs <va>`, `byarg <text>` |
| `re_luasig.py` | **Chữ ký** từng hàm script (§5): đối số đọc bằng API Lua 4.0 nào, có xem `lua_gettop` không, có cần nhân vật không, đẩy gì, trả mấy giá trị; theo cả hàm bọc (tail-jump và gọi thường với `L`), cả khối nằm sau epilogue | `callees`, `sig <tên>`, `all <out.tsv>` |
| `re_tables.py` | **Bảng settings ↔ cột/khoá** (§6): nối `KTabFile::Load(obj, path)` với mọi `Get*(obj, row, "Cột")` qua đồ thị gọi hàm, kể cả bảng truyền qua đối số, đường dẫn ghép lúc chạy, đọc theo chỉ số cột, ghi/`Save` | `objects`, `columns`, `report <out.md>` |
| `re_settings.py` | Bản thô hơn: hàm nào nhắc tới đường dẫn + các hằng chuỗi hàm đó dùng (giữ để tra nhanh) | `list`, `file <text>`, `all` |
| `re_attribmod.py <elf>` | bản đồ `KNpcAttribModify` (§10): mỗi id ma pháp → ô `KNpc` mà `ProcessFunc` của nó cộng/ghi, dừng ở tail-jump cuối hàm → `docs/linux/jx_linux_attribmod.tsv` |
| `re_tabdesc.py <elf> <reader-va>…` | mảng mô tả cột `{kiểu, đích, mặc định}` của một `KBPT_*::ReadRow` (bộ đọc chung `0x081ECF40`) |
| `re_upx.py <exe>` | mở `gamecl.exe` (UPX 3.03) của client 2.0 thành ảnh bộ nhớ cho các công cụ này ([CLIENT-2.0.md](CLIENT-2.0.md)) |
| `re_scan.py` | Ba phép quét mọi hàm (§9): **ai đọc/ghi thành viên ở offset** (`disp 1f8`), **hàm nào đổ bảng con trỏ hàm thành viên** (`pmf` — tìm ctor của `KNpcAttribModify`, `KProtocolProcess`), **lệnh có hình dạng** (`ins <regex>`) | `disp <hex>[,…] [mnemonic]`, `pmf [min]`, `ins <regex> [max]` |

Chạy: `python re_<tool>.py D:/ServerLinux/server1/jx_linux_y <lệnh>` (cần `pip install capstone`).

## 3. Bộ hàm script — 1506 (game) + 438 (gateway)

`jx_linux_y` đăng ký **1506 hàm** cho Lua, `s3relay_y` **438 hàm**. Cách tìm: quét mọi cặp dword
liên tiếp `{con trỏ tên C hợp lệ, con trỏ vào .text}` thành dãy ≥ 3 cặp (đó là bảng đăng ký), rồi
**lọc theo prologue**: hàm thật bắt đầu bằng `55` (`push ebp`) — bỏ hết cặp mà "con trỏ hàm" thực ra
trỏ vào chuỗi (đó là nhiễu). Đối chiếu với bản đồ của chủ dự án (`ReverseTools/*.luamap.full.txt`):
trùng ~1550 tên, phần lệch hai bên đều là từ khoá Lua / rác — bộ lọc prologue của ta sạch hơn.

- Danh sách đầy đủ + địa chỉ: [`linux/jx_linux_luaapi.txt`](linux/jx_linux_luaapi.txt) (1506),
  [`linux/s3relay_luaapi.txt`](linux/s3relay_luaapi.txt) (438).
- Nhóm theo miền: [`linux/jx_linux_luaapi_nhom.txt`](linux/jx_linux_luaapi_nhom.txt).
- **Chữ ký từng hàm**: [`linux/jx_linux_luasig.tsv`](linux/jx_linux_luasig.tsv) (§5).

Số hàm theo miền (tên có chứa từ khoá):

| Miền | Số hàm | | Miền | Số hàm |
|---|---:|---|---|---:|
| Bang hội / công thành (`Tong*`) | 194 | | PK / sát khí | 10 |
| Vật phẩm, túi đồ, trang bị | 123 | | Tổ đội | 10 |
| NPC | 84 | | Bang xếp hạng, thẻ thần thú | 4 |
| Nhiệm vụ | 61 | | Danh hiệu | 9 |
| Cấp / kinh nghiệm | 42 | | Trận doanh / phe | 9 |
| Kỹ năng + chiêu thức | 53 | | Xe / thuyền / bến | 7 |
| Nhân vật người chơi | 36 | | Chiến trường | 6 |
| Thú cưng / đồng hành | 35 | | Tiền tệ | 4 |
| Bản đồ / cảnh / vùng | 25 | | Thành tựu | 4 |
| Giao dịch / chợ / đấu giá | 15 | | **Khác** (chưa phân loại) | 775 |

**JX NEXT hiện đăng ký 3 hàm** (`Include`, `print`, `IncludeLib` trong `server/zone/src/KLuaScript.cpp`)
cộng với các hàm mà npclevelscript cần. Khoảng cách tới 1506 chính là phần việc M11–M13.

## 4. Hệ thống settings — 109 tệp, 736 cột/khoá đã nối

`jx_linux_y` nhắc tới **101 đường dẫn `\settings\`** (`re_settings.py list`; thêm vài đường dẫn viết `settings/`
và 62 đường dẫn `\script\`) —
[`linux/jx_settings_files.txt`](linux/jx_settings_files.txt). Đi từ mã ra (mọi `KTabFile::Load` /
`KIniFile::Load`, §6) thấy **109 tệp được nạp thật** (thêm `servercfg.ini`, `gamesetting.ini` qua
hàm bọc, các tệp `\lang\%s\...` ghép lúc chạy) và **104 tệp nối được với cột/khoá mã đọc**:
[`linux/jx_settings_cot.md`](linux/jx_settings_cot.md) — mỗi tệp: đối tượng bảng, hàm nạp, từng
cột/khoá và hàm đọc nó. Vài tệp cột mốc cho M11–M13:

| Tệp | Cột/khoá đọc | Cho hệ |
|---|---:|---|
| `settings\Skills.txt` (đối tượng `0x0830AE00`, đọc ở `0x080E9200`) | 60 | Kỹ năng (M12) |
| `settings\NpcS.txt` (`0x0830AEC0`, đọc ở `0x080A2160`/`0x080A4410`) | 94 | NPC (M12) |
| `settings\gamesetting.ini` (`0x0830CF5C`, đọc ở `0x08061650`) | 116 | Hằng số toàn cục |
| `settings\item\AbradeRate.ini` | 75 | Hao mòn trang bị (M11) |
| `settings\item\{autohang,zijingao}droprate.ini` | 27 + 27 | Tỷ lệ rơi (M11) |
| `settings\tong\tong_setting.ini`, `citywar.ini` | 30, 15 | Bang hội, công thành |
| `settings\partner\*` (11 tệp) | 95 | Đồng hành |
| `settings\logset.ini` | 19 | Ghi log |
| `settings\npc\player\{level_exp,level_add,level_lead_exp}.txt` | theo chỉ số cột | Kinh nghiệm theo cấp |
| `settings\item\*Res.txt`, `goods.txt`, `buysell.txt`, `StationPrice.txt`… | theo chỉ số cột | Vật phẩm, xe |

"Theo chỉ số cột" = mã gọi `GetInteger(nRow, nColumn, …)`: tệp đọc từng cột theo **số thứ tự**, tên
cột không có trong mã — lấy từ dòng tiêu đề của chính tệp trong `D:\ServerLinux\server1\settings\`.
Còn **5 tệp** chưa nối được gì (`HorseRes.txt`, `MeleeRes.txt`, `lottery.txt`, `npcres\人物类型.txt`,
`ui\ui3\摆摊广告条.ini`: đọc qua con trỏ chưa theo dõi được) và **6 lượt đọc** ở 5 hàm chưa nối tên tệp
(liệt kê cuối báo cáo). Kiểm chứng đã làm: `logset.ini` thật chỉ có `[LogSet]` — báo cáo cũng chỉ
gán `[LogSet]` cho nó, còn `[ENCHASER]`, `[Coin]`, `[DiceGame]`… về đúng `gamesetting.ini` (tệp thật
có đúng các section ấy).

## 5. Quy ước gọi hàm script (đã kiểm — sửa lại bản trước)

> Bản trước của mục này viết "các hàm không nhận `lua_State*`, nhận đối số nguyên trực tiếp" — **sai**.
> Đọc kỹ thân hàm và các hàm chúng gọi thì đúng là API Lua 4.0 chuẩn, giống `Script.cpp` của JX1.

Mỗi hàm là `int f(lua_State* L)`; `L` ở `[ebp+8]`. Lua 4.0 **liên kết tĩnh** trong `jx_linux_y`
(`lua_State.top` +0, `Cbase` +0x10; `TObject` 12 byte; `ttype` 1 nil, 2 số, 3 chuỗi, 4 bảng, 7 userdata):

| Địa chỉ | Hàm | Cách nhận ra |
|---|---|---|
| `0x8232490` | `lua_gettop` | `(top − Cbase) / 12` |
| `0x82338B0` / `0x82339B0` | `lua_tonumber` / bản trả `int` (`fistp`) | `o = Cbase + (i−1)·12; ttype==2 → value.n` |
| `0x8233850` | `lua_tostring` | |
| `0x8232590` / `0x8232690` | `lua_type` / `lua_touserdata` | |
| `0x8232D40` / `0x82337A0` / `0x8233730` | `lua_pushnumber` / `lua_pushstring` / `lua_pushlstring` | `top->ttype = 2; top += 12` |
| `0x8232E70` / `0x8232BE0` / `0x8232F20` | `lua_pushnil` / `lua_newtable` / `lua_pushvalue` | |
| `0x8233C10` / `0x8233360` / `0x8233620` | `lua_settop` / `lua_rawseti` / `lua_getglobal` | |
| `0x8245AF0` `0x8245BB0` `0x8245C90` `0x8245CE0` `0x8245B60` | KGLua: `check_number`, `check_string`, `check_type`, `has_arg`, `opt_number` | bọc các hàm trên |
| `0x8107860` (và `0x8107910` nhảy tới) | **`GetPlayerIndex(L)`** | `lua_getglobal(L,"PlayerIndex")`; nil → −1; else `lua_tonumber` |
| `0x8106A40` | `GetSubWorldIndex(L)` | global `"SubWorld"` |

Ví dụ `GetLevel @ 0x081111E0` (đọc bằng `re_elf.py dis`):

```
mov  ebx, [ebp+8]            ; L
mov  [esp], ebx ; call 0x8107860        ; GetPlayerIndex(L): global "PlayerIndex" -> i
lea  edx,[eax-1] ; cmp edx,0x4ae ; ja lỗi   ; 1 <= i <= 0x4AE (MAX_PLAYER)
imul eax, eax, 0x8788        ; sizeof(KPlayer) = 0x8788
mov  edx, [0x8baee60]        ; g_pPlayer
mov  eax, [eax+edx+0x3f4]    ; KPlayer::m_nIndex (chỉ số Npc)
imul eax, eax, 0x1a4c        ; sizeof(KNpc) = 0x1A4C
mov  edx, [0x836eae0]        ; Npc[]
fild dword [eax+edx+0x20]    ; KNpc::m_Level
fstp qword [esp+4] ; mov [esp], ebx ; call 0x8232d40   ; lua_pushnumber(L, level)
mov  eax, 1 ; ret            ; trả 1 giá trị
```

Nghĩa cho JX NEXT: đăng ký **cùng tên, cùng đối số Lua** (JX NEXT dùng Lua 5.4 nhưng ngữ nghĩa
`PlayerIndex`/`SubWorld` là global do engine đặt trước khi chạy script — giữ nguyên). Chữ ký của cả
1506 hàm ở [`linux/jx_linux_luasig.tsv`](linux/jx_linux_luasig.tsv) (cột: tên, địa chỉ, `args`
"i:kiểu" đọc ở chỉ số i, `gettop` = có xem số đối số (đối số tuỳ chọn), `player` = có gọi
`GetPlayerIndex`, `pushes`, `returns`, kích thước). Thống kê: **1149** hàm đọc đối số ở chỉ số cố định,
894 xem `lua_gettop` (đối số tuỳ chọn hoặc nhiều dạng), 711 cần nhân vật, 1496/1506 biết số giá trị
trả về. Ví dụ đọc ra: `SetPos(x, y)` (+tuỳ chọn), `GetPos() → 3` (map, x, y), `GetTask(id) → số|nil`,
`SetTask(id, giá trị)`, `Msg2Player(chuỗi)`. Tệp `dac_ta_17_ham_hoatdong_phuong.json` của chủ dự án
(`D:\GAMEDEVNEW\ReverseTools`) đặc tả sẵn 17 hàm — dùng đối chiếu.

## 6. Hai lớp đọc tệp: `KTabFile` và `KIniFile` (tên theo header cũ, địa chỉ đã kiểm từng thân hàm)

Header gốc: `SwordOnline/Sources/Engine/Src/KTabFile.h`, `KITabFile.h`, `KIniFile.h`. `KTabFile` kích
thước **0x20** (`vtable`, `m_Width` +4, `m_Height` +8, `m_Memory` +0xC, `m_OffsetTable` +0x14) — khớp
`lea eax,[ebx+0x20]` khi một lớp chứa 5 bảng liền nhau (`MeleeRes` +0, `RangeRes` +0x20, `ArmorRes`
+0x40, `HelmRes` +0x60, `HorseRes` +0x80 ở `0x08068D90`). Thứ tự vtable theo `KITabFile`: `+0x10 FindRow`,
`+0x14 FindColumn`, `+0x18 GetWidth`, `+0x1C GetHeight`, `+0x20…` các `Get*`.

| Địa chỉ | `KTabFile::` | Đối số (`[esp]` = this) | Cách nhận ra |
|---|---|---|---|
| `0x082284E0` | `Load(path)` | `[esp+4]` đường dẫn | mọi đường dẫn `.txt` đi qua đây |
| `0x08228170` | `GetInteger(nRow, szColumn, nDefault, int*, bColumnLab)` | cột `[esp+8]` | `FindColumn` → `GetValue` → `strtol` |
| `0x082280C0` | `GetFloat(nRow, szColumn, …)` | cột `[esp+8]` | … → `strtod` → `fstp dword` |
| `0x08228220` | `GetString(nRow, szColumn, lpDefault, buf, size, bColumnLab)` | cột `[esp+8]`, mặc định `[esp+12]` | |
| `0x08227E90` | `GetInteger(szRow, szColumn, …)` | dòng theo **tên** `[esp+4]` | gọi vtable `+0x10 FindRow`, `+0x14 FindColumn` |
| `0x08227FA0` | `GetString(szRow, szColumn, …)` | | |
| `0x08227E10` | `GetInteger(nRow, nColumn, …)` | **theo chỉ số**, không tên cột | `GetValue(row−1, col−1)` thẳng |
| `0x08227F40` | `GetString(nRow, nColumn, …)` | theo chỉ số | |
| `0x08227AD0` | `FindColumn(szColumn)` | | quét dòng 0 (tiêu đề), `strcmp`, trả `i+1` |
| `0x08227B50` | `FindRow(szRow, nStartRow)` | | quét cột 0 từ dòng `nStartRow` |
| `0x08227A00` | `GetValue(nRow, nColumn, buf, size)` (private) | | |
| `0x08228030` | `Str2Col("AB")` | | cột theo **chữ cái** khi `bColumnLab = 0` |
| `0x082279D0` / `0x08228630` / `0x082285A0` | `Clear()` / constructor / destructor | | ctor đặt vtable rồi `KMemClass()`, sau đó `__cxa_atexit` |

| Địa chỉ | `KIniFile::` | Đối số | Cách nhận ra |
|---|---|---|---|
| `0x08220140` | `Load(path)` | `[esp+4]` | |
| `0x0821F2C0` | `GetInteger(section, key, nDefault, int*)` | `[esp+4]`, `[esp+8]` | `GetKeyValue` (0x0821ED00) → `strtol` |
| `0x0821F340` | `GetString(section, key, default, buf, size)` | | |
| `0x0821F1F0` | `GetInteger2(section, key, int*, int*)` | | tách bằng `,` |
| `0x0821EF40` | đọc nhiều giá trị `x,y,…` (`GetRect`/`GetStruct`) | | |
| `0x0821F7C0` / `0x0821F810` | `WriteInteger` / `WriteString` | | `sprintf("%d")` → `SetKeyValue` (0x0821F3A0) — **ghi**, không đọc |
| `0x0821FE80` | `Save(path)` | | ghi từng dòng `"%s\r\n"` |
| `0x08220280` / `0x08220240` | constructor / destructor | | |

Tệp được **ghi** (không chỉ đọc): `AntiBotConfig.ini` (đọc 11, ghi 7 khoá), `rolenamechangehis.ini`
(ghi 4), `SysAlarmConfig.ini` (3/3), `ServerCfg.ini` (ghi 1) — xem báo cáo.

## 7. Ba cây nguồn — đừng mở nhầm (theo `HUONGDAN_DICHNGUOC_TINHNANG_LINUX.md` của chủ dự án)

`D:\ServerLinux` có **ba** gốc con: `server1` (kịch bản + bảng), `gateway` (lịch chạy ở
`relaysetting`), `Patch\settings` (34 bảng `server1` không có). Tệp Linux **trộn hai bảng mã trong
một dòng** (tiếng Việt VNI/TCVN + tiếng Trung GBK); dùng `ReverseTools\port_3hd\dec2.py::decline2`
để giải, `gbktool`/`iconv` giải sai.

## 8. Bước kế tiếp

M10 xong: **liệt kê + phân loại + định vị + chữ ký + cột**. M11–M13 làm từng hệ: lấy các cột của hệ
từ `jx_settings_cot.md`, chữ ký các hàm Lua của hệ từ `jx_linux_luasig.tsv`, hiện thực trong zone mới
với **cùng tên, cùng số**, đối chiếu kết quả với server Linux đang chạy. Khi cần đọc sâu một hàm:
`re_elf.py func <va>` (đã có tên PLT), `re_calls.py callers <va>` để biết ai gọi.

## 9. Đã tìm thấy khi làm M11 (vật phẩm) — địa chỉ đã kiểm từng thân hàm

Cách tìm khi không có tên hàm: (1) hàm đổ **bảng con trỏ hàm thành viên** lộ ra bằng `re_scan.py pmf`
(nhiều `mov [reg+disp], imm(mã)` liên tiếp; phần tử 8 byte {ptr, adj}); (2) từ một hàm đã biết, lần
theo `re_calls.py callers` và theo **offset thành viên** bằng `re_scan.py disp`; (3) chuỗi RTTI
`NKBPT_…` → typeinfo → vtable (4 ô: dtor, đọc dòng, tìm dòng, nạp) cho các lớp bảng vật phẩm.

| Địa chỉ | Là gì | Bằng chứng |
|---|---|---|
| `0x08099600` | ctor `KNpcAttribModify` (217 ô `ProcessFunc[]`, đối tượng toàn cục `0x08BAC120`, bảng từ +4, phần tử 8 byte: chỉ số = (disp − 4) / 8) | `pmf`; static init `0x08099E90` |
| `0x08095B40` | `KNpcAttribModify::ModifyAttrib(this, nIdx, pNpc, pMagic, extra)` — `type ≤ 0x12d`, gọi `ProcessFunc[type]` | đọc `[ebx + type*8 + 4]` rồi `call ecx` |
| `0x0807D210` | `KNpc::ModifyAttrib(this, nIdx, pData, extra)` → hàm trên | 29 nơi gọi |
| `0x08097E70` | `ProcessFunc[153]` = **LifePotionV**: `time = max(t1,t2)`, `value = (x1·t1 + x2·t2)/time`; `m_LifeState` = `KNpc+0x1f0` (value), `+0x1f8` (time) | giống nguồn Windows từng dòng |
| `0x08097DE0` | `ProcessFunc[154]` = ManaPotionV: `+0x200` / `+0x208` | |
| `0x0809A2F0` | `ProcessFunc[190]` = `lifereplenish_p`: cộng vào `KNpc+0x1194` (phần trăm hồi máu, log `"(Percent)"`) | |
| `0x0808B610` | **`KNpc::ProcessState`**: `+0x1184` m_RegionIndex, `+0x1904` m_LoopFrames (% 10), `+0x224` m_Doing (8 = do_sit), `+0x1190` hồi máu tự nhiên × `+0x1194`/100 (`"AddLife: %d * %d%% = %d"`), `+0x118c` m_CurrentLife chặn ở max(`+0x1a14`, `+0x1a18`) và ≥ 0; `+0x11a0`/`+0x11a4` nội lực; rồi các trạng thái mỗi frame: `+0x1c8` độc, `+0x1d8` đóng băng, `+0x1e8` choáng, **`+0x1f8` thuốc máu** (`0x0808B7BC`: `time--`, `% 10 == 0` → `+0x1f0 × percent/100`, chỉ chặn trên), `+0x208` thuốc nội | `disp 1f8`; chuỗi `"AddLifeState: %d * %d%% = %d"` |
| `0x080DA560` | ctor `KProtocolProcess` (106 ô `ProcessFunc[c2s_…]`) | `pmf` |
| `0x08204710` | **`KItemList::EatMecidine(this, nIdx)`**: `Player[m_PlayerIdx]` (0x8788 byte, `g_pPlayer` 0x8BAEE60), `Item[]` 0x830D300 (0x368 byte); chết (`m_Doing == 10`) → không; `Check_ItemUsable` trong `\script\item\forbiditem.lua`; genre 1 thuốc → `0x08204858`: cờ `KNpc+0x147a` (forbit_takemedicine), đếm `Player+0x86a4/+0x86a8`, `ApplyMagicAttribToNPC(npc, 3)` (`0x08068560`), `events.lua OnUseItem`, stack (`Item+0x14` xếp chồng, `+0x308` nStack, `+0x30c` max) → `SetItemStack(n−1)` (`0x08200D30`, gói s2c 168) hoặc `Remove` (`0x082006B0`) + `ItemSet.Remove` (`0x0806DB90`); ngồi (8) → đứng (`0x08078AA0`); genre 5 (`0x08204B00`) phù, 6 (`0x082049B8`) kịch bản | chuỗi `"Check_ItemUsable"`, `"OnUseItem"` |
| `0x08068560` | `KItem::ApplyMagicAttribToNPC(npc, nActive)` = `0x080669B0` (7 thuộc tính cơ bản, `Item+0xe4`) + `0x08066890` (6 ma pháp `Item+0x154`, hậu tố lẻ đếm `nActive`); `0x08068440` gỡ (giá trị đảo dấu) | |
| `0x081FD880` | `KItemList::UnEquip` bản JX2 (ô trang bị `this + place*8 + 0xc`, gỡ thuộc tính, tính lại bộ 15 ô — bộ đồ) | |
| `0x081ED430` | **`KBPT_Medicine` đọc dòng** qua bộ đọc chung `0x081ECF40` (mảng mô tả 12 byte {kiểu, đích, mặc định}; kiểu 0 số → `GetInteger(row+2, col)`, 1 chuỗi, 2 **bỏ cột**): 19 cột = tên(+0, 0x50), genre +0x50, detail +0x54, particular +0x58, [ảnh bỏ], obj +0x5c, rộng +0x60, cao +0x64, [giới thiệu bỏ], hệ +0x68, giá +0x6c, cấp +0x70, **是否叠放 +0x74**, 2 thuộc tính (+0xa4…+0xb8). Dòng 0xbc byte; `FindRecord(detail, level)` = `0x081ED5D0` | RTTI `13KBPT_Medicine`, vtable `0x0826A758` |
| `0x08206110` | **`KItemList::ExchangeItem`** (6,8 KB): mô hình tay cầm như Windows; phòng ở `this+0x4ca8/+0x4cc4/+0x4d18`; `FindItem` `0x081F8850`, `PickUpItem` `0x081F8720`, `PlaceItem` `0x081F8520`, `CheckSameDetailType` `0x08065A70`; gói cho client `0x080A8400` | chuỗi `"%s exchange item error"` |
| `0x080A3B80` | **bộ nạp bảng rơi đồ** (`KItemDropRate::Load`): `KIniFile::GetInteger` từng khoá với mặc định — `[Main] Count, RandRange, MagicRate, MoneyRate=20, MoneyScale=50, MinItemLevelScale=20, MaxItemLevelScale=10, MaxItemLevel=10, MinItemLevel=1, Series=−1, EnchasableRate=0, MinSocket=1, MaxSocket=1, IsTeamShare=0, TeamShareRate=0`; `[i] Genre, Quality, Detail, Particular, RandRate, MinItemLevel=−1, MaxItemLevel=−1, Series=−1, EnchasableRate=−1, MinSocket=−1, MaxSocket=−1, MagicLevel1..6` (0x44 byte/dòng, mảng ở +0x3c) | `ini_reads.py` (scratch) |
| `0x080A4410` → `0x080A41E0` | đọc `NpcS.txt` (`Treasure`, `DropRateFile`) và nạp/cache bảng rơi đồ theo đường dẫn | |
| `0x08088B60` | **`KNpc::OnDeath` phần rơi đồ**: `m_CurrentTreasure` (+0x1378) lần: `g_Random(100) < MoneyRate(+0xc)` → `LoseMoney 0x08081950`, không thì `LoseSingleItem 0x08088840`; `SubWorld+0x4f298` ≠ 0 → không rơi; `IsTeamShare` → `0x08089188`; rồi script `OnGlobalNpcDeath` | |
| `0x08081950` | `KNpc::LoseMoney`: `m_CurrentExperience(+0x1188) × MoneyScale(+0x10) / 100 × g_ServerConfig.MoneyRate(0x0830C7E4, `[ServerConfig] MoneyRate` mặc định 100) / 100` | |
| `0x08083BB0` | **`GenRandomItem(player, pDropRate, npcLevel, flag)`**: dòng theo `RandRate` cộng dồn trên `g_Random(RandRange)`; series/enchasable/socket −1 → của bảng; `genre 0 && quality ∈ {1,4}` → `detail −= 1`; may mắn = `Player+0x5958` (+ thưởng qua `0x080CC620`); cấp: `lo=(L−1)/MaxScale+1, hi=(L−1)/MinScale+1`, kẹp [MinItemLevel, MaxItemLevel] rồi `1..10`; ma pháp: `k = g_Random(4)+3`, ô `i ≤ k` = cấp (quality 0 và `g_Random(100) ≥ EnchasableRate`), quality 2 / khảm → ô = −1 (lỗ), quality := 2; `MagicLevel1..6` của dòng ghi đè; `KItemSet::Add(genre, quality, series, level, luck, detail, particular, magic[], version, 0, "GenRandomItem"…)` | chuỗi `"GenRandomItem"` |
| `0x08088840` | `KNpc::LoseSingleItem`: `0x080CC320` kiểm; `GenRandomItem`; `GetFreeObjPos 0x080F0950`; `KObjSet::Add 0x080A6A70` (`Item+0x10` = dòng ObjData); `SetItemBelong 0x080A4D90` = `{belong, belong ≥ 0 ? 600 : 0}` | |
| `0x080B8210` | `KPlayer::ServerPickUpItem` bản JX2: khoảng cách² so `0x9C40` (40000) | |
| `0x080B5180` / `0x08205830` | `KPlayer::AddItem` / `KItemList::Add` (đích của Lua `AddItem` `0x08120D30` → `0x08120B30` → `0x0811F230` đọc 18 đối số Lua → `0x0806E110` sinh vật phẩm) | `re_calls` |
| `0x0806E110` → `0x0806DD30` → `0x0806BA10` | **`KItemSet::Add`** → sinh vật phẩm: `version = −1` → `g_nItemVersion` (`0x9777F34`), phải ≤ nó; `Item+0x1fc` = version, `+0x1e0` = seed; rồi **bảng nhảy theo genre** (`0x08252D28`): genre 0 → theo quality: 0 `Gen_Equipment 0x0806B3A0`, 1 `Gen_GoldEquip 0x0806A150`, 2 `0x0806B6C0` (bạch kim: gọi Gen_Equipment rồi đổi ô −1 thành lỗ), 4 `0x0806A9D0`; genre 1 `0x0806BBA0`, 4 `0x0806BB28`, 5 `0x0806BB10`, 6 `0x0806BAD8`, 7 `0x0806BA98`, 8 `0x0806BA50`, 9 `0x0806BB40`, 10 `0x0806BB70`; genre 2, 3 → không | `re_calls`, đọc bảng nhảy bằng `Elf.u32` |
| `0x0806B3A0` | **`KItemGenerator::Gen_Equipment(this, detail, particular, series, level, pnaryMALevel, luck, pItem, bNew)`**: bNew → chép `*pItem` vào bản tạm, `+0x1e0 = g_GetRandomSeed()`, `+0x1fc = this+0x5a4c` (phiên bản bộ bảng), `+0x200 = luck`; `bMagic = pnaryMALevel && detail ≠ 11` (mặt nạ); `+0x1e4..` = 6 cấp ma pháp; dòng bảng `0x080690D0(detail, particular, level)`; `SetAttrib_CBR 0x08068100`; `+0x28 = series`; có ma pháp → `Gen_MagicAttrib`, rồi bNew → `CheckNewItemAttrib 0x08069D10`; rớt → `g_Random(100)`, **làm lại tối đa 0x15 = 21 lần** rồi trả 0; xong `SetAttrib_MA 0x08065710`, `+0x354 = time(0)`, chép về `*pItem`; khôi phục seed | chuỗi `"KItemGenerator::Gen_Equipment(%s) Error"` |
| `0x0806AF70` | **`KItemGenerator::Gen_MagicAttrib(this, type, pnaryMALevel, series, luck, pnaryMA, version)`**: với mỗi ô i khi `level[i] ≠ 0`: `pos = 1 − (i & 1)`; `GetCMIT(pos, type, series, level) 0x08070B50` (NULL → log + dừng); **`nDecide`**: `version > 3` → `g_Random(1000000)·100/(10·luck+100)`; `≤ 1` → `g_Random(100)/(luck/10+1)`; 2–3 → `g_Random(1000000)/(luck/10+1)`; duyệt ứng viên: bỏ `m_nUseFlag` (dòng+0x1a8), bỏ `DropRate[type]` (dòng+0x178+type·4) `≤ nDecide`, bỏ trùng `nPropKind` (+0x15c) với ô trước; không còn → dừng; chọn `Get(g_Random(n))`; `type = kind`, `value[k] = min_k + g_Random(max_k − min_k + 1)` (+0x160..+0x174); xoá `UseFlag` cuối hàm. Dòng `KMAGICATTRIB_TABFILE` 0x1ac byte: +0 pos, +4 tên, +0x54 class, +0x58 level, +0x15c kind, +0x160 3 khoảng, +0x178 `DropRate[12]`, +0x1a8 UseFlag | chuỗi `"[GenMagicAttrib]"` |
| `0x08070B50` / `0x08070D00` / `0x08070BD0` | `KLibOfBPT::GetCMIT` (pos ≤ 1, type ≤ 11, series ≤ 4, level 1..10 → `this+0x1f98 + pos·0x1c20 + type·0x258 + series·0x78 + (level−1)·12`, ô `{ptr, cap, count}`); bộ dựng `m_CMAIT` (mỗi dòng: `UseFlag = 0`; mọi type có `DropRate ≠ 0`; `class −1` → hệ 0..4; **cấp từ cấp dòng tới 10**); `InitMALib` (`m_CMAT[2][12]` ở `+0x57d8/+0x5868`) | |
| `0x0806BE00` / `0x08069D10` | bộ nạp **`magicattrib_limit.txt`** (`\settings\item\%03d\`; cột `MagicType, Min1, Max1, Min2, Max2, Min3, Max3`, mặc định −1, `type` 1..0x153, `std::map` ở `this+0x5a54`, nút `+0x10 type, +0x14 Max1..3, +0x20 Min1..3`; trùng type → `"mapCheckNewItemAttrib Have Same Check Data"`) / **`CheckNewItemAttrib(this, sMA)`**: mỗi thuộc tính có dòng giới hạn, mỗi k có `Max_k ≠ −1`: **rớt khi `value ≤ Min_k` hoặc `Max_k ≤ value`** (bảng 004: `139 0 0…` cấm hẳn `allskill_v`) | chuỗi `"CheckNewItemMagicAttrib"` |
| `0x08065710` | `KItem::SetAttrib_MA`: chép 6×16 byte vào `Item+0x154`; kind `0x2b` (43 `indestructible_b`) → `Item+0x308` (độ bền) = −1 | |
| `0x0806A150` | `KItemGenerator::Gen_GoldEquip(this, row, pItem, bNew)`: `row < this+0x170c` (số dòng), dòng 0x16c byte tại `this+0x1708`; `Item+0x80 = row`, `+4 = 1` (hoàng kim), nhóm bộ `+0x270/+0x274/+0x2a4..`; `SetAttrib_CBR`; 6 ma pháp từ `magicattrib_ge` (dòng+0x154.., bảng `this+0x1a44`) | chuỗi `"Gen_GoldEquipment Error"` |
| `0x0806C2E0` / `0x0805EA10` | `KItemGenerator::Init(this, version)` (`this+0x5a4c = version`, `KLibOfBPT::Init 0x080719C0` đọc thư mục `/settings/item/%03d`); `g_ItemGenerator = 0x0830D408` (new 0x5a68 byte) được Init với **`g_nItemVersion 0x9777F34`** | |
| `0x080F6A40` | **ctor `KSubWorldSet`** (`g_SubWorldSet 0x9777F00`): `+0x34 = 4` — **phiên bản vật phẩm hiện hành của bản này là 4** (thư mục `004`), không đọc từ tệp; Lua `ITEM_GetLatestItemVersion 0x081542C0` trả nó | `re_scan disp 34 mov` |
| `0x081FD2C0` | **`KItemList::GetEquipEnhance(this, place, flag)`**: `m_PlayerIdx ≤ 0` → 0; `!flag && this+0x4c7c ≠ −1` → 3; place 10..14 → 3; > 14 → 0; `n = g_IsAccrue(hệ Npc+0x28, hệ Item+0x28)`; hai ô kích hoạt `[0x082E7460 + place·8]` có đồ → `+= g_IsAccrue(hệ đồ đó, hệ món)` | |
| `0x08074190` / `0x080741D0` | `g_IsAccrue(bảng, src, des)` = `bảng[src] == des` (src ≤ 4); bảng ngũ hành: `0x0830ED18` sinh {2,3,1,4,0}, `0x0830ED2C` khắc {1,4,3,0,2}, `0x0830ED40` được sinh bởi {4,2,0,1,3}, `0x0830ED54` bị khắc bởi {3,0,4,2,1} | `re_scan disp` |
| `0x0811D5D0` → `0x08204560` | Lua **`DelItem(detail \| tên)`**: chuỗi → `KTabFile::GetInteger(dòng theo tên, "DetailType")` trên `\settings\item\questkey.txt` (`0x9780D20`, nạp ở `0x0805D580`); `KItemList::RemoveTaskItem`: duyệt danh sách mục (`this+0x4c6c`, `+0x4c74` số mục, mỗi mục 20 byte: `+0x94` idx, `+0x98` vị trí) — mục đầu genre 4 cùng detail → `Remove 0x082006B0` + `KItemSet::Remove` (cả chồng) | chuỗi `"[TASK] Can Not Del Item"` |
| `0x0811D3B0` → `0x08204350` | `DelItemEx`: như trên nhưng chỉ mục có vị trí **3 = pos_equiproom** (túi) | |
| `0x0811D250` → `0x081F9FA0` | `HaveItem(detail \| tên)` → 1/0: mục genre 4 cùng detail ở bất kỳ phòng | |
| `0x0811D6E0` → `0x081FA010` | `GetItemCount(detail \| tên)`: **đếm số mục** (chồng = 1) genre 4 cùng detail | |
| `0x0811D830` → `0x081FC550` | `GetItemCountEx`: như trên, chỉ túi (vị trí 3) | |
| `0x0811D140` → `0x081FA080` | `HaveCommonItem(genre, detail, particular)`: −1 = bất kỳ | |
| `0x0811D4C0` → `0x08204470` | `DelCommonItem(genre, detail, particular)`: xoá mục đầu khớp | |
| `0x0811FA10` | **`AddStackItem([tag,] count, genre, detail, particular, level, series, luck[, magic…])`**: chuỗi đầu bị `lua_remove`; `Lua_NewItem 0x0811F230`; `Item+0x14` chồng được và `0 < count ≤ max(Item+0x30c,1)` → `Item+0x308 = min(count, 0xffff)`; `KPlayer::AddItem(player, idx, 1, 1, 0)` → trả idx | |
| `0x080B5180` → `0x08205830` | `KPlayer::AddItem(idx, room?, bStack, ..)` → `KItemList::Add(room, idx, bStack)`: bStack và chồng được → tìm chồng cùng loại `0x081FC2C0`, gộp `0x08202980` tới khi hết (`+0x308 < 0`); không thì đặt vào chỗ trống | |
| `0x08227E10` / `0x08227A00` | **`KTabFile::GetInteger(row, col, default, &out)`** / `GetValue`: ô **trống** (độ dài 0) hoặc ngoài bảng → **mặc định**, còn lại `strtol` — khác nguồn Windows (`atoi("")` = 0). Bộ đọc dòng của từng bảng đưa mặc định theo cột: `tools/re/re_tabdesc.py` (`0x081ED830` trang bị: hệ 0, giá 0, cấp 1, khác −1; `0x081EEE30` magicattrib: mọi số −1; `0x081ED430` thuốc) | `re_tabdesc` |
| `0x08226AD0` / `0x08226B00` / `0x08226AC0` | **`g_Random(n)`** = `seed = seed·0xF25 + 0x7385; seed % n` (unsigned; `n = 0` → 0) — đúng `Engine/Src/KRandom.cpp` (IA 3877, IC 29573); `g_GetRandomSeed` / `g_RandomSeed` (`0x082E76C4`) | |

Bảng tên thuộc tính ma pháp của bản JX2 (`MAGIC_ATTRIB_STRING`, ctor `KMagicDesc` `0x080724B0` đổ
`mov [0x0830E640 + id*4], "tên"`, 0x155 ô, 335 tên, id tới 340) — lưu ở
[`linux/jx_linux_magicattrib.tsv`](linux/jx_linux_magicattrib.tsv) và trong Go
`pkg/jxold/item/KMagicAttribNames.go` (client dùng làm khoá tra `\settings\magicdesc.ini`). Khớp
`ProcessFunc`: 153 `lifepotion_v`, 154 `manapotion_v`, 155 `physicsresmax_p`, 190 `lifereplenish_p`
(hệ số hồi máu ở `KNpc+0x1194`).

## 10. Cấu trúc `KNpc` / `KPlayer` và thuộc tính nhân vật (M12) — đã kiểm từng dòng

Cách tìm: `tools/re/re_attribmod.py` duyệt 217 `ProcessFunc` của `KNpcAttribModify` (mỗi hàm chạm
npc qua thanh ghi nạp từ `[ebp+0x10]`, đọc `pMagic->nValue[k]` ở `[ebp+0x14] + 4 + 4k`) → mỗi id ma
pháp cho biết **ô nào của KNpc** nó cộng/ghi (kết quả:
[`linux/jx_linux_attribmod.tsv`](linux/jx_linux_attribmod.tsv)); rồi đọc tay `KNpc::ClearAttrib`,
`KNpc::Init`, `KNpc::SetTemplate`, `KPlayer::LoadFrom`, `KPlayer::LevelUp`, `KPlayer::UpdataCurData`
để có chiều "gốc → hiện tại". Tên đặt theo `Core/Src/KNpc.h` của nguồn cũ (`m_LifeMax` gốc,
`m_CurrentLifeMax` hiện tại).

**Kích thước và mảng**: `sizeof(KNpc) = 0x1A4C`, mảng `g_pNpc` ở `[0x836EAE0]`, số ô `[0x830CA58]`;
`sizeof(KPlayer) = 0x8788`, `g_pPlayer` ở `[0x8BAEE60]`; `Player+0x3f4 = m_nIndex` (npc của người
chơi), `Npc+0x1908 = m_nPlayerIdx`; `Npc+0x4 = m_Index` (đối số đầu của log `0x08096A30`).
`g_PlayerSet = 0x8BACAC0` (`KPlayerSet`), `g_PlayerSet.m_cLevelAdd = 0x8BAF378` (`KLevelAdd`).

### 10.1 `KNpc` — ô gốc (`m_XXX`, +0x15a8…) và ô hiện tại (`m_CurrentXXX`)

Hai bản "hiện tại" cho máu/nội/kháng/tốc độ: bản thường và bản **`_yan_`** (id 228…245 của bảng
ma pháp — `lifemax_yan_v`…); giá trị dùng thật = **max(thường, yan)** (`ProcessState` chặn máu ở
`max(+0x1a14, +0x1a18)`, `LevelUp` đổ đầy máu bằng cùng max, `OnHurt` lấy `max(+0x1a44, +0x1a48)`).

| Gốc | Hiện tại | Yan | Tên | Từ đâu |
|---|---|---|---|---|
| `+0x20` | | | `m_Level` | người chơi: WORD `TRoleData+0xc3`; quái: mẫu `+0x1138` |
| `+0x24` | | | `m_Kind` (1 = người chơi) | mẫu `+0x20` |
| `+0x28` | | | `m_Series` | `TRoleData+0xbb` / mẫu `+0x28` |
| `+0x21c` | `+0x220` | | `m_Camp` / `m_CurrentCamp` (`changecamp_b` ghi `+0x220` khi 1..6, khác → về `+0x21c`) | `TRoleData+0xbf` / mẫu `+0x24` |
| | `+0x224` | | `m_Doing` (8 ngồi, 9 bị đánh, 10 chết, 0x12, 0x15…) | |
| | `+0x22c` / `+0x230` | | `m_Frames.nTotalFrame` / `nCurrentFrame` (DoSkill `0x08088150` đặt tổng theo tốc độ; `0x08085020` bắn ở 60 %; §16) | |
| | `+0x169c..+0x1713` / `+0x1714` / `+0x1718` / `+0x171c` | | vòng 5 lệnh do_skill 24 byte / chỉ số đọc / ghi / cờ đầy (`SendCommand 0x0809B750`, `ProcessCommand 0x0809B9E0`; §16) | |
| | `+0x194c` | | 1 = rảnh (không trong hành động): DoSkill hạ 0, DoStand/kết thúc hành động/thất bại đặt 1; `SetActiveSkill`, kiểm lệnh cần ≠ 0 | |
| | `+0x14a0` / `+0x14a4` (`+0x14a8`/`+0x14ac` bản sao) | | điểm/mục tiêu của hành động: p1/p2 của CastSkill, đích đánh lùi, vị trí mình khi DoStand | |
| | `+0x240` / `+0x12a8` | | `m_ActiveSkillID` / **`m_CurrentAttackRadius`** (SetActiveSkill `0x08086D90` lấy từ instance) | |
| | `+0x248` | | `m_SkillList` (`KSkillList` 0xF38 byte: ô i = 1..79 ở `+0x250 + 0x30·i` — đủ 12 ô ở §15.1; nút cộng cấp `+0x1150`, bản đồ tăng sát thương `+0x115c`) | mẫu `+0x108` (0xF00 byte) |
| | `+0x1150` | | danh sách ma pháp mẫu (`+0x1150/54/58` từ mẫu `+0x1008..`) — `ClearAttrib` xoá | |
| | `+0x1180` / `+0x1184` | | `m_SubWorldIndex` (SubWorld 0x63FC8 byte, `[0x8FC81E0]`) / `m_RegionIndex` (0xCC4 byte) | |
| `+0x15a8` | `+0x1188` | | `m_Experience` / `m_CurrentExperience` = mẫu `+0xc0` × `[ServerConfig] ExpRate` (`[0x830C7E0]`) / 100 | `SetTemplate` |
| `+0x15ac` | `+0x1a14` | `+0x1a18` | `m_LifeMax` / `m_CurrentLifeMax` | `TRoleData+0xeb` / mẫu `+0x60` |
| | `+0x118c` | | `m_CurrentLife` | `TRoleData+0xf7` |
| `+0x15b0` | `+0x1190` | | `m_LifeReplenish` / `m_CurrentLifeReplenish` (người chơi gốc = 0) | mẫu `+0xc4` |
| | `+0x1194` | | `m_nLifeReplenishPercent` = 100 (`lifereplenish_p` cộng) | |
| `+0x15b4` | `+0x1a1c` | `+0x1a20` | `m_ManaMax` / `m_CurrentManaMax` | `TRoleData+0xf3` |
| | `+0x11a0` | | `m_CurrentMana` (`TRoleData+0xff`); `+0x119c` `m_nManaReplenishPercent` = 100 | |
| `+0x15b8` | `+0x11a4` | | `m_ManaReplenish` / `m_CurrentManaReplenish` (người chơi gốc = 0) | |
| `+0x15bc` | `+0x11ac` | | `m_StaminaMax` / `m_CurrentStaminaMax`; `+0x11a8` `m_CurrentStamina` (`TRoleData+0xfb`) | `GetStaminaBase(series, sex, level)` |
| | `+0x11b0` | | `m_CurrentStaminaSitAdd` = max(1, `m_CurrentStaminaMax × SitAdd / 1000`) — tính lại mỗi khi StaminaMax đổi | `stamina.ini SitAdd` |
| `+0x15c0` | `+0x11b4` | | `m_StaminaGain` / `m_CurrentStaminaGain` (người chơi = `NormalAdd`) | `stamina.ini` |
| | `+0x11b8` | | `m_PhysicsDamage` {`+0x11b8` type, `+0x11bc` min, `+0x11c0`, `+0x11c4` max} | `SetNpcPhysicsDamage` / mẫu `+0xd0..+0xdc` |
| | `+0x11c8`/`+0x11d8`/`+0x11e8`/`+0x11f8` | | `m_CurrentFireDamage` / Cold / Light / Poison (`KMagicAttrib` 16 byte; `addXdamage_v` ghi `nValue`) | |
| | `+0x1208`/`+0x1218`/`+0x1228`/`+0x1238`/`+0x1248` | | ma pháp vật lý/băng/lôi/hoả/độc (`addXmagic_v` 168–172) | |
| `+0x15c4` | `+0x1258` | | `m_AttackRating` / `m_CurrentAttackRating` | người chơi: `dexterity × 4 − 28`; mẫu `+0xc8` |
| `+0x15c8` | `+0x125c` | | `m_Defend` / `m_CurrentDefend` (`armordefense_v` 30, `adddefense_v` 150) | người chơi: `dexterity / 4`; mẫu `+0xcc` |
| | `+0x1260` / `+0x1264` / `+0x1268` | | `returnres_p` 205 / `melee_returnres_p` 299 / `range_returnres_p` 300 | |
| `+0x1608..+0x1618` | `+0x126c..+0x127c` | | `m_XResistMax` / hiện tại (thứ tự hoả, băng, độc, lôi, vật lý); người chơi từ `TRoleData+0x164..0x168` (0 → 75) | mẫu `+0x98..+0xa8` (hoả, băng, lôi, độc, vật lý) |
| | `+0x1280` / `+0x1284` | | `skill_enhance` 243 / `magicdamage_p` 244 | |
| `+0x161c` / `+0x1620` | `+0x1288` / `+0x128c` | `+0x1a2c` / `+0x1a30` (% `fastwalkrun_p` / yan) | `m_WalkSpeed` / `m_RunSpeed` (người chơi 5 / 10; mẫu `+0x50` / `+0x5c`); `0x08098A50`: bỏ phần `gốc × max(p, yan) / 100` cũ, `p += v0`, cộng lại `gốc × max(p, yan) / 100` | |
| | `+0x1294` / `+0x1298` | | `nomovespeed` 182 | |
| `+0x1624` | `+0x129c` | | **bước đi** 12 (`KNpc::Init 0x0807E005/0x0807DDCA`; đánh lùi `0x08081B70`, lao `0x08087CF0`) — zone `step_length`; **không** phải bán kính kỹ năng (§14 đính chính) | |
| `+0x1630` | `+0x12a4` | | `m_VisionRadius` / `m_CurrentVisionRadius` (người chơi 120; `visionradius_p` 112) | mẫu `+0xb0` |
| `+0x1638` | `+0x12ac` | | `m_ActiveRadius` / `m_CurrentActiveRadius` | mẫu `+0xac` |
| | `+0x12b0`… | | các cặp `anti_*` (2 dword mỗi cặp): `+0x12b0` anti_hitrecover, `+0x12b8` anti_stuntimereduce, `+0x12c0/c8/d0/d8/e0` anti_{physics,fire,cold,lighting,poison}res, `+0x12e8..+0x1308` bản yan, `+0x1310` anti_sorbdamage_yan, `+0x1318` anti_block_rate, `+0x1320` anti_enhancehit_rate, `+0x1328` anti_do_stun, `+0x1330` do_stun, `+0x1338` anti_do_hurt, `+0x1340` do_hurt, `+0x1348` anti_poisontimereduce; `0x08078F10` xoá tất cả | |
| `+0x1640` | `+0x1378` | | `m_Treasure` / `m_CurrentTreasure` | mẫu `+0x68` |
| | `+0x137c` / `+0x1380` / `+0x1384` / `+0x1388,+0x138c` / `+0x1394` / `+0x1398,+0x13a0` | | `meleedamagereturnmana_p` / `rangedamagereturnmana_p` / `clearallcd` / `addblockrate` / `walkrunshadow` / `manatoskill_enhance` | |
| | `+0x13a4` / `+0x13a8` / `+0x13ac` / `+0x13b0` / `+0x13b4` / `+0x13b8` | | `meleedamagereturn_p` / `_v`, `rangedamagereturn_p` / `_v`, `poisondamagereturn_p` / `_v` | |
| | `+0x13bc` / `+0x13c0` / `+0x13cc` / `+0x13d0` / `+0x13d4` | | `slowmissle_b` / `statusimmunity_b` / `damage2addmana_p` / `poison2decmana_p` / `manashield_p` | |
| | `+0x13dc` / `+0x13e0` / `+0x13e4` | | `steallife_p` / `stealmana_p` / `stealstamina_p` (ghi đè, không cộng; 136–138 `*enhance_p` cùng ô) | |
| | `+0x13e8` / `+0x13ec` / `+0x13f0` / `+0x13f4` | | `seriesres_p` / `seriesenhance_p` / `five_elements_enhance_v` / `five_elements_resist_v` | |
| | `+0x13f8` / `+0x13fc` / `+0x1400` / `+0x1404` / `+0x1408` / `+0x140c` / `+0x1410` / `+0x1414` (=100) / `+0x1418` | | `knockback_p` / `deadlystrike_p` / `stun_p` / `fatallystrikeres_p` / `block_rate` / `enhancehit_rate` / `enhancehiteffect_rate` / `add_damage_p` / `fatallystrike_p` | |
| | `+0x1420` / `+0x1424` / `+0x1428` | | `freezetimereduce_p` / `poisontimereduce_p` / `stuntimereduce_p` | |
| | `+0x142c` / `+0x1430` / `+0x1434` / `+0x1438` / `+0x143c` | | `fireenhance_p` / `coldenhance_p` / `poisonenhance_p` / `lightingenhance_p` / `addphysicsdamage_v` | |
| | `+0x1464` / `+0x1468` / `+0x146c` / `+0x1470` / `+0x1474` | | `dynamicmagicshield_v` / `staticmagicshield_v,_p` / `ignoreskill_p` / `returnskill_p` / `ignorenegativestate_p` | |
| | `+0x1478` / `+0x1479` / `+0x147a` / `+0x147b` (byte) | | `forbit_attack` / `frozen_action` / `forbit_takemedicine` / `invincibility` (`sete` sau `cmp nValue[0], 1`: cờ = 1 **khi giá trị đúng bằng 1**) | |
| | `+0x14c4` | | `randmove` 199 | |
| | `+0x1505` (32 byte) / `+0x1528` | | `m_szName` / độ dài tên | `TRoleData+0x4` / tên mẫu |
| | `+0x152c` | | `m_nSex` | `TRoleData+0x24` |
| | `+0x15cc..+0x15dc` / `+0x15e0..+0x15f0` | | `me2{metal,wood,water,fire,earth}damage_p` 276… / `{…}2medamage_p` 277… (5 hệ) | |
| `+0x15f4` / `+0x15f8` / `+0x15fc` / `+0x1600` / `+0x1604` | `+0x19ec` / `+0x19f4` / `+0x19fc` / `+0x1a04` / `+0x1a0c` | `+0x19f0` / `+0x19f8` / `+0x1a00` / `+0x1a08` / `+0x1a10` | `m_FireResist` / Cold / Poison / Light / Physics (người chơi từ `level_add`; mẫu `+0xec/+0xf0/+0xf8/+0xf4/+0xfc`) | |
| `+0x1628` / `+0x162c` | `+0x1a34` / `+0x1a3c` | `+0x1a38` / `+0x1a40` | `m_AttackSpeed` / `m_CastSpeed` (người chơi 0 / 0) | |
| `+0x163c` | `+0x1a44` | `+0x1a48` | `m_HitRecover` (người chơi 0; mẫu `+0xb8`) | |
| | `+0x1a24` / `+0x1a28` | | `sorbdamage_p` / `sorbdamage_yan_p` | |
| | `+0x1658` (10 int) / `+0x1680` / `+0x1720` | | `m_AiParam[10]` / bán kính kỹ năng lớn nhất² / `m_AiSkillRadiusLoadFlag` | mẫu `+0x70..` |
| | `+0x1750` | | tên kịch bản (`strcpy` từ mẫu `+0x103c`) | |
| | `+0x1904` | | `m_LoopFrames` | |
| | `+0x2c` | `KNpc::height` | độ cao khi nhảy (`0x080817E0`; `0x08087770` đặt 0 ngoài `m_Doing` 4/0x14) | §16.2 |
| | `+0x12a0` | `kJumpSteps` | 40 = số bước nhảy tối đa (`Init 0x0807DDD4`; chỉ `0x08087CF0` đọc) | §16.2 |
| | `+0x14a8` / `+0x14ac` | `cast_kept1/2`, `cast_kept_target` | cặp p1/p2 giữ lại của `CastSkill` (kỹ năng con của thân pháp) | §16.2 |
| | `+0x14b0` | `run_bonus` | thưởng tốc chạy `Param1` của chạy đánh (prologue `m_Doing == 0x12` trừ lại) | §16.2 |
| | `+0x164c` | `run_counter` | số khung đã chạy (chạy đánh) | §16.2 |
| | `+0x1938` / `+0x195c` / `+0x1960` / `+0x1964` | `jump_arc` / `jump_steps` / `jump_dir` / `phase` | hằng đường cong `5·(bước − 1)` / số khung nhảy / hướng nhảy / pha (nhảy đánh 0..1, thi triển nhiều lần = lần thứ i) | §16.2 |
| | `+0x199c` | | ngựa (`0x0807D520` ghi: `+0x1479` khoá → thôi; người chơi → `0x080AEBC0(3)`; lên ngựa khi đang ẩn → `0x0807D4C0`) — zone chưa có ngựa | §16.1 |
| | `+0x19a0` / `+0x19a4` | | ẩn thân `[hide]` 200 → `KNpc::hide` / cờ "đang gửi gói 0x4f" → `hide_syncing` (`SetHide 0x0807FF80`, `IsInvisibleTo 0x08079200`, vỡ `0x0807D4C0`; Init/ClearAttrib/`0x08082680` đặt 0) | §16.1 |
| | `+0x190c`, `+0x1910`, `+0x1914` (`m_HurtFrame`), `+0x1918`, `+0x191c`, `+0x1920`, `+0x1924`, `+0x1928`, `+0x192c` (`m_DeathFrame`) | | số khung | mẫu `+0x38, +0x40, +0x4c, +0x54, +0x58, +0x3c, +0x48, +0x44, +0xbc` |
| | `+0x19e8` | | cờ "đang tính lại trang bị/trạng thái" | |
| | `+0x1f0/+0x1f8`, `+0x200/+0x208` | | `m_LifeState`, `m_ManaState` (thuốc) | §9 |
| | `+0x234` | | đầu danh sách trang trạng thái: mỗi trang `+4` next, 20 `KMagicAttrib` từ `+0x24` | |

### 10.2 `KPlayer` (0x8788 byte)

`+0x3f4 m_nIndex`; `+0x3fc m_ItemList` (ô trang bị `+0xc + place·8`: `+0x408 + place·8` idx, `+0x40c`
cờ); `+0x5924 m_nAttributePoint`; `+0x5928 m_nSkillPoint`; `+0x5930 m_nStrength`, `+0x5934
m_nDexterity`, `+0x5938 m_nVitality`, `+0x593c m_nEngergy`, `+0x5940 m_nLucky`; `+0x5948..+0x5954
m_nCurStrength/Dexterity/Vitality/Engergy`, `+0x5958 m_nCurLucky`; `+0x595c/+0x5960 m_nExp` (64 bit),
`+0x5964/+0x5968 m_nNextLevelExp`; `+0x5994` (chưa rõ — đọc ở `0x0809BC70`, `0x08088B60`…; **không** phải sổ kỹ năng, sổ ở `KNpc+0x248` §15); `+0x8600` cộng MaxLevel kỹ năng khi chuyển sinh (`SetSkillMaxLevelAddons`); `+0x86b8` số lần **trùng
sinh** (byte `TRoleData+0x162`, ≤ 7) — đổi bảng kinh nghiệm và chặn dưới kháng; `+0x86a4/+0x86a8`
đếm thuốc (§9).

### 10.3 `KLevelAdd` (`0x8BAF378`, nạp ở `0x080C4FD0`)

`level_exp.txt` 200 dòng (cấp 1..200, dòng i+2): cột 2 → `this[i]` (1..2·10⁹, sai → 2·10⁹); cột 3 →
`this+0x320+4i` (≤ 2·10⁹); cột 4..10 (trùng sinh 1..7) → int64 `this + 0x640 + 4·0x640·(k−1)... ` đúng
hơn: `this + 8·(200·k + i)` = cột × 10 000. **`GetLevelExp(level, reborn)`** (`0x080C3FF0`) = `cột2[level]
+ (reborn == 0 ? 10 000 × cột3[level] : bảng64[reborn][level])`; ngoài 1..200 hoặc reborn > 7 → −1.
`level_add.txt` 5 dòng (hệ 0..4): cột 2 `LifePerLevel` (+0x3200), 3 `StaminaMalePerLevel` (+0x323c),
4 `StaminaFemalePerLevel` (+0x3250), 5 `ManaPerLevel` (+0x3264), 6 `LifePerVitality` (+0x3278), 7
`StaminaPerVitality` (+0x328c), 8 `ManaPerEnergy` (+0x32a0), 9 `LeadExpShare` (+0x32b4), 10
`FireResPerLevel` (+0x32c8), 11 Cold (+0x32dc), 12 Poison (+0x32f0), 13 Lighting (+0x3304), 14 Physics
(+0x3318), 15 `StaminaMaleBase` (+0x3214), 16 `StaminaFemaleBase` (+0x3228); mọi ô trống → 0.
**`GetXResist(series, level, reborn≠0)`** (`0x080C4220` hoả, `0x080C42A0` băng, `0x080C4320` độc,
`0x080C43A0` lôi, `0x080C4420` vật lý): series > 4 hoặc cấp ∉ 1..200 → 0; `n = level`, nhưng cấp > 120
và hệ số **âm** → `n = 120`; `res = XResPerLevel[series] × n / 100`; trùng sinh → `max(res,
[0x830CA08])`. **`GetStaminaBase(series, sex, level)`** (`0x080C4120`) = `(level − 1) ×
StaminaPerLevel[sex][series] + StaminaBase[sex][series]` (sex 0 = nam). `GetLifePerLevel`
`0x080C40A0`, `GetStaminaPerLevel(series, sex)` `0x080C40C0`, `GetManaPerLevel` `0x080C4180`.
`stamina.ini` → `KPlayerSet`: `NormalAdd` +0x14bc (1), `ExerciseRunSub` +0x14c0 (6), `FightRunSub`
+0x14c4 (6), `KillRunSub` +0x14c8 (6), `SitAdd` +0x14cc (3) — mặc định trong ngoặc khi thiếu tệp.

### 10.4 Các hàm (địa chỉ đã kiểm từng thân hàm)

| Địa chỉ | Là gì |
|---|---|
| `0x0807DBD0` | `KNpc::Init` (từ ctor `0x0807E5D0`): gốc máu/nội/thể/chính xác = 100, phòng thủ = 10 |
| `0x0807EE60` | **`KNpc::ClearAttrib(this, bClearState)`**: ô hiện tại = ô gốc (bảng 10.1), `+0x1194/+0x119c = 100`, `+0x1414 = 100`, xoá mọi ô ma pháp, `0x08078F10` xoá `anti_*`; xoá danh sách `+0x1150` (từng nút: `0x082248C0` rồi `vtbl[1]`); **79 ô kỹ năng**: `cấp hiện tại (+0x18) > cấp (+4)` → `0x080E4CA0(skilllist, i, cur, base)` rồi `cur = base`; người chơi: `Player+0x86f8/+0x86fc/+0x8700 = 0`; xoá 5 danh sách `+0x1854/+0x1878/+0x189c/+0x18c0/+0x1830` (`0x0808C140`) và `+0x18e8` (`0x0805F760`); `+0x15cc..+0x15f0 = 0`; `bClearState` → xoá `+0x1bc/+0x1cc/+0x1dc/+0x1ec/+0x1fc` (các trạng thái độc/băng/choáng/thuốc) |
| `0x08082680` | **`KNpc::Init/ResetCurData`** (sau `SetTemplate`, `LoadFrom`, Lua `0x08165750`, `0x08085E70`): `m_CurrentCamp = m_Camp`; người chơi: `0x081621B0(Player+0x8078, level, level)`; `0x080823B0(this, 1, 0, 0, 0)`; máu = máu max, nội = nội max, thể = thể max, chép mọi ô gốc → hiện tại như `ClearAttrib`, `0x08079030`; cuối: nhảy `0x08079C30` |
| `0x08082E20` | **`KNpc::SetTemplate(this, nTemplate, nLevelOff, nSeries)`**: mẫu = `[0x836EB00 + 4·(nTemplate·0xB4 + (nSeries < 5 ? nSeries+1 : 1)·0x1E + nLevelOff)]` (mỗi mẫu 6 hệ × 30 cấp con trỏ); `0x08078F10`; tên (`+0x1505`, 32 byte); ô theo bảng 10.1; kinh nghiệm × `ExpRate`; `m_AiParam[10]` + bán kính² lớn nhất của 4 kỹ năng (vtable `+0x48` = `GetAttackRadius`, `0x08BC99E0` kho kỹ năng, `0x080E6E10` nạp) — chỉ lần đầu (`+0x1720`); kết: gọi `0x08082680`, nhảy `0x080A1C20` |
| `0x080C16D0` | **`KPlayer::LoadFrom(this, TRoleData*, bFlag)`**: `0x080A8550` (xoá), `+0x86b8` trùng sinh, `+0x8600`; `0x080F6D20` map; `npc = g_PlayerSet.AddNpc`(`0x0809FB10`); kháng max từ `+0x164..` (0 → 75); `m_Kind = 1`; `0x08078BE0(npc, playerIdx)`; `m_Level` = WORD `+0xc3`; tên; 5 điểm gốc `+0xd7..+0xe7` → cả gốc lẫn hiện tại; **`SetNpcPhysicsDamage 0x080A7EC0`**, **`SetNpcAttackRating 0x080A7F90`**, **`SetNpcDefence 0x080A7FC0`**; kinh nghiệm 64 bit `= +0xc7 + WORD +0xc5 × 2·10⁹`; `m_nNextLevelExp = GetLevelExp`; `m_Series` `+0xbb`, `m_Camp` `+0xbf`, `m_nSex` `+0x24`, **`m_LifeMax = TRoleData+0xeb`**, `m_StaminaMax = GetStaminaBase`, `m_LifeReplenish = m_ManaReplenish = 0`, `m_ManaMax = +0xf3`, `m_StaminaGain = NormalAdd`; **`SetNpcResist 0x080AB7C0`** (5 kháng gốc từ `level_add`); `0x080A7FF0` (đi 5, chạy 10, tốc đánh/thi triển 0, tầm nhìn 120, hồi đòn 0); `0x08078F10`; **`0x08082680`**; máu/nội/thể hiện tại từ `+0xf7/+0xff/+0xfb`; … |
| `0x080A7EC0` | `KPlayer::SetNpcPhysicsDamage` (gốc): `m_PhysicsDamage = {min = max = m_nCurStrength / 5 + 1, nValue[1] = 0}`, xoá hoả/băng/lôi/độc |
| `0x080A7F90` / `0x080A7FC0` | `SetNpcAttackRating`: `m_AttackRating = m_nDexterity × 4 − 28`; `SetNpcDefence`: `m_Defend = m_nDexterity / 4` |
| `0x080AF740` | **`KPlayer::SetNpcPhysicsDamage` (hiện tại)**: `GetWeaponDamage 0x081F9310` (`min = (Item+0x88 + Σ weapondamagemin_v) × (100 + Σ weapondamageenhance_p) / 100`, max với `+0x98`; tay không: `m_nCurStrength / 5 + 1`); `GetWeaponType 0x081F92E0` (= `Item+0x8` detail, −1 khi trống): **0 (cận chiến) → cả hai `+= m_nCurStrength / 5`; 1 (xa) → `+= m_nCurDexterity / 5`**; khác → giữ nguyên; `KNpc::SetPhysicsDamage 0x08078E20` ghi `+0x11bc/+0x11c4` |
| `0x080B0B40` / `0x080B0AF0` | `ChangeCurStrength(n)`: `+0x5948 += n` rồi hàm trên; `ChangeCurDexterity(n)`: `+0x594c += n`, **`m_CurrentAttackRating += 4n`, `m_CurrentDefend += n/4`**, rồi hàm trên (ProcessFunc 97/98 `strength_v`/`dexterity_v` gọi hai hàm này, chỉ người chơi) |
| `0x080B0B60` | `AddBaseDexterity(n, bCheck)`: `bCheck && n > điểm` → không; gốc và hiện tại `+= n`; âm → lỗi; điểm `−= n`; `0x080AF700`/`0x080AF6C0` (chính xác/phòng thủ gốc), `UpdataCurData(0)`, `SetNpcPhysicsDamage`; gói s2c **0x5D** {attr 1, gốc, hiện tại, điểm} 14 byte |
| `0x080AF250` / `0x080AF1A0` / `0x080AF120` | `LevelAddBaseLifeMax(sign)`: `m_LifeMax += LifePerLevel(series) × sign`, cả hai max hiện tại = gốc; `LevelAddBaseStaminaMax(sign)`: `+= StaminaPerLevel(series, sex) × sign`, max hiện tại = gốc, tính lại `+0x11b0`; `LevelAddBaseManaMax(sign)` tương tự |
| `0x080AF800` | **`KPlayer::LevelUp(this, bUp)`**: `m_nExp = 0`; lên: cấp ≤ 199 → `++`, **điểm thuộc tính += 5, điểm kỹ năng += 1**; xuống (`bUp = 0`): cấp > 1 → `−−`, `−5`, `−1`; `m_nNextLevelExp = GetLevelExp(cấp mới)`; ba hàm trên với dấu; 5 kháng gốc = `GetXResist` và hiện tại = gốc, 5 kháng max hiện tại = gốc; giữ `m_CurrentCamp`, **`UpdataCurData(1)`**, `SetNpcPhysicsDamage`, trả `m_CurrentCamp`; **máu = max(+0x1a14,+0x1a18), thể = max thể, nội = max(+0x1a1c,+0x1a20)**; `0x080A86B0` (đồng bộ), `0x081E7FD0`; báo đội (7 ô `0x08BB86EC`); bang; lên cấp → `\script\global\server_playerlevelup.lua` `main(player, level)`; trùng sinh → `0x080E4A00(skilllist)` |
| `0x080AF550` | **`KPlayer::UpdataCurData(this, bClearState)`**: `ClearAttrib(npc, bClearState)`; 5 điểm hiện tại = gốc; `Player+0xc8/+0xcc/+0xd0/+0xd4 = 0`; `KNpc::ReCalcStateEffect 0x0807D270` (duyệt trang trạng thái `+0x234`, mỗi ô ≠ 0 → `ModifyAttrib(npc, idx, {type, −v0, −v1, −v2}, 0)`); **`ReCalcEquip 0x080AF3E0`** (`+0x19e8 = 1`; `0x081FD1B0`; 15 ô trang bị có đồ: `KItem::ApplyBaseAttrib 0x080669B0`, `nActive = GetEquipEnhance(place, 0)`, `ApplyMagicAttrib 0x08066890`, `0x080684A0`, `0x08068280`; `+0x19e8 = 0`); `0x080AF390` (`0x081E5F20(0x97AC300, player)`); `0x081D5800(Player+0x8704)`; đuôi `0x080CBE40(Player+0x5994, idx)` (rỗng) |
| `0x0807F780` | `KNpc::OnHurt(this, nAntiHitRecover)`: `m_RegionIndex ≥ 0`, `m_Doing ∉ {9, 10}`; log `"m_CurrentHitRecover:%d - AntiHitRecover:%d = %d"`; `hr = max(+0x1a44, +0x1a48) − anti`; `hr > 99` → không bị đánh; `g_Random(100) ≤ 49` → không; `ignorenegativestate_p` `+0x1474` > `g_Random(100)` → log `"IgnoreNegState(Hurt):%d%%, Hit!"` và không; `m_Doing = 9`, `+0x194c = 0`, `+0x230 = 0`, **`+0x22c = max(1, (100 − hr) × m_HurtFrame(+0x1914) / 100)`**; báo vùng `0x080EF710`, gói `0x0807A970(this, 9, …)` |

### 10.5 Kinh nghiệm (đã kiểm từng thân hàm)

| Địa chỉ | Là gì |
|---|---|
| `0x0809BC70` | **`KDamageRecord::Add(rec, playerIdx, damage)`**: `rec = KNpc+0x1724` (3 ô × 12 byte từ `+4`: `+0` playerIdx, `+4` sát thương, `+8` = 0x4B0 = 1200 khung); chỉ khi npc **không** phải người chơi (`+0x24 == 0`?? — `m_Kind` của npc bị đánh ≠ 1) và playerIdx hợp lệ; người chơi có đội (`Player+0x5994`) → ghi theo **đội trưởng** (`0x08BB86E8 + team·0x30`); ô của người đó có sẵn → cộng, không thì ô trống đầu, hết ô → bỏ. `0x0809BD80` xoá 3 ô; `0x0809BDA0` đặt lại |
| `0x0809BDD0` | **chia kinh nghiệm khi chết** (gọi từ `KNpc::DoDeath 0x080892E0` sau `OnDeath`): mỗi ô có sát thương: người chơi có đội → chọn thành viên **gần nhất** trong 0x100000 = 1024² đơn vị; `exp = (int)((double)m_Experience(+0x15a8) × sátThương / max(+0x1a14, +0x1a18))` → `KPlayer::AddExpTeam 0x080B03E0(player, exp, cấp npc)`; trả về ô sát thương lớn nhất (chủ đồ rơi). Đòn kết liễu ghi **nguyên** sát thương nên tổng có thể vượt 100 % |
| `0x080B03E0` | `KPlayer::AddExpTeam(this, exp, npcLevel, …)`: không đội → `AddExp`; có đội → đếm thành viên cùng map trong 1024 đơn vị, thưởng `√n × hệ số (float 0x825528C)` + `100 + n`… (làm ở lát đội, M13/M14) |
| `0x080B00C0` | **`KPlayer::AddExp(this, exp, npcLevel)`**: `exp ≤ 0`, `m_Doing ∈ {10, 0x15}`, `KNpc+0x168c == 0` → không; `exp = CalcExp(exp, cấp mình, cấp npc)`; `> 5 000 000` → `exp/100 × (100 + Player+0xd4)` không thì `(100 + +0xd4) × exp / 100`; `0x080A9B50` (kinh nghiệm thống lĩnh); đồng hành (`+0x8088`) nhận phần riêng qua `0x080A7D30`; sự kiện `[0x9781158]` + `+0x8650/+0x8654` → chia 5; `0x080AF640(exp, +0xd0, &+0xc8)` = `exp × (100 + p) / 100 + lo + g_Random(hi − lo)` (trên 999 999 chia 100 trước); cờ map `SubWorld+0x63f8c & 4` không có → `0x08176160(+0x7d00)` 1 → chia đôi, 2 → 0; rồi lõi `0x080AFEA0` |
| `0x080A7C80` | **`CalcExp(exp, cấp mình, cấp npc)`**: cấp mình ≥ 100: npc ≤ 89 → 1, không thì exp; `d = mình − npc`: `d < −54`: `d < −69` → exp, không thì `exp × (−19d − 1030) / 300` (−55 → 5 %, −69 → 93 %); `|d| ≤ 5` → exp; `≤ 15` → `exp × (25 − |d|) / 20`; hơn → `exp / 2`; kết quả ≥ 1 |
| `0x080AFEA0` | **lõi `AddExp(this, int64)`**: cấp 200 (`> 0xC7`) không cộng; `exp = min(exp + n, m_nNextLevelExp)`; `≥ next` → `LevelUp(1)` (đặt exp = 0 — phần dư **mất**, như nguồn cũ), không thì gói s2c **0xC6** {int64 exp} |
| `0x08099F40` / `0x08099F00` / `0x08099EC0` | ProcessFunc 175 `expenhance_v` → `Player+0xc8 += v0, +0xcc += v2`; 176 `expenhance_p` → `+0xd0 += v0`; 206 `add120skillexpenhance_p` → `+0xd4 += v0` (chỉ người chơi; `UpdataCurData` xoá cả bốn) |
| `0x08088B60` | `KNpc::OnDeath` phần người chơi chết: mất kinh nghiệm `GetLevelExp(cấp)/100·2` … × `(7 − PK)/7`, trần 0x1FBD0; quái chết → **không cộng kinh nghiệm trong C++** mà gọi script toàn cục `OnGlobalNpcDeath(npc, kẻ giết)` (`\script\activitysys\g_npcdeath.lua`: nhiệm vụ/sự kiện) — kinh nghiệm đi đường `DoDeath → 0x0809BDD0` ở trên |
| `0x0809A100` … | `strength_v`/`dexterity_v`/`vitality_v`/`energy_v` (97–100) → `KPlayer::ChangeCurStrength 0x080B0B40` (`+0x5948 += n`, `SetNpcPhysicsDamage`), `ChangeCurDexterity 0x080B0AF0` (`+0x594c += n`, `m_CurrentAttackRating += 4n`, `m_CurrentDefend += n/4`, `SetNpcPhysicsDamage`), `ChangeCurVitality 0x080B0E60` (`+0x5950 += n`, `KNpc::AddCurLifeMax 0x08078C30` = cả `+0x1a14` lẫn `+0x1a18` `+= LifePerVitality × n`, `AddCurStaminaMax 0x08078CA0` `+= StaminaPerVitality × n` + tính lại `+0x11b0`), `ChangeCurEnergy 0x080B0DF0` (`+0x5954 += n`, `AddCurManaMax 0x08078D10` cả hai ô `+= ManaPerEnergy × n`) |

Trạng thái `KNpc` lúc bị đánh/chết/ngồi… và công thức sát thương (`KNpc::Attack`, `ReceiveDamage`,
`CalcDamage`) → lát tiếp của M12.

## 11. Hệ kỹ năng — bảng `Skills.txt` và số theo cấp (M12 lát B, đã kiểm từng thân hàm)

Cách tìm: chuỗi `"LvlSetting%d"` → hàm nạp cấp; tham chiếu **duy nhất** tới hàm đó là vtable của
`KSkill` (`0x082587A8`, typeinfo `6KSkill` ở `0x0825880C`), thứ tự các ô khớp đúng `ISkill` của
`Core/Src/Skill.h` nên mọi hàm ảo có tên; `KSkillManager::Init` qua chuỗi `\settings\Skills.txt`;
hàm đọc dòng qua chuỗi cột `"MisslesForm"`; hai hàm còn lại theo lời gọi. Mã JX NEXT: `KSkill.h/.cpp`,
`test_KSkill.cpp`; bảng xuất bởi `jxassets export-skills` (`pkg/jxold/skill`).

| Địa chỉ | Là gì |
|---|---|
| `0x082587A8` | **vtable `KSkill`** (25 ô; mỗi getter đã đọc — B3a, §15.1): `+0`/`+4` dtor (`0x080E84E0`/`0x080E8510`), `+8` GetSkillId (`+0x24`), `+0xc` GetSkillName (`+4`), `+0x10` GetSkillStyle (`+0x28`), **`+0x14` LoadSkillLevelData `0x080EE4B0`**, `+0x18` CanCastSkill `0x080E8AE0`, `+0x1c` GetSkillCostType (`+0x9c`), `+0x20` GetSkillCost(this, npc) (`+0x98`), **`+0x24` IsExpSkill (`+0x44`)**, `+0x28` IsTargetOnly (`+0x74`), `+0x2c` IsTargetEnemy (`+0x78`), `+0x30` IsTargetAlly (`+0x80`), `+0x34` IsTargetOther (`+0x7c`), `+0x38` IsTargetObj (`+0x84`), `+0x3c` IsTargetSelf (`+0x88`), `+0x40` IsTargetNoNpc (`+0x8c`), `+0x44` PeaceCanUse (`+0x94`), `+0x48` GetAttackRadius (`+0x48`), `+0x4c` IsAura (`+0x38`), `+0x50` IsWeaponSkill (`+0x34`), `+0x54` GetSkillLevelUpScriptId (`+0x10c`), `+0x58` GetTimePerCast(this, bHorse) (`+0xa0`/`+0xa4`), `+0x5c` GetReqLevel (WORD `+0x6c`), `+0x60` rỗng (`0x080EEBF0`, KThiefSkill ghi đè). (Bản trước đặt tên theo thứ tự JX1 — sai từ `+0x24`.) |
| `0x080E7200` | **`KSkillManager::Init`** (`g_SkillManager 0x08BC99E0`): xoá 2000×64 con trỏ instance (`this+0x8AAC0`) và bảng thứ ba `this+0x107AC0` (0x18 byte/ô), nạp `\settings\Skills.txt` → `0x0830AE00` và `\settings\Missles.txt` → `0x0830AE20`, `memset(m_SkillInfo, 0, 0x8AAC0)`; mỗi dòng `row = 2..height`: `SkillId` (mặc định −1; ngoài 1..2000 bỏ, > 2000 in `"Error!!!skills is more than MAXSKILL"`), `SkillStyle` (âm → bỏ), `MaxLevel`; style 13 → nhánh trộm `0x080E7825`; còn lại **`0x080E9200(info+0xc, row)`** rồi `info = {row +0, style +4, maxlevel +8}` — **dòng sau ghi đè dòng trước** cùng id (`TSkillInfo` 0x11C byte = 3 số + 0x110 byte dữ liệu dòng). Bảng thật: 1 629 dòng, 1 628 id (id 521 có 2 dòng), style 0 ×724, 2 ×456, 3 ×398, 1 ×37, 14 ×7, 4 ×6, 13 ×1 |
| `0x080E9200` | **`KSkill::GetInfoFromTabFile(pData, nRow)`** — `pData = info+0xc = KSkill+4` (vtable ở +0; mọi offset dưới cộng 4 là offset trong `KSkill`, đã đối chiếu với `AddMagicAttrib`): 60 cột, mặc định trong ngoặc: `SkillId +0x20`, `ReqLevel +0x68` (WORD), `MisslesForm +0xc4`, `SkillStyle +0x24`, `CharAnimId +0x28`, `SkillCostType +0x98`, `MslsGenerate +0xc8`, `SkillName +0` (32 byte), `EqtLimit +0xa4` (**−2**), `HorseLimit +0xa8`, `DoHurt +0x58` (**100**; JX1 mặc định 1), `ChildSkillNum +0xb0`, `CharClass +0x6c`, `IsPhysical +0x2c`, `WeaponSkill +0x30`, `IsAura +0x34`, `IsUseAR +0x3c`, `TargetOnly +0x70`, `TargetEnemy +0x74`, `TargetAlly +0x7c`, `TargetOther +0x78`, `TargetObj +0x80`, `TargetNoNpc +0x88`, `BaseSkill +0xbc`, `ByMissle +0xc0`, `ChildSkillId +0xb4`, `FlyEvent +0xd8`, `StartEvent +0xdc`, `CollideEvent +0xe0`, `VanishedEvent +0xe4`, `FlySkillId +0xe8`, `StartSkillId +0xf0`, `VanishedSkillId +0xf4`, `CollidSkillId +0xf8`, `CostValue +0x94`, `TimePerCast +0x9c`, `TimePerCastOnHorse +0xa0`, `Param1 +0x4c`, `Param2 +0x50`, `ChildSkillLevel +0xb8`, `EventSkillLevel +0xfc`, `IsMelee +0x38`, `FlyEventTime +0xec`, `IsExpSkill +0x40`, `Series +0x54`, `MslsGenerateData +0xcc`, `MaxShadowNum +0x104`, `AttackRadius +0x44` (**50**), `WaitTime +0x48`, `ClientSend +0x5c`, `TargetSelf +0x84`, `StopWhenMove +0xac`, `HeelAtParent +0xd0`, `PeaceCanUse +0x90`, `StateSpecialId +0x60`, `StatePriority +0x64`, `RelativePosType +0xd4`; `m_eRelation +0x8c` = TargetEnemy 8 \| Ally 4 \| Self 2 \| NoNpc 0x20 \| Other 1; `LvlSetScript` / `LevelUpScript`: hạ chữ A–Z (chỉ ASCII) rồi `g_FileName2Id 0x0821DE70` → `+0x10c` / `+0x108` (KSkill `+0x110` / `+0x10c`). Cột `Property`, `SkillIcon`, `PreCastSpr`, `*CastSnd`, `LRSkill`, `NeedShadow`, `Attrib`, `ShowEvent`, `ShowAddition`, `SkillDesc`, `Param*Memo` server **không đọc** |
| `0x080E6E10` | **`KSkillManager::InstanceSkill(id, level)`**: `info = m_SkillInfo[id−1]`; `row == 0` → 0; style < 0 → 0; **13 → `KThiefSkill`** (`new 0x724`, ctor `0x080FAD70`, nạp `\SETTINGS\THIEFSKILL.TXT` `0x080FA0F0`, một instance ở `m_pOrdinSkill[id−1][0]`); > 13 phải bằng 14; ≤ 4 → tạo; **5..12 → 0**. Tạo: `new KSkill` (0x6A0 byte), ctor `0x080E9040`, chép 0x44 dword từ `info+0xc` vào `KSkill+4`, gọi **vtable+0x14 = `LoadSkillLevelData(level, row)`**, cất vào `m_pOrdinSkill[id−1][level−1]` (`this + 0x8AAC0 + ((id−1)·64 + level−1)·4`, 64 = MAX_SKILLLEVEL) |
| `0x080EE4B0` | **`KSkill::LoadSkillLevelData(this, ulLevel, nRow)`** (đối số 2 là **cấp**, đối số 3 là **dòng** — ghi chú phiên trước ngược): `+0x2b4 = +0x3f8 = +0x53c = +0x680 = 0` (4 bộ đếm của 4 mảng 20 `KMagicAttrib`, cách nhau 0x144); `nRow ≤ 1` → về; `+0x114 = ulLevel`; `+0x110` (id script) = 0 → lỗi chết; `g_GetScript 0x0805D080` → `KLuaScript*` (0 → lỗi chết); `GetTopIndex 0x08220AB0` (mặc định 1); `i = 1..20`: `sprintf("LvlSetting%d")`, `sprintf("LvlData%d")`, hai `KTabFile::GetString(0x830AE00, nRow, cột, "", buf, 300, 1)`; **bỏ khi `LvlSetting` trống hoặc `LvlData[0] == '0'`** (trống thì vẫn gọi); `CallFunction("GetSkillLevelData", 1, "ssd", setting, data, level)` (vtable KLuaScript +0x1c); kết quả: `lua_isnumber` → `fistp` (chế độ cắt) → `sprintf("%d")`; không thì `lua_isstring 0x08232640 == 1` → `lua_tostring` → chép ≤ 0x400; không thì `sprintf` câu lỗi (`[0x978AAB0]`, cấp, setting, data) → `0x0821CC00` rồi **kết thúc vòng**; mỗi kết quả → `ParseString2MagicAttrib(level, setting, chuỗi)`; cuối: `SetTopIndex 0x08220A80` |
| `0x080EE380` | **`KSkill::ParseString2MagicAttrib(this, ulLevel, szName, szValue)`**: tên trống → 0; tìm trong **`std::map<const char*, int>` tên → id ma pháp** (header `0x0830EB98`, gốc `[0x0830EB9C]`, nút: +0x10 tên, +0x14 id; do ctor `KMagicDesc 0x080724B0` và `0x08073300` đổ cùng bảng tên `MAGIC_ATTRIB_STRING`); không có → 0; **id 0x13E = 318 `skill_desc` → 0** (là hàm mô tả, không phải số); `KSG_StringSkipSymbol(&p, '"') 0x08226BC0`, `v1 = KSG_StringGetInt(&p, 0) 0x08226C20`, `SkipSymbol ','`, `v2`, `SkipSymbol ','`, `v3` → `AddMagicAttrib(this, {id, v1, v2, v3})` |
| `0x08226BC0` / `0x08226C20` | `KSG_StringSkipSymbol(&p, ch)`: bỏ khoảng trắng, đúng ký hiệu thì nhảy qua (trả 1), không thì để con trỏ ở ký tự đó (trả 0). `KSG_StringGetInt(&p, def)`: bỏ khoảng trắng, `'-'` rồi khoảng trắng, **không có chữ số → trả `def`**, có → số (âm nếu có `-`), con trỏ dừng ở ký tự đầu không phải chữ số. Hệ quả: `"25.0,3"` → 25, 0, 0 — script trả số thực làm mất hai giá trị sau |
| `0x080EDCC0` | **`KSkill::AddMagicAttrib(this, &attrib)`** chia theo **khoảng id**: (a) **15..25 và 325..338** (`missle_*`) → mảng `+0x174`, đếm `+0x2b4`; (b) **1..12 và 304..322** (`skill_*`): 1 `skill_cost_v` → `+0x98`, 2 → `+0x9c`, 3 → `+0xa0`, 4 `skill_misslenum_v` → `+0xb4`, 5 `skill_misslesform_v` → `+0xc8`, 6 → `+0x50`, 7 `skill_param2_v` → `+0x54` **lấy v2** (giữ lỗi JX1), 8 `skill_skillexp_v` → `+0x11c`, 9 `skill_waittime` → `+0x4c`, 10 → `+0xa4`, 11 `skill_appendskill` → `push_back({v1, v2})` vào vector `+0x694` khi v1 ≠ 0, 12 → `+0x100`; 304..309 `addskilldamage1..6` → `+0x120 + i·12 = {v1, v3, v2}`; 310 `skill_attackradius` → `+0x48`; 311/312/313/314 `skill_*event` → cờ `v1 > 0` ở `+0xe0/+0xdc/+0xe4/+0xe8` và id ở `+0xf4/+0xec/+0xfc/+0xf8` (= v3); 315 `skill_dohurt` → `+0x5c`; 316 `skill_bymissle` → `+0xc4`; 317 `skill_showevent`, 319..322: không làm gì (zone ghi 317 vào `row.show_event` cho chú thích của client — `CLIENT-2.0.md` §10.1; client 2.0 đánh số lệch +6 trong dải này: 310..315 = addskilldamage, 316 = bán kính, 317..320 = sự kiện, 321 dohurt, 322 bymissle, 323 showevent — tên mới là thứ script dùng); (c) **56..75** (`damage_*`) → **ô cố định** của mảng `+0x2b8` (bảng nhảy `0x08258498`): 56/57 → ô 0, 58 → 1, 59/65 → 2, 60 → 3, 61 → 4, 62 → 5, 63 → 6, 64 → 7, 66 → 8, 67 → 9, 68 → 10, 69 → 11, 70 → 12, 71 → 13, 72 → 14, 73 `addskillexp1` → 15, 74 → 16 (v1 = 0 → **id kỹ năng của chính nó** `+0x24`), 75 `seriesdamage_p` → ô 17 chỉ ghi v1; đếm `+0x3f8` chỉ tăng ở ô 0..14; **76..82 bỏ**; (d) **mọi id khác** (13, 14, 26..55, 83..303, 323, 324, 339, 340): `v2 == 0` → `+0x3fc` (tức thời, ghi `v2 = 0`, đếm `+0x53c`), khác → `+0x540` (trạng thái, đếm `+0x680`) |
| `0x080E6FB0` | `KSkillManager::Clear`/dtor: duyệt 2000×64 con trỏ (`+0x8AAC0`) và bảng `+0x107AC0`, gọi dtor ảo |

**Bố cục `KSkill` (0x6A0 byte)**: `+0` vtable; `+0x4..+0x114` bản chép của dòng (bảng cột ở trên
cộng 4; `+0x114` cấp); `+0x11c` kinh nghiệm kỹ năng; `+0x120` 6×{id, v3, v2} `addskilldamage`;
`+0x174` 20 `KMagicAttrib` đạn (đếm `+0x2b4`); `+0x2b8` 20 ô sát thương (đếm `+0x3f8`); `+0x3fc`
20 ô tức thời (đếm `+0x53c`); `+0x540` 20 ô trạng thái (đếm `+0x680`); `+0x694` vector kỹ năng
kèm (`skill_appendskill`). JX1 (`KSkills.h`) chỉ có 8 ô sát thương và không có `addskilldamage`,
`appendskill`, `WeaponSkill`, `IsExpSkill`, `TargetOther`, `TargetNoNpc`, `PeaceCanUse`,
`StatePriority`, `RelativePosType`, `TimePerCastOnHorse`, `Series`.

**Dữ liệu trên máy này**: `settings/skills.txt` tham chiếu 285 tệp `LvlSetScript`; 164 có trong
`D:\ServerLinux\server1\script` (mọi môn phái, `advancedskill.lua`, `special\platina.lua`,
`skillstate.lua`…), **156 tệp thiếu** (307 dòng: đồng hành `partner\*`, sự kiện, `npc\*` có tên GBK,
`special\boss_superskill.lua`…). Hai script của đánh thường `special\长兵物理攻击.lua` /
`远程物理攻击.lua` cũng thiếu trong bản Linux — chuỗi dự phòng `bin/Server` (JX1) có chúng nhưng mọi số
bằng 0 (bản Linux `physicattack.lua` cho `physicsenhance_p = 100`). Zone ghi cảnh báo `skill level
script missing` và kỹ năng đó chỉ có số từ bảng.

## 12. Lõi sát thương và trạng thái — `ReceiveDamage` / `CalcDamage` / `SetStateSkillEffect` (M12 lát B2a, đã kiểm từng dòng)

Zone: `server/zone/src/KNpc.cpp` (đòn đánh, trạng thái, mỗi khung hình), `KSkills.cpp` (thi triển), test
`tests/test_KNpcDamage.cpp` (mọi số tính tay từ lệnh máy). Tất cả là hàm thành viên `KSubWorld` vì một đòn cần
người đánh, mục tiêu, bộ ngẫu nhiên, và cái chết hay đánh lùi động vào thế giới. Số học giữ nguyên bản: `int`,
chia cắt (`imul` số ma thuật), `double` ở chỗ nhị phân dùng FPU (`fistp` chế độ cắt 0xC).

| Địa chỉ | Hàm — đã đọc gì | Cách tìm |
|---|---|---|
| `0x0808A4A0` | **`KNpc::ReceiveDamage(this, nLauncher, nSeries, bIsMelee, pDamage, bUseAR, nDoHurt, eRelation, nSkillId)`** → 1 trúng / 0. Từ chối: `m_Doing` 0xa/0x15, `+0x1694` ≠ 0, kẻ đánh `m_Index` 0, `pDamage` NULL, `invincibility +0x147b`. `addDmg = kẻ đánh +0x1414` (+ `+0x84 add_boss_damage` khi `0x08079750(this) == 3`: `+0x181c` ≠ 0 và không phải người chơi). `(eRelation & 0xc) == 8` (thù): **chặn** `+0x1390 + +0x1408 − kẻ đánh +0x131c` `> g_Random(100)` → `GetSkill(963, 1)` (NULL → 0) rồi `SetStateSkillEffect(this, this, 963, 1, states, n, 1, 0…)`, trả 0; **đòn tăng cường** `+0x140c − this +0x1324` `> rand` → `nMul = +0x1410 + 200` (mặc định 100). `+0x159c = nLauncher`. Tổng ô 3..7: `min += v0`; `max += (type == 63 ? v0 : v2)`; khiên động (`+0x1464 > 0`, ô ≠ 7) `dyn += max(0, (v0+v2)/2)`. **Ngũ hành**: `nSeries` (unsigned) ≤ 4 → `g_IsConquer(0x830ED2C, nSeries, this.+0x28)` (bảng **tương khắc**, không phải tương sinh `0x830ED18`): `p = pDamage[0].v0 − +0x13e8`, hai bên là người/đồng hành → `p −= (cấp this − 10 − cấp kẻ đánh) × pDamage[0].v2` khi dương; `p > 0` → 5 kháng hiện tại (`+0x1a0c/+0x19f4/+0x1a04/+0x19fc/+0x19ec`) `−= p` và `0x0807BAA0(this, kẻ đánh +0x13f0, −p)`; ngược lại `g_IsConquer(this.series, nSeries)` → 5 kháng `+= pDamage[0].v0`; phục hồi cuối hàm (và khi né). `bUseAR` → `CheckHitTarget(this, pDamage[1].v0, +0x125c, pDamage[2].v0)` trượt → phục hồi, log `闪过攻击`, trả 0. `f = addDmg × nMul / 10000.0f`; mỗi ô: `(int)(v × f)`. Ô 3 vật lý (ô 13 `deadlystrike` `> rand` → ×2) qua `CalcDamage(..., 0, bIsMelee, &dyn, &out, nDoHurt, 0)`; `physDealt = máu trước − max(máu, 0)`; kết quả 0 (chết) → bỏ các ô sau. Ô 4 băng (kiểu 2); **đóng băng** `+0x1d8 ≤ 0 && cold.v1 > 0` → `+0x1d8 = (100 − min(+0x1420, [0x830D234] = 0)) × v1 / 100`, `ignorenegativestate +0x1474 > rand` → 0. Ô 5 hoả (1), ô 6 lôi (3), ô 7 độc (4, không khiên động, `min = max`): áp xong và `v0 ≠ 0` → `reduce = +0x1424 − kẻ đánh +0x134c`; `time = reduce > 74 ? v1/4 : (100 − reduce) × v1 / 100`; `dmg ×= (100 − adjust)/100` khi ngũ hành có `adjust`; `0x0807BD60(this, launcher, dmg, time, v2)`. Ô 8 ma pháp (kiểu 5, không kháng). Ô 9/10/11 hút: kẻ đánh `máu/nội/thể += physDealt × v0 / 100` chỉ chặn trên. Ô 12 đánh lùi `v0 > rand` → `KnockBack(this, launcher, v1, v2)`. Ô 14 chí mạng `v0 > rand` → `dmg = (int)((máu/4) × max(0, 100 − +0x1404) / 100.0f)`, ghi `KDamageRecord` khi kẻ đánh là người và this không, `máu −= dmg`, qua ngưỡng máu/4 → `0x08188BB0(this+0x1898)`. Ô 15 choáng: `rate = v0 × (100 − +0x1474)/100 + kẻ đánh +0x1334 − this +0x132c > rand` và `+0x1e8 ≤ 0` → `r = +0x1428 − kẻ đánh +0x12bc`; `+0x1e8 = r > 74 ? nMul×v1/400 : (nMul×v1/100) × (100−r)/100`. Người chơi bị thương → `Abrade(+0x3fc, 1)` (B3); máu không đổi → `0x0807F9D0(this, nDoHurt, kẻ đánh)`; giảm và là người, skill ≠ 203 → `0x080AEBC0(Player, 10)` (B3). Ô 16/17 `addskillexp`: `v0 > 0 && rand ≤ 59` → `0x080E5D90` (B3). Thù: `[0x8FC62A0 + skill] == 0` → `0x08188BB0(this+0x1850)`; `[0x8FC4360 + skill] == 0` → `0x08188BB0(kẻ đánh+0x1874)` (B2b). Log `Cast Damage (min, max)`, `Hit Damage %d` | `fdump.py`, chuỗi log `BlockRate`, `EnhanceHitRate`, `DoStunRate`, `KnockBack: %d%%` |
| `0x08089C90` | **`KNpc::CalcDamage(this, nAttacker, min, max, nType, bIsMelee, int* pShield, int* pOut, nDoHurt, bReturn)`** → 1 sống / 0 chết hay từ chối. Chết/`+0x1184 < 0`/kẻ đánh 0 → 0; `min+max ≤ 0` → 1; `dmg = max−min < 0 ? max + g_Random(min−max) : min + g_Random(max−min)`; `≤ 0` → 1; `ignoredamage +0x18e0` → 0; `res = 0x0807BCD0`. **Khiên tĩnh** `+0x1468 > 0`: `dmg < khiên` → trừ, log `StaticMagicShield: Use=%d Rest=%d`, trả 1; ngược lại `dmg −= khiên`, khiên 0, `RemoveStateSkillEffect(id, 1)` cho mọi id của `[staticmagicshield_v]` (vector `0x8FBFE64`, duyệt ngược). **Khiên động** (`pShield` và `+0x1464 > 0`, `*pShield > 0`): `dmg −= (sum/2) × +0x1464 / *pShield`, tối thiểu 1. `dmg > 20 000 000` → `dmg/100 × (100−res)` không thì `dmg × (100−res)/100`; 0 → 1. `nType ≠ 6`: `bReturn == 0` → trả đòn `melee ? +0x13a4×dmg/100 + +0x13a8 : +0x13ac×dmg/100 + +0x13b0`; `bReturn ≠ 0` → chỉ độc (kiểu 4, `+0x13b4/+0x13b8` khác 0) `ret = dmg×+0x13b4/100 + +0x13b8` (không thì 0) → `CalcDamage(kẻ đánh, this, ret, ret, 6, bIsMelee, 0, 0, 0, bReturn)`. Hai bên kind 1/2 (`0x0808A350`: this và kẻ đánh đều kind 1/2) → `dmg = dmg × [0x8BADF50] / 100` — `[0x8BADF50]` = `g_NpcSet (0x8BACAC0) + 0x1490` = `m_nPKDamageRate`, `KNpcSet::Init 0x080A0810` đọc `\settings\npc\PKRate.ini [PK] rate` (`0x080A0926`, mặc định 20; tệp của máy chủ tham chiếu: 20); **nhân 32-bit `imul esi, edi`** rồi chia 100 (0x51EB851F) → đòn ≥ 107 374 183 tràn số âm (KillPlayer 200 000 000 × 20 → hồi máu thay vì chết) — zone nhân 64-bit, `pk_damage_percent` mặc định 20. `+0x1598 = kẻ đánh`. Kiểu 4 và `+0x13d0` → `nội −= dmg×p/100` (kẹp) log `Posion2Mana`. `dmg > 0` → `sorb = max(+0x1a24, +0x1a28) − kẻ đánh +0x1314`; `dmg −= dmg×sorb/1000`. **Khiên nội** `+0x13d4 > 0`: `m = dmg×p/100`; `nội −= m`; nội âm → nội 0 **và không giảm đòn**; không âm → `dmg −= m`. `0x08078A10`: `dmg += (int)(dmg × (kẻ đánh me2X[this.series] − this X2me[kẻ đánh.series]) / 100.0f)` (series −1 → 0). Nội kẻ đánh `= (int)(nội + dmg × (+0x137c hoặc +0x1380 của this) / −100.0f)`, âm → 0. Bản ghi sát thương: this không phải kind 1/2 và kẻ đánh là người (đồng hành qua chủ `+0x1698` — B3) `Add(min(dmg, máu))`. `*pOut = dmg`; `máu −= dmg`; `dmg > 0`: `nội += +0x13cc×dmg/100` (kẹp), qua ngưỡng `max/4` → `0x08188BB0(+0x1898)`, `0x0807F9D0(this, nDoHurt, kẻ đánh)`. `máu ≤ 0` → `0x08089920(this, kẻ đánh)` (không phải người: máu = 0; `DoDeath 0x080892E0`; người: `KPlayer 0x080B2790` — B3) trả 0; `máu > max` → kẹp; trả 1 | chuỗi `ReturnPosion`, `Posion2Mana`, `StaticMagicShield` |
| `0x0807BCD0` → `0x0807BB20` → `0x08078910` | **kháng theo kiểu**: bảng nhảy `0x08254A34` kiểu 0 vật lý (`+0x1a0c/+0x127c/+0x1a10`), 1 hoả (`+0x19ec/+0x126c/+0x19f0`), 2 băng (`+0x19f4/+0x1270/+0x19f8`), 3 lôi (`+0x1a04/+0x1278/+0x1a08`), 4 độc (`+0x19fc/+0x1274/+0x1a00`) = (yin, max, yan); trừ cặp anti của kẻ đánh `[+0x12c4 + kiểu×8]`, `[+0x12ec + …]`, `[+0x1354 + …]` (ô `+0x1350` là **anti kháng tối đa** — trước ghi "reserved"); `r = max(yin', yan')`; `0x08078910(r, max')` = `r ≤ max ? r : max + (r−max)×(95−max)/400`; chặn 95. Kiểu 6: `+0x1260` (+ `+0x1264` melee / `+0x1268` xa khi `bReturn == 0`), chặn 95. Khác → 0. Bảng `0x082549E4` (`0x08078950`) cùng thứ tự kiểu | `vtab.py` hai bảng nhảy |
| `0x0807CE70` | **`KNpc::AppendSkillEffect(this, bIsPhysical, bIsMelee, pSrc, pDes, nEnhance)`** (`bIsMelee` không dùng): ô 0 `seriesdamage_p` = `+0x13ec + pSrc[17].v0` khi `pSrc[17].type == 75`; ô 1 `attackrating_v` = `+0x1258` (+ `+0x15c4 × p/100` khi `pSrc[0]` 57, + v0 khi 56); ô 2 = `pSrc[1]` khi 58; ô 3..7 = `0x0807C700/0x0807C950/0x0807CD30/0x0807CA90/0x0807CBD0(this, &pSrc[2..6], &pDes[3..7], nEnhance, bIsPhysical)`; ô 8 ma pháp 64 (+ `nEnhance%`); vật lý: ô 9/10/11 = `+0x13dc/+0x13e0/+0x13e4 + pSrc[8/9/10].v0`; ô 12 = `{69, +0x13f8 + pSrc[11].v0, v1, v2}`; vật lý: ô 13 = `+0x13fc + pSrc[12].v0`; **ô 14 (71) = `+0x1400 + pSrc[13].v0`, ô 15 (72) = `{+0x1418 + pSrc[14].v0, v1, v2}` — hai ô chéo nhau so với bảng ProcessFunc (`stun_p 72` cộng vào `+0x1400`, `fatallystrike_p 71` vào `+0x1418`)**, giữ nguyên; ô 16/17 chép `pSrc[15/16]` | |
| `0x0807C700` | vật lý: `pSrc.type 65` → `min' = +0x143c + +0x11bc`, `max' = +0x143c + +0x11c4`; `pDes = {59, (p+100)×min'/100, (p+100)×max'/100}`, `+= nEnhance%`, người chơi `+= min'/max' × 0x080B0D50(Player)/100` (**`0x080B0D50`**: `0x081F92E0` = detail vũ khí đang mặc (0 gần → `+0x1440[particular 0x081F9F70]`, 1 xa → `+0x145c`, khác/−1 → `+0x1460`)); `type 59` → chép + `nEnhance%`; `!bIsPhysical && pDes.v0 > 0` → `+= (+0x1284+100) × +0x120c / 100` (v0) và `+0x1214` (v2) | |
| `0x0807C950` | băng: `type 60 && v0 > 0` → `{60, v0, +0x1430 + v1, v2}` + `nEnhance%` (v0, v2); vật lý → `v0 += +0x11dc`, `v0 ≠ 0` → `v1 = max(v1, +0x1430 + +0x11e0)`, `v2 += +0x11e4`; ma pháp và `pSrc.v0 > 0` → `v0 += +0x121c + f`, `v2 += +0x1224 + f` (**`f = 0x0807C910 → 0x080A7C70 = Player+0x5954 = m_nCurEngergy` — nội công hiện tại của người chơi**, npc 0) | |
| `0x0807CD30` / `0x0807CA90` / `0x0807CBD0` | hoả: `{61, v0, v2 + v2×+0x142c/100}` + `nEnhance%`; vật lý `+= +0x11cc/+0x11d4`; ma pháp `v0 ≠ 0` → `+= +0x123c + f / +0x1244 + f`. Lôi: `{62, v0 + (v2−v0)×+0x1438/100, v2}` + `nEnhance%`; `+= +0x11ec/+0x11f4` hay `+0x122c + f / +0x1234 + f`. Độc: `{63, v0, v1, max(1, (100−+0x1434)×v2/100)}`, `nEnhance > 0` → v0; vật lý → `MixPoisonDamage(pDes, this+0x11f8)`; ma pháp `v0 > 0` → `Mix(pDes, {+0x1248, +0x124c + f, +0x1250 (≤0→60), +0x1254 (≤0→10)})` | |
| `0x08099F90` | **`MixPoisonDamage(pDes, pSrc)`**: `pSrc` đọc `{c2 = +0, d2 = +4, t2 = +8}` (với `m_CurrentPoisonDamage +0x11f8` thì `c2` = ô **type** — không ai ghi, luôn 0); `d1 = pDes.v0, t1 = v1, c1 = v2`; `d1 == 0 || c1 == 0` → chép 4 dword; `d2 == 0 || c2 == 0` → thôi; `v0 = ((c1+c2)×d2/c2 + (c1+c2)×d1/c1)/2`; `v1 = (t2×d2×c1 + t1×d1×c2)/(c1×d2 + d1×c2)`; `v2 = (c1+c2)/2`. Hệ quả: kỹ năng vật lý không độc mang nguyên `addpoisondamage_v` của npc (`{0, v0, 60, 10}`); có độc thì không gộp | `disp 145c/1460` để ra `0x0809A7F0` |
| `0x0807BD60` | **đặt/gộp trạng thái độc `(this, launcher, dmg, time, interval)`**: `time == 0 || dmg == 0` → thôi; `+0x1c8 == 0` → `{+0x1c8 = time, +0x1c0 = dmg, +0x1c4 = interval}`; đang độc và bốn số dương → `time = (t1×d1×i2 + t2×d2×i1)/(d1×i2 + d2×i1)` (FPU, cắt); cap `[0x830D248]` người / `[0x830D24C]` npc (**0 trong bản này**, `0x08062D00` đặt, không ai ghi) `> 0` → `limit = cap×2×i2 − d1×i2/i1`, `d2 ≥ limit` → `limit > 0 ? d2 = limit : giữ nguyên`; `dmg = (d2×(i1+i2)/i2 + (i1+i2)×d1/i1)/2`, `interval = (i1+i2)/2`; luôn `+0x15a0 = launcher`; log `施加状态毒伤害` | |
| `0x0807BAA0` | `(this, enhance, p)`: `x = (enhance − +0x13f4) × 100 / (cấp×8 + 200)`; `d = (x+100) × p / 700`; 5 kháng tối đa `+0x127c/+0x1270/+0x1278/+0x1274/+0x126c += d`; log `MaxRes Modify` | |
| `0x0807ED60` | **`CheckHitTarget(this, AR, def, ignore)`**: `AR < 0` → 0; `def < 0` → 95 %; `sum = AR + def×(100−ignore)/100`; `sum == 0` → 50; `p = AR×100/sum`, `> 95 → 95`, `> 4 ? p : 5`; trúng khi `p > g_Random(100)` | |
| `0x0807F9D0` → `0x0807F780` | `(this, nDoHurt, kẻ đánh)`: `nDoHurt ≤ 0` → thôi; `rate = nDoHurt + kẻ đánh +0x1344 − this +0x133c > rand` → **`KNpc::OnHurt(this, kẻ đánh +0x12b4)`**: `+0x1184 < 0` hay `m_Doing` 9/10 → thôi; `hr = max(+0x1a44, +0x1a48)`; `hr − anti > 99` → thôi; `g_Random(100) ≤ 49` → thôi (**50 % phẳng**, khác `MIN_HURT_PERCENT` JX1); `+0x1474 > rand` → bỏ; `m_Doing 0x12` → bỏ thưởng chạy `+0x14b0`; `m_Doing = 9`, `+0x22c = max(1, (anti − hr + 100) × +0x1914 / 100)`, `SendSyncAction(9, x, y, +0x22c, 0)` (`0x0807A970`, gói 0x56, x/y = Map2Mps mình) | chuỗi `m_CurrentHitRecover:%d - AntiHitRecover:%d` |
| `0x08087940` | **`KNpc::KnockBack(this, launcher, frames, distance)`**: `+0x1184 < 0`, `m_Doing` 0xa/0x18 → thôi; Map2Mps hai bên (`0x080EF710`); cùng chỗ → hướng `+0x147c`; khác → `len = (int)sqrt(dx²+dy²)`, `t = (dy<<10)/len`, dò bảng sin `0x82E12EC` 32 ô (= `g_GetDirIndex`), `dx < 0 → 64 − k`; `+0x147c = dir`; đích `x − (cos[dir]×distance >> 10)`, `y − (sin[dir]×distance >> 10)` (`0x82E12F0` = cos cho x); `0x08081B70(this, &x, &y, &distance, 1)` tìm ô trống trên đường (chưa đọc — zone lấy ô đi được gần nhất); `+0x14a0/+0x14a4 = đích`; `m_Doing 0x12` → bỏ thưởng chạy; `m_Doing = 0x18`, `+0x22c = max(1, frames)`, **`SendSyncAction(this, 0x18, +0x14a0, +0x14a4, max(1, frames), 0)` = `0x0807A970`** → gói **0x56** 22 byte `{0x56, id, doing, x, y, frames, a6}` qua `0x0807A870(…, 0x16, 0x64, 0)` (client 2.0: `CLIENT-2.0.md` §12) | `KSubWorld::knock_back` → `ACTION_KNOCK_BACK` (aim = đích, B4c-4) |
| `0x08086260` | **`KNpc::SetStateSkillEffect(this, launcher, skill, level, pStates, n, time, a8, a9 (byte), a10, a11 (đã phản), a12)`** → −1 / số khung còn lại của nút cũ / 0. `level ≤ 0 || skill ≤ 0` → −1; `n = min(n, 20)`. `statusimmunity +0x13c0 && this ≠ launcher && returnskill_p +0x1470 > 0`: `> rand` → log `反弹诅咒 成功`, `[[0x8FBFDE0]+0]` (= `Data0` mục `[returnskill_p]` của **`attribconstdata.ini`** = 725) `> 0` → `GetSkill(725, 1)` (NULL → trả 0) → `SetState(this, this, 725, 1, states, n, [[0x8FBFDE0]+4] = 25, 0…)`; rồi `SetState(Npc[launcher], this, skill, level, pStates, n, time, a8, a9, a10, **1**, **0**)`; tiếp tục. `ignorenegativestate +0x1474 > 0`, skill 1..2000, level ≤ 63, `GetSkill(skill, level)->IsTargetEnemy()` (vtable +0x2c) → `> rand` → log, trả 0. `[0x8FC2420 + skill]` (id trong `[ignoreskill_p]` Data2..) && `ignoreskill_p +0x146c > rand` → skill `[0x8FBFDD4]` (724, thời gian `+4` = 15) lên chính mình, trả 0. `a11 == 0 && [0x8FC04E0 + skill]` (id trong `[returnskill_p]` Data2..) && `+0x1470 > rand` → phản như trên. `+0x159c = launcher`; người/đồng hành → gói 0x87 (B4). Tìm nút theo skill trong `+0x234`: có → `prev = +0x18`; `time == 0` → `+0x18 = 0`, trả prev; `!a9 && !a8`: cùng cấp → `+0x18 = time`; cấp thấp hơn → không đổi; cao hơn → làm mới `n` ô (`ModifyAttrib(nút, removing 1)`, `ModifyAttrib(mới, 0)`, lưu **âm**) + cấp; `a9 || a8` → làm mới, `+0x14 = level, +0x1c = a9, +0x20 = a10, +0x16c = a12`, `a9` → `+0x18 = time`; trả prev. Không có: `time == 0` → 0; nút mới 0x170 (`+0x164/+0x168` = `KSkill +0x64/+0x68` StateSpecialId/StatePriority), `0x08079240(this, special, priority)` (6 biểu tượng `+0x54`, ô đầu dùng `+0x50` (Init = 6), `+0x4c = 1`), áp `n` ô (removing 0) lưu âm, `AddTail`; trả 0 | chuỗi `反弹诅咒`, `忽略诅咒`, `忽略负面状态(魔法)` |
| `0x0807D310` / `0x0807D5A0` / `0x080792C0` | `RemoveStateSkillEffect(this, skill, bNotify)`: áp mọi ô ≠ 0 của nút (removing 1), gói 0x87 rỗng (level 63, B4), xoá, `+0x4c = 2`. `SetImmediatelySkillEffect(this, launcher, p, n)`: `ModifyAttrib` từng ô. `0x080792C0(npc, skill, states, n)`: `npc+0x19d8 == skill` → ô đầu có type `+0x19dc` được `value[+0x19e0] += +0x19e4` (`KStateModifier`) | |
| `0x0808B610` | **`KNpc::ProcessState`** mỗi khung (khi `+0x1950`, sau `+0x1904++` ở `0x0808BE80`; trả ≠ 0 → khung này **không** chạy AI/lệnh/di chuyển): `+0x1904 % 10 == 0` → ngồi (8) `SitAddLife/Mana = max(1, max×3/1000 × p/100)`; `+0x1190 ≠ 0` → `p == 100 || replenish ≤ 0 ? máu += replenish : += replenish×p/100`; kẹp `[0, max]`; `nội += +0x11a4` kẹp; người: thể `+= +0x11b4` (chạy trừ `[0x8BADF80/84/88]`, ngồi `+ +0x11b0`) kẹp; quái boss `+0x181c` → `0x080873B0(+0x340, +0x358)` (B2b); `+0x244` kỹ năng vũ khí bị động (B3). **Độc** `+0x1c8 > 0`: người độc `+0x15a0` không còn/máu ≤ 0 → 0; `+0x1c8−−`; `+0x1c4 == 0 → 1`; `time % interval == 0` → `CalcDamage(this, launcher, +0x1c0, +0x1c0, 4, 0, 0, 0, 0, **1**)`. **Băng** `+0x1d8 > 0` → `−−`, kết quả **lẻ** → bỏ khung (đóng băng = chậm nửa). **Choáng** `+0x1e8 > 0` → `−−`, bỏ khung. Thuốc máu `+0x1f8`/nội `+0x208`: `−−`, `% 10 == 0` → `+= +0x1f0 × +0x1194/100` (`+0x200 × +0x119c`), chỉ chặn trên. `+0x14cc` (chưa gặp). Danh sách trạng thái `+0x234`: `+0x18 == −1` giữ; `≠ 0` → `−−`; `== 0` → áp 20 ô ≠ 0 (removing 1), xoá, `+0x4c = 2` | |
| `0x0808BE80` | khung hình npc: `+0x1694 ≠ 0` → thôi; `+0x1904++`; `0x08188BB0(+0x182c, index, −1)` (tự động mỗi khung, B2b); `ProcessState` ≠ 0 → thôi; AI/lệnh/trạng thái; **`% 18 == 0`**: người → `+0x1388 > 0 && +0x138c > 0` → `+0x1390 = min(25, 0x0807A0E0(this, 256) / +0x1388 × +0x138c)` (`0x0807A0E0` đếm npc vùng và 8 vùng kề có `0 < 0x0809F370(this, khác) ≤ 256`); `+0x1398` (manatoskill) → `max = max(+0x1a1c, +0x1a20)`, `nội > 0` → `+0x139c = +0x13a0 × (nội×100/max) / 100`, không thì **`+0x139c = +0x13a0 = 0`** | |
| `0x080E9E90` | **`KSkill::CreateMissleMagicAttribsData(this, launcher)`**: `launcher < 0` hay `ClientSend +0x60` → NULL; nút 0x29c: `+0` id, `+4` cấp, `+8` chép states (`+0x540`, đếm `+0x680` → `+0x148`), `+0x14c` = con trỏ tức thời `+0x3fc` (đếm `+0x53c` → `+0x150`), `+0x294` = đếm sát thương `+0x3f8`; `launcher == 0` → chép nguyên `+0x2b8`; không thì `enh = map<skill, int> npc+0x115c[id] (tạo 0) + npc+0x139c + npc+0x1280`, `AppendSkillEffect(npc, IsPhysical +0x30, IsMelee +0x3c, this+0x2b8, nút+0x154, enh)`, `0x080792C0`; mỗi `{id, level}` ở `+0x694`: `lvl = min(level, KSkillList::GetLevel(npc+0x248, id, 1))` (> 0), `sk = manager[id][lvl]`; **`BaseSkill +0xc0 == 0` → theo `ChildSkillId +0xb8`** tới khi có `BaseSkill`; nút con nối đuôi `+0x298` | |
| `0x080EA920` | **`KSkill::Cast(this, launcher, p1, p2, wait, eLauncherType, extra)`**: kiểm npc/đạn, `p1 == −1` → `p2` hợp lệ cùng subworld; `p2 < 0 && p1 < 0` → 0; `wait < 0` → 0 (log); style > 14 → 1; bảng `0x0825843C`: 0 → `0x080ECAC0` CastMissles (bảng dạng đạn `0x08258478` 8 dạng — **B2b**), 1 → 1, 2 → `CastInitiativeSkill`, 3 → `CastPassivitySkill`, 4 → `0x080E8770` tạo npc, 5..13 → 1, 14 → `0x080EA720` đạn tức thời tại toạ độ mục tiêu (`0x080EAB90` **trước**, `KMissleSet::Add(0x836EA5C, sw, x, y, +0x118)`, `CreateMissle(this, launcher, ChildSkillId +0xb8, idx)` (= dòng `missles.txt` `0x830ED80 + id×0x188`), `0x08075710` va chạm, `0x08075210(m, 1)`). **`Cast` trả 1 dù `CastInitiativeSkill` từ chối** | |
| `0x080EAC90` | **`CastInitiativeSkill(this, launcher, p1, p2, wait, a6, a7 (thời gian ghi đè), a8, a9, a10)`**: `p1 ≠ −1` → `p2 = launcher`, cần `TargetSelf +0x88`; `p1 == −1` → `rel = GetRelation(0x8BACAC0, launcher, p2)`: `TargetSelf && rel&2`; không thì `TargetNoNpc +0x8c && kind ≠ 1` → 0; `TargetEnemy +0x78 && rel&8`; `TargetAlly +0x80 && rel&4`; không → 0. Danh sách `0x080EA2D0`; mỗi nút: `ReceiveDamage(Npc[p2], launcher, Series +0x58, IsMelee +0x3c, nút+0x154, IsUseAR +0x40, DoHurt +0x5c, relation +0x90, nút.id)`; trúng → `+0x159c = launcher`; `a7 > 0` → chép states, mọi `value[1] = a7`, `SetState(..., a7, a6, a8, a9, 0, a10)`; không → `SetState(..., states[0].value[1], a6, 0, 0, 0, a10)` (**không kiểm đếm > 0**); tức thời `+0x150 > 0` → `SetImmediately`. `0x080EAB90(this, launcher, −1, p2)`; trả 1 | |
| `0x080E8530` / `0x080EAB90` / `0x080753F0` | `CastPassivitySkill`: đếm states ≤ 0 → 1; chép, `0x080792C0`, `SetState(launcher, launcher, id, cấp, buf, n, **−1**, extra, 0…)`. `0x080EAB90(skill, launcher, p1, p2)`: `StartEvent +0xe0 && StartSkillId +0xf4 > 0` → `lvl = EventSkillLevel +0x100 (−1 → cấp)`, `lvl > 0`, id 1..2000, lvl ≤ 63 → `Cast(sk, launcher, p1, p2, 0, 0, 0)`; luôn `0x080821C0(Npc[launcher], id, cấp, p1, p2)` (bản đồ kỹ năng tự động `+0x18EC`, B2b). `KMissle::ProcessDamage(m, npc)`: `missrate +0x154 > rand` → 0; mỗi nút `+0x184`: `ReceiveDamage(npc, +0xdc, +0x50, byte +6, nút+0x154, byte +0xf, +0xc4, +0x30, nút.id)`; trúng → `SetState` **chỉ khi đếm > 0** (`+0x148`), `SetImmediately` khi `+0x150 > 0`; `+0x158` rest hit `−−` → 0 → `0x08075210(m, 1)`; trả 1 | |
| `0x08075630` / `0x080748A0` / `0x080F2280` | `ProcessCollision(m, range, ?, ?, radius)`: `+0x5c` chu kỳ va chạm (`+0x15c` so `g_CurrentFrame 0x9777F00`); `range ≤ 0 || radius ≤ 0` → 0; `+0x160/+0x164 = +0x6c/+0x70`; bộ lặp `0x080748A0(it, m, radius)` → `0x080EF300(it, 0, subworld, x, y, radius, launcher, relation|0x40)`; `0x080F2280`: kind 2 && cờ 4 → bỏ; `dx²+dy² > r²` → bỏ; cờ 2 && chính launcher → lấy; không có 0x40 → `0x08079200(npc, launcher)` ≠ 0 → bỏ; cờ 0x20 → chỉ người; `GetRelation & cờ` → lấy. Mỗi npc `+0x2c ≤ 0x14`, (`+0x38 == 0` hay `m+0x168 == 0`) → `ProcessDamage`; `m+0xf0 == 2` → dừng — **B2b** | |
| `0x080EA310` | **`KSkill::CreateMissle(this, launcher, missleId, idx)`**: id 1..999, chép mẫu `0x830ED80 + id×0x188` (`0x08074AA0`), `+0xf8 = id`, `+0x50 = Series`, `+0xd0 = cấp`, byte `+0xb` CollideEvent, `+0x58` VanishedEvent, byte `+0xa` FlyEvent, `+0x54` FlyEventTime, `+0xf4 = idx`, byte `+4` ClientSend, `+6` IsMelee, `+8` TargetSelf == 1, `+0x34` StopWhenMove, `+9` HeelAtParent, `+0xf` IsUseAR, `+0xc4` DoHurt, `+0x138` RelativePosType, `+0xd` camp launcher, `+0xe` (người: `Player+0x5a50`), StopWhenMove → Map2Mps launcher `+0x38/+0x3c`; `+0x30 = relation +0x90`; `0x08074850(m)`; thuộc tính `missle_*` (`+0x174`, đếm `+0x2b4`): 15 → `+0x14`, 16 → `+0x28`, 17 → `+0x24`, 18 → `+0x1c`, 19 → `+0x44`, 21 → `+0x154` missrate, 22 → `+0x158` rest hit, 325 → `+0x40`, byte `+0x10`, `+0x44`, 326 → `+0x5c`, 327 → `+0x20/+0xec`, 328 → byte `+0x11`, 329 → `+0x11c/+0x120/+0x124`; melee → `+0x24 = 0x08078DB0(launcher, +0x24)` không thì `+0x28 = 0x08078DC0(launcher, +0x28)`; `+0x11 = 0x0807EE20(launcher, +0x11)`; `+0x154 = 0x08079320(launcher, id, +0x154)` — **B2b** cùng `missles.txt` (`MissleId` `0x082518B1`) | |
| `0x080E7200` (`0x080E7507..`) | **`attribconstdata.ini`** (`settings`): mỗi tên thuộc tính (bảng tên `0x0830E640`, 0x154 tên) một mục `[tên]` `Count`, `Data%d` → vector `0x8FBF4E0 + id×12`; sau đó: `[0x8FBFDE0]` = `returnskill_p` (192): Data0 725, Data1 25, Data2.. → `0x8FC04E0[id] = 1`; `[0x8FBFDD4]` = `ignoreskill_p` (191): 724, 15, … → `0x8FC2420`; `[0x8FBFE10]` = `autoattackskill` (196) → `0x8FC4360`; `[0x8FBFE04]` = `autoreplyskill` (195) → `0x8FC62A0`; `[0x8FBFE64]` = `staticmagicshield_v` (203): 721. Zone: `jxassets export-skills` ghi `attrib_data` vào `skills.json`, `KSkillTable::attrib_data(id)` | `xref` các địa chỉ |
| `0x0809A7F0` | `addphysicsdamage_p` (126) đầy đủ: `kind = |v2|`, bảng nhảy `0x082557E0`: 0..5 → `+0x1440[map]` (map tĩnh `{0..5 → 0..5, 10 → 6}`); 6 → cả 7 ô + `+0x1460` + `+0x145c`; 7 → `+0x145c` (xa); 8 → cả 7 ô; 9 → `+0x1460` + ô 6; 10 → ô 6; > 10 → không. Trước zone thiếu 6..9 | |
| `0x08074190` / `0x080741D0` | `g_IsAccrue(table, a, b) = table[a] == b` với `table = 0x830ED18` (tương sinh `{2,3,1,4,0}`) hay **`0x830ED2C` (tương khắc `{1,4,3,0,2}`) — `ReceiveDamage` dùng bảng khắc** | `fdump 080741D0` |

Hệ đạn (`0x080ECAC0` CastMissles, 6 hàm sinh, `missles.txt`, bay, va chạm, sự kiện) đọc ở §13; kỹ năng tự động, ô trống
khi đánh lùi, `CanCastSkill`, kỹ năng tạo npc ở §14. Chưa đọc (B3): `0x080E5D90` kinh nghiệm kỹ năng, `0x08089920`/`0x080B2790`
chết của người chơi và PK (`[0x8BADF50]` = `PKRate.ini rate` — đã tìm, xem hàng `0x08089C90`).

## 13. Hệ đạn — `CastMissles` / `KMissle` / `missles.txt` (M12 lát B2b, đã kiểm từng dòng)

> Client: server 2.0 không đồng bộ đạn (client tự chạy `CastMissles` từ gói 0x5a); zone mới báo `G2C_MISSLE` khi sinh / bay (mỗi 6 khung) / tan — sai khác có chủ ý, `CLIENT-2.0.md` §11 (M12 lát B4c-3).

Zone: `server/zone/src/KMissle.cpp` (bảng mẫu, sinh đạn 6 hàm, bay, va chạm, sự kiện — đều là hàm thành viên
`KSubWorld`), `include/jx/zone/KMissle.h` (`KMissleTemplate`, `KMissleTable`, `struct KMissle` tên theo `Core/Src/KMissle.h`
cũ, ghi kèm offset JX2), test `tests/test_KMissle.cpp` (14 ca, mọi toạ độ/khung tính tay từ lệnh máy). Go:
`services/pkg/jxold/missle/KMissle.go` + `jxassets export-missles` → `client/assets/missles.json` (441 dòng, 57 cột;
`config/zone.json` `missles_file`).

**Toạ độ.** Đạn của nhị phân giữ `vùng (+0x100) + ô trong vùng (+0x6c/+0x70) + lệch 1/1024 trong ô (+0x78/+0x7c)`;
ô = `KSubWorld+0x24/+0x28` = 32 đơn vị (`CELL_LENGTH`), vùng 16×32 ô (`[0x08258BE4] = 16`, `[0x08258BE8] = 32`).
Zone giữ **ô tuyệt đối + cùng lệch** (`map_x/map_y`, `x_offset/y_offset`); lệch chạy 0..32768 **kể cả 32768**
(`0x08074FFA`: chỉ đổi ô khi `> cellW<<10`), nên đạn đúng mép ô vẫn thuộc ô cũ như nhị phân. Thiếu vùng kề = rơi khỏi
bản đồ. `Mps2Map 0x080EF7F0`: vùng = `((y/(cellH×32)) − top)×cols + (x/(cellW×16)) − left`, ô = `(p − gốc vùng)/ô`,
lệch = phần dư `<<10`; `Map2Mps 0x080EF710`: `p = gốc + ô×32 + (lệch>>10)`. Vị trí npc ở zone là 1/256 → `×4` khi so
1/1024. Bảng sin/cos: `[0x082E12EC] = 0x082E1080` (`g_nSin`), `[0x082E12F0] = 0x082E1180` (`g_nCos`), **cắt** (979, 791,
649… — bảng cũ của zone làm tròn 980/792/650: đã thay bằng bảng nhị phân, `KMath.h`); `g_GetSinCos 0x08074160`
`(bảng, hướng, 64)` → `bảng[hướng]`, −1 ngoài 0..63.

| Địa chỉ | Hàm — đã đọc gì | Cách tìm |
|---|---|---|
| `0x0805D210` / `0x08074300` | **Nạp `missles.txt`** (`0x0830AE20`): mọi dòng có `MissleId` 1..999 → mẫu `0x0830ED80 + id×0x188` (`+0xf4 = +0xf8 = id`), dòng sau đè dòng trước. `0x08074300` chỉ đọc (mặc định 0): `MissleHeight → +0x1c` (`<<10`), `MoveKind +0x14`, `FollowKind +0x18`, `LifeTime +0x24`, `Speed +0x28`, `ResponseSkill +0x2c`, `CollidRange +0x40`, `ColVanish` byte `+0x11`, `IsRangeDmg` byte `+0x10`, `DmgRange +0x44`, `Zacc +0xec`, `Zspeed +0x20`, `MissRate +0x154`, `Param1..3 +0x11c/+0x120/+0x124`, `AutoExplode` byte `+7`, `DmgInterval +0x5c`. 39 cột còn lại (hình, âm thanh, sáng) là của client | `xrefstr MissleId` |
| `0x080EA310` | **`KSkill::CreateMissle(this, launcher, missleId, idx)`**: id 1..999, idx > 0, launcher > 0; chép mẫu `0x08074AA0` (chỉ các ô: byte `+0xb/+0x11/+0x10/+0xa/+7`, `+0x18/+0x14/+0x14c/+0x40/+0x44/+0x1c/+0x24/+0x28/+0x11c..+0x124/+0x54/+0xec/+0x20/+0x5c/+0x154/+0x168/+0x158`, **`+0x74 = +0x1c>>10`**, xoá `+0x60`, `+0xd4/+0xd8`, `+0x88..+0xc0`, byte `+0xc`, `+0x128/+0x12c`, `+0x130/+0x134/+0x13c/+0x140`); rồi từ kỹ năng: `+0xf8` id, `+0x50` Series, `+0xd0` cấp, byte `+0xb` CollideEvent, `+0x58` VanishedEvent, byte `+0xa` FlyEvent, `+0x54` FlyEventTime, `+0xf4` idx, byte `+4` ClientSend (**giữ giá trị 2**), `+6` IsMelee, `+8` `TargetSelf == 1`, `+0x34` StopWhenMove, `+9` HeelAtParent, `+0xf` IsUseAR, `+0xc4` DoHurt, `+0x138` RelativePosType, byte `+0xd = npc+0x220` (camp), byte `+0xe = Player+0x5a50` (người chơi; đối tượng PK — B3, zone 0), `+0x38/+0x3c` = Map2Mps(launcher) khi StopWhenMove ≠ 0, `+0x30` relation `+0x90`, `0x08074850` → `+0xf0 = 0`. Vòng `missle_attribs` (`+0x174`, đếm `+0x2b4`): 15 → `+0x14 = v0`, 16 → `+0x28`, 17 → `+0x24`, 18 → `+0x1c` (**không đổi `+0x74`**), 19 → `+0x44`, 21 → `+0x154`, 22 → `+0x158`, 325 → `+0x40 = v0, byte +0x10 = v1, +0x44 = v2`, 326 → `+0x5c`, 327 → `+0x20 = v0, +0xec = v1`, 328 → byte `+0x11 = v1`, 329 → `+0x11c..+0x124 = v0..v2`; 20/23..25 không đọc. Sau đó: IsMelee → `+0x24 = 0x08078DB0(npc, +0x24)` (trả nguyên); không → `+0x28 = 0x08078DC0(npc, +0x28)` = `slowmissle_b +0x13bc ≠ 0 ? tốc độ/2 : tốc độ`; byte `+0x11 = 0x0807EE20(npc, +0x11≠0)` = cờ ? 1 : `npc+0x141c > g_Random(100)` (**không ProcessFunc nào ghi `+0x141c`** → `KNpcCurrentAttrib::missle_vanish_rate` = 0); `+0x154 = 0x08079320(npc, id, +0x154)` = `npc+0x19d8 == id && +0x19dc == 21 ? +0x154 − +0x19e4 : +0x154` | `fdump 080EA310` |
| `0x08076F00` | **`KMissleSet::Add(set, sw, x, y, param)`**: `sw < 0` → −1; hết ô trống → `puts` "no free missle" → −1; `idx = [set+0].+4`; Mps2Map(x, y) vào `+0x100/+0x6c/+0x70/+0x78/+0x7c`; vùng < 0 → −1 (ô chưa bị lấy); byte `+5 = 0`, `+0xf4 = idx`, `+0xfc = sw`, `+0x74 = +0x1c`, `+0x168 = param` (= `KSkill+0x118`, luôn 0 — bị chép mẫu đè); thêm vào danh sách vùng `0x080E21D0` + danh sách dùng. `0x08076E00` gỡ: `0x08074CF0` (giảm đếm tham chiếu `+0x184` → giải phóng, `+0x184 = 0`, `+0x160/+0x164 = −1`, `+0x50 = 0`, byte `+0xd/+0xe = 0`) | |
| `0x080ECAC0` | **`KSkill::CastMissles(this, launcher, p1, p2, wait, eLauncherType)`**: `launcher ≤ 0` → 0; `ClientSend +0x60 == 2` → 1; ngữ cảnh `[ebp-0x5c]` (`TOrdinSkillParam`): `+0` launcher, `+8` 0, `+0xc` đạn cha, `+0x10` kiểu cha (2 khi đạn bắn), `+0x20` wait, `+0x24` mục tiêu; `MisslesForm +0xc8 > 7` (không dấu) → chỉ `StartEvent 0x080EAB90`. Vị trí/mục tiêu: **`0x080EED70(skill, launcher, p1, p2, &x, &y, 0)`**: `p1 == −1` → Map2Mps(Npc[p2]) và trả p2; `p1 == −2` → giữ (0,0), trả −1; khác → `((p+31 nếu âm)&~31)+16` (tâm ô), trả −1; `x<0||y<0` → log, (0,0). Hướng `0x080EEEC0(lx, ly, tx, ty)` = **`g_GetDirIndex` bản JX2**: `nsin = (dy<<10)/len`, tìm ô cuối của `g_nSin[0..31]` còn `≥ nsin`, lấy ô kề nếu gần hơn, `k == 0 → 0`, `dx < 0 → k`, không → `64 − k` (JX1 dùng bảng nửa bước và `63 − k`: phải 47 nay 48; `KnockBack 0x08087BC6` cũng thế). Bảng nhảy **`0x08258478`**: 0 → `0x080ECBD8` (tường, gen A), 1 → `0x080ECDE0` (thẳng, gen B/C), 2 → `0x080ECCF8` (quạt, gen D), 3 → `0x080ED258` (vòng, gen E), 4 → `0x080ECB4C` (chỉ StartEvent), 5 → `0x080ED0A0` (lưới tại người bắn, giữ mục tiêu), 6 → `0x080ED018` (lưới tại mục tiêu, giữ mục tiêu), 7 → `0x080ED188` (lưới tại người bắn, mục tiêu −1, hướng tới (0,0)). Chi tiết: dạng 0: `p1 == −2` → 0; hướng' = `dir+16 ≥ 64 ? dir−48 : dir+16`; `word [+0x56]` (Param2 nửa cao) ≠ 0 → tại người bắn, không → tại mục tiêu; **kiểu 2 (đạn bắn)**: ghi `ctx+0xc = 2` (lỗi gốc: đạn cha = ô số 2, kiểu 0). Dạng 1: `p1 == −2` → `p2 > 64` → 0, gen B(p2) tại người bắn (kiểu 2 → **gen A** — bản sao lỗi); mẫu con `[0x0830ED94 + ChildSkillId×0x188]` (`+0x14`) `== 7` → cùng điểm → 0, `len = (int)sqrt`, `uy = ((ty−ly)<<10)/len`, `|uy| > 1024` → 0, `ux` tương tự → gen C(dir, l, ux, uy, t); khác → gen B(dir) tại người bắn. Dạng 2: `p1 == −2` → `p2 > 64` → 0, gen D(p2); khác gen D(dir) tại người bắn. Dạng 3: `Param1 +0x50 ≠ 0` → gen E tại mục tiêu, không → tại người bắn. Kiểu 2 mọi dạng: vị trí đạn, hướng `đạn+0x148`, `ctx = {đạn+0xdc, 0, idx đạn, 2, wait, đạn+0xd4}`, `Npc[+0xdc].id == +0xe0` bắt buộc; cuối: `StartEvent(skill, launcher/đạn+0xdc, p1, p2)`, trả 1 | `vtab 08258478` |
| `0x080E8650` | **Độ trễ khởi hành** `(skill, i)` bảng `0x08258424` theo `MslsGenerate +0xcc` (không dấu, > 5 → Wait): 0 → `WaitTime +0x4c`; 1 → `Data +0xd0 + Wait`; 2 → `Data×i + Wait`; 3 → `g_Random(2) == 1 ? i×Data + Wait + g_Random(Data) : i×Data + Wait − g_Random(Data/2)`; 4 → `Wait + g_Random(Data)`; 5 → `n = ChildSkillNum ≤ 1 ? Wait : |i − n/2|×Data + Wait` | `vtab 08258424` |
| `0x080EAFF0` | **Thi triển con** `(skill, ctx, px, py, i)`: `cấp = ChildSkillLevel +0xbc (−1 → cấp)`, `≤ 0` → 0; `ChildSkillId` 1..1999, cấp ≤ 63, `m_pOrdinSkill[id][cấp]` (InstanceSkill); `ctx+0xc ≠ 0` → launcher = đạn cha, kiểu `ctx+0x10`; không → npc, kiểu `ctx+8`; `Cast(sk, launcher, px, py, 0x080E8650(skill,i) + ctx+0x20, kiểu, 0)` → ≠ 0 | |
| `0x080EBB20` | **gen A `CastWall`**`(skill, ctx, dir', x, y)`: `ctx+8 ≠ 0` → 0; `n = ChildSkillNum +0xb4`, `spacing = Param1 +0x50`; list = `0x080EA2D0` (= CreateMissleMagicAttribsData); `offset = −(spacing×n)/2`; mỗi i: `px = x + (cos[dir']×offset >> 10)`, `py = y + (sin×offset >> 10)`; `px<0||py<0` → bỏ **không tăng offset**; `BaseSkill +0xc0 == 0` → con tại (px,py); không → Add(set, sw, px, py, skill+0x118) (< 0 → bỏ, không tăng offset); `word [+0x54]` (Param2 nửa thấp) `== 0` → `+0x148 = dir'`, khác → `dir'−16 (< 0 → dir'+48)`; `+0x144` như thế; CreateMissle(ChildSkillId); `+0xd4 = ctx+0x24` (> 0 → `+0xd8` id), `+0x150 = sw+0x38` (khung), `+0xdc/+0xe0` launcher, `+0xe4 = ctx+0xc`, `+0x2c` id kỹ năng, `+0x64 = 0x080E8650(i) + ctx+0x20`, `+0x24 += +0x64`, `+0x80/+0x84 = px/py`, MoveKind ∈ {100,1,5,7} → `+0xc8/+0xcc = cos/sin[+0x148]`, `+0x184 = list` (ref++); đếm++, offset += spacing. List không ai lấy → giải phóng | `fdump` |
| `0x080EC2F0` | **gen B `CastLine`**: bước `(cos[dir]×spacing, sin×spacing)`, tích luỹ **bắt đầu từ 1×**; `px = x + (acc>>10)`; âm → bỏ (vẫn tăng); con hay Add như A; `+0x148 = dir` nguyên | |
| `0x080EBF00` | **gen C `CastExtractiveLineMissle`**`(skill, ctx, dir, lx, ly, ux, uy, tx, ty)`: `BaseSkill == 0` → con tại (0,0), i = 0; Add tại (lx,ly); `+0x148 = dir`; CreateMissle; MoveKind 7 → `len = (int)sqrt((lx−tx)²+(ly−ty)²)`, `+0x20 (Zspeed) = ((len/speed) − 1) × Zacc / 2`; `+0x64 = trễ(0) + wait`, `+0x24 +=`, byte `+0xc = 1` (bắn chính xác), `+0x80/+0x84 = lx/ly`; `speed == 0` → trả 0 (đạn để nguyên, không tải); `+0x128 = +0x64 + len/speed`, `+0x12c = +0x128 + cellW/speed + 2`; {100,1,5,7} → `+0xc8/+0xcc = ux/uy`; list; trả 1 | |
| `0x080EB150` | **gen D `CastSpread`**`(skill, ctx, dir, x, y)`: `dist = Param2 +0x54`; mục tiêu `ctx+0x24 > 0` (Npc `+4 > 0`, vùng ≥ 0 → Map2Mps): `len = (int)sqrt`; `len == 0` → `(ux,uy) = (cos[dir], sin[dir])`, khác `((t−p)<<10)/len`; `y + (uy×dist>>10) < 0` hay x → 0. `half = n/2`; mỗi i: `b = dir − half×Param1` (âm +64, ≥ 64 −64); có mục tiêu: `a = half×Param1 + 48 (≥ 64 → half×Param1 − 16)`, `vy = (cos[a]×uy − sin[a]×ux)>>10`, `vx = (sin[a]×uy + cos[a]×ux)>>10`; không: `(vx,vy) = (cos[b], sin[b])`; `px = x + (dist×vx>>10)`, `py = y + (dist×vy>>10)`; âm → bỏ (half giữ); con → **tại (x,y)**, half−−; Add thất bại → bỏ (half giữ); `+0x148 = b`, `+0xc8/+0xcc = vx/vy` **luôn**, half−− | |
| `0x080EB720` | **gen E `CastCircle`**: `step = 64/n`, `radius = Param2`; góc `dir + i×step` gói một lần (âm +64, ≥ 64 −64); `px = x + (cos[d]×radius>>10)`, `py = y + (sin×radius>>10)`; âm → bỏ; `+0x148 = d`; vector theo kiểu | |
| `0x080EC690` | **gen F `CastZone`**: `n ≠ 1` → `x0 = x − (cellW×n)/2`, `y0 = y − (cellH×n)/2`; lưới `j, i`: BaseSkill: `Param1 == 1` → `c = n/2`, `(j−c)²+(i−c)² > n²/4` → bỏ; `px = x0 + cellW×i`, `py = y0 + cellH×j`, Add, `+0x148 = dir`, trễ theo chỉ số `j×n+i`; không BaseSkill → con tại ô, chỉ số `j×n+i` (không kiểm tròn) | |
| `0x080EA720` | **Đạn tức thời (style 14)** `(skill, launcher, p1, p2, extra)`: `ChildSkillId ≤ 0` → 0; `p1 == −1` → Map2Mps(Npc[p2]); khác `(x,y) = (p1,p2)` (không làm tròn; kiểm vùng đọc ô stack chưa gán — coi như qua); **StartEvent trước**; list (null → 0); Add; CreateMissle; `+0x150`, `+0xdc`, `+0xfc`, `+0x184`, `+0xe0`, `+0x2c`, `+0x80/+0x84`; `0x08075710` (đòn vùng DmgRange) rồi `0x08075210(1)` (biến mất, có sự kiện); trả 1. Không đặt `+0xd4` | |
| `0x080E2660` → `0x08076950` | **Khung của vùng**: npc (`0x0808BE80`), vật thể, rồi **từng đạn** `0x08076950(m)`: `+0xf4 ≤ 0`, vùng < 0, `+0xdc ≤ 0` → thôi; `Npc[+0xdc].id ≠ +0xe0`, subworld ≠ `+0xfc`, vùng npc < 0 → `0x08075210(0)`; người chơi: `camp +0x220 ≠ byte +0xd` hay `Player+0x5a50 ≠ byte +0xe` → `0x08075210(0)`; mục tiêu `+0xd4 > 0` mà id/subworld sai → `+0xd4 = 0`. `+0x60 ≥ +0x24` (hết đời): trạng thái 2/3 → như thường; khác: `AutoExplode` → `0x08075710` + CollideEvent (`0x080EEB00`) → `0x08075210(1)`, `+0x60++`, trả 1. `+0x60 == +0x64`: trạng thái 2 → `+0x60++`; **`PrePareFly 0x08076550`** thất bại → `0x08075210(1)`; `+0xf0 = 1`. Trạng thái ≠ 1 → `+0x60++`. **`Activate 0x080760E0`**; `FlyEvent` byte `+0xa` và `(+0x60 − +0x64) % +0x54 == 0` (**FlyEventTime 0 = chia 0**, zone bỏ qua): cấp `+0xd0 ≤ 0` → trả 0 **không tăng khung**; `0x08076CE0(mgr, +0x2c, cấp)` (id 1..1999, cấp 1..63) → `0x080EEB30` = sự kiện 2; `+0x60++` | `callers 08076EB0` |
| `0x08076550` | **`PrePareFly`**: kiểu 100 → `+0x134 = (+0x24 − +0x64)/2 + +0x64`; kiểu 3 && `Param2 +0x120 == 0` → Map2Mps(launcher), `y − 50 − speed` (< 0 → 0), Mps2Map: vùng **≤ 0** (`jle`, kể cả vùng 0 = góc trái trên) → giữ chỗ cũ; `StopWhenMove +0x34 ≠ 0` → Map2Mps(launcher) ≠ `+0x38/+0x3c` → 0; `HeelAtParent` byte `+9`: cha `+0xe4 == 0` → vị trí launcher, khác → đạn cha phải cùng `+0xe0` (không → 0), vị trí cha; `mới = mình + cha − (+0x80/+0x84)` → Mps2Map (vùng −1 vẫn ghi → đạn nằm im mãi; zone gỡ cuối khung); trả 1 | |
| `0x080760E0` | **`Activate`**: `+0x34 == 2` và launcher đã rời `+0x38/+0x3c` → `0x08075210(1)`. `Zacc +0xec ≠ 0`: `+0x1c += +0x20`, `< 0 → 0`, `+0x74 = +0x1c>>10`, `+0x20 −= +0xec`. `DmgInterval +0x5c ≠ 0` và `+0x15c ≥ [0x09777F00]` (khung hiện tại) → **chưa tới lượt** (`ecx = 0`, `[ebp-0x34] = 1`), không → `ecx = 1`. **MoveKind 1** (`0x08076247`): `n = speed / [0x082E1B00] = 10`; mỗi bước: `+0x160 == +0x6c && +0x164 == +0x70` (ô = ô va chạm cuối) → **`ecx = 0` từ đó**; `OnFly(m, 10, ecx)`; 1 → `0x08075340` (nếu trạng thái ≠ 1 thì thôi); 2 → `0x08075210(1)` thôi; cờ bước sau = `[ebp-0x34] == 0 && ô đổi trong bước`; cuối `OnFly(m, speed % 10, ecx)`: 1 → `0x08075340` thôi; 2 → biến mất. **Kiểu 0**: `OnFly(speed, ecx)`; 1 → `0x08075340`, còn trạng thái 1 → `OnFly(speed, 0)`; 2 → biến mất. **Kiểu khác**: `ecx = 0` nếu ô = ô va chạm cuối; rồi như kiểu 0. Đổi vùng → nút `0xfa2` (chuyển danh sách vùng, hoãn) | |
| `0x080758E0` | **`OnFly(m, speed, check)`** → 0 bay/1 chạm/2 mất: `speed == 0 && kiểu ≠ 0` → 0; **`TestBarrier 0x080F05C0`** `(sw, −1, vùng, ô, lệch, 0, 0)` → `0x080E0A30` = loại chướng ngại **1 (`Obstacle_Normal`) hay 3 (`Obstacle_Jump`)** → 2 (zone: byte `obstacle.bin` giữ nguyên loại → `== 1 || == 3`); `check`: **`0x08075770`** → −1 (chạm đất): AutoExplode → `0x08075710` + CollideEvent → 2; 1 → 1; `+0xf0 ≠ 1` → 2; theo kiểu: **1, 7**: `dx = +0xc8×speed`, `dy = +0xcc×speed`; **3** (vòng): `a = +0x14c`, `prev = a−1 (−1 → 63)`, `+0x148 = a+16 (> 63 → a−48)`, `r = speed + 50`, `dx = (cos[a] − cos[prev])×r`, `dy = (sin[a] − sin[prev])×r`, `Param1 ≠ 0 → +0x14c = a+1 (> 63 → 0)` không `a−1 (< 0 → 63)`; **4** (xoắn): như 3 với `r = speed + +0x60 + 50`, `Param2 == 0` → đặt lại vị trí = vị trí launcher (vùng/ô/lệch npc); **5** (bám): `+0x11c++`; cũ `> 7` → `+0x11c = 0`, mục tiêu hợp lệ → `+0x88/+0x8c = Map2Mps(mục tiêu)`, `len = (int)sqrt`, `≠ 0` → `+0xc8 = (dx<<10)/len`, `+0xcc = (dy<<10)/len`, `+0x120 = len/speed + 1`, `+0x148 = +0x144 = g_GetDirIndex` (nội tuyến); `+0x120 ≠ 0 && −−+0x120 == 0` → `dx/dy = (+0x88/+0x8c − vị trí) << 10` (nhảy đúng điểm), không → `+0xc8/+0xcc × speed`; **100** (quay lại): `+0x130 == 0 && +0x134 ≤ +0x60` → `+0x130 = 1`, đảo dấu `+0xc8/+0xcc`, `+0x148 −= 32 (< 0 → +32)`; dx/dy theo vector; **khác** (0, 2, 6, 8..99, > 100): (0,0). Rồi **`0x08074F10(m, dx, dy)`** ≠ 0 → 0, không → 2 | `fdump` |
| `0x08074F10` | **`CheckBeyondRegion(m, dx, dy)`** (1/1024): vùng < 0 → 0; `RelativePosType +0x138 ≠ 0` → `+0x13c += dx`, `+0x140 += dy`, mốc **`0x08074D70`** (1 → `+0x80/+0x84`; 2 → launcher; 3 → npc mục tiêu (id/subworld sai → 0); 4 → đạn cha `+0xe4 > 0` hoặc launcher; khác → (0,0) và trả 1) → Mps2Map(mốc + (+0x13c>>10), …); không: `dx == dy == 0` → 1; `|dx| > cellW<<10` hay `|dy|` → 0; `lệch += d`; `< 0` → `+= cellW<<10`, ô−1; `> cellW<<10` → `−=`, ô+1; ô < 0 → vùng trái `+0x40` (+16), ≥ 16 → vùng phải `+0x50` (−16); y: `+0x48` (+32) / `+0x38` (−32); thiếu vùng → 0; ghi vùng/ô/lệch → 1 | |
| `0x08075770` | **`CheckCollision(m)`** → −1/0/1: `+0x74 ≤ 0` → −1; `> 20` → 0; vùng < 0 → −1; `CollidRange +0x40 > 1` → bộ lặp vuông `0x080748A0`/`0x080F2610` (npc có `|Δô| < range` cả hai trục, đúng quan hệ, `npc+0x2c ≤ 20`) → không → 0; `≤ 1`: byte `+0xc` và `+0x128 ≤ +0x60 ≤ +0x12c` (`== +0x12c` → xoá cờ) → **`0x080749A0`** (npc trong ô kề `|Δô| ≤ 1` có `|Δx|, |Δy| ≤ cellW<<10` tính cả lệch) → ≤ 0 → 0; không → **`0x080E20B0(vùng, ô, launcher, relation|0x40)`** (npc đầu tiên đúng ô: byte đầy `vùng+0x8c4+cy×16+cx`, cờ 4 bỏ đồng hành, cờ 2 nhận chính mình, cờ 0x20 chỉ người chơi, cờ 0x40 bỏ `0x08079200`, `GetRelation 0x0809EE50 & cờ`) → 0 → 0. Có → `DmgRange +0x44 == 1` → `ProcessCollision(m, launcher, vùng/ô npc, 1, relation)` không → `0x08075710(m)`; trả `≠ −1` | |
| `0x08075630` / `0x08075710` | **`ProcessCollision(m, launcher, vùng, x, y, range, relation)`** (vùng/x/y/relation **không đọc**): `+0x5c ≠ 0`: `+0x15c ≥ khung` → −1; `+0x15c = khung + +0x5c`; `launcher ≤ 0 || range ≤ 0` → 0; `+0x160/+0x164 = +0x6c/+0x70`; bộ lặp tròn `0x080F2280` từ **ô của đạn** (`dx²+dy² ≤ range²` theo ô, cùng bộ lọc quan hệ, `npc+0x38 ≠ 0 && +0x168 ≠ 0` → bỏ — cả hai luôn 0) → `ProcessDamage` mỗi npc, đếm `≥ 1`, dừng khi `+0xf0 == 2`. `0x08075710`: byte `+4` (ClientSend) ≠ 0 → 0; không → `ProcessCollision(…, DmgRange, …)` | |
| `0x080753F0` | **`ProcessDamage(m, npc)`**: `+0x154 ≠ 0`: `> g_Random(100)` → log "Miss!" → 0; "Hit!"; `+0x184` null → 0; mỗi nút (`+0x298` kế): `ReceiveDamage(npc, +0xdc, +0x50, byte +6, nút+0x154, byte +0xf, +0xc4, +0x30, nút+0)` ≠ 0 → `nút+0x148 > 0` → `SetStateSkillEffect(npc, launcher, nút+0, nút+4, nút+8, đếm, nút+0x10, 0,0,0,0,0)`; `nút+0x150 > 0` → `SetImmediatelySkillEffect`; sau vòng: `+0x158 ≠ 0` → `−−`, `== 0` → `0x08075210(1)`, log "nRestHitCount"; **trả 1 hễ có list** | |
| `0x08075340` / `0x08075210` | **`DoCollision`**: trạng thái 2/3 → thôi; CollideEvent → `0x08076CE0` (id `+0x2c`, cấp `+0xd0`) → `0x080EEB00` (sự kiện 3); ColVanish byte `+0x11` → `DoVanish(1)`, không → `+0xf0 = 1`. **`DoVanish(m, cờ)`**: `+0xf0 == 2` → thôi; cờ && `+0x58` → `0x080EEB60` (sự kiện 4); nút gỡ `0xfa1` (hoãn tới sau khung); `+0xf0 = 2` | |
| `0x080EE810` | **`OnMissleEvent(skill, kiểu, m)`** (`0x080EEB30` = 2 bay, `0x080EEB00` = 3 chạm, `0x080EEB60` = 4 mất): đạn `+0xf4 > 0`, launcher `+0xdc` hợp lệ (`+4 > 0`, id `+0xe0`, subworld `+0xfc`, vùng ≥ 0); cấp `EventSkillLevel +0x100 (−1 → +0x114)`, ≤ 0 → thôi; 2: `FlyEvent +0xdc && FlySkillId +0xec > 0`; 3: `CollideEvent +0xe4 && CollidSkillId +0xfc`; 4: `VanishedEvent +0xe8 && VanishedSkillId +0xf8`; vị trí: `ByMissle +0xc4` → Map2Mps(đạn), không → launcher; id ≤ 1999, cấp ≤ 63; `m_pOrdinSkill` (InstanceSkill); **`vtable+0x10` = `GetStyle` (`0x080EECA0` trả `+0x28`) phải 0** (chỉ kỹ năng đạn mới làm sự kiện); `ByMissle` → `CastMissles(sk, idx đạn, x, y, 0, 2)`, không → `CastMissles(sk, launcher, x, y, 0, 0)` | `vtab 082587A8` |
| `0x080EA920` | **`KSkill::Cast`** với **`eLauncherType 2`**: `launcher < [0x0830CA64]`, `đạn+0xf4 ≥ 0`; `p1 == −1` → Npc[p2] hợp lệ, `đạn+0xfc == npc subworld`; `p1 < 0 && p2 < 0` → 0; wait < 0 → log, 0; bảng `0x0825843C`: 0 → CastMissles(kiểu 2) — **các style khác nhận chỉ số đạn như chỉ số npc** (lỗi gốc; zone từ chối, log) | |

Sai khác có chủ ý của zone (ghi để không tưởng là lỗi): (1) `+0x160/+0x164` của ô mới là −1 (nhị phân: 0 cho ô chưa ai dùng
— chỉ khác khi đạn ở ô (0,0)); `+0x15c` cũ của ô luôn ở quá khứ → đặt 0; (2) đạn `HeelAtParent` rơi khỏi bản đồ bị gỡ
cuối khung thay vì nằm im vô hạn; (3) `FlyEventTime 0` bỏ sự kiện thay vì chia 0; (4) thi triển từ đạn với style ≠ 0 bị từ
chối; (5) `ctx+0xc = 2` của dạng 0 do đạn bắn được **giữ** (đạn con có cha = ô 2) nhưng con của kỹ năng không BaseSkill
bị từ chối thay vì bắn qua "npc số 2"; (6) `Player+0x5a50` (chế độ PK) so với 0 tới B3.

Đọc tiếp ở §14: `0x080E8770` kỹ năng tạo npc, `0x08188BB0` + `0x080821C0` kỹ năng tự động, `0x08081B70` ô trống khi đánh
lùi, `0x080E8AE0` CanCastSkill. Chưa đọc (B3): `0x080E5D90` kinh nghiệm kỹ năng, `0x08089920`/`0x080B2790` chết người chơi
và PK, `0x080F2280`/`0x080EF150` chi tiết đi qua các vùng kề của bộ lặp (zone: lưới ô tuyệt đối), `Player+0x5a50`.

## 14. Kỹ năng tự động, bản đồ thi triển kèm, ô trống khi đánh lùi, `CanCastSkill`, kỹ năng tạo npc (M12 lát B2c, đã kiểm từng dòng)

Zone: `KNpc.h` (`KAutoSkillList`, `KAutoSkillEntry`, `KNpc::auto_skills[5]`, `on_cast_skills`), `KNpcAttribModify.cpp`
(272/195/196/274), `KNpc.cpp` (`trigger_auto_skills`, `knock_back_free_spot`), `KSkills.cpp` (`cast_on_cast_skills`),
`KMissle.cpp` (`barrier_kind`), test `tests/test_KAutoSkill.cpp` (5 ca). **Đính chính §10.1**: `+0x1624`/`+0x129c` **không phải**
`m_AttackRadius/m_CurrentAttackRadius` — `KNpc::Init 0x0807DDCA/0x0807E005` đặt cả hai = 12, `ClearAttrib 0x0807F0CD` chép
`+0x1624 → +0x129c`, không hàm nào ghi bán kính kỹ năng vào đó; chỉ `0x08081B70` (bước tìm ô trống) và `0x08087CF0`
(`+0x12a0 × +0x129c` = quãng lao, `+0x195c = quãng / bước` khung) đọc → zone `KNpcAttrib::step_length` (bán kính kỹ năng
đang chọn của zone (`cur.attack_radius`) là khái niệm JX1 do `KNpcAI` dùng, giữ nguyên).

| Địa chỉ | Hàm — đã đọc gì | Cách tìm |
|---|---|---|
| `0x0807DBD0` (`0x0807E2C6..0x0807E33D`) | **`KNpc::Init`** dựng 5 danh sách qua `0x08188F60(list, npc, tên)`: `+0x182c` "auto cast per n frame" (mỗi khung, `0x0808BEDF` trước `ProcessState`), `+0x1850` 受到攻击自动施放 (bị đánh trúng, `0x0808B507`), `+0x1874` 攻击命中自动施放 (đánh trúng, `0x0808B1D3` — danh sách của **kẻ đánh**), `+0x1898` 生命濒危自动施放 (máu tụt dưới 1/4, `0x0808B13A`), `+0x18bc` 死亡自动施放 (không nơi nào duyệt). Mỗi danh sách 0x24 byte: cây rb `+4..`, `+0x1c` npc, `+0x20` tên; nút 0x3c: `+0x10` khoá `id<<8 | cấp`, `+0x14` %, `+0x18` khung giữa hai lần (MinPerCastTime), `+0x1c` cây con `mục tiêu → khung được bắn tiếp`, `+0x34` cờ "kỹ năng của mình", `+0x38` `== 1` bắn vào mục tiêu đưa sang | `re_scan disp 0x182c,0x1850,0x1874,0x1898,0x18bc` |
| `0x08189000` | **Thêm/bớt nút** `(list, khoá, giá trị, own, at_target)`: `% = byte thấp của |giá trị|`, âm khi `giá trị ≤ 0` (trạng thái bị gỡ đưa số âm), `khung = |giá trị| >> 8`; có nút → `% +=`, `== 0` → xoá (log "AutoSkill(%s) Remove"), khác → `+0x38 = at_target`; chưa có → nút mới `{%, khung, own, at_target}` (kể cả % âm) | `callers` từ 3 ProcessFunc |
| `0x08097420` / `0x080973D0` / `0x08097380` | ProcessFunc **272 `autocastskill`** → `0x08189000(+0x182c, |v0|, v2, v1 == 1, 0)`, rồi `v0 > 0` → **`0x08188A10(list, chỉ số npc, |v0|, khung hiện tại)`**: nút con `[chính mình] = khung + +0x18` (chờ trước lần đầu); **195 `autoreplyskill`** → `(+0x1850, |v0| & 0xffffff, v2, v1 == 1, |v0| >> 24)`; **196 `autoattackskill`** → `(+0x1874, |v0|, v2, 0, 0)`. Không ProcessFunc nào ghi `+0x1898`/`+0x18bc` → hai danh sách ấy luôn rỗng. `ClearAttrib 0x0807F5AC` xoá cây `+0x1830` (mỗi khung) và `0x0807F5F3` xoá bản đồ `+0x18e8`; ba danh sách kia giữ | `jx_linux_attribmod.tsv` |
| `0x08188BB0` | **Duyệt danh sách** `(list, index, target)`: `index == 0` hay npc chủ null → 0; `khung = subworld+0x38`; mỗi nút: cây con có `index` và `khung < giá trị` → bỏ; `% > g_Random(100)` không thì log "Failed!"; `id = khoá >> 8`, `cấp = khoá & 0xff`; `+0x34 ≠ 0` → **`0x080E4540(npc+0x248 KSkillList, id, khung, npc+0x20 cấp)`** phải ≠ 0 (ô kỹ năng có id với cấp > 0, `+0x34` của ô = 0, `khung ≥ ô+0x8` (hết hồi chiêu), `cấp npc ≥ ô+0x30`) — zone: npc `skill_list_level > 0`, người chơi chưa (B3); id 1..1999, cấp 1..63, `m_pOrdinSkill` (InstanceSkill), `GetStyle` phải 14 hoặc ≤ 4; `p2 = +0x38 == 1 ? target : index`; `Cast(sk, npc, −1, p2, 0, 0, 0)` ≠ 0; `+0x34` → **`0x080847B0(npc, id, cấp)`** (đặt hồi chiêu: `cd = vtable+0x58(sk, +0x199c ngựa) − bộ sửa trạng thái (+0x19d8 == id && +0x19dc == 3 ? +0x19e0/+0x19e4)`, `0x080E4640(KSkillList, id, khung, cd)` — B3); gói `0x85` ra client (`0x0807A870`, B4); cây con `[index] = khung + +0x18`. **Đối số thật**: mỗi khung `(+0x182c, chỉ số mình, −1)`; bị đánh `(+0x1850 của mục tiêu, chỉ số mục tiêu, kẻ đánh)`; đánh trúng `(+0x1874 của kẻ đánh, chỉ số **mục tiêu**, kẻ đánh)` — nên "không at_target" của danh sách đánh trúng bắn vào **nạn nhân**; máu tụt `(+0x1898, chỉ số mình, kẻ đánh)`. Hai danh sách của `ReceiveDamage` chỉ chạy khi kỹ năng vừa đánh **không** nằm trong `attribconstdata [autoreplyskill]`/`[autoattackskill]` (`0x0808B4F9`/`0x0808B1AC`) — dữ liệu thật liệt kê chính các kỹ năng tự động ở đó, không thì đòn đáp đòn vô hạn (tràn stack) | `fdump 08188BB0` |
| `0x0809AE60` / `0x080821C0` | **274 `oncastskill`** → bản đồ `npc+0x18ec`: `[|v0|][|v2|] += v1`; `≤ 0` → xoá nút trong (nút ngoài giữ); chưa có mà `v1 ≤ 0` → không tạo. **`0x080821C0(npc, id, cấp, p1, p2)`** (gọi cuối `StartEvent 0x080EAB90`): mỗi `{id2, %}` dưới `id`: `% > g_Random(100)`, `cấp > 0`, id2 1..1999, cấp ≤ 63, `GetStyle ≤ 14` và `(1 << style) & 0x4007` (0, 1, 2, 14) → `Cast(sk2, npc, p1, p2, 0, 0, 0)` + gói `0x85` (p1 == −1 → id của Npc[p2]) | |
| `0x08081B70` | **Ô trống trên đường** `(npc, &x, &y, &quãng, bay)`: `bước = +0x129c` phải 1..32; `quãng ≤ 0` → 0; Map2Mps(npc) = gốc; `len = (int)sqrt`; `== 0` → 0; `bx = (dx×bước<<10)/len`, `by` như thế; `n = min(len, quãng)/bước`; `n ≤ 0` → `(quãng 0, gốc)` trả 1; mỗi bước i: `ax += bx` (1/1024), điểm `(ax>>10, ay>>10)`: **`0x080F1E50(sw, x, y)`** = Mps2Map → **`0x080E0990(vùng, ô)`** (bản đồ `vùng+0x8a4` khoá `(cx, cy)` — script lập, zone không có) có → **dừng tại bước i** trả 1; **`0x080F0530(sw, npc+0x168c, x, y)`** = Mps2Map → **`0x080E0A30(vùng, mode, cx, cy, ox>>10, oy>>10)`**: dword ô `vùng + (cx×32 + cy + 0x28)×4`, `hình = (ô>>4)&0xf`: 3 → qua khi `ox < oy`, 4 → `ox > oy`, 5 → `ox+oy ≤ 31`, 2 → `ox+oy > 32`; qua hay `ô & 0xf == 0` → `mode ≠ 0` (hay `mode == 0 && [0x0830CA2C] == 1`) và `byte vùng+0x8c4+cy×16+cx ≠ 0` (có npc) → **4** không → 0; không qua → `ô & 0xf`; ngoài bản đồ → −1 (byte 0xff > 4 → 0). Bảng `0x08254A48`: 0 → `0x08081DA8` ghi nhớ bước tốt; 1, 2 → `0x08081D62` kết thúc ở bước tốt cuối; 3, 4 → `0x08081D58` `bay` ? đi tiếp (không ghi nhớ) : kết thúc; > 4 → 0. Kết quả `quãng = tốt×bước`, `(x,y) = gốc + (tốt×bx>>10, tốt×by>>10)`. **`KnockBack 0x08087AD5`** gọi với `bay = 1`; trả 0 → **không đánh lùi**. `obstacle.bin` giữ byte thấp của dword ô → zone `barrier_kind` (`KMissle.cpp`) dùng chung cho `TestBarrier` đạn (chỉ 1/3) — trước đó zone so cả byte, sai với ô có hình | `fdump 08081B70 080F1E50 080F0530 080E0990 080E0A30`, `vtab 08254A48` |
| `0x080E8AE0` | **`KSkill::CanCastSkill(this, launcher, &p1, &p2)`** → 0/1 (chưa dùng, B3 khi client xin thi triển): byte `npc+0x1478` ≠ 0 → 0; `*p1 ≠ −1` (điểm/hướng): `TargetSelf +0x88` → đổi thành `*p1 = −1, *p2 = launcher` (tự thi triển), không thì `TargetOnly +0x74` → 0; `*p1 == −1`: `p2` 1..MAX, `rel = GetRelation 0x0809EE50`; TargetSelf && `rel&2` → qua; `TargetNoNpc +0x8c` → Npc[p2] phải người chơi; `TargetEnemy +0x78 && rel&8`; `TargetAlly +0x80` → kind 2 (đồng hành) → 0, `rel&4`; TargetSelf && `rel&2`; `TargetOther +0x7c && rel&1`; không → 0. Người chơi: `vtable+0x50` (WeaponSkill) → `0x08079A90(npc)` (id kỹ năng vật lý theo vũ khí, bảng `\settings\武器物理攻击对照表.txt` cột `DetailType/ParticularType/PhysicsSkillID`, nạp `0x0805F18D`) phải `== +0x24`; `EqtLimit +0xa8 ≠ −2` → vũ khí đang mặc (`Player+0x3fc`: `0x081F92E0` detail, `0x081F9F70` particular): detail 1 → particular+100, −1 → −1, 0 → (particular == 6 ? −1 : particular), khác → particular; phải `== EqtLimit`; `HorseLimit +0xac`: 1 → `+0x199c` (ngựa) phải 0, 2 → phải ≠ 0. Style 4 → đếm npc đã tạo (Player+0x7db0/+0x7db8, cùng byte `+0x50`) `< ChildSkillNum`, `Player+0x7d34` (ô trống) ≠ 0 (không → nhắn `0x0978A2A0`); rồi như 0. Style 0: `MisslesForm +0xc8 > 6` → 1; dạng 0/5/6 → kiểm tầm; 3 → `Param1 == 1` ? tầm : 1; 1/2 → `TargetOnly && *p1 == −1` ? tầm : 1. Style 2/4/14 → tầm; 1/3/5..13 → 1. **Tầm**: `*p1 == −1` → `0x0809F370(launcher, p2)` (khoảng cách) ≤ `vtable+0x48` (AttackRadius); điểm → `(int)sqrt((lx−p1)²+(ly−p2)²) ≤ AttackRadius` | `fdump 080E8AE0` |
| `0x080E8770` | **Kỹ năng tạo npc (style 4)** `(skill, launcher, p1, p2)`: launcher phải người chơi; mỗi trạng thái type **179 `createnpc`** (`+0x540`, 20 ô): `v0` mẫu, `v1` cấp, `v2` thời gian (đều ≥ 0); đếm npc đã tạo cùng byte `+0x50` (Param1) trong `Player+0x7db0` (`+0x7db8` số, nút → `Player+0x7d38 + i×36`, byte `+0x18` = loại) `< ChildSkillNum` không thì 0; `Player+0x7d34 == 0` → nhắn, 0; **`0x0813A770(mẫu, cấp, Series +0x58, subworld, p1, p2, 1, "", 0)`** (NpcSet::Add) → 0 → 0; lấy ô trống (`+0x7dac`, `+0x7da4`), `+0x7d34−−`; tên `"%s [%s]"` (tên chủ, tên mẫu `0x0836EB00 + mẫu×0xb40`) → `npc+0x1505`, `+0x1528 = g_FileName2Id`; nút `{+0 = v2, byte +4 = 1, +8 = launcher, +0xc = +0x14 = id npc mới, +0x10 = chỉ số, byte +0x18 = Param1, byte +0x19 = 0, +0x1c = +0x20 = 0}`; camp `+0x21c` = của chủ; `0x0807B850(npc mới, camp hiện tại chủ)`; `npc mới +0x1828 = chỉ số chủ`. Chưa đọc: đếm ngược `+0` của nút (nơi gỡ npc hết giờ) — B3 cùng sổ nhân vật | **port §16.5** (`KSubWorld::cast_create_npc`, `flush_pending_summons`, `KPlayer::summons`) |
| `0x080E4540` / `0x080847B0` / `0x08099500` | `KSkillList` (B3): `0x080E4540(list, id, khung, cấp)` như trên; `0x080847B0` đặt hồi chiêu như trên; **180 `autoattacknpc`** `0x08099500`: người chơi → `Player+0xb0 = (v0 == 1)`, gói `0xab` ra client (chế độ đánh tự động — không phải danh sách) | |

Sai khác có chủ ý: (1) ~~người chơi chưa có `KSkillList`~~ — B3a (§15): sổ `KSkillList` và hồi chiêu `0x080847B0` đã có, nút "kỹ năng của mình" (`+0x34`) dùng `CanCast 0x080E4540` thật cho cả người chơi; (2) gói `0x85` chưa gửi (B4); (3) bản đồ ô do script `vùng+0x8a4` coi như rỗng; (4) `mode`
của đánh lùi (`npc+0x168c`) và `[0x0830CA2C]` coi là 0 → không kiểm ô có npc; (5) danh sách mỗi khung chỉ duyệt cho npc
còn sống (nhị phân duyệt cả xác, chỉ chặn `+0x1694`).

## 15. Sổ kỹ năng `KSkillList` (`KNpc+0x248`), điểm kỹ năng, kinh nghiệm kỹ năng, nút cộng cấp (M12 lát B3a, đã kiểm từng dòng)

Cách tìm: 15 chỗ tăng bộ đếm `[0x8BC99B4]` (mỗi lần `FindSame`) → mọi hàm tìm ô theo id nằm trong
`0x080E4230..0x080E5D90`; `re_calls callers` từng hàm → đường gọi (gói client, KPlayer, Lua). Tất
cả các thân hàm dưới đây đã đọc trọn bằng `fdump`/`re_elf dis`. Zone: `KSkillList.h/.cpp` (thuần
dữ liệu + luật, phần cần thế giới đưa vào qua `KSkillListHost`), phần người chơi ở `KSkills.cpp`
(`KSubWorld::add_skill_point_request`, `give_skill_exp`, `set_skill_cool_time`, `forbit_skill`,
`load_skills`/`save_skills`, `send_skill_list`/`send_skill_level`), API script ở `ScriptFuns.cpp`.

### 15.1 Bố cục

- **`KSkillList` (0xF38 byte tại `KNpc+0x248`)**: `+0 m_nNpcIndex`; `+4` cờ cấm chung (`ForbitSkill`,
  ô mới sinh lấy giá trị này); `+8` **80 ô × 0x30** (ô 0 không dùng; `FindSame` duyệt 1..79); `+0xf08/+0xf0c/+0xf10`
  `KList` các **`KSkillLevelIncNode`** (typeinfo `0x0825830C`, vtable `0x08258300`, nút 0x18 byte: `+4` next, `+0x10`
  id kỹ năng (0 = mọi kỹ năng), `+0x14` số cấp cộng); `+0xf14..+0xf28` **`std::map<int,int>`** id kỹ năng → phần trăm
  tăng sát thương (= `KNpc+0x115c` mà `CreateMissleMagicAttribsData 0x080E9E90` đọc — B2a gọi là `skill_enhance`;
  nay nằm trong sổ: `KSkillList::enhance`); `+0xf2c..+0xf34` ba dword = 0 (không thấy ai đọc). Ctor `0x080E4950`
  (gọi từ `KNpc` ctor `0x0807E5D0`, `0x080854F0`, `0x0808C200`), dtor phần map `0x080E4920` → `0x0808C1A0`.
- **Ô (0x30 byte, ô i tại `KNpc+0x250 + 0x30·i`)**: `+0` SkillId; `+4` SkillLevel (cấp đã học); `+8`/`+0xc`
  MaxTimes/RemainTimes (chỉ bị ghi 0, không ai đọc); `+0x10` NextCastTime (khung được thi triển lại); `+0x14` độ dài
  hồi chiêu vừa đặt; `+0x18` CurrentSkillLevel (cấp + cộng thêm); `+0x1c` cờ **chỉ-nhờ-cộng** (ô sống nhờ nút cộng
  cấp, không lưu, không xoá hồi chiêu); `+0x20` kinh nghiệm kỹ năng; `+0x24` MaxLevel (của dòng + cộng chuyển sinh);
  `+0x28` ReqLevel của instance lúc thêm; `+0x2c` cờ cấm (1 = không thi triển được). §10.1 cũ ghi `+4 cấp, +0x18 cấp
  hiện tại` — đúng, nay đủ 12 ô.
- **`KSkillManager` (`0x08BC99E0`)**: `m_SkillInfo[id−1]` 0x11c byte: `+0` dòng, `+4` style, `+8` **MaxLevel** (cột
  `MaxLevel` của Skills.txt — không nằm trong khối 0x44 dword chép vào `KSkill`; zone: `KSkillRow::max_level` từ
  `skills.json` `max_level`, không phải một ô của dòng); bảng instance `+0x8AAC0` = `0x08C544A0 + ((id−1)·64 +
  cấp−1)·4`; mọi chỗ đọc đều `table[id][cấp]` rồi `InstanceSkill 0x080E6E10` khi rỗng.
- **vtable `KSkill` `0x082587A8` (đã kiểm từng getter — sửa danh sách §11)**: `+0` `+4` dtor; `+8` GetSkillId (+0x24);
  `+0xc` GetSkillName (+4); `+0x10` GetSkillStyle (+0x28); `+0x14` LoadSkillLevelData; `+0x18` CanCastSkill;
  `+0x1c` GetSkillCostType (+0x9c); `+0x20` GetSkillCost(this, npc) (+0x98); **`+0x24` IsExpSkill (+0x44)**; `+0x28`
  IsTargetOnly (+0x74); `+0x2c` IsTargetEnemy (+0x78); `+0x30` IsTargetAlly (+0x80); `+0x34` IsTargetOther (+0x7c);
  `+0x38` IsTargetObj (+0x84); `+0x3c` IsTargetSelf (+0x88); `+0x40` IsTargetNoNpc (+0x8c); **`+0x44` PeaceCanUse
  (+0x94)**; `+0x48` GetAttackRadius (+0x48); **`+0x4c` IsAura (+0x38)**; **`+0x50` IsWeaponSkill (+0x34)**;
  **`+0x54` GetSkillLevelUpScriptId (+0x10c)**; **`+0x58` GetTimePerCast(this, bHorse)** (+0xa0, ngựa +0xa4);
  **`+0x5c` GetReqLevel** (WORD +0x6c); `+0x60` = hàm rỗng (`0x080EEBF0`, `KThiefSkill` ghi đè).

### 15.2 Các hàm của sổ (`KSkillList.cpp`)

| Địa chỉ | Hàm (tên theo `Core/Src/KSkillList.h`) | Nội dung đã kiểm |
|---|---|---|
| `0x080E4290` | `FindSame(id)` | id 0 → 0; duyệt ô 1..79, ô đầu có id → chỉ số, không → 0 |
| `0x080E42E0` | `FindFree()` | ô 1..79 đầu tiên có id 0, không → 0 |
| `0x080E43F0` | `GetLevel(id)` | `+4`, > 63 → 0 |
| `0x080E4440` | `GetCurrentLevel(id, bWithInc)` | `+0x18` (> 63 → 0); `bWithInc == 0` → trừ tổng `+0x14` các nút có id = id hoặc 0. `0x080E9E90`, `0x080848B0`, Lua `GetCurrentMagicLevel` gọi với 1; `GetSkillNextExp` với 0 |
| `0x080E43B0` | `GetAllInc()` | `+0x14` của nút id 0 (Lua `GetAllSkillCurInc`) |
| `0x080E4380` | `GetCount()` | số ô có id |
| `0x080E4B30` | `GetCount(bAll)` | ô 0..79, id 1..2000 có instance cấp 1; `bAll == 0` bỏ dòng `WeaponSkill`, `IsExpSkill` và ô cấp 0 (Lua `GetSkillCount`) |
| `0x080E4C00` | `GetTotalLevel()` | tổng `+4` các ô không WeaponSkill/IsExpSkill (Lua `GetTotalSkill`) |
| `0x080E4540` | **`CanCast(id, khung, cấp npc)`** | ô có `+0x18 > 0`, `+0x2c == 0`, `khung ≥ +0x10` (so không dấu); `cấp ≤ 0` → 1, không thì `cấp ≥ +0x28` |
| `0x080E44D0` | `IsCooling(id, khung)` | `khung < +0x10` (không ai gọi) |
| `0x080E4640` | `SetNextCastTime(id, khung, cd)` | `+0x14 = cd; +0x10 = khung + cd` |
| `0x080E46A0`/`0x080E46F0` | `GetNextCastTime`/`GetCoolDownTime` | `+0x10` / `+0x14` (đồ mặc `0x081FEE60` đọc) |
| `0x080E4740` | `ReduceCoolTime(id, n)` | `+0x10 > n` (không dấu) → `−= n`; `n < +0x14` → `+0x14 −= n` — ProcessFunc **288..290 `reduceskillcd1..3`** `0x08097250`: `v0 ≥ 0` → gọi `(v0, v2)`, `v0 > 0` → gói 0xdd 21 byte `{v0,v1,v2, id npc}` cho xung quanh (B4) |
| `0x080E47B0` | `ClearCoolTime()` | ô 1..79 không chỉ-nhờ-cộng, có id, `+0x10 > khung` (`subworld+0x38` của `Npc[+0]`) → `+0x10 = +0x14 = 0` (DoSkill khi `+0x1384 > g_Random(100)`) |
| `0x080E45C0`/`0x080E4610` | `IsForbidden(id)` / `SetForbidAll(v)` | `+0x2c == 1`; `+4 = v` và mọi ô có id `+0x2c = v` |
| `0x080E4310` | `SetNpcSkill(no, id, cấp)` | cấp > 0, no 1..79, id > 0: `+0 = id, +0x18 = +4 = +0x24 = cấp, +8 = +0xc = 0` (mẫu npc: Skill1..4 vào ô 1..4 ở `KNpc::Init`; `0x08085250` đặt ô 5 từ mẫu `+0x10fc/+0x1100`) |
| `0x080E5420` | **`Add(id, cấp, exp, maxTimes, remainTimes, maxLevel)`** | `cấp < 0`/`id ≤ 0` → 0; `maxLevel ≤ 0` → `m_SkillInfo[id−1]+8`; instance `(id, cấp)` nếu cấp 1..63; `0x08078A80`; người chơi chuyển sinh (`Player+0x86b8 ≠ 0`) → `max += Player+0x8600`; có ô: `delta = cấp − +4`, `+0x1c = 0, +8 = +0xc = 0`, `IncreaseLevel(i, delta)`, `+0x20 = exp, +0x24 = max, +0x28 = ReqLevel(instance) hay 0`; không: ô trống (không → 0) `+0 = id, +4 = +0x18 = +0x1c = 0`, `IncreaseLevel(i, cấp)`, `+8 = 0, +0x20 = exp, +0xc = 0, +0x24 = max, +0x2c = list+4, +0x28 = ReqLevel` → trả chỉ số ô |
| `0x080E52D0` | **`Remove(id)`** | id 1..2000, có ô: `IncreaseLevel(i, −cấp)`, `+0x20 = 0`; `+0x18 > 0` → `+0x1c = 1` (ô chỉ-nhờ-cộng), không → `+0 = 0` (Lua `DelMagic`, `0x0809E070`, `0x08163C30`) |
| `0x080E5010` | **`IncreaseLevel(i, delta)`** | i 1..79 (không → 0), delta 0 → 1; `old = +0x18`; delta > 0 và cấp 0 → `+0x18 += GetAllInc()` (học mới nhận ngay cộng chung); delta < 0 và cấp+delta == 0 → `+0x18 −= GetAllInc()` (bỏ học trả lại); `cur = delta + +0x18`; `+4 += delta`, `+0x18 = cur`, **`+0x20 = 0`**; `cur == 0` → instance cấp 1, khác → id 1..2000, cur 1..63, instance cấp cur (rỗng → 0); style `≤ 14 && (1<<style) & 0x400f` → `0x080E4CA0(i, old, +0x18)`; **style 3**: `+0x18 > 0` → cấp npc ≥ ReqLevel → `Cast(sk, idx, −1, idx, 0, 0, 1)`; `+0x18 ≤ 0` → `RemoveStateSkillEffect(npc, id, 0)`; trả 1 |
| `0x080E4CA0` | **`UpdateEnhance(i, cấp cũ, cấp mới)`** | dòng phải style 0..3/14 (theo `m_SkillInfo+4`); instance cấp cũ (1..63) và cấp mới (1..63): 6 bộ `+0x120 + k·12` (`addskilldamage1..6`: `{id, giá trị, tham số}`) id > 0 → `map[id] −= giá trị cũ`, `+= giá trị mới` (`operator[]` tạo nút 0). Gọi từ `0x0807EE60` ClearAttrib, `0x0809F560`, `0x080A01F0`, `0x08165750` |
| `0x080E56A0` | **`ChangeCurrentLevel(i hoặc 0 = mọi ô, delta)`** | mọi ô: id > 0 && `+4 > 0`: `cur = min(64, +0x18 + delta)`; `cur ≤ 0` → instance cấp 1, style 3 → gỡ trạng thái; `cur > 0` (≤ 63, instance cấp cur) style 3 và cấp npc ≥ ReqLevel → Cast; `+0x18` đổi → instance cấp 1 (rỗng → **trả 0**) style mask → `UpdateEnhance`. Một ô: `cur > 64 → 64`; `cur > 0`: id hợp lệ, **`cur == 64 → trả 0`** (giữ nguyên lỗi), instance (rỗng → 0), style 3 → Cast; đổi → UpdateEnhance; `cur ≤ 0`: style 3 → gỡ trạng thái; **`+0x1c == 1` → xoá ô** (`+0, +4, +8, +0xc, +0x20, +0x18, +0x1c = 0`; giữ hồi chiêu/max/req/cấm) |
| `0x080E5BF0` | **`AddSkillLevelInc(id, delta)`** — ProcessFunc **139 `allskill_v`** `0x080993A0`: `(list, id = v2, delta = v0)` | delta 0 → 0; `|id|`; id ≠ 0 không có ô → ô trống (không → 0) `{id, cấp 0, cur 0, +8/+0xc = 0, +0x1c = 1, +0x2c = list+4}`; nút cùng id: `+0x14 += delta`, `ChangeCurrentLevel(i, delta)`, tổng 0 → gỡ + delete; không: `new KSkillLevelIncNode{id, delta}` vào KList, `ChangeCurrentLevel(i, delta)` (id 0 → i = 0 = mọi ô). Đồ mặc gọi từ `0x08157A20/0x08157B50/0x08158240/0x081584B0` (M11 chưa đọc), Lua `AddTempMagic 0x0812C1B0` |
| `0x080E4A00` | `CastPassivesAtLevel()` (KPlayer::LevelUp `0x080AFCBC`, sau gói thuộc tính) | ô 1..79 có id, `+0x18 > 0`: id/cấp ngoài 1..2000/1..63 hay không instance → **thôi hẳn** (trả 0); style 3 và **`cấp npc == ReqLevel`** → Cast(… 1) |
| `0x080E5370` | `Rollback()` (Lua `RollbackSkill`) | ô 0..79 có id, instance cấp 1 không WeaponSkill/IsExpSkill: `tổng += +4`, `IncreaseLevel(i, −(+4))` → trả tổng (Lua chỉ gửi gói 0x5e `{0,0,điểm,0}`, **không** cộng điểm lại) |
| `0x080E4F20` | `GetExpPercent(i)` | `+0x20 > 0`, id 1..2000, cấp 1..63, instance, `IsExpSkill`; `need = instance+0x11c` (`skill_skillexp_v`); `exp ≥ need` (không dấu) → 1024, không → `(exp<<10)/need` (64 bit) |
| `0x080E5D90` | **`AddSkillExp(&attrib, bPercent)`** — `attrib = {type, v0 id, v1 exp, v2 cờ}` | npc index hợp lệ; loại 1 → `Player` (`0x08078A80`), loại 2 (đồng hành) → chủ qua `npc+0x1698` (`+4 == 1` → `+8`), khác → không ai; id 1..2000, ô có `+4` 1..63, instance cấp đó, IsExpSkill; `oldPct = GetExpPercent`; `need = +0x11c`; `add = bPercent ? need·v1/10000 (64 bit) : v1`; **`+4 < MaxLevel` (không dấu)**: `exp = add + +0x20`; tràn âm: add < 0 → `+0x20 = 0` (need ≤ 0 mới lên cấp), add ≥ 0 → `+0x20 = 0x7fffffff` rồi lên cấp; thường `+0x20 = exp`, `need ≤ exp` → **`+0x20 = need`, `v2 & 1 == 0` → `IncreaseLevel(i, 1)`** (xoá exp về 0); người chơi → `ExecuteScript(Player, vtable+0x54 (LevelUpScript), "OnLevelUp", +4 + 1, 0)`; đồng hành → `0x080AD300(..., "OnLevelUp", 0, 0x825597c, +4+1, chỉ số đồng hành+1)`. Rồi (cả khi đã tối đa): không có Player → 0; `newPct`; cấp không đổi và `((newPct ^ oldPct) & 0xfff8) == 0` → 0 (thanh nhích < 8/1024 không báo); gói **0x5e** `{byte 0x5e, id, cấp, Player+0x5928, word pct}` 15 byte (`0x080A8400`); cấp không đổi → 0; đổi → thông báo `[0x978a290]` (chuỗi ngôn ngữ, tên kỹ năng) `0x080A8510` → 1 |
| `0x080E48D0` | `Serialize(out)` | ô 0..79 có id, `+0x1c ≠ 1`: `{int16 id, int16 cấp, int32 exp}`; trả số bản ghi (`KPlayer 0x080BF340`: ghi vào `TRoleData+[0x187]`, đếm `+0x176`, mốc `+0x183`) |
| `0x080C0240` / `0x080C0390` | `KPlayer::LoadPlayerFightSkillList` / `...LifeSkillList(this, role, &pBuf, &nParam)` | khối `[role+0x183]..[role+0x17f]` (và `[+0x187]..[+0x183]`) chia 8: mỗi bản ghi → `Add(list, id, cấp, exp, 0, 0, 0)`, 10 bản ghi mỗi lần gọi; lệch → log `"(ERR) [PLAYER] Load DBSkillData MisMatch!"` → −1 |
| `0x080E4820` | `GetNext(&pos, out)` (`0x081D1C30` gửi sổ cho client) | từ ô `*pos`: id > 0 và `+0x1c == 0` → chép 12 dword, `*pos = i+1` |

### 15.3 Người chơi và gói tin

| Địa chỉ | Nội dung |
|---|---|
| `0x080DA560` ctor `KProtocolProcess` | ô = **(disp − 0x18)/8** (đính chính B4d-1: mảng 8 byte từ `+0x18`; client 2.0 gửi hào quang byte 0x6f = 111 → disp 0x390 `0x080DC460`): **77 → `0x080DD130` NpcSkillCommand** (disp 0x280; sổ cũ ghi 80), 78/79 đi/chạy, 76 xin npc, 81 nhảy, 82 nói, … (theo thứ tự ctor cũ: 73 RemoveRole, 76 NpcRequest, 77 ObjRequest, 169 mới, 78 walk, 79 run, **80 skill**, 81 jump, 82 talk…) |
| `0x080DD130` | **`NpcSkillCommand(this, playerIdx, pMsg)`**: gói `{byte, int p1, int id, int p2, int sync}`; id 1..1999 (`id−1 ≤ 0x7cf`, ≠ 2000); `0x080A79B0(Player, byte proto, sync) ≠ 0` (kiểm đồng bộ); instance cấp 1; **`vtable+0x4c IsAura == 0`** (khí công không thi triển bằng gói này); `p1 ≥ 0`; `0x080AEBC0(Player, 1)`; npc index 1..max; `p2 ≥ 0` → `SendCommand(npc, 5, id, p2, p1, 0, 0x12)` (p2 = x hoặc −1 theo p1?); `p2 == −1` → `idx = 0x080B12C0(Player, p1)` (id → chỉ số npc) > 0 → `SendCommand(npc, 5, id, −1, idx, 0, 0x12)` |
| `0x0809B750` | **`KNpc::SendCommand(this, 5, id, p2, p3, p4, p5)`** — chỉ nhận lệnh 5 (do_skill): **`FindSame(+0x248, id) ≠ 0`** (npc phải có kỹ năng trong sổ); `+0x171c` (đầy) == 0; vòng 5 ô 24 byte tại `+0x169c + 24·[+0x1718]`: `{5, id, p2, p3, p4, p5}`; `+0x1718 = (+1) % 5`; bằng `+0x1714` → `+0x171c = 1` |
| `0x0809B9E0` | **`KNpc::ProcessCommand`**: có lệnh (`+0x171c` hay `+0x1714 ≠ +0x1718`) → `0x0809B840` → 2: bỏ lệnh (`0x0809B4B0`); 0: `p2 == −1` → mục tiêu `p3 > 0`: `Npc[p3]+0x118c ≥ 0`, `m_Doing ≠ 10`, vùng > 0 (không → `0x08080030` dừng, bỏ lệnh); `dist = 0x0809F370(p3, +4)`; `dist ≤ +0x12a8` (bán kính kỹ năng hiện tại) → `0x08088350(this, −1, p3)` + bỏ lệnh; `≤ +0x12a8 + 300` → đi tới (`0x080EF710` bước đường, `0x0807B780`) **giữ lệnh**; xa hơn → bỏ; `p2 ≠ −1` → `0x08088350(this, p2, p3)` + bỏ; 1: giữ lệnh; rồi `0x0809B510` |
| `0x0809B840` | **kiểm lệnh** `(this, 5, id, p2, p3, p4, p5)`: lệnh ≠ 5 → 2; `p5 ≤ 0`/vùng < 0 → 2; **`+0x194c == 0` → 1** (đang bận, chờ); id 1..1999, instance cấp 1; **`+0x168c == 0` (chưa chiến đấu) → `vtable+0x44 PeaceCanUse` phải ≠ 0**; `FindSame` → `0x08086D90(this, ô)`; `0x080848B0` (kỹ năng hiện tại) ≠ 0; `CanCast(list, id, khung, 0)`; `vtable+0x18 CanCastSkill(sk, +4, &p2, &p3)`; người chơi → `0x08078B10(this, loại, phí, 1)` (chỉ kiểm) → 0 = được |
| `0x08086D90` | **`KNpc::SetActiveSkill(ô)`**: ô 1..79, `+0 ≠ 0`, `+0x18 ≠ 0`, `+0x194c ≠ 0`; `+0x240 = id`; id 1..1999, `+0x18` 1..63 → instance → **`+0x12a8 = GetAttackRadius`** (bán kính kỹ năng hiện tại — `+0x129c` là bước đi, §14) |
| `0x080848B0` | `KNpc::GetCurrentSkill()`: `GetCurrentLevel(+0x240, 1)` > 0, id 1..1999, ≤ 63 → instance |
| `0x08088350` | **`KNpc::CastSkill(this, p1, p2)`** (đường thật của lệnh 5; B3b sẽ port): vùng ≥ 0, `m_Doing ≠ 5`; `sk = GetCurrentSkill`; người chơi: `+0x168c == 0 → vtable+0x44 PeaceCanUse` phải ≠ 0; `+0x1908 > 0 → 0x08201940(Player+0x3fc, 0)` (đồ mặc, chưa đọc); `p2` 1..max → `+0x1594 = p2`; `CanCast(list, +0x240, khung, +0x20)` không → `+0x1594 = +0x15a4 = 0`, `0x08080030` dừng; `CanCastSkill(sk, +4, &p1, &p2)` (sửa p1/p2 tại chỗ); người chơi: `phí = vtable+0x20(sk, this)`, `loại = vtable+0x1c` → `0x08078B10(this, loại, phí, 0)` (trừ thật) không → dừng; `+0x19a0 > 0 → 0x0807D4C0`; **gói 0x5a 25 byte** `{0x5a, p1, id, p2 (id npc mục tiêu khi p1 == −1, cùng subworld), id mình, GetCurrentLevel(id,1), 0}` `0x0807A870(this, gói, 0x19, 0x64, 0)`; `+0x14ac = +0x14a4 = p2; +0x14a8 = +0x14a0 = p1`; style 14 hay ≤ 4 → **`0x08088150(this, sk, p1, p2)`**; 13 → `vtable+0x60` |
| `0x08088150` | **`KNpc::DoSkill(this, sk, p1, p2)`**: `anim = sk+0x2c CharAnimId`; **style 1** → `0x08087F70(this, sk)` (chưa đọc — B3b) 0 → thất bại (`+0x1594 = +0x15a4 = 0, +0x194c = 1, 0x08080030`); `sk+0x30 IsPhysical == 0`: `anim == 14 → +0x22c = 0` không thì `+0x22c = +0x191c·100 / (max(+0x1a3c, +0x1a40) + 100)`; `m_Doing == 0x12` → `+0x194c = 1; +0x128c −= +0x14b0; +0x14b0 = 0`; `m_Doing = 6`; vật lý: `+0x22c = +0x1918·100 / max(1, max(+0x1a34,+0x1a38)+100)` (anim 14 → 0); `m_Doing = 7`; chung: **`+0x1384 > g_Random(100)` → `ClearCoolTime`** (thuộc tính "xoá hồi chiêu"); `+0x194c = 0`; `+0x230 = 0`; trả 1 |
| `0x080847B0` | **`KNpc::SetSkillCoolTime(this, id, cấp)`**: `mod = 0`; `+0x19d8 == id && +0x19dc == 3 (skill_mintimepercast_v)` → `mod = +0x199c (ngựa) ? +0x19e0 : +0x19e4`; id 1..1999, cấp 1..63, instance → `cd = vtable+0x58(sk, +0x199c) − mod` → `SetNextCastTime(list, id, khung, cd)` (gọi sau kỹ năng tự động `0x08188BB0` "của mình", `0x08085020`) |
| `0x08078B10` | `KNpc::CostSkill(this, loại, phí, bChỉKiểm)`: không phải người chơi → 1; loại 0 nội lực `+0x11a0`, 1 thể lực `+0x11a8`, 2 sinh lực `+0x118c`, khác → 0; `cur ≥ phí`; chỉ kiểm → 1; trừ → 1 |
| `0x08078A80` | `KNpc::GetPlayerIdx()`: loại 1 → `+0x1908` |
| `0x080DCB20` | handler gói **cộng điểm kỹ năng** (`c2s_playerapplyaddskillpoint`): `0x08176160(Player+0x7d00) == 2` → từ chối (chuỗi `[0x978a91c]`), không → **`jmp 0x080BD460`** |
| `0x080BD460` | **`KPlayer::AddSkillPoint(this, pMsg{+1 id, +5 điểm})`** theo thứ tự: `i = FindSame` (0 → về, không gói); `cấp = +4`; id 1..1999; **`+0x1c == 1` → về**; instance cấp 1; `IsExpSkill == 1` → bỏ kiểm điểm, `== 0` → `Player+0x5928 ≥ điểm`; instance cấp `cấp` (0 → cấp 1); **`vtable+0x54` LevelUpScript ≠ 0 → `ExecuteScript(Player, script, "main", điểm, 0)`**, đọc lại `+4`, gói 0x5e `{id, cấp, điểm còn, GetExpPercent}` → về; style 13 → về; > 13 khác 14 → gói (pct 0) về; 4..12 → gói về; 0..3/14: `IsExpSkill` → chuỗi `[0x978a118]` (không gói) về; `MaxLevel == 1` → về; `new = điểm + cấp`; `addon = chuyển sinh ? Player+0x8600 : 0`; `new > MaxLevel + addon` → chuỗi `[0x978a120]`, **gói** (cấp cũ, pct 0); `new > cấp nhân vật + 1 − ReqLevel (word +0x6c)` → chuỗi `[0x978a124]` (`cấp + điểm + Req − 1`), **gói**; chuyển sinh: `k = new − MaxLevel` 1..5 và `cấp nhân vật < [0x0830CA14 + 4k]` → chuỗi, về (không gói); `IncreaseLevel(i, điểm)` 0 → về; **`Player+0x5928 −= điểm`**; gói 0x5e `{id, cấp + điểm, điểm còn, 0}` |
| `0x080B2950` | `KPlayer::ForbitSkill(byte v)`: `SetForbidAll(list, v)`; gói **0x63** 21 byte (`{0x63, word 0x14, byte 1, byte 1, dword 4, v}`) |
| `0x080AE9E0` | `KPlayer::SetAForbitSkill(id, v)`: ô có → `+0x2c = v`; gói 0x63 25 byte (`{…, dword 8, id, v}`) |
| `0x0808AA38` | **ReceiveDamage, ô 16/17 `addskillexp1/2`** của `pDamage`: `v0 > 0`, `g_Random(100) ≤ 59` (6/10); `v2 & 2` → nạn nhân là người chơi (hoặc kẻ đánh là đồng hành) → **sổ nạn nhân**; không → kẻ đánh loại 1/2 → sổ kẻ đánh; `AddSkillExp(list, &attrib, 0)` |
| `0x0807F341` | `KNpc::ClearAttrib`, phần sổ: ô 1..79 có id, `+0x18 > +4` → `UpdateEnhance(i, +0x18, +4)`, `+0x18 = +4` |
| `0x08108B70`/`0x08108B20` | Lua `SetSkillMaxLevelAddons(n)` (`n ≤ 99` → `Player+0x8600`) / `Get…` |
| `0x0830CA14 + 4k` | 5 ô `.bss` `0x0830CA18..0x0830CA28` (`AddSkillPoint 0x080BD922`: `k = cấp mới − MaxLevel`, `k−1 ≤ 4` → `cấp nhân vật < [0x830CA14 + 4k]` → từ chối). **Đã kiểm 2026-09-18**: không hàm nào ghi khối `0x0830CA08..0x0830CA28` (`xref` chỉ có 4 hàm đọc `0x080C4220/0x080C42A0/0x080C43A0/0x080C4420` dùng `0x830CA08/10/14/0C` làm sàn kháng, `re_elf xref` quét cả hằng tức thời) → bằng 0 trên máy chủ này; zone: `kRebornSkillLevelNeed = 0` là đúng |

### 15.4 API script (tất cả `GetPlayerIndex(L)` trước; tên kỹ năng → `KTabFile::GetInteger(Skills.txt, tên, "SkillId")`)

`SetSkillLevel(id|tên[, cấp]) 0x0812C430`: `Add(list, id, cấp (mặc định 0), 0,0,0,0)` rồi gói 0x5e `{id, GetLevel, điểm, 0}` (cấp > 1 đi nhánh `0x0812C620` chưa đọc, coi như thế); `HaveMagic 0x0811C930`: không có → −1, có → cấp (đuôi chưa đọc, `0x080E43F0`); `DelMagic 0x0811C7E0`: `Remove` + gói `{id, −1, điểm, 0}`; `GetCurrentMagicLevel(id|tên[, bWithInc=1]) 0x0811C3A0`; `GetSkillMaxLevel(id) 0x080FDAD0`: `m_SkillInfo+8` (0 không dòng, −1 không đối số); `GetSkillExp(id) 0x0812AB40`: instance cấp 1 IsExpSkill, ô → `+0x20`, không ô → 0, sai → −1; `GetSkillNextExp(id) 0x0812AC50`: `GetCurrentLevel(id, 0)` → instance → IsExpSkill → `+0x11c`; `AddSkillExp(id, exp[, bLevelUp[, bPercent]]) 0x08129DC0`: attrib `{73, id, exp, v2 = (bLevelUp == 0)}` (2 đối số → **không** lên cấp), `AddSkillExp(list, &attrib, bPercent)` → kết quả; `RollbackSkill() 0x0811C640`; `ForbitSkill(v) 0x08121620`; `SetAForbitSkill(id, v) 0x08121580`; `GetSkillCount([bAll]) 0x0811C510`; `GetTotalSkill() 0x0811C5D0`; `IsExpSkill(id) 0x0812CF30` (boolean); `UpdateSkill() 0x08114A10` = **không làm gì** (zone gửi lại sổ — sai khác có chủ ý); `GetAllSkillCurInc 0x0811C320`; `AddTempMagic 0x0812C1B0` → `AddSkillLevelInc` (chưa đọc).

### 15.5 Sai khác có chủ ý của zone

(1) `KSkillListHost` thay `g_SkillManager`/`Npc[]`; (2) bảng chuyển sinh `0x0830CA14` = 0 (đúng nhị phân: không nơi ghi); (3) thông báo chuỗi
`[0x978a290]`/`[0x978a118]`/`[0x978a120]`/`[0x978a124]` (bảng ngôn ngữ, chưa trích) thay bằng cờ `level_up` của
`G2C_SKILL_LEVEL` và log `skill points refused` (client 2.0 tự hiện chữ — B4); (4) gói 0x5e/0x63/0xdd → `G2C_SKILL_LEVEL`,
`G2C_SKILL_FORBID`, (0xdd chưa gửi); (5) `UpdateSkill()` gửi lại sổ; (6) `HaveMagic` trả cấp khi có (đuôi chưa đọc);
(7) ngựa `+0x199c` = 0 → `SetSkillCoolTime` dùng `+0x19e4`; (8) đường lệnh thi triển (`0x0809B750` → `0x08088350` →
`0x08088150`), `CanCastSkill`, `0x08087F70` style 1, `0x08201940`, `0x080AEBC0`, `0x080A79B0` là **B3b**; hiện client
vẫn đánh thường qua `attack_request` (`swing_skill`); (9) đồng hành (loại 2) chưa có → không nhận kinh nghiệm kỹ năng.

## 16. Lệnh thi triển kỹ năng: `SendCommand` → `ProcessCommand` → `CastSkill` → `DoSkill` → bắn ở 60 % (M12 lát B3b, đã kiểm từng dòng)

Đường đi thật của một cú click kỹ năng từ client 2.0: gói 80 (`NpcSkillCommand`) → vòng lệnh 5 ô của npc →
mỗi khung `ProcessCommand` → `CastSkill` (kiểm + trừ phí + gói 0x5a) → `DoSkill` (đặt hành động, số khung)
→ khung 60 % `0x08085020` gọi `KSkill::Cast` rồi đặt hồi chiêu. Zone: `KSkills.cpp` (`cast_skill_request`,
`send_command`, `check_command`, `process_command`, `cast_skill`, `do_skill`, `on_skill`, `can_cast_skill`,
`weapon_physics_skill`, `weapon_eqt_limit`, `cost_skill`, `set_active_skill`, `current_skill`,
`skill_instance`), `KNpc::commands` (`KNpcCommand`), `KDoing::magic`, proto `CastSkillReq` (`C2G_CAST_SKILL`),
`EntityAction.skill_id/skill_level/aim`, bảng `KWeaponSkillTable` (`weapon_skill.json`, `jxassets export-weapon-skill`).

| Địa chỉ | Nội dung đã kiểm |
|---|---|
| `0x080DD130` | **`NpcSkillCommand(this, playerIdx, pMsg)`** (ô 80 của ctor `0x080DA560`): gói `{byte 80, int A, int id, int B, int sync}`; `id−1 ≤ 0x7cf` và `≠ 2000`; **`0x080A79B0(Player, 80, sync)`** ≠ 0; instance cấp 1 (`table[id][1]`/`InstanceSkill`); **`vtable+0x4c IsAura == 0`**; `A ≥ 0`; `0x080AEBC0(Player, 1)`; chỉ số npc 1..max; `B ≥ 0` → `SendCommand(npc, 5, id, B, A, 0, 0x12)`; `B == −1` → `idx = 0x080B12C0(Player, A)` (id npc → chỉ số) > 0 → `SendCommand(npc, 5, id, −1, idx, 0, 0x12)`. Ô lệnh `+8` = B (y hoặc −1), `+0xc` = A (x hoặc chỉ số) — thứ tự x/y của gói cũ chưa đối chiếu với client; zone nhận `{skill_id, target, x, y}` tường minh |
| `0x080A79B0` | kiểm đồng bộ gói: bảng hằng theo loại gói `[0x8BAEF00 + loại·4]` (khởi tạo lần đầu 6 hằng), có hằng và `sync ≠ 0` → `(Player+0x3b8 ^ Player+0x3bc ^ sync) % hằng == 0`; không hằng → 1. Chống gian lận của giao thức cũ — zone không cần |
| `0x080AEBC0` | `(Player, n)` → **`0x081D0C00(Player+0x86e0, n, Player)`: bộ phát sự kiện script của người chơi** — `n ≤ 16`; duyệt `std::map` ở `Player+0x86e0` (mỗi nút: `+0x14` id script, `+0x18` mặt nạ bit sự kiện); nút có bit `n` → chạy script (`0x080ACB10(player, …)`, `0x804E4B0([0x82E8CAC], id)`). Mã đã thấy: 1 xin thi triển, 3 lên ngựa, 9 đổi chế độ chiến, 10 mất máu, 11 chết, 12 xin hồi sinh. Zone chưa có bảng đăng ký script theo sự kiện (M13 script) |
| `0x0809B750` | **`KNpc::SendCommand(this, cmd, id, p2, p3, p4, p5)`** — chỉ nhận `cmd == 5` (do_skill): `FindSame(+0x248, id) ≠ 0`; `+0x171c` (đầy) == 0; ô 24 byte tại `+0x169c + 24·[+0x1718]` = `{5, id, p2, p3, p4, p5}`; `+0x1718 = (+1) % 5`; bằng `+0x1714` → `+0x171c = 1` → 1 |
| `0x0809B510` | mỗi khung: ô lệnh đang chờ `+0x14 (p5) −−`, về 0 → gỡ (dồn vòng) — p5 = 0x12 = 18 khung sống |
| `0x0809B4B0` | bỏ lệnh đầu: đầy → `+0x1714 = (+1) % 5, +0x171c = 0`; không đầy và `+0x1714 ≠ +0x1718` → `+0x1714 = (+1) % 5` |
| `0x0809B9E0` | **`KNpc::ProcessCommand`**: có lệnh → `0x0809B840` → **2**: bỏ; **1**: giữ (bận); **0**: `p2 == −1` → mục tiêu `p3 > 0`: `+0x118c ≥ 0`, `m_Doing ≠ 10`, `+0x1184 ≥ 0` (không → `DoStand 0x08080030`, bỏ); `dist = 0x0809F370(p3, +4)`; `dist ≤ +0x12a8` → `CastSkill(this, −1, p3)` + bỏ; `dist ≤ +0x12a8 + 300` → `0x080EF710` (Map2Mps mục tiêu) + `0x0807B780` (đi tới) **giữ lệnh**; xa hơn → bỏ; `p2 ≠ −1` → `CastSkill(this, p2, p3)` + bỏ; rồi `0x0809B510` |
| `0x0809B840` | **kiểm lệnh** `(this, 5, id, p2, p3, p4, p5)` → 0/1/2: cmd ≠ 5 → 2; `p5 ≤ 0` → 2; `+0x1184 < 0` → 2; **`+0x194c == 0` → 1** (đang trong hành động); id 1..1999, instance cấp 1 (rỗng → 2); **`+0x168c == 0` (chưa chiến đấu — `SetFightMode 0x08079B30`, `KNpc::Init` đặt 0) → `vtable+0x44 PeaceCanUse` phải ≠ 0**; `FindSame` → `SetActiveSkill 0x08086D90(this, ô)`; `GetCurrentSkill 0x080848B0` ≠ 0; `CanCast(list, id, khung, 0)`; **`vtable+0x18 CanCastSkill(sk, +4, &p2, &p3)`** (sửa tại chỗ — vì thế mục tiêu ngoài tầm của kỹ năng có kiểm tầm **bị bỏ ngay**, nhánh "đi tới +300" chỉ đến được với kỹ năng CanCastSkill không kiểm tầm); người chơi → `CostSkill 0x08078B10(this, loại, phí, 1)` → 0 |
| `0x08086D90` | **`KNpc::SetActiveSkill(ô)`**: ô 1..79, `+0 ≠ 0`, `+0x18 ≠ 0`, `+0x194c ≠ 0`; `+0x240 = id`; instance `(id, +0x18)` → **`+0x12a8 = GetAttackRadius`** (bán kính kỹ năng hiện tại) → 1 |
| `0x080848B0` | `KNpc::GetCurrentSkill()`: `GetCurrentLevel(list, +0x240, 1)` > 0, id 1..1999, cấp ≤ 63 → instance |
| `0x08088350` | **`KNpc::CastSkill(this, p1, p2)`**: `+0x1184 ≥ 0`, `m_Doing ≠ 5`; `sk = GetCurrentSkill`; **người chơi**: `+0x168c == 0 → PeaceCanUse`; `+0x1908 > 0 → 0x08201940(Player+0x3fc, 0)` (`KItemList` — chuỗi "Item AbradeToZero"/"Item Damage": mòn vũ khí, M11/B3c); `p2` 1..max → `+0x1594 = p2`; `CanCast(list, +0x240, khung, +0x20)` không → `+0x1594 = +0x15a4 = 0`, `DoStand`; `CanCastSkill(sk, +4, &p1, &p2)` không → như trên; người chơi: `phí = vtable+0x20(sk, this)`, `loại = vtable+0x1c` → `CostSkill(this, loại, phí, 0)` (trừ thật) không → như trên; `+0x19a0 > 0 → 0x0807D4C0` (ẩn thân vỡ, §16.1); **gói 0x5a** 25 byte `{0x5a, p1, id, p2 (id npc mục tiêu khi p1 == −1, phải cùng subworld), id mình, GetCurrentLevel(id,1), 0}` → `0x0807A870(this, gói, 0x19, 0x64, 0)`; `+0x14ac = +0x14a4 = p2; +0x14a8 = +0x14a0 = p1`; style 14 hay ≤ 4 → **`DoSkill 0x08088150`**; 13 → `vtable+0x60`; khác → không |
| `0x08088150` | **`KNpc::DoSkill(this, sk, p1, p2)`**: `anim = sk+0x2c (CharAnimId)`; **style 1** → `0x08087F70` (bảng dạng 8..13 ở dưới) 0 → thất bại (`+0x1594 = +0x15a4 = 0`, `+0x194c = 1`, `DoStand`); `sk+0x30 IsPhysical == 0`: `anim == 14 → +0x22c = 0` không thì **`+0x22c = +0x191c (CastFrame) ·100 / (max(+0x1a3c, +0x1a40) + 100)`**; `m_Doing == 0x12` → `+0x194c = 1; +0x128c −= +0x14b0; +0x14b0 = 0`; **`m_Doing = 6`** (do_magic); vật lý: **`+0x22c = +0x1918 (AttackFrame) ·100 / max(1, max(+0x1a34, +0x1a38) + 100)`**, **`m_Doing = 7`** (do_attack); chung: **`+0x1384 (clearallcd) > g_Random(100)` → `ClearCoolTime`**; `+0x194c = 0`; `+0x230 = 0` → 1 |
| `0x08087F70` | style 1 theo `MisslesForm +0xc8` (bảng `0x08254AC0`, dạng 8..13; khác → `+0x194c = 1`, 0): 8 → `0x08084930`; 9 → lao tới điểm `0x08087CF0(this, +0x14a0, +0x14a4)` thành công → `0x0807B320`, 1; thất bại → `0x0807B620`, 0; 10 → lao tới npc (`+0x14a0 < 0` → Map2Mps của `Npc[+0x14a4]` → `+0x14a0 = x+1, +0x14a4 = y`) rồi `0x08087CF0` → `0x080807E0`; 11 → `+0x1964 = 0; 0x08084A10`; 12 → `0x08084B40`; 13 → `0x08084C90`; mỗi nhánh thành công → **`SetSkillCoolTime(this, GetSkillId, sk+0x114)`** ngay. **Đã port (§16.2)** |
| `0x08087770` | **hành động mỗi khung** theo `m_Doing` (bảng `0x08254A5C`, 0..24): 1 → `0x08087810` (`+0x230++ ≥ +0x22c → 0`, lặp: đứng); 2 → `0x08080B70`; 3 → `0x08080BD0`; 4 → `0x080818F0`; **6/7 → `0x08085020`**; 8 → `0x08087880` (giữ khung cuối: ngồi); 9 → `0x08080D90`; 10 → `0x08083B50` (chết); 14 → `0x08087620`; 15 → `0x08085180`; 18 → `0x080853B0`; 19 → `0x08086E50`; 20 → `0x08084E00`; 21 → `0x08086220`; 23 → `0x08080760`; 24 → `0x08080E70`; 0/5/11/12/13/16/17/22 → không. Trước bảng: `m_Doing` 4/0x14 hay `+0x2c = 0`; `+0x1694 ≠ 0` → thôi; `+0x14c4 ≠ 0` (bị đánh lùi?) và `m_Doing` không 10/0x15/9/0x18, `+0x1654 ≠ 10` → `0x08081DF0` |
| `0x08085020` | **khung của do_magic/do_attack**: `khung = ++(+0x230)`; `khung < +0x22c`: `khung == +0x22c·60/100` → **bắn** (0x0808505A) không thì thôi; `khung ≥ +0x22c`: `+0x230 = 0`; `+0x22c ≠ 0` → `DoStand` + `+0x194c = 1` thôi; `+0x22c == 0` → bắn. **Bắn**: `+0x14a0 == −1` → `Npc[+0x14a4]` phải > 0 và `+0x1184 ≥ 0`; `sk = GetCurrentSkill`; `IsForbidden(list, GetSkillId)` → thôi; **`KSkill::Cast(sk, +4, +0x14a0, +0x14a4, 0, 0, 0)`**; **`SetSkillCoolTime(this, +0x240, sk+0x114)`**; sau đó `+0x22c == 0` → `+0x194c = 1`. Một hành động 1 khung không bao giờ bắn (60 % của 1 = 0, khung 1 ≥ 1 → dừng) — zone giữ (`reach_frame`); 0 khung bắn ngay |
| `0x08080030` | **`KNpc::DoStand`**: `+0x230 = 0; +0x22c = max(1, +0x1910)`; `m_Doing == 1` → thôi; `== 0x12` → `+0x194c = 1; +0x128c −= +0x14b0; +0x14b0 = 0`; `m_Doing = 1`; `0x080EF710(subworld, vùng, +0x148c.., &+0x14a0, &+0x14a4)` (Map2Mps của mình vào +0x14a0/+0x14a4) |
| `0x08078B10` | `KNpc::CostSkill(this, loại, phí, bChỉKiểm)`: không phải người chơi → 1; 0 nội lực `+0x11a0`, 1 thể lực `+0x11a8`, 2 sinh lực `+0x118c`, khác → 0; `cur ≥ phí`; chỉ kiểm → 1; trừ → 1 |
| `0x080E8AE0` | `KSkill::CanCastSkill` — đã đọc §14; port `can_cast_skill` (TargetSelf tại điểm → về mình; TargetNoNpc/Enemy/Ally (đồng hành loại 2 → 0)/Self/Other (bit 1); người chơi: `WeaponSkill → 0x08079A90 == id`, `EqtLimit ≠ −2 → 0x080E8C05`, `HorseLimit` 2 → 0 (zone không ngựa); tầm theo style: 0 → dạng > 6 có, 0/5/6 kiểm, 3 kiểm khi `Param1 == 1`, 1/2 kiểm khi `TargetOnly && p1 == −1`, 4 không; 2/4/14 kiểm; 1/3/5..13 không; `0x0809F370 ≤ AttackRadius` hay `(int)sqrt ≤`) |
| `0x08079A90` | **`KNpc::GetWeaponPhysicsSkill()`**: người chơi; `detail = 0x081F92E0(Player+0x3fc)` (`Item[list+0x24]+8`), `particular = 0x081F9F70` (`+0xc`); detail 0 → `[0x0830AF00 + particular·4]`; 1 → `[0x0830B0A0 + particular·4]`; −1 → `[0x0830B230]`; khác → 0 |
| `0x0805F18D` | **nạp `\settings\武器物理攻击对照表.txt`** (trong `0x0805F100`, sau `taxrates.ini`/`lastupdate.ini`): cột `DetailType`, `ParticularType`, `PhysicsSkillID`; mỗi dòng: detail 0 → `particular ≤ 99` và `0 < skill ≤ 1999` → `[0x0830AF00 + particular·4] = skill`; 1 → `0x0805F3F0` (bảng `0x0830B0A0`); −1 → `skill−1 ≤ 0x7ce` → `[0x0830B230] = skill`. **Tệp không có trên máy** (`settings/` chỉ có `clientweaponskill.txt`: `Id, WeaponType, SkillId` = tay không/đoản/song → 53, thương/côn → 1, cung → 2) → zone `weapon_skill.json` khi có, không thì đánh thường 1 (cận)/2 (xa) **Tệp có trong pak client 2.0** (`slistcl.pak:\settings\武器物理攻击对照表.txt`, 92 dòng, BOM UTF-8; client nạp cùng tệp ở `0x005CB396`, `CLIENT-2.0.md` §7.1) — `jxassets export-weapon-skill` rơi về pak client khi thư mục server thiếu → zone nạp bảng thật (`weapon skill table loaded count=11`), không còn cần xin chủ dự án (B4b-6a). |
| `0x0809F370` | **khoảng cách hai npc** `(a, b)` → −1 khi khác subworld/vùng < 0; cùng vùng: `dx = (a+0x148c − b+0x148c)·vùng+0x24 + ((a+0x1498 − b+0x1498) >> 10)`, `dy` với `+0x1490/+0x149c` và `+0x28`; `(int)sqrt(dx²+dy²)` (`fsqrt`/`sqrt`); khác vùng → qua toạ độ tuyệt đối (0x0809F3F9) |
| `0x08079B30` | `KNpc::SetFightMode(v)` — nơi duy nhất đổi `+0x168c` (Init đặt 0); AI npc gọi thẳng `CastSkill` (qua `+0x193c` ở `0x08088640`) nên chỉ lệnh xếp hàng mới kiểm `PeaceCanUse` |
| `settings/npc/player/newplayerini00.ini` | `[FSKILLS] COUNT=7: S1..S7 = 53, 1, 2, 229, 230, 231, 232` (cấp 1) — kỹ năng khởi đầu (`CPlayerCreator` Bishop, `KPlayerSet.go` `NewPlayer.Skills`) → persist `NewRole` thêm `RoleData.skills`; `NewPlayerBaseAttribute.ini` (`0x080C4B36`) chỉ 5 điểm |

Sai khác có chủ ý: (1) lệnh xin thi triển của client được xử lý **ngay** trong `cast_skill_request` (nhị phân: khung sau)
và `attack_request` cũ (AttackReq của client Godot) vẫn **tự đi tới** khi ngoài tầm rồi mới gửi lệnh và **bật
`fight_mode`** (`+0x168c`) — client 2.0 tự đi tới và vào chế độ chiến đấu trước khi gửi gói (đánh thường `PeaceCanUse = 0`); B4 sẽ chuyển client sang `CastSkillReq`; (2) người chơi tự lặp lệnh khi mục tiêu còn sống
(`update_action`) — client cũ gửi lại mỗi đòn; (3) `0x080A79B0`, `0x080AEBC0`,
`0x080B12C0` không port (style 1 `0x08087F70`: §16.2; mòn đồ `0x08201940`: §16.3); (4) vòng 5 lệnh là `std::deque` cùng luật (đầy, sống 18 khung);
(5) `+0x194c` = "không trong hành động" ≡ `doing ∉ {attack, magic}`; (6) `KDoing::magic` gửi client như `ACTION_ATTACK`
kèm `skill_id/skill_level/aim` (0x5a) — client phân biệt ở B4; (7) do_magic không chặn chia 0 ở nhị phân (tốc độ −100),
zone `max(1, …)`; (8) bảng vũ khí thiếu → đánh thường 1/2, `bare` 53 chưa dùng; (9) style 4 (tạo npc) trong
`CanCastSkill` coi đủ chỗ (B3c).

### 16.1 Ẩn thân `[hide]` 200 — `SetHide` / `IsInvisibleTo` / ẩn thân vỡ (M12 lát B3c-0, đã kiểm từng dòng)

| Địa chỉ | Nội dung (đã đọc từng dòng) | Zone |
|---|---|---|
| `0x08097860` | **ProcessFunc 200 `hide`** `(obj, launcher, npc, attrib)`: `[obj+0]` (= đối số thứ 5 của `KNpc::ModifyAttrib 0x0807D210`, cờ "đang gỡ" — `0x08095B40` ghi vào trước khi gọi và xoá sau) ≠ 0 và `+0x19a0 == 0` → thôi; còn lại `SetHide(npc, +0x19a0 + v0)`; log `"Hide + %d = %d"` | `KNpcAttribModify` `magic_hide` (`ctx.removing`, móc `ctx.set_hide`) |
| `0x0807FF80` | **`KNpc::SetHide(this, n)`** (Lua `SetHide(n)` `0x0810AD80` trên npc của người chơi, `NpcSetHide(idx, n)` `0x08101C10`): `+0x19a0 == 0 && n ≠ 0` → `+0x19a4 = 1`; **gói 0x4f** 5 byte `{0x4f, id npc (+0)}` → `0x0807A870(this, gói, 5, 0x64, 0)`; `+0x19a4 = 0`; `+0x19a0 = n`. `+0x19a0 ≠ 0 && n == 0` → `0x0807FBB0(this, 0)` (gói 0x4c đồng bộ npc đầy đủ tới người xung quanh — lúc đó vẫn đang ẩn nên chỉ chính mình nhận) rồi `+0x19a0 = 0`. Hai trường hợp còn lại chỉ ghi `+0x19a0 = n` | `KSubWorld::set_hide`: 0→n = `entity_gone(e, keep_self)` (`G2C_ENTITY_DESPAWN` cho mọi người xem trừ chính mình), n→0 = `wake_viewers_near` (người xem quanh nhìn lại ngay khung sau) |
| `0x08079200` | **`KNpc::IsInvisibleTo(this, nNpcIdx)`**: `+0x19a4 ≠ 0` → `+4 == idx` (đang gửi 0x4f: chỉ chính mình bị bỏ); `+0x19a0 == 0` → 0; còn lại `+4 ≠ idx` (ẩn: mọi người trừ chính mình). `+4 = m_Index` | `KSubWorld::invisible_to(e, viewer)` — hỏi trong `look_around` (quên và không học) |
| `0x0807A870` | **gửi gói tới người chơi quanh npc** `(this, gói, cỡ, tối đa 0x64, cờ)`: vùng `+0x1184 ≥ 0`; `+0x1654 == 8` → đường riêng `0x0807A940`; `0x080E1C80(vùng, mảng 0x82E9380, tối đa, +4 (m_Index), +0x148c, +0x1490, cờ)` → gọi ảo `[[0x82E8CA0]]+0x2c` (server) gửi cho n kết nối | `broadcast(e, …)` = `e.watchers` |
| `0x080E1C80` / `0x080E1B80` | **duyệt 9 vùng** (vùng + 8 lân cận qua bảng `0x08258040`): mỗi nút người chơi (`+0x2c` danh sách, `+0x10` chỉ số): `Player+0x3f8` (kết nối) ≠ 0, `|y − y_npc| ≤ 0x20`, `|x − x_npc| ≤ 0x20`; cờ 0 → bỏ người có `Player+0x290 ≠ 0`; **`idx npc > 0` → `IsInvisibleTo(npc, Player+0x3f4)` → bỏ**; ghi `Player+0x3f8` vào mảng | vùng nhìn của zone (`KInterest.cpp`) |
| `0x0807D4C0` | **ẩn thân vỡ**: `+0x19a0 ≠ 0` → vector `[0x8FBFE40]` = `attribconstdata.ini [hide]` (id 200; `0x8FBF4E0 + 200·12`): `i = size−1 … 1` → `RemoveStateSkillEffect(this, Data_i, 1)` — **bỏ `Data0`**. Tệp (`settings/attribconstdata.ini`, GBK): `Count=5; Data0=70` (độ trong suốt của chính client khi ẩn, 0 trong suốt … 255 đục), `Data1..4 = 713, 1235, 1258, 1267` (kỹ năng trạng thái phải gỡ khi hết ẩn). Gọi từ `CastSkill 0x080884A3` (sau trừ phí, trước gói 0x5a), `DoDeath 0x08089359` (sau danh sách chết `0x08188BB0`, trước `m_Doing = 0xa`), lên ngựa `0x0807D520`, Lua `OpenProgressBar 0x081082D0` | `KSubWorld::break_hide` (`attrib_data(magic_hide)` từ `[size−1 … 1]`); gọi trong `cast_skill` và `do_death` |
| `0x080E75A0` | bộ nạp `attribconstdata.ini` ghi `vector[i] = Data_i` với `i = 0 … Count−1` (không có ô đếm ở đầu) | `KSkillTable::attrib_data` cùng chỉ số |
| `0x0807FBB0` | **đồng bộ npc đầy đủ** `(this, player)` gói **0x4c**: kind `+0x24`, `+0x21c`, `+0x220`, hệ `+0x28`, `m_Doing +0x224`, máu·128/max(`+0x1a14`,`+0x1a18`), toạ độ vùng (`0x080EF710`), id `+0`, `+0x1530<<16 \| +0x20`, băm tên (`0x0809CC90` trên `+0x88`; `+0x181c ≠ 0` → `[0x8BADEE8]+1`), `+0x19d4 == 1`; kind ≠ 1 → máu 0xff; người chơi thêm `Player+0x5700`, tên `+0x570c` (≤ 0x1e), `+0x86b8`; `player ≠ 0` → `0x080A8400(player, gói)` + log `0x0821CC00`, không → `0x0807A870` | `fill_info` + `G2C_ENTITY_SPAWN` |
| `0x080810C0` | **gói 0x4d** (trạng thái npc ngắn, tới một người hay quanh): `m_Doing` 0xa/0x15 → thôi; `+4` phải ≠ 0; toạ độ vùng, id, máu (`+0x118c` bậc 2048), `+0x168c` (chiến), `+0x38`, `+0x3c`, `+0x19d4`, `+0x1290/+0x1294`, `+0x220`, **byte cờ: bit0 `+0x1d8 > 0`, bit1 `+0x1c8 > 0`, bit2 `+0x19a0 ≠ 0` (ẩn), bit3 `+0x1e8 > 0`**; gọi bởi `0x081EAA80`, `0x08164D20`, `0x080E2660`, `0x080FC0D0`, `0x08117940`, `0x081D4F80`, `0x081EB040`; **kind 1 → thêm gói 0x4b** (`0x08081390`: id, `+0x1a34..+0x1a40`, phiên bản `+0x1504`, mũ `+0x14dc`, ngựa `+0x14e8`, áo `+0x14e0`, vũ khí `+0x14e4`, áo choàng `+0x14ec`, `+0x14f0/+0x14f4`, word `Player+0x7dc4`, bang) | `EntityInfo.hide` (chỉ chính client của npc ẩn nhận spawn của nó); dáng: §16.15 |
| `0x0809F190` | **đồng bộ một npc theo yêu cầu client** (từ `0x080DBA90`): tìm id trong cây `subworld+0x14f0` (`+0x10` id → `+0x14` chỉ số); **`IsInvisibleTo(npc, Player+0x3f4)` → từ chối**; `m_Doing` 0xa/0x15 → từ chối; a4 == 0 → `0x0807FBB0(npc, player)`; `+0x1900` … | người xem nhìn lại (`look_around`) |
| `0x080873B0` | `(this, id kỹ năng, a3)`: `a3 > 0 && +0x19a0 > 0` → thôi (npc ẩn không phát gói **0x85** hiệu ứng kỹ năng); gói 0x85 25 byte → `0x0807A870`; kỹ năng `IsAura` (vtable+0x4c) → `InstanceSkill(mgr, sk+0xb8, cấp)` → `+0x118 = 1; Cast(sk, +4, x, y, 0, 0, 0); +0x118 = 0` (aura thi triển kỹ năng con lên chính mình) | chưa port (gói 0x85, aura — B4) |
| `0x08099170` | **ProcessFunc 186 `stealfeature`** (mượn dáng): launcher ≠ 0, `0x08078A80(launcher) > 0`, `0x08079170(npc)`; `[0x830AE40] DisguiseMask/ForbitFeature` (danh sách `+0x14d8` bị cấm, tách dấu phẩy); `KItemList 0x081FB240(Player+0x3fc, {2, 0xb})` → đồ `+0xe4 == 0xbb` và `+0xe8 > 0` → `+0x88 = +0x14d8` của mục tiêu, `+0xe8 −= 1`, gói 0xc4 16 byte tới người chơi, rồi `0x0807AF10(npc, {0x10, 1, 0, …, +0x14d8})` | chưa port (hệ dáng/mặt nạ) |
| `0x08099370` | **ProcessFunc 187 `addstealfeatureskill`**: `AddSkillLevelInc(list, v1, v1 > 0 ? 1 : −1)` — cộng 1 cấp cho kỹ năng `v1` khi trạng thái còn (gỡ → `−v1` → trừ) | `magic_addstealfeatureskill` → `add_level_inc(v1, ±1)` |
| `0x0807D520` | **lên/xuống ngựa** `(this, n)` — **port §16.6** (`KSubWorld::set_horse`): byte `+0x1479` (khoá hành động) → thôi; `+0x199c = n`; người chơi → `0x080AEBC0(player, 3)`; `n ≠ 0 && +0x19a0 > 0` → `0x0807D4C0`. Gọi từ `0x081FE380`, `0x080AEFA0`, `0x080C16D0` (LoadFrom), `0x081FFFB0` | chưa port (ngựa) |
| `0x08082680` / `0x0807EE60` | `Init/ResetCurData` và `ClearAttrib` đặt `+0x19a0 = +0x19a4 = 0` (không gửi gì; trạng thái áp lại sẽ `SetHide` lại) | `KNpc::clear_attrib` |

Sai khác có chủ ý: (1) người xung quanh nhận `G2C_ENTITY_DESPAWN` thay gói 0x4f, và **học lại** npc hết ẩn ở khung sau
(`wake_viewers_near`; nhị phân chờ client xin `0x0809F190`); (2) chính client của npc ẩn chưa được báo (nhị phân báo qua gói
trạng thái 0x87 — B4; `EntityInfo.hide` mang bit của 0x4d); (3) `NpcSetHide` chờ quy ước "chỉ số npc" của API script; (4) một
sửa ngoài ẩn thân: người chơi **không tự lặp** lệnh với kỹ năng không đánh kẻ địch hay nhắm chính mình (buff lên mình thi triển
một lần — trước đó lặp mãi và mỗi lần lặp làm vỡ ẩn thân).

### 16.2 Thân pháp — style 1: `0x08087F70` và sáu dạng `MisslesForm` 8..13 (M12 lát B3c-1, đã kiểm từng dòng)

Cột `Param1 = sk+0x50`, `Param2 = sk+0x54`, `ChildSkillNum = sk+0xb4`, `ChildSkillId = sk+0xb8`, `MisslesForm = sk+0xc8`, cấp `sk+0x114`
(bảng cột §11 tính từ `KSkill+4`). `+0x14a0/+0x14a4` bị ghi đè bằng **điểm đến** của thân pháp; kỹ năng con lấy mục tiêu ở
**`+0x14a8/+0x14ac`** (CastSkill ghi cả hai cặp). `+0x194c` (rảnh) = 0 suốt thân pháp, `DoStand` trả 1.

| Địa chỉ | Nội dung (đã đọc từng dòng) | Zone |
|---|---|---|
| `0x08087F70` | `(this, sk)`: `dạng = MisslesForm − 8 > 5` → `+0x194c = 1`, 0. Bảng nhảy `0x08254AC0`: **8** → `0x08084930`; **9** → `0x08087CF0(this, +0x14a0, +0x14a4)` (đường nhảy) rồi `0x0807B320`; thất bại → `0x0807B620` (DoWalk tới điểm — bị `DoStand` của DoSkill huỷ ngay), 0; **10** → `+0x14a0 < 0` → `Npc[+0x14a4]` Map2Mps → `+0x14a0 = x+1, +0x14a4 = y`; `0x08087CF0` rồi `0x080807E0`; **11** → `+0x1964 = 0; 0x08084A10`; **12** → `0x08084B40`; **13** → `0x08084C90`. Thành công → **`SetSkillCoolTime(GetSkillId, sk+0x114)` ngay** | `KSubWorld::do_special_skill` (từ `do_skill`; thất bại → `do_stand`) |
| `0x08087CF0` | **đường nhảy** `(this, &x, &y)`: `quãng = +0x12a0 (40, Init 0x0807DDD4) × +0x129c (bước 12)`; `0x08081B70(this, &x, &y, &quãng, bay = 1)` (ô trống trên đường, bay qua vật cản nhảy) 0 hay `quãng ≤ 20` → 0; `+0x14a0/+0x14a4 = x, y`; **`+0x195c = quãng / bước`** (số khung); Map2Mps mình → `dx, dy`; cùng chỗ hay `len = (int)sqrt = 0` → `+0x1960 = −1`; không thì dò bảng `[0x082E12EC] = g_nSin` với `(dy<<10)/len` (chỉ số gần nhất `k`, 0 → 0) → **`+0x1960 = dx < 0 ? k : 64 − k`** (= `g_GetDirIndex`) | `jump_to` (`knock_back_free_spot(fly)`, `kJumpSteps = 40`, `kMoveMinDistance = 20`, `g_GetDirIndex`) |
| `0x0807B320` | **bắt đầu nhảy**: vùng ≥ 0; `m_Doing == 4` → thôi; `== 0x12` → prologue chạy; **`m_Doing = 4`**; `+0x147c = +0x1960` (hướng); `+0x194c = 0`; **`+0x1938 = 5·+0x195c − 5`** (hằng đường cong độ cao); **gói 0x54** 13 byte `{0x54, id, +0x14a0, +0x14a4}` → `0x0807A870(…, 0xd, 0x64, 0)`; `+0x230 = 0; +0x22c = max(1, +0x195c)` | `start_jump` → `ACTION_JUMP` (`aim` = điểm rơi, `frames` = số khung) |
| `0x080818F0` / `0x080817E0` | **mỗi khung nhảy** (bảng `0x08254A5C[4]`): `0x080817E0`: vùng ≥ 0, `m_Doing` 4 hay 0x14; `f = +0x230`; **`+0x2c = max(0, ((+0x1938 − 5f)·f) / 8)`** (độ cao, chia làm tròn về 0); Map2Mps mình; `còn = max(1, +0x22c − f)`; `0x0807C2F0(this, ((+0x14a0 − x)<<10)/còn, ((+0x14a4 − y)<<10)/còn)` (dời theo phần nghìn `+0x1498/+0x149c`, cập nhật ô/vùng); rồi `+++0x230 < +0x22c` → 1; hết → `+0x230 = 0; DoStand; +0x194c = 1` → 0 | `jump_frame` (`fx/fy` phần `kSub`; rơi → `doing = stand`, `emit_move`) |
| `0x08084930` | **dạng 8 (đòn con)**: `m_Doing == 0xe` → 0; `sk = GetCurrentSkill` null → 0; `+0x22c = max(1, +0x1918 (AttackFrame)·100 / max(1, max(+0x1a34,+0x1a38)+100))`; `+0x230 = 0`; `== 0x12` → prologue; **`m_Doing = 0xe (14)`** → 1 | `start_special_skill` (`attack_length`) |
| `0x08087620` | mỗi khung dạng 14: `++f < tổng`: `f == tổng·60/100` → **`Cast(ChildSkillId @ cấp sk+0x114, +4, +0x14a8, +0x14ac, 0,0,0)`** (mọi style; `InstanceSkill` khi chưa có), thôi; `f ≥ tổng`: `f = 0`; tổng ≠ 0 → `DoStand; +0x194c = 1`; tổng == 0 → bắn rồi `+0x194c = 1` | `special_skill_frame` |
| `0x08084A10` | **dạng 11 (chạy đánh)**: `sk` null → 0; `+0x230 = 0; +0x22c = max(1, +0x1620 (m_RunSpeed))`; `== 0x12` → prologue; **`+0x128c += Param1; +0x14b0 = Param1`** (thưởng tốc độ chạy); **`m_Doing = 0x12 (18)`**; `+0x164c = 0`; `+0x14a0 == −1` → Map2Mps của `Npc[+0x14a4]` vào `+0x14a0/+0x14a4` → 1 | `start_run_attack` (`walk_to` với `speed += Param1·tick_hz`, `run_bonus`) |
| `0x080853B0` | mỗi khung dạng 18: `sk = GetCurrentSkill` null → DoStand; **`0x08080BD0(this)`** (bước chạy tới `+0x14a0/+0x14a4` với `+0x128c`; người chơi hết thể lực `+0x11a8 < [0x8BADF80..88]` → `0x0807B430` đi bộ); vẫn 0x12 → `đếm = +0x164c++`; **`đếm ≤ 0x080E8650(sk, 0)` (không dấu) → thôi**; hết chạy hay quá trễ → `ChildSkillId` **chỉ style 0** (`vtable+0x10 == 0`) → `Cast(child, +4, +0x14a8, +0x14ac, 0,0,0)` ≠ 0 → `0x08078790` (DoAttack: `m_Doing = 7`, khung đánh) rồi **`DoStand`** (huỷ ngay DoAttack) `+0x194c = 1` | `run_frame` (`!moving` hay `run_counter > missle_start_life_time(sk, 0)` → `cast_child_skill(style 0)`, `do_stand`) |
| `0x08084B40` | **dạng 12 (thi triển nhiều lần)**: `sk` null → 0; **`+0x1964 ≥ ChildSkillNum` → `DoStand; +0x194c = 1; +0x1964 = 0` → 1**; Map2Mps; `khung = 0x080E8650(sk, +0x1964)` (độ trễ đạn thứ i, §13); `+0x230 = 0; +0x22c = max(1, khung)`; prologue; **`m_Doing = 0x13 (19)`**; `+0x194c = 0` → 1 | `start_special_cast` |
| `0x08086E50` | mỗi khung dạng 19: `++f < tổng` → thôi; `f = 0`; `ChildSkillId` **chỉ style 0** → `Cast(child, +4, +0x14a8, +0x14ac, 0,0,0)`; **`+0x1964 += 1`** → `0x08084B40` (lần kế / đứng) | `special_cast_frame` |
| `0x08084C90` | **dạng 13 (thuấn di)**: Map2Mps mình; `sk` null → 0; `quãng = Param1`; **`== −1`** → log `"GM MovePos by %s"`, bỏ kiểm (đi bất kỳ đâu); không thì `0x08081B70(this, &+0x14a0, &+0x14a4, &quãng, 1)` 0 hay `quãng ≤ 20` → 0; `== 0x12` → prologue; **`m_Doing = 0x17 (23)`**; `+0x194c = 0`; `+0x22c = max(1, Param2)`; `+0x230 = 0` → 1 | `start_blink` |
| `0x08080760` | mỗi khung dạng 23: `++f < tổng` → 1; `f = 0`; **`0x0807AFA0(this, +0x14a0, +0x14a4, 0)`** (SetPos: đổi vùng `0x080E2280/0x080E25C0`, gói **0x4f** cho người xung quanh — họ xin lại npc ở chỗ mới, `0x080F0D20`); `DoStand; +0x194c = 1` → 0 | `blink_frame` (`set_pos` rồi `do_stand`) |
| `0x080807E0` | **dạng 10 (nhảy đánh)** theo pha `+0x1964`: `+0x194c = 0`; **0** → `0x0807B320` (nhảy) rồi `== 0x12` → prologue; **`m_Doing = 0x14 (20)`**; `+0x230 = 0` → 1; **1** → `+0x22c = max(1, AttackFrame·100/(tốc đánh+100))`; prologue; `m_Doing = 0x14` → 1; **2, 3** → `DoStand; +0x194c = 1; +0x1964 = 0` → 0; khác → `+0x230 = 0` → 1 | `start_jump_attack` |
| `0x08084E00` | mỗi khung dạng 20: pha **0** → `0x080818F0` (khung nhảy) còn bay → 1; rơi (`DoStand` bên trong) → **`+0x1964 += 1; +0x164c = 0; 0x080807E0`** (đòn); pha **1** → `++f < tổng`: `f == tổng·60/100` → `ChildSkillId` **chỉ style 0** → `Cast(child, +4, +0x14a8, +0x14ac…)` rồi **`DoStand` ngay** (`+0x194c = 1; +0x1964 = 0`); `f ≥ tổng` → `f = 0; DoStand…`; pha khác → `DoStand; +0x1964 = 0` → 0 | `jump_attack_frame` |
| `0x08087770` | bộ chia hành động mỗi khung: `m_Doing` 4/0x14 giữ `+0x2c` (độ cao), khác → `+0x2c = 0`; `+0x1694 ≠ 0` → thôi; `+0x14c4 ≠ 0` (randmove) và `m_Doing ∉ {10, 0x15, 9 (→0x08080D90), 0x18 (→0x08080E70)}`, `+0x1654 ≠ 10` → `0x08081DF0`; rồi bảng `0x08254A5C` (§16 ở trên). `0x08080BD0` = bước đi mỗi khung (`m_Doing` 3, tốc `+0x128c`; `+0x1290 ≠ 0` → tốc `+0x1294`), `0x08080B70` = đi bộ (2); `0x0807C2F0(this, dx<<10, dy<<10)` = dời npc theo phần nghìn (`+0x1479` khoá → không) | `update_action` |
| `0x0807B620` | **DoWalk** tới `+0x14a0/+0x14a4`: npc: `+0x128c ≠ 0` → `+0x22c = +0x1620·+0x1924/+0x128c` không thì `+0x1924`; **gói 0x51** `{id, x, y}`; `m_Doing = 3` (trừ 3/0x12); người chơi: `Player+0x5a50` 1/2/0 → ngưỡng thể lực `[0x8BADF84]/[0x8BADF88]/[0x8BADF80]` với `+0x11a8` → dưới ngưỡng `0x0807B430` (đi bộ) | `walk_to` |
| `0x08078790` / `0x08078850` | **DoAttack / DoMagic (hành động)**: `m_Doing == 7/6` → thôi; `+0x22c = max(1, +0x1918·100/(tốc đánh+100))` / `+0x191c·100/(tốc thi triển+100)`; `+0x230 = 0`; `== 0x12` → prologue; `m_Doing = 7/6`; `+0x194c = 0` | (`do_skill`) |

Sai khác có chủ ý: (1) đường nhảy dời theo `fx/fy` (phần `kSub` của zone) — mỗi khung `(đích − hiện tại)/khung còn` như nhị phân,
khung cuối đặt đúng đích; (2) chạy đánh dùng `walk_to` (đường của zone) với `speed += Param1·tick_hz` thay `+0x128c`; thưởng bỏ
khi `do_stand`/`do_skill`/thân pháp khác (prologue `m_Doing == 0x12`); (3) khung nhảy đánh/chạy đánh/thi triển nhiều lần gửi
client `ACTION_ATTACK` (id kỹ năng, khung), nhảy gửi **`ACTION_JUMP`** (aim, khung) — client Godot chạy tới điểm rơi (hoạt
hình nhảy 2.0: B4), `+0x2c` độ cao chưa gửi; (4) `DoWalk` khi nhảy không có đường và `DoAttack` sau đòn của chạy đánh đều bị
`DoStand` huỷ ngay trong nhị phân → zone không gọi; (5) lệnh đi khi đang nhảy/thuấn di bị từ chối (nhị phân: `+0x194c = 0` giữ
lệnh lại), khi đang chạy đánh/đòn con/thi triển nhiều lần → dừng thân pháp; (6) `SetPos` của thuấn di = `set_pos` (đồng bộ
`teleport` của zone) thay gói 0x4f; (7) `0x08084930` và `0x08084A10` không đổi `+0x194c` (thân pháp vẫn "rảnh" với AI nhị phân)
— zone coi mọi thân pháp là bận (`KNpc::in_action`).

### 16.3 Mòn đồ — `KItemList 0x08201940`, bảng `AbradeRate.ini`, trang bị hỏng (M12 lát B3c-2, đã kiểm từng dòng)

`0x830D420` = **`g_ItemSet`** (`KItemSet`); `[0x830D300]` = mảng vật phẩm (0x368 byte/ô). Trong `KItem`: `+0` genre, `+4` detail, `+8`
particular, `+0xc` level, `+0x304` id, `+0x308` `m_nCurrentDur`, `+0x344` (byte) cấp ngọc bội 0..10 (`0x0806AD90` nâng), `+0x358` chủ.
Ô mặc của `KItemList`: `list+0xc + slot·8` (slot = `KItemPart`: 0 Head … 11 Mask, 12..14 phần JX2).

| Địa chỉ | Nội dung (đã đọc từng dòng) | Zone |
|---|---|---|
| `0x0806E250` | **`KItemSet::Init`**: log `Logs/KSG_ItemLog`, `KSG_ItemGenerate`, `IB_ItemExpired`; `\settings\item\ItemTradeLogFilter.ini` (`FilterNum`, `DetailType`); **`\settings\item\AbradeRate.ini`** qua `KIniFile::GetInteger`: `[Repair] ItemPriceScale → +0x188, MagicPriceScale +0x18c, WarningBaseline +0x190`; `[Attack]` Head `+0x20`, Body `+0x24`, Belt `+0x28`, Weapon `+0x2c`, Foot `+0x30`, Cuff `+0x34`, Amulet `+0x38`, Ring1 `+0x3c`, Ring2 `+0x40`, Pendant `+0x44`, Horse `+0x48`, Mask `+0x4c` (15 ô/chế độ: chỉ số `8 + chế độ·15 + slot`); `[Defend]` từ `+0x5c`, `[Move]` từ `+0x98`; `[AdvPlatina_Attack/Defend/Move]` từ `+0xd4/+0x110/+0x14c` (chỉ số `0x35 + chế độ·15 + slot`). Tệp thật: Attack Weapon 256; Defend Head/Body/Belt/Cuff 320; Move Foot 2560, Horse 5120; AdvPlatina_Defend thêm Amulet/Ring1/Ring2/Pendant 320 | `KAbradeRate` (`abrade_rate.json` — `jxassets export-abrade-rate`, Go `KAbradeRate.go`) |
| `0x0806D560` | **`KItemSet::GetAbradeRate(this, item, chế độ, slot)`**: `chế độ > 2` hay `slot > 14` (không dấu) → 0; **`item+4 == 4` (ngọc bội) và byte `item+0x344 > 5`** → bảng AdvPlatina, không thì bảng thường | `KAbradeRate::range_of` (`amulet_tier` zone chưa sinh → luôn bảng thường) |
| `0x08066570` | **`KItem::Abrade(this, N)`**: `N == 0` → −1; `+0x308 == −1` hay `≤ 0` → −1; `g_Random(N) ≠ 0` → trả `+0x308` (không đổi); không thì `+0x308 −= 1`, trả giá trị mới (0 = vừa hỏng) | `KItem::abrade` (sửa: độ bền 0 trả −1) |
| `0x08201940` | **`KItemList::Abrade?(this, chế độ)`** (gọi: `CastSkill 0x08088350` chế độ 0 sau `PeaceCanUse` khi `+0x1908 > 0`; `ReceiveDamage 0x0808B148` chế độ 1 khi người chơi và `+0x118c < máu trước`; **mỗi bước đi `0x0807C2F0`** chế độ 2 khi `+0x1908 > 0`; Lua `AbradeEquipments(chế độ) 0x08107AA0`): `[0x830CA78] == 1` → thôi (không ai ghi → luôn 0); duyệt slot 0..13 **trừ 11 (Mask)**: `idx = list+0xc+slot·8`, `N = GetAbradeRate`, `r = KItem::Abrade`; `r == −1` hay `r == +0x308 cũ` → bỏ; `r > 0` → **gói 0x9b** 13 byte `{0x9b, +0x304, +0x30c, +0x308}` cho người chơi (`0x080A8400`); **`r == 0` (hỏng)**: `[0x830D244] ≠ 0` (không ai ghi) → log "Item Damage", gói 0x86 `{0x86, 8, 0x10, id}`, `KItemList::Remove 0x082006B0`, `KItemSet::Remove 0x0806DB90(…, 3, 1)`; **không thì "AbradeToZero"**: (`item+0x350 ≠ 0` hay `list+0x4c80 && +0x4c84` → `0x081F8A90(list+0x4c8c)` đếm ô trống > 0, không thì `list+4 ≠ 0` → bỏ) → log "Item AbradeToZero.Name(%s) Gener(%d) DetailType(%d) Player(%s) Account", `sprintf([0x978A614] = G_STR_ITEM_ABRADETOZERO, tên)` → `0x081C9220(1, player, [0x978A650], msg)` (thông báo), **`KItem 0x08067540(item, 1)`** (thành phế phẩm), `KItemList::Remove(idx)`, `KPlayer::AddItem 0x080B5180(player, idx, phòng 0 rồi 0xe)` không được → `KItemSet::Remove(g_ItemSet, idx, player, 0, 1)` (huỷ) | `KSubWorld::abrade_equipments` + `wear_result` (`item_changed` = 0x9b; hỏng: thông báo, `genre = broken`, `unequip` vào túi hay `take_item`, `recalc_player`); móc `cast_skill`, `receive_damage`, vòng `moved` của tick, Lua `AbradeEquipments` |
| `0x08067540` | **`KItem::ChangeToBroken?(this, cờ)`**: genre phải 0 hay 7; `genre = 7`; cờ ≠ 0 → dò bảng phế phẩm của `g_ItemGenerator+0x1e88` (mỗi dòng: genre, detail (−1 mọi), cấp (`+8`, hay khoảng `+0x10..+0x14` khi byte `+0x121`), particular `+0x10`…) khớp → chép `+0x58/+0x5c/+0x60` (detail/particular/cấp phế phẩm) vào `item+0x18/+0x1c/+0x20`, byte `+0x128..+0x12d` → `item+0x35c..+0x361` | chưa port (bảng phế phẩm chưa đọc): zone chỉ đặt `genre = broken` |
| `0x082006B0` | **`KItemList::Remove(this, idx)`**: tìm mục `+0x94 == idx` trong `list+0x4c6c` (`+0x4c74` mục, 20 byte/mục); phòng `+0x98 ≤ 0x1d` nhảy theo bảng `0x826B468`; đang mặc → `0x081FFE90(list, idx, −1, 0)` (gỡ thuộc tính), `0x081E8060(item, player, 2)` (log), xoá mục, bit `+0x4c60/+0x4c6c`, `item+0x358 = 0`, **gói 0x60** `{0x60, id}` | `KItemList::remove` / `unequip` + `G2C_ITEM_MOVE/REMOVE` |
| `0x0806DB90` | **`KItemSet::Remove(this, idx, player, lý do, cờ)`**: `idx` trong 1..max, `+0xce4 == 0`; log `0x0818A1E0(0x978B320, item, lý do, cờ)`; `KItem::Release 0x08067830`; bit `+0x10/+4`; `+0x1c −= 1` | `take_item` |
| `0x080B5180` | `KPlayer::AddItem(player, idx, …)` → `KItemList::Add 0x08205830(list, 0 (túi), idx, …)` không được → thử phòng `0xe` | `unequip(part, room_equipment)` (zone không có phòng 0xe) |
| `0x08201D90` / `0x080658B0` | **`KItemList::AbradePercent?(this, n)`** (gọi từ `KPlayer 0x080B9FA0` — chết/PK, B3c sau): `n ≤ 0` → thôi; slot 0..13 trừ 11: **`KItem 0x080658B0(item, n)`**: `+0x308 == −1` → −1; `n ∉ 1..100` hay `+0x308 ≤ 0` → không đổi; **`+0x308 −= n·+0x308/100`**; rồi cùng đường gói 0x9b / hỏng như trên | `KItem::abrade_percent`, `KSubWorld::abrade_equipments_percent` |
| `lang\vn\stringtable_core.txt` | `G_STR_ITEM_ABRADETOZERO` (TCVN3): " %s  đã tổn hại, không thể tiếp tục sử dụng, hiện trong hành trang. Hãy đến Lâm An tìm <Thần bí Thương Nhân> sử dụng tiền đồng để sửa chữa!" (bảng chuỗi nạp ở `0x0817F0B0`, con trỏ `[0x978A614]`) | `kAbradeToZeroMessage` → `msg_to_player` |

Sai khác có chủ ý: (1) gói 0x9b (độ bền) = `G2C_ITEM_ADD` cả món; (2) phế phẩm giữ nguyên detail/particular/cấp (bảng
phế phẩm `g_ItemGenerator+0x1e88` chưa đọc), chỉ `genre = broken`; (3) không có phòng 0xe → không vừa túi thì huỷ ngay;
(4) `amulet_tier` (+0x344) chưa được bộ sinh vật phẩm của zone đặt → ngọc bội luôn dùng bảng thường; (5) mòn khi đi: mỗi
tick người chơi còn di chuyển (nhị phân: mỗi khung `0x0807C2F0`).

### 16.4 Chết và hồi sinh của người chơi (M12 lát B3c-3: đường thường đã port; PK đã đọc, chưa port)

Chuỗi: `CalcDamage 0x08089C90` → **`KNpc::Death 0x08089920(this, kẻ giết)`** → `KNpc::DoDeath 0x080892E0` (nhánh người chơi
`0x080896C0`) → **`KNpc::OnDeath 0x08088B60`** (nhánh `0x08088D50`) → hết khung chết (`0x08083B50` bảng `m_Doing` 10) →
`0x08083720` (script chết) → **`KNpc::Revive 0x080833B0`** (`m_Doing = 0x15`, xác nằm chờ) → client xin (ô **118** của ctor
`0x080DA560` = `0x080DBFE0`: `0x080AEBC0(player, 12)` rồi **`KPlayer::Revive 0x080AD9F0(player, 0, 0)`**) → hành động 21
(`0x08078AA0` → `0x080886F0`: `DoStand`, `+0x194c = 1`, `+0x1950 = 1`).

| Địa chỉ | Nội dung (đã đọc từng dòng) | Zone |
|---|---|---|
| `0x08089920` | **`KNpc::Death(this, launcher)`**: người chơi → **`KPlayer::CheckAndReviveOnDeath 0x080B2790`** ≠ 0 → không chết. Không thì `+0x118c = 0`; `0x08078E80(npc, &idx, &player)` (chủ: người chơi `+0x1908`, đồng hành → chủ `+0x1698`); `0x0807A350(this, playerA, idxB, &out)` (quan hệ PK giữa hai người chơi: `+0x168c` chiến, cờ bản đồ `subworld+0x63f8c & 1`, `+0x1818 == 3`); kẻ giết là người chơi → bảng bảo hộ `Player+0x809c[[0x830D230]]` (`0x080CB5A0` đọc / `0x080CB5C0` ghi 256 ô), vật phẩm `0x081FA8A0(list, 6, 1, [0x830D22C], 1, 3, 0)`, thông báo `MSG_TONG_CREATE_ERROR01` / `MSG_PK_YUXI_2`, `Player+0x86f8` (% bảo hộ, `g_Random(100)`), **`0x080C3930(Player+0x5a50, n)`** (cộng điểm PK: `+0x34` % né, `+0x2c` ≤ 9 → `0x080C38C0`); rồi **`DoDeath(this, loại, kẻ giết)`**; người chơi sau đó `0x080C32C0(Player+0x5a50)` (`0x080C3100` + `0x080C31D0`) | `do_death` nhánh người chơi (PK: B3c-4) |
| `0x080B2790` | **`KPlayer::CheckAndReviveOnDeath`**: `+0x390` (DeathReliveFlag, Lua `SetDeathReliveFlag 0x08110600`) ≠ 0 → 0; **`KItemList 0x08202410(list, 6, 1, [0x830D128], 1, 3, 1)`** (tìm+dùng vật phẩm genre 6 detail 1 particular cấu hình) có → máu/nội đầy (`max(+0x1a14,+0x1a18)`, `max(+0x1a1c,+0x1a20)`), gói `0x343` qua `0x08125D70`, thông báo `G_PLAYER_30` → 1; không → script `+0x5f98` `OnCheckAndReviveOnDeath(…)` (`0x080ABDF0`) → `+0x78ec ≠ 0` | chưa port (vật phẩm hồi sinh, script) |
| `0x080896C0` | **DoDeath nhánh người chơi**: `0x080AEBC0(player, 0xb)`; `+0x168c ≠ 0` → `0x080AE4B0(player)` (đội: `+0x5920`, `+0x5910` trưởng `0x080AE380`); `0x080AB610(player, [0x9777F00] khung)`: `+0x28c = khung chết`, `+0x290 = 0` (cờ bỏ qua đồng bộ của `0x080E1B80`), gói 0x94 6 byte; `0x0809BBC0` xoá vòng lệnh `+0x169c..+0x171c`; `[ebp-0x30] = Player+0x384` (đấu trường). Chung: danh sách chết `0x08188BB0(+0x18bc)`, ẩn thân vỡ, `m_Doing = 0xa`, `+0x1950 = 0`, `+0x22c = max(1, +0x190c)`; rồi **`OnDeath`**; `Player+0x384 ≠ 0 && kẻ giết người chơi` → `0x08083720`, máu/nội/thể lực đầy, thông báo `G_PLAYER_27`, `0x080AD9F0(player, 2, 0)`, `\script\global\pk10_deathpunish.lua main` (không có trên máy) | `do_death`: `commands.clear()`, khung chết 15 (`spawn_player`), `ACTION_DEATH` + `emit_life`; đấu trường: B3c-4 |
| `0x08088B60` / `0x08088D50` | **`KNpc::OnDeath(this, loại chết, kẻ giết, a4)`** người chơi: `KItemList 0x08203530(list)` (rơi đồ theo bảng bảo hộ `+0x809c[[0x830D230]] == 1`, `0x080A7E90`; B3c-4); **loại 0**: `Player+0x5960 < 0` → bỏ; `+0x595c/+0x5960 == 0` → bỏ; `level_exp = KLevelAdd::GetLevelExp(0x8BAF378, player, +0x20)`; **cấp ≤ 10: 2 %** (`> 99999 → /100·2`, không thì `/50`), **cấp > 10: 3 %** (`> 99999 → /100·3`, không thì `·3/100`); **cắt 0x1fbd0 = 130 000**; **đội** (`KPlayerTeam` `Player+0x5994` m_nFlag, `+0x5998` m_nID — đính chính B3c-4: trước ghi "tông") `≠ 0 && ≥ 0` → `0x080CC620(g_Team[id] = 0x8BB86E0 + 0x30·id, player)` = **số đồng đội đang ở gần** (đội trưởng `+8` + 7 thành viên `+0xc..`, khoảng cách `0x0809E660` 0..0xfffff) → `loss·(7 − n)/7`; `loss = min(loss, exp)`; **`0x080AFEA0(player, −loss)`**; `loss > 0 && [0x830D0F0] == 1` → log "NewWorld(%d, %d, %d)" (vị trí chết); **tiền**: `half = 0x081F9E70 (list+0x4c90) / 2` > 0 → `0x081FC940(list, half)` trừ, log "C_Death" (`0x081E8EA0`), gói 0x86 `{8, 0xa, half}`, `half ≠ 1` → **`0x0807FA50(this, half/2)`** rơi tiền tại xác (`0x080F0950` ô trống, `0x080A7390` tạo vật thể tiền, `0x080A4D90`); **loại 1/3/4** → không phạt; **khác** → **`KPlayer 0x080B9FA0(player, kẻ giết)`** phạt PK | `on_death_player` (`KPlayer::lose_exp`, `cost_money` + `send_money`, `drop_money`); tông = 0; `0x08203530`/`0x080B9FA0`: B3c-4 |
| `0x080B9FA0` | **phạt chết PK** `(player, kẻ giết)`: `pk = 0x080C30F0(Player+0x5a50) (+0x2c)`, `min(10, pk)`; `+0x5a8c > 0` → `+0x5a88` (byte thấp/cao, cắt 100); bảng `0x08256EE0[pk]` kinh nghiệm mất (cap 2 000 000 000), `0x080AFEA0`; `[0x830D0F0] == 1` → log; `0x080A8120(player, &n)`, ngưỡng `[0x8BADF78]`; tiền `> 99 999` → mất `tiền/1000·0x8BB2A34[pk]` (+ % `[ebp-0x44]`), gói 0x86, rơi nửa `0x0807FA50`; `[0x830D0EC] == 1` → chuyển "NewWorld" (nhà tù, toạ độ `[0x9777F08..1C]`, log `0x978A700`); `0x08203BE0(list, 0x8BB2A38[pk])` (rơi đồ), **`0x08201D90(list, 0x8BB2A44[pk])` (mất % độ bền)**; `[0x8BADF70]` → `0x080C3930(…, [0x8BADF60])`; kẻ giết `+0x1598` người chơi → `0x080BA938`, đồng hành → `0x081C8B20` | chưa port (B3c-4; `abrade_equipments_percent` sẵn) |
| `0x08083B50` / `0x08083720` | **khung chết (`m_Doing` 10)**: `++f ≥ tổng` → log `[0x978A6E0]`, `+0x230 = tổng − 1` (giữ khung cuối) → **`0x08083720`**: npc → script `OnDeath` của `+0x1588` (`0x0807DA00`), `0x0809E070(tên)`; người chơi → `Player+0x808c` ≠ 0 → `0x08162440(Player+0x8078, 0)`; `+0x5f98` → script `OnDeath` (`0x080AC1A0`); `+0x8764 = +0x8768 = 0`; `\script\global\player_default_death.lua OnDeath(kẻ giết)` (bản đồ chiến tông: `AddPvPKilledNum`); rồi **`KNpc::Revive 0x080833B0`**; đồng hành (`+0x1698`) đường riêng `0x08164EB0` | `update_action` death → `do_revive` → `player_corpse` |
| `0x080833B0` | **`KNpc::Revive`**: vùng ≥ 0; `m_Doing == 0x15` → thôi; `+0x181c == 1` → `0x08083618` (nút hồi sinh "GoldBoss"), `== 3` hay byte `+0x1824` → nút `0x82549C0` vào danh sách `subworld+0x3c` (hồi sinh sau 0x3e9); `== 0x12` → prologue chạy; **`m_Doing = 0x15; +0x194c = 0; +0x1950 = 0`**; **`0x080823B0(this, 0, 1, 0, 1)`** (mọi ô ≠ 0 của từng nút trạng thái `+0x234` → `ModifyAttrib(…, 1)` gỡ, gói 0x87 trống 0x14 byte cho người chơi/đồng hành); **`0x08079C30`** (xoá `+0x1bc..+0x20b` = 5 khối thuốc/độc; người chơi `[0x977FF40] == +0x3f0` → `0x080A7E50(player, 0)`); `+0x1594 = +0x1598 = 0`; **người chơi → thôi** (xác nằm); npc: `+0x22c = max(1, +0x192c)`, rời vùng `0x080E2280`, `0x080F0F60(…, −2, idx)` | `player_corpse` (`KDoing::revive`, gỡ mọi trạng thái, xoá khối thuốc, mục tiêu) |
| `0x08086220` | khung `m_Doing` 21: **người chơi → thôi** (xác chờ mãi); npc `++f ≥ tổng` → `0x08085E70` (Init lại: `0x08082680`, mẫu `0x836EB00[…]`, `0x080A1C20`) | `update_action` revive: chỉ npc `revive()` |
| `0x080AD9F0` | **`KPlayer::Revive(player, kiểu, ép)`**: không ép → `m_Doing` phải 0xa/0x15, không thì `puts "Client Want to Revive But he is no deaded!"`, `0x08078AA0(npc, 1, 0,0,0)` (hành động đứng) → thôi; **gói 0x89** 6 byte `{0x89, id, byte kiểu}` cho người chơi (`0x080A8400`) và quanh (`0x0807B200`); **kiểu 0** → **`0x08080110(npc, Player+0x20, +0x28, +0x2c)`** (NewWorld tới điểm hồi sinh: `0x080F68A0` tìm subworld, cùng subworld → `0x0807AFA0`; khác → rời vùng, gói 0x4f, `0x080E3000/0x080E25C0`, `+0x1180/+0x1184/+0x148c..` = chỗ mới, `m_Doing == 0x15` → người chơi `0x08080610`: `0x080F0C80/0x080F0A60/0x080F0AD0`, `0x080AEC70`), `0x08080F70(npc, player, 1)` (gói 0xc5 vị trí 0x11 byte), `SetFightMode(0)`; **kiểu 1** → `SetFightMode(1)`; **kiểu 2** → `SetFightMode(0)`; chung: `+0x118c = max(+0x1a14,+0x1a18)`, `+0x11a0 = max(+0x1a1c,+0x1a20)`, `+0x11a8 = +0x11ac`; **hành động 21** `0x08078AA0(npc, 0x15, 0,0,0)` → `0x080886F0`: `DoStand`, `+0x194c = 1`, `+0x1950 = 1`; `\script\global\revive.lua main(kiểu)` (chiến quốc: `nationalwar_login`) | `player_revive` + `revive_request` (`C2G_REVIVE`; `ACTION_REVIVE`, `emit_life`, `send_player_attrib`; kiểu 0 → `set_pos`/`change_world_request`) |
| `0x080DBFE0` | ô **116** của bảng handler (`0x18 + 8·id`, id = gói **0x74**; đính chính B3c-4 — trước ghi 118) của `KProtocolProcess`: `0x080AEBC0(player, 0xc)` rồi `KPlayer::Revive(player, 0, 0)` — client chỉ xin về điểm hồi sinh (`0x0067D1C4`: gói 0x74 13 byte) | `C2G_REVIVE {seq}` |
| `0x0811B370` / `0x08110790` / `0x080B1E50` | Lua **`SetRevPos(map, ref)`** → `0x080B1E50(player, map, ref)`: `+0x10 = map`, `0x080F6D20(g_SubWorldSet, map, ref, &xy)` (điểm tham chiếu của bản đồ) → `+0x14 = ref, +0x18/+0x1c = xy, +0x20 = map, +0x24 = ref, +0x28/+0x2c = xy`; không tìm được → ghi `revive_error` (`[0x978A89C]`); **`SetTempRevPos(map[, x, y])`** → `+0x20`, `+0x28`, `+0x2c` trực tiếp | `l_SetRevPos` (giải điểm qua `KSubWorld::revive_point` = bảng `revive_pos.json`; không có → 0 + cảnh báo `revive point not found`), `l_SetTempRevPos` (ô × 32); `RoleData.revive_map/revive_pos/revive_ref` |
| `0x080F6D20` | **`KSubWorldSet::GetRevivePos?(set, map, ref, int* xy)`**: `sprintf("%u", map)` = mục, `sprintf("%d", ref)` = khoá, mặc định `x = 0xc800, y = 0x19000`, `KIniFile 0x0821F1F0(set+0x98, mục, khoá, &x, &y)` đọc **`\settings\revivepos.ini`** (`0x08259030`; 136 mục `[map]`: `region=a,b` = các id của bản đồ, `id=x,y` Mps tuyệt đối — map 20: `region=10,12`, `10=113472,199232`; map 53: `19=52032,101696`; map 1: `0=51104,102592`…) → trả 1 và ghi xy, không có khoá → 0 | `KRevivePosTable` (`KRevivePos.h`, `jxassets export-revive-pos` → `client/assets/revive_pos.json`), `KSubWorld::revive_point(map, ref)`; `zone.revive_pos_file` |
| `0x080C171D` (trong `KPlayer::LoadFrom 0x080C16D0`) | **Vị trí vào game**: `+0x10 = irevivalid` (map hồi sinh — Bishop ghi = thôn đã chọn), `+0x14 = irevivalx` (id điểm), `0x080F6D20(0x9777F00, map, ref, &xy)` → có: `+0x18/+0x1c = xy`; **byte `+0x68` (cUseRevive) ≠ 0** (nhân vật mới: `PlayerCreator` ghi 1) → không tìm được điểm → **`0x080C2038`: map 0x39 = 57 tại (0xc720, 0x18f40) = (50976, 102208)**; tìm được → vào `+0x10` tại `+0x18/+0x1c`; `+0x68 == 0` → `0x080C1790`: vào `ientergameid/x/y` (`role+0x87/+0x8b/+0x8f`, chỗ đứng lúc thoát); map không tìm được (`0x080F68A0 SearchWorld` < 0) → `0x080C208A`: cùng điểm 57 mặc định | `KSubWorld::spawn_player`: không có `position.pos` (nhân vật mới) → `revive_point(revive_map, revive_ref)` → `to_local`; thiếu → điểm sinh của map (thay cho 57); có `position.pos` → chỗ đã lưu; gateway `NewRole`: `revive_ref = id đầu của `region` (Bishop `GetRevivalID`) |
| `0x080AFEA0` | **`KPlayer::AddExp(player, gain64)`** (trong): `gain > 0` → cấp > 199 → thôi; `exp += gain` (giới hạn `+0x5964` cần cấp sau → lên cấp `0x080AF800`); **`gain ≤ 0`** → `exp + gain` (âm → về 0, cấp giữ) | `KPlayer::lose_exp` |
| `0x08117BC0` | Lua **`KillPlayer()`** (`GetPlayerIndex(L)` ≤ 0 → thôi): dựng 20 ô `KMagicAttrib` (0x140 byte, xoá 0): ô 0 `{75 seriesdamage_p, 100}`, ô 1 `{56 attackrating_v, 50000}`, ô 2 `{58 ignoredefense_p, 1}`, ô 3 (ô vật lý, kiểu để 0) `v0 = v2 = 200 000 000` → **`KNpc::ReceiveDamage(npc, chính mình (Player+0x3f4), series 0, không cận chiến, bảng, không kiểm AR, nDoHurt 1, quan hệ 0x1f, skill 0)`**; trả 0 giá trị. `KillNpc(tên\|số) 0x08117E00` / `KillNpcWithIdx 0x081181D0` → `0x08117CE0(người chơi, npc)`: đối số số (Lua 4 `LUA_TNUMBER` = 2) → `0x080B12C0(player, số)` (chỉ số npc theo quy ước script — chưa đọc), chuỗi → `0x080EFEE0(subworld của người chơi, tên)`; `0 < idx < [0x830CA58]` | `l_KillPlayer` (`ScriptFuns.cpp`) → `receive_damage(p, p, 0, false, 20 ô, false, 1, 0x1f, 0)`; `KillNpc*` chờ quy ước chỉ số npc của API script; client `--auto` gọi `?gm ds KillPlayer()` rồi `C2G_REVIVE` để chụp `auto_death.png` / `auto_revive.png` |
| `0x0811A140` | Lua **`AddExp(exp[, npcLevel])`**: `lua_gettop ≤ 2`, hai số → `KPlayer::AddExp 0x080B00C0(player, exp, npcLevel)` (luật chênh cấp của một lần giết; nhánh `esi ≠ 0` không bao giờ chạy) | `l_AddExp` → `KSubWorld::give_player_exp` (dùng chung với chia kinh nghiệm khi giết: lên cấp, `emit_life`, `send_player_attrib`, thụ động mở ở cấp mới); client `--auto` dùng để đạt cấp 10 trước khi thử kỹ năng Thiếu Lâm |
| `0x080A0810` (`0x080A08F6..0x080A0B30`) | **`KNpcSet::Init` đọc `\settings\npc\PKRate.ini [PK]`** (`KIniFile::GetInteger 0x0821F2C0`, không có tệp → mặc định): `rate` → `+0x1490` (mặc định 20, tệp 20) = phần trăm sát thương giữa người chơi (`CalcDamage 0x0808A368`); `FactionPKFaction` `+0x1494` (1), `KillerPKFaction` `+0x1498` (1), `EnmityPK` `+0x149c` (2), `BeKilled` `+0x14a0` (−1), `LevelDistance` `+0x14a8` (25), `ButcherPKExercise` `+0x14ac` (1), `NotSubPKExpPercent` `+0x14b0` (−50), `NotEnmityExpPercent` `+0x14b4` (−50), `NotFightExpPercent` (−80 trong tệp), `KillPartnerPK` (1) — `0x0807A350` đọc `+0x1494/+0x1498/+0x149c/+0x14a4/+0x14ac`, `0x080B9FA0` đọc `+0x14a0/+0x14b0…` (tệp máy chủ tham chiếu: `rate=20 FactionPKFaction=1 KillerPKFaction=1 EnmityPK=2 KillPartnerPK=1 BeKilled=-1 ButcherPKExercise=1 NotSubPKExpPercent=-50 NotEnmityExpPercent=-50 NotFightExpPercent=-80`) | `KSubWorldConfig::pk_damage_percent = 20`; chín khoá còn lại: B3c-4 (xuất `pk_rate.json` khi port) |
| `0x0807A350`, `0x080CB5A0/0x080CB5C0`, `0x081FA8A0`, `0x080C30F0/0x080C3740/0x080C3930/0x080C32C0`, `0x08203530`, `0x08203BE0` | PK: quan hệ, ô tạm của `KPlayerTask` `Player+0x809c` (`m_nTaskTemp[256]`, §21; ô số `[0x830D230]` giữ bảo hộ), vật phẩm bảo hộ, điểm PK `Player+0x5a50` (`+0x2c` điểm, `+0x34` % né, `+0x8/+0x10` …), rơi đồ | B3c-4 (đã đọc đầu hàm) |

Sai khác có chủ ý: (1) vật phẩm/script hồi sinh tại chỗ (`CheckAndReviveOnDeath`) chưa có; (2) đội, gói 0x94, notify
`0x080AEBC0` bỏ; (3) đồng đội gần = 0 (mất trọn 2 %/3 %); (4) điểm không có trong `revivepos.ini` → điểm sinh của bản đồ hiện tại thay cho map 57 (50976, 102208) của nhị phân;
(5) người chơi chết trên bản đồ khác điểm hồi sinh → `change_world_request` sau khi hồi máu (nhị phân `NewWorld` trong
tiến trình); (6) hành động 21 chạy ngay trong `player_revive` (nhị phân: khung sau); (7) PK/đấu trường/nhà tù/rơi đồ:
B3c-4.

### 16.5 Kỹ năng tạo npc — style 4 `0x080E8770`, sổ npc của `KPlayer +0x7d30..`, gỡ npc (M12 lát B3c-5, đã kiểm từng dòng)

| Địa chỉ | Nhị phân làm gì | Zone |
|---|---|---|
| `0x080EA920` (`KSkill::Cast`) | bảng nhảy `0x0825843C` theo style `+0x28`: style 4 → `0x080E8770(skill, launcher, p1, p2)` với **p1/p2 nguyên** (điểm x,y khi thi triển vào chỗ; `−1, chỉ số` khi vào mục tiêu — nhị phân không đổi, npc sẽ được đặt tại (−1, idx)) | `skill_cast` case `skill_style_create_npc` → `cast_create_npc`; không có chỗ → chân người thi triển (sai khác có chủ ý) |
| `0x080E8770` | **Kỹ năng tạo npc** `(sk, launcher, p1, p2)` → 1/0: launcher `+0x24 == 1` (người chơi) không thì 0; `player = g_PlayerSet + GetPlayerIndex·0x8788`; **duyệt 20 ô trạng thái `sk+0x540`** (bước 0x10): kiểu `0xb3` (`createnpc` 179) với `v0` mẫu, `v1` cấp, `v2` thời gian — **ô nào âm → trả 0 ngay** (kể cả sau khi đã tạo ô trước); `kind = byte sk+0x50` (Param1); `kind ≠ 0` → đếm sổ đang dùng (`+0x7db0`, `+0x7db8`) có byte `+0x18 == kind`; `sk+0xb4 (ChildSkillNum) <= đếm → 0`; **`+0x7d34 == 0`** → `0x080E8A94`: `0x081C9220(1, player, "", [0x978A2A0] = G_SkillList_4, len)` → 0; `idx = KNpcSet::Add 0x0813A770(v0, v1, sk+0x58 Series, npc+0x1180 subworld, p1, p2, 1, "", 0)`; 0 → 0; lấy nút đầu danh sách trống `+0x7da4` (`0x08220370` gỡ, `0x082202D0` nối vào `+0x7db0`), **`+0x7d34 −= 1`**; bản ghi `+0x7d38 + nút·36`: `+0 = v2`, `+4 = 1`, `+8 = launcher`, `+0xc = +0x14 = id npc mới ([npc+0])`, `+0x10 = idx`, byte `+0x18 = kind`, byte `+0x19 = 0`, `+0x1c = +0x20 = 0`; tên `sprintf("%s [%s]", tên mẫu 0x836EB00[v0·0xb40], npc+0x1505 launcher)` → `strncpy(npc mới+0x1505, 0x20)`, `+0x1528 = g_FileName2Id`; `npc mới+0x21c = launcher+0x21c` (camp); `[npc mới+4] > 0` → `0x0807B850(npc mới, launcher+0x220)`; **`npc mới+0x1828 = launcher`**; hết vòng → 1 | `KSubWorld::cast_create_npc` (kiểm và lấy sổ ngay, **tạo npc cuối tick** `flush_pending_summons` vì bảng thực thể có thể dời), `spawn_npc(tên, chỗ, mẫu, 0, monster, cấp, hệ)`, `KNpc::remove_on_death`, `summon_master`, `camp/current_camp` chép |
| `0x080E8F4E` (trong `CanCastSkill 0x080E8AE0`) | style 4 của người chơi: đếm sổ có `+0x18 == byte Param1` (không bỏ qua kind 0), `sk+0xb4 <= đếm → 0`; `+0x7d34 == 0` → `0x080E8FEC` cùng thông báo G_SkillList_4 → 0 | `can_cast_skill` khối người chơi |
| `0x0813A770` | **`KNpcSet::Add(mẫu, cấp, hệ, subworld, x, y, bRemoveOnDeath, tên, bKind)`**: mẫu/cấp âm → 0; `0x0809FB10(g_NpcSet 0x8BACAC0, hệ, mẫu<<16 \| cấp, subworld, x, y, −1)` → idx; **byte `npc+0x1824 = (arg7 ≠ 0)`**; tên khác "" → `+0x1505`/`+0x1528`; `arg9 == 1` → `0x08085250` rồi `+0x181c = 2` (hay 3 khi arg7) | `spawn_npc` + `remove_on_death` |
| `0x0807B850` | **`KNpc::SetCurrentCamp(this, camp)`**: `+0x220 = camp`; người chơi → `0x080AF…`; npc → gói `0x58` 6 byte `{0x58, id, camp}` `0x0807A870(this, gói, 6, 0x64, 0)` | `current_camp` (client chưa nhận gói 0x58 — B4) |
| `0x080BC1F0` / `0x080B60A0` (`0x080B68F0`) | `KPlayer` ctor: 3 bản ghi 36 byte `+0x7d38`, `+0x7d30 = +0x7d34 = 2`, danh sách trống `+0x7da4` nút 2 rồi 1, danh sách dùng `+0x7db0` (đếm `+0x7db8`); **`KPlayer::Clear`**: mỗi nút đang dùng → `0x0813A690(bản ghi)`, trả nút về danh sách trống, `+0x7d34 += 1` | `KPlayer::KSummonRecord[3]`, `summon_free`, `summon_count`, `free_summon`; `release_summons` trong `remove_player` |
| `0x0813A690` | `(bản ghi)`: `idx = +0x10`; npc `[idx+4] > 0`; vùng `+0x1184 ≥ 0` → **nút `0x82549C0`** 0x20 byte `{vtbl, 0, 0, 0, +0x10 = 0x3e9, +0x14 = idx, +0x18 = id npc (+0x14 bản ghi), +0x1c = 0}` nối vào `subworld+0x3c` (`0x082204C0`); vùng < 0 → bỏ; rồi xoá 9 dword bản ghi, trả 1 | `remove_npc` (ngay) |
| `0x080F28A8` (vòng xử lý `subworld+0x3c` mỗi khung) | nút `+0x10 == 0x3e9`: npc `idx`, **`[npc+0] ≠ id` → log `"in %d: NpcIndex(%d) remove twice"`** bỏ; vùng < 0 → `KNpcSet::Remove 0x0809F560(g_NpcSet, idx)`; vùng ≥ 0 → `0x080F2A50` gỡ khỏi vùng rồi Remove; `0x7d1` = nút khác (đồ vật) | `flush_doomed` cuối tick |
| `0x080833B0` (`KNpc::Revive`, §16.4) | `+0x181c == 3` **hay byte `+0x1824`** → nút 0x3e9 vào `subworld+0x3c` thay vì hồi sinh: **npc triệu hồi chết là bị gỡ ở khung sau** | `do_revive`: `remove_on_death` → `doomed_` → `flush_doomed` |
| `0x0817F0B0` (`0x08180075`) | bảng chuỗi: `[0x978A2A0] = GetString(0x978B0C0, "G_SkillList_4")` = *"Số lượng lính đánh thuê tuy đã đạt cực hạn, vẫn có thể thuê thêm! "* (`lang/vn/stringtable_core.txt`, TCVN3) | `kSummonLimitMessage` (`KSkills.cpp`), `msg_to_player` |

Không ai đọc: thời gian `+0` của bản ghi (npc triệu hồi **không hết hạn**), `npc+0x1828` (chủ) — đã quét `re_scan disp`; **chết không
trả sổ**: một kind chỉ triệu hồi tối đa ChildSkillNum lần cho tới khi nhân vật rời (`Clear`). Sai khác có chủ ý: (1) npc tạo cuối
tick (nhị phân tạo ngay trong cast; bảng thực thể của zone có thể dời khi chèn); (2) thi triển vào mục tiêu → chân người thi
triển thay cho (−1, idx); (3) gói 0x58 (camp hiện tại) chưa ra client; (4) `Series −1` giữ hệ của mẫu.

### 16.6 Ngựa — `KNpc::SetHorse 0x0807D520`, `+0x199c`, mặc/cởi ngựa, lên/xuống ngựa, `HorseLimit` (M12 lát B3c-6, đã kiểm từng dòng)

| Địa chỉ | Nhị phân làm gì | Zone |
|---|---|---|
| `0x0807D520` | **`KNpc::SetHorse(this, n)`**: byte `+0x1479` (`frozen_action` 251) ≠ 0 → thôi; `+0x199c = n`; người chơi → `0x080AEBC0(player, 3)` (sự kiện script 3); `n ≠ 0 && +0x19a0 > 0` → `0x0807D4C0` (ẩn thân vỡ). Không gửi gói (cờ 0x20 đi theo gói 0x4c/0x4d kỳ sau) | `KSubWorld::set_horse` (+ `G2C_ENTITY_RIDE` ngay khi đổi) |
| `0x081FE380` (`KItemList` mặc, ô 10 ngựa, `0x081FE752`) | `0x080688B0(g_ItemSet, item+0xc cấp, item+0x24)` ≥ 0 → `SetHorse(npc, 1)`, < 0 → `SetHorse(npc, 0)`. **`0x080688B0(set, cấp, k)`**: `k == 0` → −1; không thì `KTabFile::GetInteger(set+0x80, dòng k + cấp·10 + 2, cột 2, mặc định 2)` − 2 (bảng ngựa của `KItemSet+0x80` — chưa xuất) | `item_equip_request`: ô `itempart_horse` → `set_horse(1)` rồi `recalc_player` (mọi ngựa đều cưỡi được — chưa có bảng) |
| `0x081FFFB0` (cởi, `0x08200311`) | ô 10 → `+0x14e8 = −1`, `SetHorse(npc, 0)` | `item_unequip_request` → `set_horse(0)` |
| `0x080C1F83` (`KPlayer::LoadFrom`) | `+0x14e8 = 0x080688B0(set, 0, 0)` = −1, `SetHorse(npc, 0)`; đồ mặc nạp qua đường mặc → ngựa đang mặc thì cưỡi | `KPlayer::load_from`: `npc.horse = ngựa đang mặc ? 1 : 0` trước `updata_cur_data` |
| `0x080AF3E0` (`KPlayer::ReCalcEquip`, `0x080AF4EA`) | ô 10 (ngựa) chỉ cộng thuộc tính khi `+0x199c ≠ 0`; `+0x19e8 = 0` cuối hàm | `updata_cur_data`: bỏ qua `itempart_horse` khi `horse == 0` |
| `0x0808BD3D` (thể lực người chơi trong `ProcessState`, mỗi 10 khung) / `0x08080C50` (bước chạy `m_Doing` 3) / `0x0807B430` (đi bộ) | `gain = Player+0x86b4 (ForbitStamina, Lua 0x0810CCC0) ≠ 0 ? 0 : +0x11b4`; `m_Doing == 3` (chạy) → `thể += gain − [0x8BADF80/84/88]` theo `Player+0x5a50` (0 `ExerciseRunSub`, 1 `FightRunSub`, khác `KillRunSub`; server thật 1/1/18); `== 8` (ngồi) → `+= gain + +0x11b0` (SitAdd ‰ max); khác → `+= gain`; kẹp `[0, +0x11ac]`. Bước chạy mỗi khung `0x08080C50`: người chơi `thể < ngưỡng` (cùng ba mốc) → `0x0807B430` (`m_Doing = 2`, gói 0x50, khung = `+0x161c·+0x1920/+0x1288 + 1`) rồi bước đi bộ `0x08080B70` (không trừ thể); đủ thể → bước `0x08080900(+0x128c)` = **`m_CurrentRunSpeed` đơn vị/khung** (người chơi 10 → 180/giây), `+0x1290 ≠ 0` → `+0x1294` | `process_state` (gain/cost/ForbitStamina), `player_move_speed` (chạy `run_speed·tick_hz`, kiệt sức `walk_speed·tick_hz`, kiểm mỗi tick khi đang đi → `emit_move`), `KPlayer::pk_state/forbid_stamina`, Lua `ForbitStamina`; bỏ `RoleStats.move_speed` (200 của persist không phải luật); ngồi: §16.14 (`stamina_sit_add`); test `[stamina]` |
| `0x0807D270` (`KNpc::ReCalcStateEffect`, gọi từ `KPlayer::UpdataCurData` qua `0x080AF3B0` và `0x08164F70`) | `+0x19e8 = 1`; duyệt danh sách trạng thái `+0x234` (nút kế `+4`), mỗi ô 20 × 16 byte từ `+0x24`: `type ≠ 0` → `ModifyAttrib(npc, idx, {type, −v0, −v1, −v2}, 0)` `0x0807D210` (nút giữ giá trị **đã đổi dấu** nên đổi dấu lần nữa = áp lại); `+0x19e8 = 0`. Thứ tự trong `UpdataCurData 0x080AF550`: `ClearAttrib` → 5 điểm → `ReCalcStateEffect` → `ReCalcEquip` | `KPlayer::updata_cur_data` (đã có từ 2026-09-18): vòng `state_skills` áp `−states` trước trang bị; `recalc_player` truyền `skill_host`/`set_hide`; test `[command]` buff 1102 giữ +10 qua `recalc_player`, hết hạn về gốc |
| `0x080AEFA0` (từ handler `0x080DBA90`) | **lên/xuống ngựa theo gói client** `(player, gói)`: `0x080A9400(player, 1)` ≠ 0 → thôi; npc `m_Doing == 8` (ngồi) → thôi; `0x08078E00(npc, 8)` ≠ 0 → thôi; `+0x1479` → thôi; trạng thái hiện tại == yêu cầu (byte `gói+0xa`) → thôi; `Player+0x458` (chỉ số ngựa đang mặc) ≤ 0 → thôi; **gói 0x9c** 16 byte `{0x9c, gói+0..+0xb, word +0xc, byte +0xe}` cho người chơi (`0x080A8400`); `SetHorse(npc, byte gói+0xa == 0)`. Không `UpdataCurData` (thuộc tính ngựa đổi ở lần tính sau) | `ride_request(sid, on, seq)` (`C2G_RIDE`): `frozen_action`, cùng trạng thái, không ngựa → từ chối; `set_horse` + `recalc_player` ngay (sai khác có chủ ý); đang ngồi → từ chối (B6a); `0x08078E00` chưa có |
| `0x080E8C9F` (`CanCastSkill`) | `HorseLimit +0xac`: 0 → qua; **1 → `0x080E8ED2`: `+0x199c ≠ 0` → 0** (chỉ khi đi bộ); **2 → `0x080E8CBB`: `+0x199c == 0` → 0** (chỉ khi cưỡi); khác → 0 | `can_cast_skill` (trước: 1 luôn qua, 2 luôn từ chối) |
| `0x080847B0` (`SetSkillCoolTime`, `0x080847F9` / `0x0808482E`) | `+0x19d8 == id && +0x19dc == 3` → `mod = +0x199c ? +0x19e0 : +0x19e4`; `GetTimePerCast(bHorse = +0x199c)` (vtable+0x58) − mod | `set_skill_cool_time`: cột `TimePerCastOnHorse` + `state_modifier.index` (+0x19e0) khi cưỡi |
| `0x0807BF20` / `0x080810C0` | gói **0x4a** (sau 0x4c của người chơi, `0x0807FF5C`) / **0x4b** (phần người chơi của `0x080810C0`, `0x08081390`): bit 0x20 của byte cờ = `+0x199c ≠ 0`; cùng gói mang hàng dáng trang bị `+0x14dc..` (§16.15) | `EntityInfo.riding`, `EntityRide` |
| `0x08111730` | Lua **`GetRideState()`** → `+0x199c` | `l_GetRideState` |
| `0x080DC300` | handler ngồi (`0x08078AA0(npc, 1|8 …)`): **đang cưỡi → từ chối** | `sit_request`: `horse ≠ 0` → thôi (B6a, §16.14) |
| `0x080CAD40`, `0x080FABA0` | đọc `+0x199c` (`0x080FABA0`: cùng `vtable+0x58` GetTimePerCast(bHorse) khi tính hồi chiêu chỗ khác; `0x080CAD40`: trả −1 khi …) — chưa cần | — |

Sai khác có chủ ý: (1) ~~không có bảng ngựa `KItemSet+0x80`~~ (B6b: `HorseRes.txt` qua `export-item-res` → `horse_res(particular, level) ≥ 0` mới cưỡi, §16.15); (2) đổi trạng thái gửi `G2C_ENTITY_RIDE`
ngay và tính lại thuộc tính ngay; (3) sự kiện script 3, gói 0x9c, `0x08078E00` chưa có (ngồi: B6a); client chưa vẽ ngựa (B4).

### 16.7 Môn phái — `KFaction` (`g_Faction 0x830BEC0`, `门派设定.ini`), sổ môn phái `KPlayer+0x59cc`, `SetFaction`, gói 0x7b/0x7c/0x59/0x58 (M12 lát B4b-1, đã kiểm từng dòng)

Tệp: `\settings\faction\门派设定.ini` (**thiếu trong `server1/settings/faction`**, có trong `D:\ServerLinux\Patch\settings\faction\`
với tên GBK đọc thành cp1252 `ÃÅÅÉÉè¶¨.ini`; `jxassets export-faction` tìm cả hai tên qua chuỗi `server1;Patch` trong
`config/oldgame.local.json`) và `factionskill.txt` (`FactionId, SkillId` — chỉ script `script/global/factionskill.lua` đọc, C++ không).
Script gia nhập (`script/global/pgaming/npc/chuongmoncacphai/thieulam.lua` `defection_complete`): `SetRevPos(103,51)`,
`SetTask(7,10*256)`, **`SetFaction("shaolin")`** (tên mã `Name`, không phải `ShowName`), `SetCamp(1)`, `SetCurCamp(1)`, `SetRank(1)`,
`AddMagic(10)`, rồi sau mỗi nhiệm vụ thăng cấp `add_sl(bậc)` của `script/global/skills_table.lua` (mỗi phái một hàm: `add_tw add_sl add_tm
add_wu add_em add_cy add_tr add_gb add_wd add_kl add_hs`; `add_misc(bit)` kỹ năng chung 210/400/397). **`add_sl(bậc)`** (dòng 439): bậc ≥ 10 →
14, 10 (nhập môn); ≥ 20 (nhiệm vụ cấp 10) → 8, 4, 6 (ba nhánh Quyền/Bổng/Đao); ≥ 30 → 15; ≥ 40 → 16; ≥ 50 → 20; ≥ 60 → 271, 11, 19; ≥ 70 (hồi sư)
→ 273, 21 (trấn phái); ≥ 90 → `AddMagic(318,1)`, 319, 321 ("Cao" — 高级绝学, chỉ ở nhiệm vụ 90); mỗi kỹ năng chỉ khi `HaveMagic(id) == -1`.
Vậy nhân vật mới **không** có ô "Cao"; `factionskill.txt` (`G_FactionSkill` của `factionskill.lua`) là danh sách phẳng để gỡ khi rời phái, không
phải thứ tự phát. `faction_def.lua` `AddFacSkill(nFacId, nLv)`: với mỗi kỹ năng của `tbSkills[fac][lv]` (`tbSkillID` — Thiếu Lâm chỉ có `[10] = {14, 10}`),
`HaveMagic(id) == -1` → `AddMagic(id, lv)`. Zone chạy được `Include("\\script\\global\\skills_table.lua")` rồi `add_sl(20)` (cây script đã chuyển
`data/script/server1`); `--auto` của client làm đúng thế.

| Địa chỉ | Nhị phân làm gì | Zone |
|---|---|---|
| `0x08060C70` | **`KFactionSet::Init`** (gọi từ `0x0805EA10` "Initting g_Faction"): 11 mục 0xcc byte tại `g_Faction`: `+0 = i`, `+4 = 0` (ngũ hành), `+8 = 1` (phe C_JUSTICE), `+0xc` `Name[64]`, `+0x4c` `ShowName[128]`; `KIniFile::Load("\settings\faction\门派设定.ini")` thất bại → 0; mỗi mục `i` (`sprintf("%d")`): `Name`, `ShowName`, `Series` so `S_GOLD S_WOOD S_WATER S_FIRE S_EARTH` → 0..4 (không khớp giữ 0), `Camp` so `C_BEGIN C_JUSTICE C_EVIL C_BALANCE C_FREE C_ANIMAL C_EVENT` → 0..6 (không khớp giữ 1) | `KFaction::load(faction.json)`; Go `jxold/player/KFaction.go` `ParseFaction` (+ `[Name] New/Old`) |
| `0x08060C00` | `FindByName(g, ngũ hành, tên)`: ngũ hành > 4 (so **không dấu** → âm cũng thôi) hay tên rỗng → −1; `strcmp(tên, mục+0xc)` bằng → chỉ số | `KFaction::id_by_name(series, name)` |
| `0x08060BB0` | `Allows(g, ngũ hành, chỉ số)`: ngũ hành ≤ 4, chỉ số ≤ 10 và có mục `+0 == chỉ số && +4 == ngũ hành` | `KFaction::allows` — **nhân vật khác ngũ hành với môn phái thì không vào được** |
| `0x080C2590` / `0x080C25C0` | `KPlayerFaction` ctor (gọi từ `KPlayer` ctor `0x080BC423`) / `ClearFactionRecord`: `+0 hiện tại = −1`, `+4 đầu tiên = −1`, `+8 sau cùng = −1`, `+0xc số lần = 0` | `KPlayerFaction{-1,-1,-1,0}`, `reset()` |
| `0x080C25F0` | `ClearFaction`: chỉ `+0 = −1` (số lần và sau cùng giữ) | `clear_current()` |
| `0x080C26F0` | `Add(rec, ngũ hành, chỉ số)`: `Allows` sai → 0; `+0 = chỉ số`, `+0xc += 1`, lần đầu → `+4 = chỉ số`; `+8 = chỉ số`; 1 | `KPlayerFaction::add` |
| `0x080C2750` | `Add(rec, ngũ hành, tên)` = `FindByName` rồi `Add` | `KSubWorld::set_faction` |
| `0x080C2610` | `Camp(rec)`: hiện tại 0..10 → `mục+8` (âm → 0); hiện tại −1 → số lần ≠ 0 ? **4 (C_FREE)** : **0 (C_BEGIN)** | `KPlayerFaction::camp` |
| `0x080C2680` / `0x080C27B0` | `Name(rec, buf, n)` / `LastName`: hiện tại (sau cùng) 0..10 → `Name` của mục; −1 → số lần ≠ 0 ? chuỗi `G_FACTION_OLD` (`[0x978a0b0]`, bảng chuỗi toàn cục `0x0817F0B0` — **tệp bảng chuỗi không có trong dữ liệu**; zone lấy `[Name] Old` của ini, cùng nghĩa) : "" | `name()` / `last_name()` |
| `0x080AEEC0` | **`KPlayer::SetFaction(player, tên)`**: `0x080C2750(rec, npc+0x28 ngũ hành, tên)` → 0 → thôi; `SetCamp(npc, Camp(rec))` (`0x0807B7B0`); **gói 0x7b** `0x080A8880` | `set_faction(e, name)` |
| `0x080AEDE0` | **`KPlayer::ClearFaction`**: `0x080C25F0`; `SetCamp(npc, 4)`; **gói 0x7c** 1 byte cho người chơi | `clear_faction(e)` |
| `0x080A8880` | **gói 0x7b** 8 byte `{0x7b, byte phe npc+0x21c, byte +0x59cc hiện tại, byte +0x59d4 sau cùng, dword +0x59d8 số lần}` cho người chơi (`0x080A8400`) | `G2C_PLAYER_FACTION {camp, faction, faction_last, faction_count}` (xoá = cùng gói với −1) |
| `0x0807B7B0` | **`KNpc::SetCamp(npc, phe)`**: `+0x21c = phe`; npc người chơi → `0x081621B0(Player+0x8078, phe, +0x220)` (danh sách móc, gọi `vtable[0](obj, 1, &out)` từng mục — không có gì cho zone); **gói 0x59** 6 byte `{0x59, dword id npc, byte phe}` quanh 100 ô (`0x0807A870`) | `set_camp(e, camp)` → `G2C_ENTITY_CAMP {entity_id, camp, current_camp}` |
| `0x0807B850` | `KNpc::SetCurrentCamp`: `+0x220`, gói 0x58 (đã dùng ở §16.5) | `set_current_camp` |
| `0x080C1A62` (trong `LoadFrom 0x080C16D0`) | từ bản ghi DB: `+0x59cc = int8 [rec+0x66]`, `+0x59d4 = int8 [rec+0x65]`, `+0x59d8 = int [rec+0x6b]`; `+0x59d0` (đầu tiên) **không nạp** | `KPlayer::load_from`: `RoleData.faction / faction_last / faction_count` (persist v4: bản ghi cũ 0 → −1) |
| `0x080B23B9` (`SaveTo 0x080B22B0`) | ghi `byte hiện tại`, `byte sau cùng`, `word số lần` | `save_to` |
| `0x080A9750` (+0xb0e/+0xb12; gọi từ `0x080AE090`) | đồng bộ lúc vào game mang hiện tại + sau cùng | `PlayerAttribSync.faction / faction_last` |
| `0x0811A540` | Lua **`SetFaction(tên)`** → 1/0: không người chơi → 0; tên rỗng → `ClearFaction` → 1; else `KPlayer::SetFaction` | `l_SetFaction` |
| `0x0811A5D0` / `0x08113C60` / `0x0811A4C0` / `0x0810E6D0` / `0x0810E4B0` / `0x0811A480` | Lua `GetFaction()` (tên hiện tại, "" không người chơi) / `GetFactionNumber()` (`+0x59cc`, −1) / `GetLastAddFaction()` (tên sau cùng) / `GetLastFactionNumber()` (`+0x59d4`; **có tham số → −1**) / `SetLastFactionNumber(n)` / `ClearFactionRecord()` | cùng tên |
| `0x0811B2E0` / `0x0811B1D0` / `0x08114650` / `0x081146C0` | Lua `SetCamp(n)` (n ≥ 0 → `SetCamp`) / `SetCurCamp(n)` / `GetCamp()` (`+0x21c`, nil không người chơi) / `GetCurCamp()` (`+0x220`) | cùng tên |
| `0x0812C430` | Lua **`AddMagic(id|tên [, cấp])`**: ≥ 1 tham số + người chơi; tên → `SkillId` của dòng (`0x830ae00`); cấp mặc định 0; cấp > 1 → id 1..1999 và cấp ≤ 63 và phiên bản (id, cấp) tồn tại (`0x0812C620`: cache `0x8bc99e0` hay `InstanceSkill`); `KSkillList::Add(npc+0x248, id, cấp, 0,0,0,0)` `0x080E5420` → **gói 0x5e** 15 byte `{0x5e, id, cấp đang giữ `0x080E43F0`, Player+0x5928 điểm còn, word 0}`; không trả gì | `l_AddMagic` |
| client `0x00651280` (ô 0x7c của bảng `0x0065AEE0` = gói **0x7b**, bảng lệch một) / `0x006511B0` (gói 0x7c) / `0x006500C0` (gói 0x59) | xem `CLIENT-2.0.md` §6 | |

Chưa: `SetRank` (`0x081109E0`, danh hiệu `factiontitle.txt`), `Msg2Faction`, `0x081621B0` (danh sách móc `+0x8078`), gia nhập qua NPC
(script `thieulam.lua` cần `SetTask`/`AddNote`/`SetRank`…), "門派多修" (client có UI, `jx_linux_y` không có).

### 16.8 Trạng thái chiến đấu `+0x168c` — không có gói client; bẫy cổng, thổ địa phù, khôi phục lúc vào game (M12 lát B4b-1, đã kiểm)

`re_scan disp 0x168c`: chỉ `KNpc::SetFightMode 0x08079B30` (và `Init 0x0807E0E8`) ghi; không handler gói client nào gọi (bảng `0x080DA560` rút hết).
Người gọi `SetFightMode`: **script bẫy** `SetFightState 0x08117A10` (`script/maps/*`: ra cổng thành `SetFightState(1)`, vào thành `(0)` — cách JX2 vào/ra
chiến đấu), **khôi phục lúc vào game** `0x080B5DF0` (`Player+0x8074 = 1`, `SetFightMode(npc, +0x1690)` = giá trị lưu; zone: `RoleData.fight_mode`),
`KNpcSet::Add 0x0809F910` (npc thường: bật), `KPlayer::Revive 0x080AD9F0` kiểu 0 (tắt), **thổ địa phù** Lua `UseTownPortal 0x08117900` → `0x080B59D0`
(đang chiến → lưu `Player+0x30/+0x38/+0x3c` = map/toạ độ hiện tại, `+0x34 = 1800` khung, `0x08080110(npc, Player+0x20/+0x28/+0x2c)` về điểm hồi sinh,
`SetFightMode(0)`; cũng gọi từ dùng vật phẩm `0x08204710` với sự kiện `UseTownPortalEvent:OnEvent`) / `ReturnFromPortal 0x081178C0` → `0x080AD960`
(về chỗ đã lưu, `SetFightMode(1)`), `0x080DD37D` (một lệnh con của handler `0x080DBA90` → `UseTownPortal`), `0x081EAA80` (đồng hành chép trạng thái
chủ), `0x08050580` (khôi phục `+0x1690`). Vậy client 2.0 không có nút/gói "vào chiến đấu"; `--auto` của zone dùng `SetFightState(1)` qua GM đúng như
bẫy cổng. Chưa port: thổ địa phù (`+0x34` đếm lùi ở đâu tiêu — đọc tiếp khi làm vật phẩm), `+0x8074`.

### 16.9 Gói trạng thái 0x87 — `SetStateSkillEffect 0x08086892` / `RemoveStateSkillEffect 0x0807D40A` (M12 lát B4b-4, đã kiểm)

Sau khi nút trạng thái mới đầy (`0x08086870` xoá 20 ô, rồi điền), chỉ khi npc là người chơi (`+0x24 == 1`, `0x08086902`): gói `{byte 0x87; word cỡ =
0x153 − (0x14 − n)·16 = 19 + n·16; dword id npc; dword kỹ năng (`[ebp+0x10]`); dword cấp (`[ebp+0x14]`); dword thời gian khung (`[ebp+0x20]`); byte a9
(`[ebp+0x34]`); memcpy n × 16 byte trạng thái (`[ebp+0x18]`, n = `[ebp+0x1c]`)}` → `0x080A8400(người chơi, buf, cỡ)`. Các nhánh gặp nút cũ (`0x08086730`:
`time == 0`, cùng cấp, cấp thấp hơn, làm mới) trả về trước — **không gửi**. `RemoveStateSkillEffect` với `bNotify` (`0x0807D40A`): gói rỗng `{0x87, 0x13,
id npc, kỹ năng, cấp 0x3f, thời gian 0, byte 0}` cho người chơi, và cho chủ của đồng hành (`+0x1698`, `0x0807D457`). Client: ô 0x88 của bảng handler
`0x006526E0` → `KNpc::SetStateSkillEffect 0x005EDFC0` phía client, sổ trạng thái nuôi `KUiSkillState` (`CLIENT-2.0.md` §9).
Zone: `KSubWorld::emit_state` (`G2C_ENTITY_STATE 2122`: `entity_id, skill_id, level, time, special_id, removed, states[]`), gọi sau `push_back` nút mới và trong
`remove_state_skill_effect(notify)`; giá trị gửi là giá trị kỹ năng áp (nút giữ bản âm). Chưa: đồng hành.

### 16.10 Hào quang — `KNpc::SetAura 0x08087290`, gói client 111 (`0x080DC460`), tick `0x080873B0`, biểu tượng `0x08087160` / gói 0x7a `0x08079F60` (M12 lát B4d-1, đã kiểm từng dòng)

Hào quang (`IsAura`, 50 dòng `skills.txt`, tất cả `LRSkill 2`, style 2, dạng 7, có `StateSpecialId`) **không thi triển bằng lệnh kỹ năng** (`NpcSkillCommand 0x080DD130` từ chối
`IsAura`); nó được **bật** vào `KNpc+0x244` rồi mỗi 10 khung server tự thi triển kỹ năng con lên chính npc.

| Địa chỉ | Làm gì | Zone |
|---|---|---|
| `0x08087290` | **`KNpc::SetAura(this, id)`**: `id − 1 > 0x7ce` → **xoá** (`+0x244 = 0`, `+0x4c = 2`); `cấp = GetCurrentLevel(sổ +0x248, id, 1)` phải 1..63; bản `[0x8bc99e0 + ((id<<6) + cấp + 0x22ab0 − 1)·4]` (không có → `InstanceSkill 0x080E6E10`) phải `IsAura` (vtable+0x4c); → `+0x244 = id`, **`0x08079240(this, bản+0x64 StateSpecialId, bản+0x68 StatePriority)`** (thêm biểu tượng); không đạt → xoá như trên | `KSubWorld::set_aura` |
| `0x080DC460` (ô **111** của ctor `0x080DA560`: **ô = (disp − 0x18)/8**, disp 0x390 — đính chính công thức §16: NpcSkillCommand ở disp 0x280 là ô **77**, không phải 80) | gói client `{byte 0x6f, dword id}`: npc của người chơi hợp lệ; **`Player+0x375 ≠ 0` (Lua `ForbitAura`) → `SetAura(npc, 0)`**; không → `SetAura(npc, id)` | `C2G_SET_AURA` → `set_aura_request` |
| `0x0808BB83` (trong khối `% 10` của `ProcessState`, sau boss `+0x181c`) | `+0x244 ≠ 0` → `find_same` ô ≤ 79 và **`ô+0x28 ReqLevel ≤ cấp npc +0x20`** → `0x080873B0(npc, +0x244, GetCurrentLevel(+0x244, 1))` | `process_state` |
| `0x080873B0` | `(npc, id, cấp)`: `cấp ≤ 0` hay `+0x19a0` ẩn → thôi; `con = bản(id, cấp)+0xb8` (ChildSkillId); **gói 0x85** 25 byte `{0x85, −1, con, id npc, id npc, cấp, byte 0}` → `0x0807A870(…, 0x19, 0x64, 0)` khi `con > 0` (người chơi: chỉ khi **`Player+0x388 == 1`**, Lua `ForbitSyncAura`); `IsAura` → `InstanceSkill(con, cấp)` → `+0x118 = 1; Cast(con, npc+4, x, y, 0, 0, 0); +0x118 = 0` (`+0x118` → `KMissleSet::Add` tham số 5 → `+0x168`, bị mẫu đè — không tác dụng) | `cast_skill_effect` (không gói 0x85: đạn con đã đồng bộ qua `G2C_MISSLE`) |
| `0x08087160` | **dựng lại 6 biểu tượng** (`+0x54`, `+0x50 = 6`, `+0x4c = 1`): người chơi → `0x08079240(Player+0x7dec, 100)`; `+0x244` ở cấp 1..63 → biểu tượng của bản hào quang; rồi mỗi nút trạng thái `+0x234` (`+0x164/+0x168`) | `rebuild_state_icons` (chưa có `Player+0x7dec`) |
| `0x0808BF5C` (`KNpc::Activate 0x0808BE80`) | mỗi khung: `+0x4c > 1` → `0x08087160`; `+0x4c ≠ 0` → **`0x08079F60`** | vòng tick (`state_flag`) |
| `0x08079F60` | **gói 0x7a** 11 byte `{0x7a, 6 byte biểu tượng (`+0x54 + i·8`; người chơi: 0 trừ khi `+0x388 == 1`), dword id npc}` → người chơi trong 100 ô (`0x080E1C80`, vtable+0x2c); rồi `+0x4c ≤ 1 → 0` | `emit_state_icons` → `G2C_STATE_ICONS 2125` |
| `0x0809E054` / `0x08105879` | **đính chính (B4d-2)**: `0x0809E054` nằm trong `KNpcGold::SetGoldTypeAndBackData 0x0809D8D0` (quái vàng: `SetNpcSkill(sổ, 5, Skill5 của mẫu vàng, "Level5")` rồi `SetAura`) — không phải mẫu npc thường (§16.11); Lua `SetNpcAuraSkill(idx, id)` `0x081057B0` → `SetAura` | quái vàng chưa; `SetNpcAuraSkill` chờ quy ước chỉ số npc |
| `0x08111560` Lua `ForbitAura(n)` | `Player+0x375 = (n ≠ 0)`; cấm → `SetAura(npc, 0)` | `l_ForbitAura` |
| `0x0810CC10` Lua `ForbitSyncAura(n)` | `Player+0x388 = 0` khi `n ≠ 0`, 1 khi 0 (`0x08079F60` gửi lại) | `l_ForbitSyncAura` |

Zone: `KNpc::aura_skill_id`, `KPlayer::forbid_aura / sync_aura`, `set_aura` / `set_aura_request` / `cast_skill_effect` / `rebuild_state_icons` /
`emit_state_icons` (`KNpc.cpp`, `KSubWorld.cpp`), test `[command][aura]`. Sổ đánh lùi cũ ghi `+0x244` là "kỹ năng vũ khí bị động" — **sai**: là hào quang đang bật.
Client 2.0: `CLIENT-2.0.md` §13.

### 16.11 Hào quang và kỹ năng bị động của mẫu npc — `AuraSkillId`/`AuraSkillLevel`, `PasstSkillId`/`PasstSkillLevel` của `npcs.txt`, `InitNpcLevelData 0x080A37A0`, `0x08085250`, `+0x181c`, `ProcessState 0x0808BAF6` (M12 lát B4d-2, đã kiểm từng dòng)

`settings\npcs.txt` của server có bốn cột cuối `AuraSkillId`, `AuraSkillLevel`, `PasstSkillId`, `PasstSkillLevel` (457 dòng có hào quang: Lưu Thủy 86, Vòng tròn miễn dịch 539,
Ngũ hành trận 146, La Hán Trận 16, Thất Tinh Trận 159…; 746 dòng có kỹ năng bị động: Miễn dịch băng 297, Sát thủ 547, Tất cả trạng thái miễn dịch 309…). Bảng client 2.0
không có (client không cần).

| Hàm | Luật (đã đọc) | Zone |
|---|---|---|
| `KNpcTemplate::InitNpcLevelData 0x080A2160` (phần `0x080A37A0`) | sau `Level4`: `AuraSkillId` = `GetInteger(dòng, 0)` (`0x080A37C5`) > 0 → `+0x10fc = id`; chuỗi `AuraSkillLevel` (`0x080A380C`, 100 byte, mặc "") **rỗng → xoá cả hai**; không rỗng → `GetNpcLevelDataFromScript(script, hệ, "AuraSkillLevel", cấp, chuỗi) 0x080A2070` → `+0x1100`; `≤ 0 → xoá cả hai`; **`> 0x40 → 0x40`** (`0x080A3AB2`). `PasstSkillId`/`PasstSkillLevel` y hệt → `+0x1104/+0x1108` (`0x080A3867..0x080A3B1D`). Script: `npclevelscript.lua` `GetData(cấp, a, b) = floor(b·cấp + a)` với "a\|b"; **`makeboss.lua`/`property.lua` `SetAuraSkillLevel = Linear(cấp, a, b) = floor(a·cấp + b)`, ≥ 64 → 63** (thứ tự tham số ngược nhau!) | `KNpcLevelData::aura_skill_id/level`, `passive_skill_id/level` (`skill_pair` trong `KNpcTemplate.cpp`; không script: "a\|b" = a + b·cấp như `npclevelscript.lua`) |
| `0x08085250(npc)` | bản ghi cấp `0x836EB00[(mẫu·0x2d0 + (cấp+1)·0x78 + hệ)]`; `+0x10fc > 0` → **`SetNpcSkill(sổ, 5, id, +0x1100)`** (không `SetAura`); `+0x1104` 1..1999 và `+0x1108` 1..63 → bản `[(id−1)<<6 + cấp + 0x22aaf]` (`0x080E6E10` tạo nếu chưa) → **`vtable+0x10 == 3`** (style bị động) → `SetNpcSkill(sổ, 6, id, cấp)` rồi **`KSkill::Cast 0x080EA920(bản, npc, −1, npc, 0, 0, 0)`** (trạng thái lên mình vĩnh viễn) → trả 1; cấp `> 63` → trả 0 (không đặt gì) | `KSubWorld::init_template_skills` |
| Ai gọi `0x08085250` | **Đính chính B5a (2026-09-18)**: `0x080F0320` KHÔNG phải bộ nạp vùng — nó là bộ sinh npc của bản ghi SDB "GoldBoss" (`0x0820A460` đọc bản ghi `Adventure/LuckyStar/GoldBoss/Lottery…` → `0x0820AC50`), chọn vùng ngẫu nhiên ≤ 200 lần rồi `0x0809FB10` + `0x08085250` + **`+0x181c = 1`** (`0x080F0412`). Npc đặt sẵn của bản đồ đi đường `KRegion::LoadNpc 0x080E2850` (`Region_S.dat`, `%s\v_%03d\%03d_%s`) → `KNpcSet::Add 0x0809FBD0(g_NpcSet, subworld, &KSPNpc, 1)` → `0x0809FB10` → `0x0809F910`: **không** gọi `0x08085250`, **`+0x181c` giữ 0** (chỉ `0x0807DBD0/0x0807E5D0/0x080833B0` ghi 0; ghi khác 0 chỉ ở `0x080F0412` (1), `0x0811BF12/0x0811BF2A`, `0x0811C2F2/0x0811C30A` (3/2) — `re_scan disp 181c`). Lua `AddNpc 0x0811BB10` tham số 8 = 1 → `0x08085250` + `+0x181c = 3` khi tham số 6 xoá-khi-chết, không thì 2; tham số 8 = 2 → quái vàng (§16.12); `AddNpcEx 0x0811BF40` y hệt; `KNpcSet::Add 0x0813A770` **chỉ khi bKind == 1** (kỹ năng tạo npc `0x080E8770` truyền 0 → không hào quang/bị động); `0x08085E70` (Init lại sau hồi sinh, chỉ khi `+0x181c ≠ 0`); `0x081EB040` | `spawn_npc(…, boss_flag)`: **bản đồ 0** (sửa B5a; trước ghi 1), npc thử nghiệm 0, kỹ năng tạo 0; `AddNpc` Lua chưa có |
| `ProcessState 0x0808B610` khối `% 10` `0x0808B727` | `+0x24` 1 (người) → nhánh riêng; 2 (đồng hành) → bỏ qua boss; khác: **`+0x181c ≠ 0`** → `0x0808BAF6`: `+0x358` (cấp hiện ô 5) > 0 và `+0x340` (id ô 5) > 0 → **`0x080873B0(npc, id, cấp)`**; rồi mới `+0x244` (§16.10) | `process_state` |
| `0x080873B0` | `cấp > 0x3f → thôi` (`0x08087468`): **cấp bị kẹp 64 không bao giờ thi triển** — với `npclevelscript.lua` ("0\|20"/"0\|30" = 20–30·cấp) hào quang mẫu chỉ chạy khi npc cấp ≤ 3; với `makeboss.lua` ("0\|1" = 1) chạy ở cấp 1 | `cast_skill_effect` giữ nguyên chặn `> 63` |

### 16.12 Quái vàng — `KNpcGoldTemplate` (`\settings\npc\NpcGoldTemplate.txt`, `0x0809CCC0`), `KNpcGold` (`KNpc+0x88`: `BackData 0x0809D560`, `SetGoldTypeAndBackData 0x0809D8D0`, `RecoverBackData 0x0809E070`), `maplist.ini` `%d_AutoGoldenNpc/%d_GoldenType/%d_GoldenDropRate/%d_NormalDropRate` (M12 lát B5a, đã kiểm từng dòng)

Mã 2004 không có; là tính năng JX2 ("quái xanh"/"蓝色怪" — Lua `NPCINFO_AddBlueNpc`). Bảng `NpcGoldTemplate.txt` server 16 dòng (Hữu tuyến, Ngoan cường, Cửu Đầu Ô, Cấp tốc,
Hung Tàn, Lanh lẹ, Kim, Mộc, Hỏa, Thủy, Thổ, Lãnh tụ, Thông dụng thấp/vừa/cao, Lấy bảo vật); bản trong `reslst.dat` của client 2.0 có 17 (thêm Cổ Thụ) và khác vài số
(Treasure của Lấy bảo vật 9 ≠ 4) — luật lấy bản server, client chỉ dùng **số dòng** để tô màu tên (`CLIENT-2.0.md` §16).

| Hàm | Luật (đã đọc) | Zone |
|---|---|---|
| `KNpcGoldTemplate::Init 0x0809CCC0` | `KTabFile` `\settings\npc\NpcGoldTemplate.txt`, dòng 2.. (tối đa 30, mảng `0x8BACAC0` 30 × 0xac, số dòng `+0x1428 = [0x8BADEE8]`), dừng ở dòng có cột 1 (类型) rỗng — **tên không được lưu**; cột → bản ghi 0xac: 2 Exp `+0` (mặc định 1), 3 Life `+4` (1; `≤ 0` → in `GoldTemplate:%d settings is error! life <= 0` rồi 1), 4 LifeReplenish `+8`, 5 AttackRating `+0xc`, 6 Defense `+0x10`, 7 MinDamage `+0x14`, 8 MaxDamage `+0x18` (đều mặc 1), 9 Treasure `+0x1c`, 10 WalkSpeed `+0x20`, 11 RunSpeed `+0x24`, 12 AttackSpeed `+0x28`, 13 CastSpeed `+0x2c` (mặc 0), 14 SkillName → `0x080A1D80(tên)` = `SkillId` của dòng `skills.txt` đầu tiên cùng tên (`0x08226FA0`: cùng độ dài rồi so không phân biệt hoa/thường; "Vòng tròn  miễn dịch " → 539, dòng 540 cùng tên bị bỏ) → `+0x30`, 15 SkillLevel chuỗi 32 byte `+0x34`, 16..25 FireResist/FireResistMax/ColdResist/ColdResistMax/LightingResist/LightingResistMax/PoisonResist/PoisonResistMax/PhycicsResist/PhycicsResistMax `+0x54..+0x78` (mặc 0), 26 AiMode `+0x7c`, 27..36 AiParam1..10 `+0x80..+0xa4`, 37 AiMaxTime `+0xa8` (mặc **100**); cột 38..47 (`PhysicalDamageBase`…) **không đọc** | `npcres/KNpcGoldTemplate.go` `ParseNpcGoldTemplate` + `SkillNameLookup`; `jxassets export-npc-gold` → `npcres/npc_gold.json` {`rows` (server), `client_rows` (bản client)}; `KNpcGoldTemplateSet` (`KNpcGold.h`) |
| `KNpcGold` `KNpc+0x88` (0x12c byte, ctor `0x0809D4C0(this, idx)`) | `+0` idx, `+4` IsGold (ứng viên), `+8` IsGolding (đang vàng), `+0xc` GoldType (0-based); sao lưu `+0x18..+0x3c` kháng đang dùng **`max(âm, dương)`** (`+0x19ec/+0x19f0`…) và kháng tối đa `+0x126c..`, `+0x40` bảng rơi `+0x174c`, `+0x44` AiMode `+0x1654`, `+0x48..+0x6c` AiParam `+0x1658..`, `+0x70` AiMaxTime byte `+0x14c0`, `+0x74..+0x110` 40 dword `+0x11b8..+0x1254` (10 khối sát thương), `+0x114` kinh nghiệm gốc `+0x15a8`, `+0x118` `max(+0x1a14, +0x1a18)`, `+0x11c` `+0x1190`, `+0x120` `+0x1258`, `+0x124` `+0x125c`, `+0x128` `+0x1378`. `GetGoldKind 0x0809CC90` = IsGold && IsGolding ? GoldType+1 : 0; `0x08079750(npc)` = người 0, `+0x181c ≠ 0` → 3 (boss), GoldKind ≥ 1 → 2, khác 1 (`ReceiveDamage` cộng `add_boss_damage` khi == 3) | `KNpcGold` struct, `gold_back_data`, `npc_class` (`KNpcGold.cpp`) |
| `BackData 0x0809D560(gold)` | `idx ≤ 0` → thôi; `IsGold = 1, IsGolding = 0, GoldType = 0` rồi sao lưu như trên. Ai gọi: `KNpcSet::Add 0x0809FBD0` (npc `Region_S.dat`: `KSPNpc.bSpecialNpc` (`+0xc`) ≠ 0 → luôn; không thì chỉ khi tham số 4 = 1 **và** `subworld+0x63e7c` (`AutoGoldenNpc`) ≠ 0, ngược lại `0x0809CC70(gold, 0)` IsGold = 0; **sau đó** mới `+0x174c = subworld+0x63e88` (`NormalDropRate`) khi có → bản sao lưu giữ bảng của **mẫu**); `0x08085E70` khi hệ đổi (`NpcSeriesAuto`) và `+0x8c ≠ 0`; Lua `AddNpc/AddNpcEx` tham số 8 = 2 (`0x0811BE3C`) và `NPCINFO_AddBlueNpc 0x081C25F0` (6 tham số, `0x081C27A2`) — cả hai gọi tiếp `SetGoldTypeAndBackData(gold, 1 000 000, 0)` = **vàng ngay** | bộ đặt npc trong `KSubWorld()` (`n.special \|\| settings.auto_golden_npc ≠ 0`), `drop_rate_file` của npc; `zone.test_npc_gold` cho npc thử nghiệm |
| `SetGoldTypeAndBackData 0x0809D8D0(gold, rate, type)` | `idx ≤ 0`, IsGold == 0 hay IsGolding ≠ 0 → thôi; **`g_Random(1 000 000) ≥ rate` → thôi**; số dòng ≤ 0 → thôi; `0 < type < số dòng` → `GoldType = type − 1` (**type = số dòng đi đường ngẫu nhiên**); không thì `subworld+0x63e84` (`GoldenType`) ≠ 0 → `GoldType = nó − 1`, không thì `g_Random(số dòng)`; `IsGolding = 1`; kháng đang dùng `+0x19ec/+0x19f4/+0x1a04/+0x19fc/+0x1a0c` **× cột/100 HAI LẦN** (`0x0809D969..0x0809D998`: `x·FR/100·FR/100`, nhân đôi cùng một trường — lỗi gốc, giữ nguyên), kháng tối đa `+0x126c/+0x1270/+0x1278/+0x1274/+0x127c` × cột Max/100 một lần; bản ghi `+0x30 ≠ 0` và `+0x34` không rỗng → `cấp = 0x080A1F90(g_pNpcLevelScript [0x830B234], "Level5", hệ, cấp npc, chuỗi)` = `GetNpcLevelData(hệ, cấp, "Level5", chuỗi)` (script trả nguyên chuỗi → "60" = 60) → `SetNpcSkill(sổ, 5, id, cấp) 0x080E4310` + **`SetAura(npc, id) 0x08087290`**; rồi: `+0x15a8 ×Exp/100`, `+0x1a18/+0x1a14 ×Life/100`, `+0x1190 ×LifeReplenish`, `+0x1258 ×AttackRating`, `+0x125c ×Defense`, min/max của 8 khối `+0x11bc/+0x11c4`, `+0x120c/+0x1214`, `+0x11dc/+0x11e4`, `+0x121c/+0x1224`, `+0x11ec/+0x11f4`, `+0x122c/+0x1234`, `+0x11cc/+0x11d4`, `+0x123c/+0x1244` ×MinDamage/MaxDamage, độc `+0x11fc > 0` → `+0x1200 = 60, +0x1204 = 10, +0x11fc ×MaxDamage` (và `+0x124c/+0x1250/+0x1254` y hệt), **`+0x1378 = Treasure`** (thay, không nhân), `+0x1288 += WalkSpeed`, `+0x128c += RunSpeed`, `+0x1a38/+0x1a34 += AttackSpeed`, `+0x1a40/+0x1a3c += CastSpeed`, `+0x118c = max(+0x1a14, +0x1a18)` (đầy máu), `+0x1654 = AiMode`, `+0x1658.. = AiParam1..10`, byte `+0x14c0 = AiMaxTime`; IsGold && IsGolding → **gói 0x9a** `{0x9a, id npc, word GoldType+1}` 7 byte `0x0807A870(npc, gói, 7, 100, 0)` (người chơi quanh 100 ô) | `KSubWorld::set_gold_type` + `gold_apply`; `KNpcTemplateSet::level_string("Level5")`; `G2C_NPC_GOLD` (`NpcGold{entity_id, gold_type}`); `speed` của npc tính lại từ `cur.walk_speed` |
| `RecoverBackData 0x0809E070(gold)` | `idx ≤ 0`, IsGold == 0, IsGolding == 0 → thôi; `IsGolding = 0`; 10 kháng ← sao lưu (`+0x19ec = +0x18` = max(âm, dương) cũ, dương giữ); **`0x080E52D0(sổ, id ô 5)`** (gỡ kỹ năng ô 5), `SetAura(npc, 0)`; `+0x174c/+0x1654/+0x14c0/+0x1658..` ← sao lưu; `0 ≤ GoldType < số dòng` → `+0x15a8`, `+0x1a14 = +0x1a18 = +0x118`, `+0x1190`, `+0x1258`, `+0x125c`, 40 dword sát thương, `+0x1378` ← sao lưu, `+0x1288/+0x128c/+0x1a38/+0x1a34/+0x1a40/+0x1a3c −= cột tốc độ của dòng`, **`+0x118c = +0x118`** (đầy máu). Ai gọi: `0x08083720` (**cuối khung chết** của npc, sau script `OnDeath`, trước `Revive 0x080833B0`) | `KSubWorld::recover_gold` (trong `do_revive`) + `gold_recover` |
| Hồi sinh `0x08085E70` (từ `0x08086220` hết khung 21) | … sau đặt lại vùng, `+0x194c = +0x1950 = 1`: `+0x1588` (script) == 0 hay **`+0x181c == 0`** → `rate = subworld+0x63e7c`; **`rate == 0 → 2 000 000`** (`0x080861AE`: luôn qua `g_Random(1e6)`); `SetGoldTypeAndBackData(&npc+0x88, rate, 0)`; `+0x181c == 0 && IsGolding && subworld+0x63e80 ≠ 0` → `+0x174c = GoldenDropRate`. `+0x181c ≠ 0` → không quay (`0x08086088`). Hệ quả: bản đồ không khai `AutoGoldenNpc` (như `cavelist.ini`) → npc `bSpecialNpc` **vàng ở mọi lần hồi sinh**; bản đồ có (2000 = 0,2 %, 3000, 1, 1 000 000) → mọi npc đặt sẵn quay số mỗi lần hồi sinh; sau lần vàng đầu bảng rơi của npc là bảng **mẫu** (không còn `NormalDropRate`) | `KSubWorld::revive` (cuối) |
| `maplist.ini [List]` (`0x080F1416..`) | `%d_AutoGoldenNpc` `GetInteger` mặc 0 → `subworld+0x63e7c`; `%d_GoldenType` mặc 0, âm → 0 (`0x080F149C`) → `+0x63e84`; `%d_GoldenDropRate` chuỗi (200 byte) → `0x080A41E0(đường dẫn)` = chỉ số bảng rơi (nạp/cache) → `+0x63e80` (rỗng → 0); `%d_NormalDropRate` → `+0x63e88` (+ chuỗi `+0x63e8c`); server: 164 bản đồ khai `AutoGoldenNpc` (2000 ×96, 3000 ×63, 1 ×2, 1000000 ×2, 0 ×1), `GoldenType` 13 ×80 / 14 ×47 / 15 ×34 / 16 ×3, `GoldenDropRate` `\settings\droprate\goldennpc\*.ini` | `map.json` `settings` (`export.MapSettings`, `mapSettings()` trong `jxassets`, bản server trước) → `KMapSettings` (`KMapData.h`); `export-npcres` nạp thêm các bảng rơi `maplist.ini` (`mapListDropRates`) |
| `KNpcSet::Add 0x0809FBD0` phần bảng rơi | tham số 4 ≠ 0 và `+0x63e88 ≠ 0` → `+0x174c = NormalDropRate` — **mọi** npc đặt sẵn của bản đồ có `%d_NormalDropRate` rơi theo bảng của bản đồ chứ không theo `DropRateFile` của mẫu (`OnDeath 0x08088B60`/`LoseMoney 0x08081950`/`LoseSingleItem 0x08088840` đọc `+0x174c`; `SetTemplate 0x080830BA` đặt từ mẫu; `re_scan disp 174c`) | `KNpc::drop_rate_file`; `lose_treasure` dùng nó |

Zone: `KNpcGold.h/.cpp`, `KSubWorld::set_gold_type/recover_gold/emit_gold/map_settings`, `KMapSettings`, `KNpcPlacement::special`, `EntityInfo.gold_type` (word của gói 0x4c:
`GetGoldKind`, boss = số dòng + 1), `G2C_NPC_GOLD`; `zone.test_npc_gold` = n: npc thử nghiệm vàng loại n ngay (đường `AddBlueNpc`); test `[gold]` (7 ca: bảng, sao lưu/nhân/trả
(kháng âm `max(−20, 0) = 0`), lớp 0x08079750, đặt sẵn + gói 0x9a + word 0x4c, chết → trả → hồi sinh quay số + `GoldenDropRate`, `bSpecialNpc` trên bản đồ không khai
(2 000 000) + boss không quay, word boss = 3). Chưa: Lua `AddNpc/AddBlueNpc` (chưa có API npc). `NpcSeriesAuto`/`NpcAutoLevel`: §16.13 (B5b).

### 16.13 Hệ và cấp ngẫu nhiên của npc đặt sẵn — `maplist.ini` `%d_NpcSeriesAuto` + `%d_NpcSeriesMetal/Wood/Water/Fire/Earth`, `%d_NpcAutoLevelFlag/Max/Min` (`0x080F11F0`, `0x080F1CDA`), `KRegion::LoadNpc 0x080E28E0`, `0x080EFBE0`/`0x080EFB90`, hồi sinh `0x08085E92` (M12 lát B5b, đã kiểm từng dòng)

| Hàm | Luật (đã đọc) | Zone |
|---|---|---|
| Bộ nạp bản đồ `0x080F11F0..0x080F1370` | `%d_NpcSeriesAuto` `GetInteger` mặc 0 → `+0x63f54`; ≠ 0 → năm trọng số `%d_NpcSeriesMetal/Wood/Water/Fire/Earth` (mặc 0) → `+0x63f58..+0x63f68` rồi **cộng dồn** (`+0x63f5c += +0x63f58`, `+0x63f60 += +0x63f5c'`, `+0x63f64 += +0x63f60'`, `+0x63f68 += +0x63f64'`, `0x080F1346`); = 0 → năm ô về 0 (`0x080F1BBF`) | `KMapSettings::npc_series_auto`, `npc_series` (thô), `series_sums` (`finish()`) |
| `0x080F1379..`, `0x080F1CDA..0x080F1D96` | `%d_NpcAutoLevelFlag` mặc 0 → `+0x63f6c`; = 0 → `Max = Min = 1`; ≠ 0 → `%d_NpcAutoLevelMax` (`+0x63f70`, mặc 1), `%d_NpcAutoLevelMin` (`+0x63f74`, mặc 1); `Max ≤ 0`, `Min ≤ 0` hay `Max < Min` → in `MapList.ini error:npc level error!` rồi `1/1`. (`+0x63f6c ≠ 0 && [0x9777FF8] ≠ 0` → `0x080F6CF0(0x9777F00, &id)` đăng ký bản đồ — chưa đọc tiếp) | `npc_auto_level_flag/max/min`, `finish()` + cảnh báo cùng chữ |
| `KRegion::LoadNpc 0x080E2850` (mỗi `KSPNpc`) | **chỉ `shKind == 0`** (`0x080E28E0`): `+0x63f54 ≠ 0` → `cSeries = 0x080EFBE0(subworld)`; `+0x63f6c ≠ 0` → `nLevel = 0x080EFB90(subworld)`; rồi `KNpcSet::Add 0x0809FBD0(…, 1)` | vòng đặt npc trong `KSubWorld()`: `n.kind == 0` → `random_series()` / `random_level()` |
| `0x080EFBE0(subworld)` hệ ngẫu nhiên | cờ 0 hay tổng `+0x63f68 ≤ 1` → 0; `r = g_Random(tổng)`: `r < +0x63f58 → 0`; `< +0x63f5c → 1`; `< +0x63f60 → 2`; `< +0x63f64 → 3`; `< +0x63f68 → 4`; khác 0 (Kim 0, Mộc 1, Thủy 2, Hỏa 3, Thổ 4) | `KSubWorld::random_series` |
| `0x080EFB90(subworld)` cấp ngẫu nhiên | cờ 0 → 1; `Max < Min` → 1; `Max == Min` → Max; khác `g_Random(Max + 1 − Min) + Min` | `KSubWorld::random_level` |
| Hồi sinh `0x08085E70` phần đầu (`0x08085E92..0x08086196`) | `+0x63f54 ≠ 0` → `hệ mới = 0x080EFBE0`; `+0x28 = hệ mới`; **khác hệ cũ** → giữ tên `+0x1505`, `+0x24`, `+0x21c`; `0x08085DA0(this, +0x1530, +0x20, +0x28)` (= `0x0809C4D0(+0x1978, id)` + `0x080854F0(mẫu, max(1, cấp), hệ)` Init lại theo bản ghi cấp của hệ mới, `+0x220 = +0x21c`); `+0x181c == 0 && +0x63e88 ≠ 0` → `+0x174c = NormalDropRate`; trả `+0x21c/+0x220/+0x24`, tên + băm `+0x1528`; kind 3 → `+0x18e4 = 0x08062B50(0x830C7A0, tên, subworld+0xc)` (script đối thoại) không thì 0; `+0x8c (IsGold) ≠ 0 → BackData` lại (số của hệ mới); `+0x181c ≠ 0 → 0x08085250`. Không có bước con cấp: cấp chỉ quay lúc nạp | `KSubWorld::revive` đầu: `random_series()` khác → `apply_template` (giữ tên/kind/camp), `NormalDropRate`, `gold_back_data`, `init_template_skills`; log `series rerolled` |

Zone: test `[series]` (tổng cộng dồn `{20,0,30,0,50} → {20,20,50,50,100}`; cặp cấp sai → 1/1; bản đồ Thổ 100 % cấp 25: kind 0 → hệ 4 cấp 25, kind 3 giữ 0/5;
boss hệ 0 hồi sinh → hệ 4, giữ tên/camp/`+0x181c`, không vàng; 300 lượt trong [20,30] và không ra Mộc/Hỏa). `map.json` `settings` thêm `npc_series_auto`,
`npc_series`, `npc_auto_level_flag/max/min` (server: 164 bản đồ khai, ví dụ Phượng Tường 5 × 20, Kim Quang Động Kim 80/Thủy 20 cấp 35).

Zone (§16.11): `KNpc::boss_flag` (int, `+0x181c`), `KSubWorld::init_template_skills`, `spawn_npc(…, boss_flag)`, `process_state` nhánh ô 5; test `[command][aura][template]` (mẫu 950:
hào quang 1103 "0\|1" → ô 5 cấp 3, con 1102 lên mình sau 10 khung; bị động 1130 "1\|0" → ô 6 + trạng thái ngay; mẫu 951 "0\|30" → ô 5 = 64, không thi triển; npc do kỹ
năng tạo → không ô 5/6). Go `LevelCells` thêm bốn cột → `npcs.json` (`dev.py assets`). Còn: quái vàng `KNpcGold` (`NpcGoldTemplate.txt` 16 loại, `0x0809D8D0`), Lua
`SetNpcAuraSkill`. Chạy thật (Tiễn Tháp 1375 làm npc thử nghiệm: hào quang 313 cấp 1 → con 316 `allres_p` bán kính 180 lên đồng minh): lúc đầu `hit` 4 thú mà không `state added` —
nguyên nhân là **lỗi in số của Lua 5.4** (xem HANDOVER §0.5): `15+level` với `level` đẩy vào là số thực → `"16.0,12,0"` → `KSG_StringGetInt` đọc 16 rồi hỏng ở `.0`
(state count 0). Sửa ở `KLuaScript` (số nguyên đẩy vào là integer, chuỗi trả về in số theo Lua 4 `%.14g`): `state added` 5, client thấy biểu tượng 51 + vòng dưới chân 4 thú.

### 16.14 Ngồi thiền — gói client 0x71 (`0x080DC300`), `KNpc::DoSit 0x0807B550`, gói 0x83 / 0x9f, khung `0x08087880`, `ProcessState 0x0808BBE6` (M12 lát B6a, đã kiểm từng dòng)

| Hàm | Luật (đã đọc) | Zone |
|---|---|---|
| `0x080DC300` (ô 113 của bảng handler = gói `{0x71, byte ngồi}`) | player 1..0x4ae; `0x080AEBC0(player, 2)` (bộ phát sự kiện script, mã 2 — M13); npc `Player+0x3f4` 1..`[0x830ca58]`; **`npc+0x199c ≠ 0` (đang cưỡi) → thôi**; byte +1 == 0 → `0x08078AA0(npc, 1, 0, 0, 0)` (đứng), ≠ 0 → `0x08078AA0(npc, 8, 0, 0, 0)` (ngồi) | `KMapInstance` `C2G_SIT` → `KSubWorld::sit_request(sid, sit)`: `horse ≠ 0` → thôi |
| `0x08078AA0(npc, hành động, p1, p2, p3)` | hành động 1/2/3/4/8 bị từ chối khi `+0x1479` (`frozen_action`) ≠ 0; đưa vào bộ xử lý `0x08088640` (bảng nhảy `0x8254ad8` theo mã): 1 → `DoStand 0x08080030`, 8 → `KNpc::DoSit 0x0807B550` | `cur.frozen_action` → thôi; xác chết / đang bị đánh / bị đánh lùi giữ khung của nó |
| `KNpc::DoSit 0x0807B550` | `m_Doing (+0x224) == 8` → thôi; `== 0x12` (chạy đánh) → `+0x194c = 1`, `+0x128c −= +0x14b0`, `+0x14b0 = 0` rồi tiếp; `m_Doing = 8`; gói **0x83 `{byte 0x83, dword npc}`** cho người quanh 100 ô (`0x0807A870(npc, gói, 5, 100, 0)`); `0x080796D0(npc, Player)` → gói **0x9f `{6, 1}`** cho chính mình; `+0x230 = 0`, `+0x22c = max(1, +0x1930)` (`m_SitFrame` = **15**, `KNpc::Init 0x0807E09B`) | `do_sit`: `in_action` → `stop_action`, đang đi → `set_pos` tại chỗ, `doing = sit`, `frame_cur = 0`, `frame_total = KNpc::kSitFrame` 15, `emit_action(ACTION_SIT)` (một gói cho cả người xem lẫn mình) |
| khung mỗi tick `0x08087880` (nhánh `m_Doing` 8 của bộ chia `0x08087770`) | `+0x230 += 1`; `≥ +0x22c` → `+0x230 = +0x22c − 1`: **giữ khung cuối**, không tự đứng | tick giữ `doing = sit` |
| `ProcessState 0x0808BBE6` (mỗi 10 khung, người chơi `m_Doing == 8`) | `max = max(+0x1a14, +0x1a18)`; `add = max·3·lifereplenish_p(+0x1194)/100000` (nhân `0x14f8b589` `>> 13`); `lifereplenish_p ≠ 100` → log `"SitAddLife: %d * %d%% = %d"` (`max·3/1000`, %, add); **`máu (+0x118c) += max(1, add)`**, kẹp `max`; nội lực y hệt (`+0x1a1c/+0x1a20`, `manareplenish_p +0x119c`, `"SitAddMana"`); thể lực (`0x0808BE36`, §16.13 dòng `0x0808BD3D`): `+= gain + +0x11b0` (`SitAdd` ‰ max, đặt trong `ClearAttrib`) | `process_state`: `sit_add(max, %) = max(1, max·3·%/100000)` cho máu và nội lực, `stamina += gain + cur.stamina_sit_add`; test `[sit]` |
| Rời ngồi | `DoStand 0x08080030` (gói 0x71 = 0), `DoWalk 0x0807B620`/`DoRun` (ghi `m_Doing` 3 đè), `DoAttack/DoMagic 0x08078790/0x08078850`, `OnHurt 0x0807F780` (`m_Doing` 6) — không hàm nào kiểm 8 riêng | `leave_sit` (`ACTION_STAND`) từ `sit_request(false)` và `move_request`; đánh / thi triển / bị đánh ghi `doing` đè như cũ |

Client 2.0 (`CLIENT-2.0.md` §18): nút `Switch([[sit]])` → gói 0x71; gói 0x83 / 0x9f → `KNpc::DoAction(8)`; `GetNpcPate 0x005EBCF0` hạ tên 24/26/28 điểm ở ba khung
cuối. Sprite ngồi = hành động 36 (`*_ZZ01.spr`) — `jxassets export-npcres` nay xuất thêm `DoSit` (chạy lại `python tools/dev.py assets`).

### 16.15 Dáng trang bị của người chơi — `KNpc+0x14dc..+0x14ec`, `KItemChangeRes` (`KItemSet 0x08068D90`, `0x08068AC0`), `0x081FE0E0/0x081FE1E0/0x081FE230`, `0x0807ACB0`, gói 0xad `0x0807A9D0`, gói 0x4a `0x0807BF20` / 0x4b `0x080810C0` (M12 lát B6b, đã kiểm từng dòng)

| Hàm | Luật (đã đọc) | Zone |
|---|---|---|
| `KItemSet` bảng dáng `0x08068D90` | năm `KTabFile` liền nhau `MeleeRes +0`, `RangeRes +0x20`, `ArmorRes +0x40`, `HelmRes +0x60`, `HorseRes +0x80` (`\settings\item\*.txt`); ba `std::map` `GoldEquipRes +0xa0`, `PlatinaEquipRes +0xb8`, `ClothesEquipRes +0xd0` (`0x08068D60/D30/D00` → **`0x08068B70(set, tệp, map)`**: dòng 2.. → khoá `(word cột3 << 16) \| word cột1` (mặc 0) → giá trị cột 2 (mặc 2)) | `jxassets export-item-res` → `items/item_res.json` (`melee/range/armor/helm/horse` = mọi dòng đã `atoi`, `gold` = bảng thô); `KItemChangeRes::load` dựng map như `0x08068B70`; `ClothesEquipRes` chưa dùng |
| `0x08068900` (mũ) / `0x08068980` (áo) | `cấp == 0` → `GetInteger(bảng, 2, 2, 19)`; khác → dòng `particular·10 + cấp + 2`, cột 2, mặc 19; **−2** | `helm_res` / `armor_res` |
| `0x08068A00` (vũ khí) | `cấp == 0` → dòng 2 `MeleeRes` (mặc 2); detail 0 (cận chiến) → dòng `particular·10 + cấp + 2` của `MeleeRes`; detail 1 (tầm xa) → dòng `particular·10 + cấp + 1` của `RangeRes`; loại khác → ô chưa gán (rác); −2 | `weapon_res` (loại khác → 0) |
| `0x080688B0` (ngựa) | `cấp == 0` → −1; khác `GetInteger(HorseRes, particular·10 + cấp + 2, 2, 2) − 2` | `horse_res` |
| `0x080686A0(set, GenParam, kind)` (hoàng kim) | khoá `(kind << 16) \| (word GenParam + 1)` trong map `+0xa4` → giá trị − 2; không có → −1 | `gold_res` |
| `0x08068730(set, GenParam, kind, bậc +0x344)` (bạch kim) | `bậc ≤ 5` → map hoàng kim, `> 5` → map bạch kim `+0xbc`; trượt mà `kind ≠ 0` → thử lại `kind = 0` (`0x080687A8`); không có → −1 | `platina_res` |
| `0x080685B0(part)` | bảng cặp `0x8252ba0` (11 cặp): part 0→1, 1→2, 2→3, 3→5, 4→6, 5→7, 6→8, 7→9, 8→9, 9→10, 10→0; không có → 0 = **kind** cho khoá hoàng kim/bạch kim | `kPartKind[]` trong `equip_res` |
| **`0x081FE0E0(list, item, part)`** | `item == 0` → `0x08068AC0(set, 0, 0, 0, part)` (không mặc gì); `item+0x210 ≠ 0` → giá trị đã tính − 1; `item+4 == 1` (hoàng kim) → `0x080686A0(set, item+0x80, kind)`; `== 4` (bạch kim) → `0x08068730(set, item+0x80, kind, byte item+0x344)`; khác → **`0x08068AC0(set, detail item+8, particular item+0xc, cấp item+0x24, part)`**: bảng nhảy `0x8252b40` theo part: 0 → mũ, 1 → áo, 3 → vũ khí, 10 → ngựa, 12 → −1, khác → 0 | `KItemChangeRes::equip_res(item, part)` (`ex_type` 1 hoàng kim, 2 bạch kim) |
| `0x081FE1E0(list, part)` | ô mặc `list+0xc + part·8` > 0 → vật → `0x081FE0E0`, không → `0x081FE0E0(list, 0, part)` | `update_equip_res`: `list->find(list->equipped(part))` |
| `0x081FE230(list, npc, item, part)` (khi mặc, `0x081FE380`) | bảng nhảy `0x826b3c0` theo part: 0 → `npc+0x14dc`, 1 → `+0x14e0`, 3 → `+0x14e4`, 10 → `+0x14e8`, 12 → `+0x14ec` = kết quả `0x081FE0E0` | (tính lại cả năm trong `update_equip_res`) |
| **`0x0807ACB0(npc)`** (khôi phục dáng thật; từ `0x0807AF10` khi vật không mang dáng riêng, `0x081FDEA0/0x081FECC0/0x081FFE90/0x081FFFB0`) | `+0x14cc` (đang mượn dáng) → xoá `+0x14c8..+0x14d4`, `+0x14d8 = +0x1530`; kind 1: `+0x14dc/e0/e4/e8 = 0x081FE1E0(Player+0x3fc, 0/1/3/10)`, `Player+0x46c ≠ 1` → `+0x14ec = −1`, `0x08079790(npc)`; thời trang `Player+0x480/+0x488`, `+0x14f0/+0x14f4` (chưa đọc hết); **`+0x1504 += 1`; `0x0807A9D0(npc, 0)`** | `update_equip_res(e)` sau `item_equip_request` / `item_unequip_request` / `load_from`: đổi → `res_version++` + `emit_res` (`G2C_ENTITY_RES`) |
| `0x0807AB20(npc, p1, p2, p3, resIdx, mũ, áo, vũ khí, ngựa, áo choàng)` (mượn dáng: `0x0807AF10` từ vật có dáng riêng `+4 == 1`, ưu tiên `+0 ≥ +0x14c8`) | `+0x14cc = 1`, `+0x14c8/d0/d4 = p1..p3`, `+0x14d8 = resIdx`; `resIdx` −1/−2 (nhân vật) → năm hàng như đối số, `+0x34 = 0x54`; khác → mẫu npc `[0x836ece0 + resIdx·0xb40]` (tạo `0x11c` byte nếu chưa có), `+0x34` = `mẫu+0x1134`, năm hàng 0/−1; `+0x1504++`; `0x0807A9D0(npc, 0)` | chưa port (mượn dáng 186 / thời trang) |
| **`0x0807A9D0(npc, player)`** gói **0xad** 32 byte | `{0xad, dword npc, byte +0x14c8, byte +0x14cc, dword +0x14d8, dword mũ, dword áo, dword vũ khí, dword ngựa, dword áo choàng (5 word mở rộng), byte +0x1504}`; `player ≠ 0` → `0x080A8400(player)`; không: `+0x1184 > 0` → quanh 1200 (`0x0807A870(…, 0x20, 0x4b0, 1)`), kind 1 → chính mình, kind 2 → chủ (`+0x1698`) | `emit_res` → `EntityRes{entity_id, helm/armor/weapon/horse/mantle_res, version}` cho người xem |
| Gói 0x4a `0x0807BF20` (sau 0x4c người chơi) / 0x4b `0x080810C0` | mang byte `+0x14dc` mũ, `+0x14e0` áo, `+0x14e4` vũ khí, `+0x14e8` ngựa (0x4b: `+0x14ec`, `+0x14f0`, `+0x14f4`, phiên bản `+0x1504`), cờ bit 0x20 cưỡi | `EntityInfo.helm_res/armor_res/weapon_res/horse_res/mantle_res` (`fill_info`), `riding` |

**Đối chiếu bố cục gói với `gamecl.exe` 2.0 của chủ dự án** (`CLIENT-2.0.md` §19): handler 0x4a `0x0065D180` đọc cờ ở `+0x15`, dword `+0x16/+0x1a`, áo choàng `+0x1e`,
trong khi `0x0807BF20` ghi hai byte `+0x14f0/+0x14f4` ở `+0x15/+0x16` rồi cờ ở `+0x17`; 0x4b client (`0x0065D460`: phiên bản `+0xe`, mũ `+0xf`, ngựa `+0x10`, đi bộ
`+0x11`…) khác `0x08081390` (bốn dword rồi phiên bản `+0x15`, mũ `+0x16`…); 0xad client `0x006515A0` 0x18 byte (6 word từ `+0xb`, phiên bản `+0x17`) khác 32 byte của
`0x0807A9D0` (dword, phiên bản `+0x1f`) → **hai nhị phân không cùng phiên bản giao thức** ở ba gói này (0x4c/0x83/0x9f/0x71 thì khớp); zone theo **ý nghĩa** trường
(mũ/áo/vũ khí/ngựa/áo choàng + cờ cưỡi + phiên bản), không theo byte.

Zone: `KItemChangeRes.h/.cpp`, `KNpc::helm_res/armor_res/weapon_res/horse_res/mantle_res/res_version`, `KSubWorld::update_equip_res/emit_res`, `KSubWorldConfig::item_res`
(`zone.item_res_file`, mặc `items/item_res.json` cạnh bảng vật phẩm), `item_equip_request` ngựa: `horse_res ≥ 0` mới `SetHorse(1)` (`0x081FE752`); test `[res]` (bảng tổng hợp:
tay không/dòng 2, cận chiến/tầm xa, mũ/áo mặc 19, ngựa cấp 0 = −1, map hoàng kim/bạch kim + thử lại kind 0; spawn → 18/18/0/−1/−1 phiên bản 1; mặc ngựa → 8, `G2C_ENTITY_RES`
phiên bản 2; lên/xuống ngựa không đổi dáng; kiếm hoàng kim → map (kind 5 = part 3) 31; kiếm thường → 5).

### 16.16 PK — `KPlayerPK` (`Player+0x5a50`: `SetPKState 0x080C3740`, `SetPKValue 0x080C38C0`, `AddPKValue 0x080C3930`, tick `0x080C35E0`), gói 0x76 `0x080DBE00`, `KNpc::GetPKRelation 0x0807A350`, phạt chết PK `0x080B9FA0`, `PKRate.ini` + `PKPunish.txt` (M12 lát B3c-4, đã kiểm từng dòng)

| Hàm | Luật (đã đọc) | Zone |
|---|---|---|
| `KPlayerPK` `Player+0x5a50` (mã 2003 `Core/Src/KPlayerPK.h`) | `+0` trạng thái (2003 `m_nNormalPKFlag`; bản Linux **ba trạng thái**: 0 tu luyện, 1 chiến đấu, 2 sát nhân — cũng là ô chọn `ExerciseRunSub/FightRunSub/KillRunSub` §16.6), `+4` chỉ số player, `+8` khoá (`ForbidChangePK`), `+0x10` giây trong trạng thái, `+0x14` chế độ cừu sát (`m_nEnmityPKState`), `+0x18` mục tiêu, `+0x1c` thời gian, `+0x20` cờ người phát, `+0x24/+0x28` tỉ thí (`m_nExercisePKFlag/Aim`), `+0x2c` giá trị PK, `+0x30..+0x3c` khối đếm ngược, `+0x34` % né `AddPKValue`; `Init 0x080C3630` xoá `+4/+8/+0xc/+0x10/+0x30..` | `KPlayer::KPlayerPK {state, locked, state_time, value, dodge_percent, reduce_seconds, reduce_value, punish_enhance, punish_weaken}`; cừu sát / tỉ thí (`0x080C31D0`, `0x080C3100`, gói 0x77/0x91/0x92) **chưa port** |
| `0x080C35E0(pk)` (từ giây của người chơi `0x080B6F5F`) | `+0x10 += 1`; `+0x3c` đếm ngược → 0 thì xoá `+0x30..+0x3c`; rồi `0x080C32E0` (đồng hồ cừu sát) | `++pk.state_time` mỗi 18 khung (cạnh `per_second_attribs`) |
| **`SetPKState 0x080C3740(pk, trạng thái, ép)`** | `!ép && +8` → thôi; 1/2: `!ép` → `0x080C3680(pk, 1)` (giá trị > 2 và tài khoản có **khoá bảo mật** `Player+0x507c` (đã ràng) + `+0x5080` (đang khoá; `tbSecurityLock`, `0x080B1870`) → ép về trạng thái 0, huỷ cừu sát `0x080C31D0`, gói 0x86 `{8, 0x2b, giá trị}`, thôi — đính chính: không phải đội); `+0 == 0` → `+0x10 = 0`; `+0 = trạng thái`; 0: `ép` → `+0 = 0, +0x10 = 0`; không ép: `+0 == 0` hay `+0x10 < [0x8bb2b38]` (NormalPKTimeLong 3240 s) → gói 0x86 `{4, 0x23}` (chưa được), không thì `+0 = 0, +0x10 = 0`; cuối cùng gói **0x90 `{byte trạng thái}`** cho chủ | `pk_set_state(e, state, force)` (không có khoá bảo mật); `G2C_PK_STATE{state, value, refused}` cho chủ + `G2C_ENTITY_PK{state}` cho người xem khi đổi |
| **`SetPKValue 0x080C38C0(pk, v)`** | `+0x2c = kẹp(v, 0, 10)`; `0x080C3680(pk, 1)` (khoá bảo mật, xem trên); gói **0x93 `{dword giá trị}`** cho chủ | `pk_set_value` |
| **`AddPKValue 0x080C3930(pk, n)`** | `n > 0 && +0x34 > 0 && +0x34 > g_Random(100)` → thôi (né); `SetPKValue(+0x2c + n)`; `+0x2c ≤ 9` → `Player+0x384 = 0` (cờ chết đấu trường) | `pk_add_value` |
| gói client **0x76** `{0x76, byte trạng thái}` → ô 118 `0x080DBE00` | trạng thái ≠ 0: `% kinh nghiệm (0x080A8120 = exp / (cần / 100), cắt 100) ≤ NotFightExpPercent` → thôi; npc đang chiến (`+0x168c`) hay khoá `+0x5a58` → `SetPKState(s, 0)`; không thì giá trị ≤ 2 hay không có khoá bảo mật → `SetPKState(s, 1)`, có khoá → `(s, 0)` | `pk_state_request(sid, state)` (`C2G_PK_STATE`; `forced = !fight_mode && !locked`; không có khoá bảo mật). Client 2.0 của chủ dự án: `Switch([[pk]])` (F9 / Ctrl+H) → `OperationRequest(0x14)` → `0x005FB240(1)` → gói `{0x6d, 2}` — **ô 0x6d của jx_linux_y là giao dịch `0x080B2C70`** (khác phiên bản, xem §16.15); zone theo handler 0x76 |
| **`KNpc::GetPKRelation 0x0807A350(this = nạn nhân, A = chủ kẻ giết, B = chủ nạn nhân, &điểm)`** (2003 `KNpc::DeathCalcPKValue`) | `điểm = 0`; A ngoài 1..[0x830ca58] → 0; B ≤ 0 hay B không phải người chơi → **1**; `npcB+0x1818 == 3` → **3** (đấu PK); A không phải người chơi → 0; `this+0x168c == 0` (nạn nhân không chiến) → 0; cờ bản đồ `subworld+0x63f8c & 1` (của B rồi A) không có → hai bang `Player+0x5abc` → map bang chiến `0x8bc6a84` → **4**; `pkA+0x14 == 2 && pkA+0x18 == B` (cừu sát) → `0x0806FBA0(B, A)`; `PlayerB+0x5a70` → `this` kind 2 → `KillPartnerPK` khác `EnmityPK`; **2**; kiểm cấp `0x0807A454`: `cấp B > 50 || 2·cấp A < 3·cấp A` (**so cấp A với chính nó** — lỗi của nhị phân; mã 2003: `killer.level·2 ≥ victim.level·3`) → luôn sang `0x0807A4F8`; `pkA+0 ≠ 0` → `0x0806FBA0`, **2** (0 điểm); `pkB+0 == 2` → kind 2 → `KillPartnerPK`, khác `PlayerA+0x86fc (pk_punish_enhance) + ButcherPKExercise − PlayerB+0x8700 (pk_punish_weaken)` (≥ 0) → **2**; còn lại `0x0806FBA0`, **2** (0 điểm); nhánh phe `0x0807A726` (camp 4 → `KillerPKFaction`, khác `FactionPKFaction`) **không tới được** | `death_calc_pk_value(victim, A, B, points)`: 0/1/2/3 như trên (4 bang chiến, cừu sát chưa có); `owner_of` = chủ đồng hành (`summon_master`) hay chính npc |
| `KNpc::Death 0x08089920` phần PK (`0x0808996A..0x08089B10`) | `0x08078E80` lấy chủ hai bên; `rel = GetPKRelation`; kẻ giết người chơi: ô tạm `KPlayerTask` `+0x809c[[0x830D230]]` hai bên (`GetClearVal 0x080CB5A0`, §21), vật bảo hộ `0x081FA8A0`, `MSG_TONG_CREATE_ERROR01`/`MSG_PK_YUXI_2`; `điểm > 0` → `Player+0x86f8` (`not_add_pkvalue_p`) ≠ 0 → `g_Random(100)` né, không thì **`AddPKValue(pkA, điểm)`**; `DoDeath(this, rel, kẻ giết)`; nạn nhân người chơi → `0x080C32C0` (huỷ tỉ thí + cừu sát) | `do_death`: `owner_of` hai bên, `death_calc_pk_value`, né theo `not_add_pkvalue_p`, `pk_add_value`, rồi `on_death_player(e, killer, mode)`; bảng bảo hộ / vật bảo hộ chưa port |
| `KNpc::OnDeath 0x08088D50` theo kiểu chết | 0 → chết thường (§16.4); 1/3/4 → không phạt; khác (2) → **`0x080B9FA0`** | `on_death_player`: 1/3/4 thôi, 2 → `death_punish_pk`, 0 như cũ |
| **phạt chết PK `0x080B9FA0(player, kẻ giết)`** (2003 `KNpc::DeathPunish` kiểu `PLAYER_PUNISH`) | `pk = kẹp(+0x2c, 0, 10)`; `Player+0x5a8c > 0` → `+0x5a88`: byte thấp = % tăng (cắt 100), byte cao = % giảm (cắt 100, không dùng tiếp); **kinh nghiệm**: cấp > 129 → bảng `0x08256EE0[pk]` = {500 000, 1 000 000, 2 000 000, 5 000 000, 8 000 000, 12 000 000, 16 000 000, 20 000 000, 30 000 000, 40 000 000, 50 000 000} (cắt 2·10⁹), khác `GetLevelExp` > 99 999 → `/1000 · cột2[pk]`, không thì `·cột2/1000`; `0x080AFEA0(player, −mất)`; `[0x830D0F0] == 1` → log; `% exp` ≤ `NotFightExpPercent` → `SetPKState(0, ép)`; **tiền** > 99 999 → `/1000 · cột3[pk]` (không thì `·cột3/1000`), `% tăng` → `> 20 000 000 ? + tăng·mất/100 : + mất/100·tăng`, `0x081FC940` trừ, gói 0x86 `{8, 0xa, mất}`, `> 1` → `0x0807FA50(npc, mất/2)` rơi tại xác; `[0x830D0EC] == 1` → **nhà tù** `NewWorld([0x9777F08..])`; **đồ**: `0x08203BE0(list, cột4[pk])`; **độ bền**: `0x08201D90(list, cột9[pk])`; `NotSubPKExpPercent < % exp` → `AddPKValue(BeKilled)`; kẻ giết `npc+0x1598` người chơi / đồng hành → `0x081C8B20(tên nạn nhân, tên kẻ giết)` (`KPlayerChat::MakeEnemy`) | `death_punish_pk(e, killer)` đúng thứ tự; nhà tù, log `[0x830D0F0]`, kẻ thù chưa port |
| `0x08203BE0(list, ‰)` | `0x080A7E90(player) ≠ 0` → thôi; ‰ ngoài 1..1000 → thôi; duyệt **túi** (`list+0x4c8c`): bỏ vật `+0x350 ≠ 0` (ràng buộc), byte `+0x360` (khoá), genre 4 (nhiệm vụ) / 7 (hỏng); mỗi ứng viên `g_Random(1000) < ‰` → rời túi (`0x082006B0`), vật thể rơi tại chỗ (`0x080B5050`, `0x080A6290/0x080A6A70/0x080A4D90`), gói 0x86 `{0x54, 0x1b}` | vòng `list->each` phòng túi, bỏ nhiệm vụ/hỏng, `take_item` + `drop_item` |
| `\settings\npc\player\PKPunish.txt` (`0x080C5B45..`, 11 dòng → `KPlayerSet+0x3710 + 24·pk`) | cột 2 (`+0x3710`, mặc 1) ‰ kinh nghiệm cấp; cột 3 (`+0x3714` = `0x8bb2a34`) ‰ tiền; cột 4 (`+0x3718` = `0x8bb2a38`) ‰ đồ; cột 5 (`+0x371c`, mặc 1; 2003 = % rơi trang bị đang mặc); cột 8 (`+0x3720`, mặc −1); cột 9 (`+0x3724` = `0x8bb2a44`) % độ bền; `+0x3818 = dòng 2 cột 7` = **NormalPKTimeLong** 3240 (mặc 0xca8) | `jxassets export-player` → `player.json` `pk_punish {rows[11], normal_pk_time_long}` → `KPKPunish` |
| `\settings\npc\PKRate.ini [PK]` (`0x080A08F6..`, §16.4) | `rate 20`, `FactionPKFaction 1`, `KillerPKFaction 1`, `EnmityPK 2`, `BeKilled −1`, `KillPartnerPK 1`, `LevelDistance 25`, `ButcherPKExercise 1`, `NotSubPKExpPercent −50`, `NotEnmityExpPercent −50`, `NotFightExpPercent −80` | `player.json` `pk_rate` → `KPKRate` (`rate` vẫn là `pk_damage_percent`) |
| thuộc tính `KNpcAttribModify` 254 `not_add_pkvalue_p` `0x08095BE0` / 256 `pk_punish_weaken` `0x080961D0` / 257 `pk_punish_enhance` `0x08096210` | `Player+0x86f8 = v0`; `Player+0x8700 += v0`; `Player+0x86fc += v0`; `ClearAttrib 0x08082C73..` xoá cả ba | `KPlayer::not_add_pkvalue_p/pk_punish_weaken/pk_punish_enhance`, `KNpc::clear_attrib` |
| Lua `GetPK 0x081103C0`, `SetPK 0x08110420`, `SetPKFlag 0x0810F610` (`SetPKState(s, ép 1)`), `ForbidChangePK 0x0810F590` (`+0x5a58 = (n == 1)`), `IsForbidChangePK 0x0810F540`, `SetPkReduceState 0x08109570(giây, giá trị, giảm, tăng)` (`+0x5a8c/+0x5a84/+0x5a88 = (giảm << 8) \| tăng`), `GetPkReduceState 0x081094D0`, `SetDeathPunish_PK10 0x08110680` (`Player+0x384 = n`) | | `ScriptFuns.cpp` cùng tên |
| Lưu / nạp | `0x080BFCDD/0x080BFCEF`: byte trạng thái `+0x5a50` và `GetPKValue` vào bản ghi; `LoadFrom 0x080C1C79` `Init` + đặt lại | `RoleData.pk_state / pk_value / pk_locked` |

Zone: test `[pk]` 2 ca (65 khẳng định: 0x76 → 1 ngay; về 0 ngoài chiến ép, trong chiến cần `NormalPKTimeLong` (30 s trong bảng thử) → `refused`; khoá; đồng hồ 18 khung/giây;
giá trị kẹp 0..10, né `dodge_percent`, cờ `Player+0x384`; `G2C_ENTITY_PK` cho người xem; lưu; quan hệ 0/1/3/2 + điểm đồ tể `tăng + 1 − giảm` ≥ 0; phạt: kinh nghiệm
‰, tiền ‰, `BeKilled −1`, giữ trạng thái; chết thật qua `receive_damage` đi đường 2). `KillPlayer()` của `--auto` giết chính mình → kẻ giết = chính mình là người
chơi → kiểu 2 → phạt PK (đúng nhị phân: `GetPKRelation` không loại trừ tự sát). Chưa port: cừu sát/tỉ thí, bang chiến (4), khoá bảo mật `+0x507c/+0x5080` (giá trị > 2 → ép về 0), bảng bảo hộ,
nhà tù, `MakeEnemy`, `0x080A7E90`, giảm PK theo thời gian (không thấy trong nhị phân — chỉ Lua `SetPK`/vật phẩm).

## 17. Tổ đội — `KPlayerTeam` (`Player+0x5994`), `g_Team` (`0x8BB86E0`), `KTeamSet` (`0x8bc67e0`), gói client 0x53 (`0x080DCC90`, 11 lệnh con), `AddExpTeam 0x080B03E0`, sổ sát thương theo đội trưởng `0x0809BC70`/`0x0809BDD0`, rơi đồ chia đội `0x080843A0` (M14 lát T1, đã kiểm từng dòng)

Mã 2004 để đặt tên: `Core/Src/KPlayerTeam.h/.cpp` (`KPlayerTeam::CreateTeam/InviteAdd/GetInviteReply/SetCanTeamFlag`, `KTeam::*`, `KTeamSet::*`),
`Core/Src/KPlayer.cpp` 1026–1560 (`S2CSendTeamInfo`, `SendSelfTeamInfo`, `SetTeamState`, `S2CSendAddTeamInfo`, `AddTeamMember`, `LeaveTeam`,
`TeamKickOne`, `TeamChangeCaptain`, `TeamDismiss`). Bản Linux có **7 ô đội viên** (2004: 5) và thêm cờ `+0x30` "đội trưởng do hệ thống chỉ định".

| Hàm / cấu trúc | Luật (đã đọc) | Zone |
|---|---|---|
| **`KPlayerTeam` `Player+0x5994`** | `+0` `m_nFlag` (đang trong đội), `+4` `m_nID` (chỉ số `g_Team`), `+8` `m_nFigure` (0 đội trưởng / 1 đội viên), `+0xc` `m_nApplyCaptainID` (**id npc** của đội trưởng mình xin vào), `+0x10..+0x28` `m_nInviteList[7]` (chỉ số player được mời), `+0x2c` `m_nListPos`, **`+0x30`** đội trưởng do hệ thống chỉ định (`0x080CD4FE` ghi 1; xem `0x080CD480`), `+0x34` `m_bCanTeamFlag`. `Release 0x080CC5D0` xoá `+0..+0x30` (giữ `+0x34`). `SetCanTeamFlag 0x080CC580(team, idx, cờ, rời)`: `+0x34 = cờ`; cờ 0 và `rời` → `LeaveTeam 0x080B7C60` | `KPlayerTeam` (`KPlayerTeam.h`: `flag/id/figure/apply_captain/invite_list[7]/list_pos/auto_captain/can_team`; thêm `captain_npc` = id npc đội trưởng cho sổ sát thương, `lua_disabled` = bit 0x400 của giá trị nhiệm vụ 0x87) |
| `m_dwLeadExp +0x596c`, `m_dwLeadLevel +0x5970` | bảng `0x8bb26a4` từ `\settings\npc\player\level_lead_exp.txt` (cột 1 cấp, 2 kinh nghiệm, 3 số đội viên): `0x080C4560(cấp)` = số đội viên (**1** ngoài 1..100), `0x080C4580(cấp)` = kinh nghiệm. Kinh nghiệm thống lĩnh chưa thấy nơi cộng trong nhị phân (`lead_exp_share` của `level_add.txt` không ai đọc) | `KPlayer::lead_exp/lead_level` (`RoleData.lead_exp/lead_level`), `KPlayerSet::lead_members/lead_level_exp` (`player.json` `lead_exp[100]` từ `export-player`) |
| **`g_Team 0x8BB86E0`** (1200 dòng × 0x30) | `+4` `m_nState` (1 `Team_S_Open` nhận người / 0 đóng), `+8` `m_nCaptain` (chỉ số player; **−1** = dòng trống), `+0xc..+0x24` `m_nMember[7]` (−1 trống), `+0x28` `m_nMemNum` (không kể đội trưởng), **`+0x2c`** cờ giới hạn thống lĩnh (1 khi lập; Lua `ChangeTeamFeature`) | `KTeam` (`state/captain/members[7]/count/lead_limit`; sid thay chỉ số player, 0 = trống) |
| `KTeamSet::CreateTeam 0x080CC290(set, idx)` | dòng đầu có `+8 < 0`: `+8 = idx`, `+4 = 0`, 7 ô = −1, `+0x28 = 0`, `+0x2c = 1`; trả chỉ số; đủ 1200 → **−2** | `KTeamSet::create` (dự trữ 1200, con trỏ không đổi) |
| `FindMemberID 0x080CBF00`, `IsOpen 0x080CBF80`, `CalcCaptainPower 0x080CC960` (= `0x080C4560(Player[đội trưởng]+0x5970)`), `CheckFull 0x080CC990` (`+0x2c == 0` → không bao giờ đầy; không thì `+0x28 ≥ CalcCaptainPower`) | | `find_member`, `team_members_max`, `team_full` |
| **`KTeam::AddMember 0x080CC9D0(team, idx)`** | đội trưởng hợp lệ; `CheckAddCondition`: người mới **camp ≠ 0** mà đội trưởng **camp == 0** → từ chối (`0x080CCA18..0x080CCA36`); `CheckFull` → từ chối; ô trống đầu tiên, `+0x28++` | `team_add_member` |
| `0x080CC320(set, a, b)` (`KTeamSet::CheckIn`) / `0x080CC210(team, idx)` | a ≠ b, cả hai trong đội, b là đội trưởng hay đội viên của đội a; `0x080CC210`: idx là đội trưởng hay đội viên của `team` | `KTeam::check_in` |
| **`0x080CC620(team, player)`** | số đội viên (kể đội trưởng, trừ chính mình) cùng bản đồ trong `0xfffff` khoảng cách² (`0x0809E660` = −1 khi khác bản đồ) — dùng cho **may mắn rơi đồ** `GenRandomItem 0x08083DA2` (`+ m_nCurLucky`) và mất kinh nghiệm khi chết `× (7 − n) / 7` | `team_near_count` |
| **`KTeam::SetTeamOpen 0x080CD960`** | đội trưởng hợp lệ; `CheckTrading 0x080A7E90` (menu trạng thái 3 hay `+0x5920` đang giao dịch) → thôi; `+0x5f88` đang nạp → thôi; **`+0x59c4`** → gói 0x86 `{4, 0x28}` (`MSG_TEAM_ERROR05`) thôi; `CheckFull` → thôi; `+4 = 1`; gói **0x69 `{6, 1}`** cho đội trưởng; `0x080C29D0` (biểu tượng "tìm người" 0x76 quanh vùng — client này không vẽ). `SetTeamClose 0x080CCA80`: `CheckTrading`; `+4 = 0`; 0x69 `{6, 0}` | `team_set_open` / `team_set_close` (không có giao dịch) |
| **`0x080CD480(team)`** chuyển chức tự động | `SetTeamClose`; `+0x28 == 0` → `0x080CD5A8` (LeaveTeam); `0x080CC0D0` = **đội viên đầu tiên cùng camp với đội trưởng** (< 0 → `0x080CD5D0`); hoán ô; đội trưởng cũ `+0x599c = 1`; người mới `+0x599c = 0`, **`+0x59c4 = 1`**; gói **0x69 `{0xd, npc mới, npc cũ}`** (0xc byte) cho mọi người | `team_hand_over` |
| **`KTeam::DeleteMember 0x080CD5E0(team, idx)`** | đội trưởng đi: `+0x2c == 0 && +0x28 > 0` → `0x080CD480` (chuyển chức) rồi rời như đội viên; không thì **giải tán**: mỗi người `MSG_TEAM_DISMISS` + `s2c_teamleave {npc mình}`, `Release`, `RestoreCurrentCamp 0x0807B8F0`; `KTeam::Release 0x080CC3C0`. Đội viên đi: `s2c_teamleave {npc}` cho cả đội, ô = −1, `+0x28--`, `Release`, camp về `m_Camp` | `team_delete_member` |
| **gói client 0x53** `{0x53, word dài ≥ 4, byte lệnh con +3, dword npc +4}` → ô `0x080DCC90` (bảng nhảy `0x8257f48`) | 1 `S2CSendTeamInfo 0x080B1AD0`; 2 lập `0x080B1E20 → KPlayerTeam::CreateTeam 0x080CE3C0`; 3 mở/đóng `SetTeamState 0x080B1CF0`; 4 xin vào `S2CSendAddTeamInfo 0x080B8000`; 5 nhận `AddTeamMember 0x080B75B0`; 6 rời `LeaveTeam 0x080B7C60`; 7 đuổi `TeamKickOne 0x080B9880`; 8 nhường chức `TeamChangeCaptain 0x080B9400`; 9 giải tán `TeamDismiss 0x080B7DE0`; 10 mời `InviteAdd 0x080CE150`; 11 trả lời mời `0x080B1990 → GetInviteReply 0x080CCBA0` | `C2G_TEAM` `TeamReq{cmd 1..11, target, flag}` → `team_request` (gateway chuyển tiếp) |
| **`KPlayerTeam::CreateTeam 0x080CE3C0`** | npc **camp 6** → thôi; `+0x34 == 0` hay giá trị nhiệm vụ 0x87 (`0x080A8C80` = `GetTaskValue(+0x809c, 0x87)`) **bit 0x400** (Lua `DisabledTeam`) → 0x69 `{5, 4}`; đang trong đội → `{5, 0}`; `KTeamSet::CreateTeam` −2 → `{5, 2}`; thành công: `+0 = 1, +8 = 0, +4 = id`, `RestoreCurrentCamp 0x0807B8F0` (trừ `Player+0xd8/+0xdc`), gói **0x69 `{word 8, 4, dword id, byte camp hiện tại}`**, `SetTeamOpen`, **`+0x59c4 = 0`** (`0x080CE54C`) | `team_create` → `TEAM_EV_CREATE_OK` / `CREATE_FAIL{0,2,4}` |
| `KPlayer::SetTeamState 0x080B1CF0` | `CheckTrading` → thôi; không phải đội trưởng → `SendSelfTeamInfo 0x080AA7F0`; `+0x5700 == 2` → menu `SetState 0x080C29D0`; cờ 0 → `SetTeamClose`, khác → `SetTeamOpen` | `team_set_state` |
| **`S2CSendAddTeamInfo 0x080B8000`** (xin vào) | vùng ≥ 0; `+0x59c8 == 0` → 0x86 `{4, 0x12}`; camp 6 → thôi; đội trưởng theo id npc trong vùng (`0x080E0D80`, rồi 8 vùng quanh); đội trưởng **camp 0** mà mình camp ≠ 0 → thôi; `+0x59a0 = id npc`; gói **0x69 `{word 7, 7, dword npc mình}`** cho đội trưởng | `team_apply_add` → `TEAM_EV_APPLY` |
| **`AddTeamMember 0x080B75B0`** (nhận) | không phải đội trưởng / đội đóng / `+0x28 > 6` / `≥ CalcCaptainPower` → `SendSelfTeamInfo` thôi; **`+0x59c4`** → 0x86 `{4, 0x24}` (`MSG_TEAM_ERROR01`); `FindAroundPlayer 0x080B1610` (3×3 vùng); người xin `+0x59c8 == 0` → 0x86 `{4, 0x13}` (`MSG_TEAM_TARGET_CANNOT_ADD_TEAM`); npc camp 6 → thôi; đang trong đội → thôi; `+0x59a0 ≠ npc mình` → thôi; `AddMember`; `+0x28 > 6` hay `CheckFull` → `SetTeamClose`; `KPlayerTeam::Release` người mới rồi `+0 = 1, +8 = 1, +4 = id`; **`SetCurrentCamp(npc mới, m_Camp đội trưởng)`** `0x0807B850`; `s2c_teamaddmember` cho cả đội, `SendSelfTeamInfo` + `MSG_TEAM_SELF_ADD` cho người mới | `team_accept` + `team_join` |
| **`LeaveTeam 0x080B7C60`** | ngoài đội → thôi; đội trưởng đội đang mở → `SetTeamClose`; đội viên → `MSG_TEAM_LEAVE` cho đội; `DeleteMember`. **Rời game `0x080C5530..0x080C55D7`** cũng gọi | `team_leave`; `remove_player` |
| **`TeamKickOne 0x080B9880`** | đội trưởng; **`+0x59c4`** → 0x86 `{4, 0x25}` (`MSG_TEAM_ERROR02`); npc phải là đội viên (`FindMemberID`); `DeleteMember`; `MSG_TEAM_KICK_ONE` (tên) cho người bị đuổi và cả đội | `team_kick` → `TEAM_EV_KICK` |
| **`TeamChangeCaptain 0x080B9400`** | đội trưởng; **`+0x59c4`** → 0x86 `{4, 0x26}` (`MSG_TEAM_ERROR03`); npc là đội viên; đội viên **camp 0** mà đội trưởng camp ≠ 0 → 0x86 `{4, 7}` (`FAIL1 + FAIL3`); `+0x28 > 0x080C4560(cấp thống lĩnh người mới)` → 0x86 `{4, 6}` (`FAIL1 + FAIL2`); đội mở → đóng, đổi xong mở lại; hoán ô, `+8` đổi vai, **`+0x59c4` người mới = 0** (`0x080B9579`), `RestoreCurrentCamp` người mới, mọi đội viên `SetCurrentCamp(camp người mới)`; `s2c_teamchangecaptain {mới, cũ}` cho cả đội | `team_change_captain` → `TEAM_EV_CHANGE_CAPTAIN{arg 1 = chính mình}` |
| **`TeamDismiss 0x080B7DE0`** | đội trưởng; mỗi người `MSG_TEAM_DISMISS` + `s2c_teamleave`, `Release`, camp về; `KTeam::Release` | `team_dismiss` → `TEAM_EV_DISMISS` + `TEAM_EV_LEAVE` |
| **`KPlayerTeam::InviteAdd 0x080CE150`** | `+0 && +8 == 0 && +4 ≥ 0`; **`+0x30`** (auto) → 0x86 `{4, 0x27}` (`MSG_TEAM_ERROR04`); đội đóng → `SetTeamOpen` phải thành công; `FindAroundPlayer`; npc camp 6 → thôi; người được mời `+0x59c8 == 0` hay bit 0x400 của 0x87 → 0x86 `{4, 0x13}` (+ `0x080BDD70`); `m_nInviteList[m_nListPos] = idx; m_nListPos = (m_nListPos + 1) % 7`; gói **0x69 `{0xc, dword player, tên}`** (0x20 byte) cho người được mời | `team_invite` → `TEAM_EV_INVITE` |
| **`GetInviteReply 0x080CCBA0`** (phía đội trưởng, từ `0x080B1990`) | đội trưởng đội mở; người trả lời phải nằm trong `m_nInviteList`; từ chối → `MSG_TEAM_REFUSE_INVITE` (tên) cho đội trưởng; `+0x28 > 6` hay `≥ CalcCaptainPower` → thôi; người trả lời đang trong đội → **`LeaveTeam` trước** (`0x080CCC6A`); `AddMember`; đầy → `SetTeamClose`; cùng đuôi với `AddTeamMember` | `team_reply_invite` → `TEAM_EV_REFUSE` |
| `S2CSendTeamInfo 0x080B1AD0` | npc mình → `SendSelfTeamInfo`; npc quanh vùng, trong đội, đội trưởng có mặt, đội **mở** → `s2c_teaminfo {đội trưởng, đội viên}`; khác → `s2c_teamapplyinfofalse` | `team_info` → `TEAM_EV_INFO` / `INFO_FALSE` |
| `SendSelfTeamInfo 0x080AA7F0` | trong đội: `s2c_teamselfinfo` (đội trưởng, 7 ô, trạng thái, cấp/tên từng người); ngoài: `s2c_teamleave {npc mình}` | `G2C_TEAM_SELF` `TeamSelf{in_team, team_id, state, captain, leader, members, lead_level, lead_exp, members_max}` — gửi cả đội sau mỗi thay đổi |
| **`KNpc::DoDeath 0x080892E0` (`0x080893AA..`)** | đội trưởng của đội có `+0x2c == 1` → `0x080B1DE0` → **`0x080CD480`** chuyển chức | `do_death` → `team_hand_over` |
| **`KDamageRecord::Add 0x0809BC70(sổ, idx, sát thương)`** | npc người chơi không ghi; `Player[idx]+0x5994 ≠ 0` → **khoá ghi = `g_Team[+0x5998].captain`** (chỉ số đội trưởng); 3 ô `{player, sát thương, 0x4b0}` | `KNpc::damage_record_key` (`captain_npc`) trong `receive_damage` |
| **`0x0809BDD0(sổ, player kẻ giết)`** (từ `DoDeath 0x08089698`, chỉ khi kẻ giết là người chơi có `+0x1908 > 0`; kẻ khác → `0x08088B60(npc, kẻ giết, 0, 0)` không rơi) | mỗi ô: chủ trong đội → **mốc** = đội trưởng nếu `dist² ≤ 0xfffff` (`0x0809E660`), không thì đội viên **gần nhất** trong `0x100000`; không ai → bỏ ô; chủ lẻ → chính nó; `exp = int(m_Experience (+0x15a8) × sát thương / max(+0x1a14, +0x1a18))` → **`AddExpTeam(Player[mốc], exp, cấp npc +0x20, kẻ giết)`**; trả **ô sát thương lớn nhất** (`best`) | `share_experience(dead, killer)` → `EntityId best` |
| **`KPlayer::AddExpTeam 0x080B03E0(player, exp, cấp npc, kẻ giết)`** | trong đội: đếm người **cùng bản đồ trong `0x100000`** (đội trưởng trước, rồi 7 ô) → `n`, tổng cấp `sum`; `n ≤ 0 \|\| sum ≤ 0 \|\| n > 8 \|\| sum < cấp mình \|\| n == 1` → `AddExp 0x080B00C0` thường; `k = int(sqrt(n) × 100.0)` (`0x0825528C`); **kẻ giết**: `exp × (100 + n) / 100`; **người khác**: `exp × min(60, cấp_i × k / sum) / 100`; mỗi phần ≥ 1 | `add_exp_team` |
| **`KNpc::OnDeath 0x08088B60(npc, kẻ giết, best, player kẻ giết)`** phần rơi | `best ≤ 0` → không rơi; bảng `+0x174c`; `SubWorld+0x4f298` → không rơi; **`bảng+0x34` (IsTeamShare)** → `0x080843A0(npc, player kẻ giết, bảng+0x38)`; không thì `m_CurrentTreasure` lần: `g_Random(100) < MoneyRate` → `LoseMoney 0x08081950(npc, best)`, không thì `LoseSingleItem 0x08088840(npc, best, kẻ giết)`; rồi `OnGlobalNpcDeath` với kẻ giết nếu `CheckIn(best, kẻ giết)` (cùng đội) không thì `best` | `lose_treasure(dead, killer, best)` |
| `LoseSingleItem 0x08088840(npc, best, kẻ giết)` | `CheckIn(best, kẻ giết)` (kẻ giết là đồng đội của best) → `GenRandomItem` với **may mắn kẻ giết**, không thì của `best`; `Player[best]+0xb0 == 1` → `AddItem` thẳng vào túi (`0x08088A70`); không thì rơi tại chỗ, `SetItemBelong(best)` 600 khung | `lose_treasure` (`luck = cur_lucky + team_near_count`; belong = `best`) |
| **`0x080843A0(npc, player kẻ giết, rate)`** rơi chia đội (`IsTeamShare = 1`: 4 bảng `citydefence\*.ini`) | `0x0809C090(sổ, buf, rate)`: mỗi ô → nhóm: chủ lẻ nặng = sát thương; chủ trong đội → những người trong `0xfffff` (đội trưởng trước): mỗi người `(100 − rate) × dmg / 100 / n`, riêng chủ ô thêm `rate × dmg / 100` (rate 0 → `dmg / n`); mỗi lần rơi: tiền → `LoseMoney(npc, kẻ giết)`; đồ → `GenRandomItem` (may mắn kẻ giết) → quay `(rand() \| rand() << 15) % tổng` theo cân nặng cộng dồn → **`KPlayer::AddItem 0x080B5180(người trúng, vật, 1, 1, 0)`** thẳng vào túi (+ `G_STR_TEAMSHAREITEM_MODEPUNISH1/2` cho nhóm), túi đầy → `KItemSet::Remove` (mất) | `lose_treasure_shared` (`give_item`) |
| **`ServerPickUpItem 0x080B8210`** phần đội (`0x080B826C..0x080B83BB`, `0x080B84F0`) | `belong == −1` → tự do; trong đội: **tiền** → của mình hay `0x080CC210(đội mình, belong)` (chủ là người trong đội mình); **vật** genre ≠ 4 → như tiền; genre 4 (nhiệm vụ) → chỉ chủ; sai → 0x86 `{4, 8}` / `{4, 0x11}` | `team_may_take` trong `pick_up_request` |
| Lua `IsCaptain 0x08115690` (`+0x5994 && +0x599c == 0`), `GetTeam 0x08115630` (id, nil ngoài đội), `GetTeamSize 0x08115480([đội])` (`+0x28 + 1`; 0 ngoài đội), `GetTeamMember 0x08115530(n)` (1 = đội trưởng, n ≥ 2 = ô có người thứ n−1; −1), `LeaveTeam 0x08121060`, `SetCreateTeam 0x08120FC0(n)` (`SetCanTeamFlag(n ≠ 0, rời 1)`), `DisabledTeam 0x08126590(n)` (bit 0x400 của giá trị nhiệm vụ 0x87 qua `0x080A9370` + gói 0xa4), `IsDisabledTeam 0x0812EF50`, `ChangeTeamFeature 0x08103630(đội, đặc tính, giá trị)` (1 = cờ `+0x2c`), `Msg2Team 0x081152C0` (`0x081C9220(1, …)` cho đội trưởng + 7 ô), `BuildATeam 0x08123680 → 0x080CDB20` (lập đội từ danh sách — chưa port) | | `ScriptFuns.cpp` cùng tên (`GetTeamMember` trả id thực thể; `DisabledTeam` giữ ở `lua_disabled` tới khi có bảng giá trị nhiệm vụ M13) |

**Thông điệp 0x86 `{0x86, word 4, word id}`** (client: `0x00657AD0`, bảng nhảy `0x658674` theo `id − 1`, chuỗi nạp từ **`\lang\<vn|zh|tw>\stringtable_core.txt`** qua `0x005DBA60` — `CLIENT-2.0.md` §21): 2 `MSG_TEAM_DISMISS_CAPTAIN`, 3 `MSG_TEAM_LEAVE_SELF_MSG`, 5 `MSG_TEAM_SELF_ADD`, 6 `CHANGE_CAPTAIN_FAIL1+FAIL2`, 7 `FAIL1+FAIL3`, 8 `MSG_OBJ_CANNOT_PICKUP`, 0x11 `MSG_MONEY_CANNOT_PICKUP`, 0x12/0x13 `MSG_TEAM_TARGET_CANNOT_ADD_TEAM`, 0x24..0x28 `MSG_TEAM_ERROR01..05` ("Nhóm trưởng do hệ thống chỉ định, không thể tăng thêm thành viên / đuổi / đảm nhận tân nhóm trưởng / mời / mở rộng nhóm").

Zone: `KPlayerTeam.h/.cpp` + `KSubWorldTeam.cpp`; test `[team]` 10 ca (341 khẳng định: `KTeamSet` dùng lại dòng trống; bảng thống lĩnh; lập đội (camp 6, `can_team`, `DisabledTeam`, lần hai); xin/nhận
(chưa xin, đội viên không nhận, giới hạn cấp thống lĩnh đóng đội khi đầy, cấp tăng mở lại, luật camp 0); mời/trả lời (vòng 7 ô, từ chối, đội trưởng đội khác rời đội mình rồi vào, `can_team`);
rời/đuổi/nhường/giải tán (camp về, thông điệp 6, sự kiện cho cả đội); đội trưởng chết → chuyển chức, đội trưởng chỉ định bị chặn 0x24..0x28 tới khi lập đội mới, `lead_limit` tắt → rời đội chuyển chức,
rời game rời đội; `AddExpTeam` 1030/430/600 với n = 3 sum = 40, xa 1024 không tính, một mình = thường; sổ sát thương ghi theo đội trưởng + mốc gần nhất; nhặt đồ của đồng đội). **Chưa port**:
`0x080CDB20` (`BuildATeam`), `CheckTrading`, biểu tượng tìm người 0x76, `Player+0xb0` (đồ vào thẳng túi), thông điệp vào nhóm khi chia đồ, `OnGlobalNpcDeath`, kinh nghiệm thống lĩnh (không thấy nơi cộng).

## 18. Giao dịch giữa hai người chơi — `KPlayerMenuState` (`Player+0x5700`), `KTrade` (`Player+0x5910`), `TradeApplyOpen 0x080AE590`, gói 0x6a/0x6b/0x6c/0x6d (`0x080AE320/0x080B4DE0/0x080AE510/0x080B2C70`), `c2sTradeReplyStart 0x080BAFD0`, `SyncTradeState 0x080A85B0`, huỷ `0x080AE380`, ô giao dịch của `ExchangeItem 0x08206110` (M14 lát G1, đã kiểm từng dòng)

Mã 2002–2003 để đặt tên: `Core/Src/KPlayerMenuState.h/.cpp` (`PLAYER_MENU_STATE_NORMAL/TEAMOPEN/TRADEOPEN/TRADING/IDLE`, `SetState`, `RestoreBackupState`),
`Core/Src/KPlayerTrade.h` (`KTrade`: `m_nIsTrading/m_nTradeDest/m_nTradeState/m_nTradeLock/m_nApplyIdx`), `Core/Src/KPlayer.cpp` 3040–3160 / 3821–3911 / 4142 / 4210
(`TradeApplyOpen/Close/Start`, `TradeMoveMoney`, `TradeDecision`, `TradeApplyLock`, `c2sTradeReplyStart`, `SyncTradeState`), `Headers/KProtocolDef.h` (`c2s_tradeapplystateopen` 113…`c2s_tradereplystart` 127 — bản Linux đánh số khác).

| Hàm / cấu trúc | Luật (đã đọc) | Zone |
|---|---|---|
| **`KPlayerMenuState` `Player+0x5700`** | `+0` `m_nState` (0 thường, 1 mở đội, 2 mở giao dịch, 3 đang giao dịch, 4 rảnh), `m_nBackState`, câu rao `m_szSentence` (≤ 255) + bản sao. `SetState 0x080C29D0(menu, chỉ số player, trạng thái, câu, dài, npc đối tác)`: trạng thái đổi → lưu bản cũ; **0 từ 1** mà đội đang mở → `SetTeamClose`; cho mình gói `s2c_tradechangestate` `{0}` / `{1}` / `{2, npc đối tác}`; quanh vùng `s2c_npcsetmenustate {id, trạng thái, câu}`. `RestoreBackupState 0x080C2ED0`: về bản cũ (huỷ giao dịch) | `KPlayerMenuState` (`KPlayerTrade.h`), `set_menu_state` / `restore_menu_state` / `emit_menu_state` → `G2C_TRADE_STATE` (mình) + `G2C_ENTITY_MENU_STATE` (người xem); `team_set_open/close` đặt 1 / 0 |
| **`KTrade` `Player+0x5910`** (`Release 0x080D82D0`, `StartTrade 0x080D8300`) | `+0` `m_nTradeDest` (chỉ số player; −1), `+4` `m_nTradeState` (đã bấm OK), `+8` `m_nTradeLock` (đã khoá), `+0xc` `m_nApplyIdx` (**người xin nhớ mình xin ai**), `+0x10` `m_nIsTrading`. `CheckTrading 0x080A7E90` = `+0x5700 == 3 \|\| +0x5920` | `KTrade` (sid thay chỉ số), `trading()` |
| `KPlayer::TradeApplyOpen 0x080AE590` `{word dài, câu}` (gọi thẳng từ bộ xử lý chính) | sống (`0x080A8ED0`), không giao dịch, không đang nạp `+0x5f88`, luật cấp `0x080AB930` (`[0x9789f14] == 0` → luôn được), câu cắt 255 → `SetState(2, câu)` | `trade_apply_open` (`TRADE_APPLY_OPEN{text}`) |
| gói **0x6a** `0x080AE320` | không giao dịch → `SetState(0)` | `trade_apply_close` |
| gói **0x6b** `0x080B4DE0` `{npc, dword mã}` (`TradeApplyStart`) | sống, không giao dịch, `[0x830ca40] != 2` (công tắc thế giới), luật cấp; đích = `FindAroundPlayer 0x080B1610` và **`+0x5700 == 2`**; cả hai `0x080A8FC0`; không khoá bảo mật (`tbSecurityLock:CheckTrade` `0x080B29F0`); **`0x080A79B0(player, loại gói, dword)`** = kiểm mã gói (`(Player+0x3b8 ^ +0x3bc ^ dword) % bảng[loại]`, bảng `0x8baef00`: loại 0x4b/0x4c/0x4d/0x6b/0x6e/0x75 — mã chống bot của client 2.0, không port); **`+0x591c` của người xin = đích**; gói **0x8b** `{dword chỉ số player mình (+0xc0), dword npc mình}` cho đích | `trade_apply_start` → `G2C_TRADE_APPLY{entity, name}` |
| **`c2sTradeReplyStart 0x080BAFD0`** `{byte đồng ý, dword chỉ số người xin, dword mã}` (gọi thẳng) | mình không giao dịch, không nạp; người xin hợp lệ và **`+0x591c` của nó == chỉ số mình**; người xin đang giao dịch mà đồng ý → thôi; mã gói; `[0x830ca40] != 2`; **`Player+0x374`** (cấm giao dịch) bên nào → 0x86 `{8, 0x2c}` cho bên ấy; từ chối hay khác bản đồ → 0x86 **`{8, 0xd, npc mình}`** (`MSG_TRADE_REFUSE_APPLY`) cho người xin; mình phải còn **trạng thái 2**, cả hai `0x080A8FC0`; **script `\script\global\trade.lua` `CheckPlayerTrade(chỉ số người xin)`** với `PlayerIndex`/`SubWorld` toàn cục — trả ≠ 1 → thôi (chưa port); đối tác đang mở đội (1) → `SetTeamState(đóng)`; ai đang ngồi (`+0x224 == 8`) → `DoStand`; `0x081FC900(list)` (ô 2 và 4 về túi), `KTrade::Release`, `StartTrade(đối tác, 0, 0)`, `SetState(3, npc đối tác)` — cả hai | `trade_reply` (`TRADE_REPLY{target = người xin, arg}`) → `G2C_TRADE_STATE{2, partner}` + `trade_sync` |
| gói **0x6c** `0x080AE510` `{dword tiền}` (`TradeMoveMoney`) | đang giao dịch, chưa khoá, `0 ≤ tiền ≤ tiền túi` (`0x081F9E70`) → `0x081FB020(list, tiền, 0)` = tiền ô giao dịch (`list+0x4cc8` = `Player+0x50c4`) | `trade_money` → `set_money(room_trade)` + `trade_sync` |
| **`SyncTradeState 0x080A85B0`** | đang giao dịch: gói **0x77 `{dword tiền ô của mình}`** cho **đối tác**; gói **0x81 `{khoá mình, khoá đối tác, OK mình, OK đối tác}`** cho mình và (đảo vai) cho đối tác | `G2C_TRADE_SYNC{self_lock, dest_lock, self_ok, dest_ok, self_money, dest_money}` cho cả hai |
| ô giao dịch trong `KItemList::ExchangeItem 0x08206110` (`0x08207172..`) | phòng: `list+0x4c8c + phòng·0x1c`, phòng 0..0xe (bản Linux 15 phòng; 2 = ô giao dịch, 4 = ô thứ hai — bảng `玩家间交易.ini` có `*BindGold`); đặt vào ô 2 → gói **0xcc byte** (vật phẩm đủ) cho `Player[+0x5910]` (đối tác) + 7 byte cho mình | `item_move_request`: phòng `room_trade` vào/ra chỉ khi đang giao dịch và chưa khoá (không nhận vật nhiệm vụ) → `trade_item_sync` → `G2C_TRADE_ITEM{item, removed}` cho đối tác |
| gói **0x6d** `0x080B2C70` `{byte quyết định}` (`TradeDecision`) | `+0x591c = −1`; đang giao dịch, đối tác trỏ ngược lại (`Player[dest]+0x5910 == mình`), cả hai `0x080A8FC0`, cùng bản đồ (`+0x1180`), không khoá bảo mật. **0** → huỷ `0x080AE380`; **2** khoá: chưa khoá → `OK mình = 0, khoá = 1, OK đối tác = 0`; rồi `SyncTradeState`; **3/4** → chỉ sync; **1** OK: cả hai chưa khoá → sync; đối tác chưa OK → `OK mình = 1` + sync; đối tác đã OK → **trao đổi** | `trade_decision` |
| **trao đổi** `0x080B2EC7..0x080B44CC` | `0x081FF5D0(list, 2/4)` hai bên (ô hợp lệ); **tiền**: `túi ≥ tiền ô ≥ 0`, `túi + tiền ô đối tác` không tràn (mỗi bên) — sai → huỷ + thông báo `[0x978a174]/[0x978a148]`; **chỗ trống**: `0x081FC410(list đối tác)` gom ô đối tác → `0x081FA250(list mình)` mô phỏng đặt vào lưới túi 6×10 (`[0x830d7e8]`) — ≠ 0 mới vừa; bên nào không vừa → **OK bên ấy = 0**, 0x86 `{4, 0xb}` cho bên ấy (`MSG_TRADE_SELF_ROOM_FULL`) và `{4, 0xc}` cho bên kia, rồi sync; vừa cả → mỗi vật: dòng nhật ký `"%s\t%s\tnăm-tháng-ngày giờ\tNewWorld(map, x, y)\tItemName[%s]I…"` (`fwrite`), `0x080BDC70/0x080BDCF0` chuyển; tiền `0x081F9EF0/0x081F9F30`; gói **0x78 `{1}`** cho cả hai; `SetState(0)` cả hai; `KTrade::Release` cả hai | `trade_exchange`: kiểm tiền, mô phỏng chỗ bằng bản sao `KItemList` (`add_or_stack` vào túi), `G2C_ITEM_REMOVE` bên cho + `give_item` bên nhận (log `item traded`), tiền qua `set_money` + `G2C_MONEY`, `G2C_TRADE_END{ok}` |
| **huỷ `0x080AE380(player, đối tác)`** | `0x08207DE0(list)` (kiểm trùng trang bị: log `"(ERR) [ITEM] %s(%s) is doubtful of duplicating equipments"`), `0x081FC8D0(list, 2)` và `(list, 4)` (ô về túi), `KTrade::Release`, gói **0x78 `{0}`**, `RestoreBackupState` — cả hai bên. Gọi từ `DoDeath 0x08089701 → 0x080AE4B0` (đang giao dịch và đối tác trỏ ngược), `KPlayer::Clear 0x080B60A0` (rời game) | `trade_cancel` (đồ về túi, không vừa → rơi xuống đất cho chủ), `do_death`, `remove_player` |

| **bảng rao trong gói đồng bộ đầy đủ 0x4c** `0x0807FBB0` nhánh người chơi `0x0807FDD8` | byte `+0x10` của gói = `Player+0x5700` (trạng thái menu); sau tên (`+0x21` = `" "`, size = `0x21 + strlen`) chép **`min(strlen(+0x570c), 0x1e)`** byte câu rao (`memcpy 0x0807FE61`, size cộng thêm); byte `+0x20` = `Player+0x86b8` (số lần trùng sinh). Người mới vào tầm nhìn (`0x0809F190` → `0x0807FBB0`) thấy ngay bảng rao đang treo | `fill_info`: `EntityInfo.menu_state` + `menu_sentence` (cắt 0x1e byte, tròn ký tự UTF-8 — `kMenuSyncSentenceMax`) → client `KNpc.setup` (`menu_state`/`menu_sentence` của dict spawn) |
| **`Earn(n)` `0x08118970`** → `KPlayer::Earn 0x080AAED0` → `KItemList::Earn(list, phòng 0, n) 0x081FC990` | `n ≤ 0` → không làm gì (trả 0 giá trị); `n < 0` → 0; phòng > 14 → 0; `KRoom::AddMoney 0x081F8B10` (`tiền + n < 0` → 0, không thì ghi); `SyncMoney 0x081FAFD0` (gói **0x61** 13 byte `{tiền túi +0x4c90, tiền rương +0x4cac, tiền ô giao dịch +0x4cc8}`); gọi script `\script\global\events.lua` `OnRoomMoneyChange(phòng, tiền)` ("dd", `0x080ACED0`); dòng log tiền `0x081E8EA0(idx, int64 n, "Lua_Earn", 0)`; `n > 99 999` → ghi thêm 10 tầng gọi Lua (`Sln`) vào log | `KSubWorld::earn` (+ `send_money` = `G2C_MONEY`), `l_Earn` (log `script money` reason `Lua_Earn`); `OnRoomMoneyChange` chưa (eventsys chưa port) |
| **`Pay(n)` `0x08118A90`** → `KPlayer::Pay 0x080A9450` → `KItemList::Pay(list, n) 0x081FC940` | `n ≤ 0` → không trả; `n < 0` → 0; `n > tiền túi +0x4c90` → **0**; `KRoom::AddMoney(phòng 0, −n)`; `SyncMoney`; trả **1** (log `"Lua_Pay"`, `> 99 999` → dấu vết) hay **0** | `KSubWorld::pay`, `l_Pay` |
| **`GetCash()` `0x081116D0`** | đẩy `Player+0x508c` (= `ItemList+0x4c90`, tiền túi) | `KSubWorld::cash`, `l_GetCash` |

Thông điệp 0x86 (client §21): 0xb `MSG_TRADE_SELF_ROOM_FULL`, 0xc `MSG_TRADE_DEST_ROOM_FULL`, 0xd `MSG_TRADE_REFUSE_APPLY`, 0xe `MSG_TRADE_TASK_ITEM`, 0x2c (cấm giao dịch `Player+0x374`).

Zone: `KPlayerTrade.h` + `KSubWorldTrade.cpp`; proto `TradeCmd/TradeReq/TradeState/TradeSync/TradeItem/TradeApply/TradeEnd/SysMsg/EntityMenuState`, `C2G_TRADE` 1120, `G2C_TRADE_*` 2132..2136, `G2C_SYS_MSG` 2137, `G2C_ENTITY_MENU_STATE` 2138. Test `[trade]` 3 ca (154 khẳng định: mở/đóng bảng + câu cắt 255, xin/từ chối 0xd/đồng ý → cả hai 3 + bản sao trạng thái, huỷ khôi phục, ô giao dịch chỉ khi giao dịch và chưa khoá, đối tác thấy vật vào/ra, tiền ≤ túi, OK trước khoá chỉ sync, khoá xoá OK đối tác, trao đổi 500−120/50+120 + đồ đổi chủ + 0x78 {1}, túi đầy → 0xb/0xc đúng bên, chết/rời huỷ). **Chưa port**: script `trade.lua CheckPlayerTrade`, `[0x830ca40]`, `Player+0x374` cấm giao dịch, mã gói `0x080A79B0`, ô 4 (bind gold), kiểm trùng trang bị `0x08207DE0`, nhật ký giao dịch ra tệp, client (G2).

## 19. Kênh chat — relay (`s3relay_y`, `relay_channcfg.ini`) + kiểm phía game server `0x081E3710`, chi phí `0x080502A0` (`chatcost.ini`), `ForbitTalk 0x0810CB70`, `SetChatFlag 0x08111460` (M14 lát C1, đã kiểm từng dòng)

Mã 2003 để đặt tên: `Core/Src/KPlayerChat.h/.cpp` (`SendSystemMsg`, `SendSystemInfo`, `SendGlobalSystemInfo`), `Core/Src/CoreUseNameDef.h` (`CHAT_CHANNEL_NAME_ALL/SCREEN/SINGLE/TEAM/FACTION/TONG/SCREENSINGLE/SYSTEM`), `Headers/KProtocol.h` (`CHAT_CHANNELCHAT_CMD {packageID, filter, channelid, cost, sentlen}`, `CHAT_SOMEONECHAT_CMD`, `enum {tgtcls_team, tgtcls_fac, tgtcls_tong, tgtcls_scrn, tgtcls_bc}`), `Headers/KProtocolDef.h` (`c2s_playersendchat` 94, `chat_channelchat`, `chat_someonechat`).

**Kiến trúc JX2**: client gửi dòng chat cho **relay** (gateway `s3relay_y`); relay giữ bảng kênh (`gateway/s3relay/relay_channcfg.ini`: `[team] name=TEAM escSpec=T cost=0`, `[faction] escSpec=F cost=3`, `[tong] escSpec=O cost=0`, `[screen] name=NEARBY escSpec=S cost=0`, `[broadcast] name=CITY escSpec=B cost=2`, `[tongunion] escSpec=U cost=0`, `[system] name=GM defCost=0`; `relay_channel.ini`: `[WORLD] cost=4`) và gửi cho game server một bản sao bảng (≤ 3 mục 10 byte `{dword id, byte loại chi phí, byte chữ cái}` → `0x97ac2d4`, nhận ở `0x081E1F50`). Game server **chỉ kiểm và trả về relay** kèm lớp đích; relay phát cho người nhận (đội theo id, môn phái, bang, thế giới; kênh `S` gần = người xem quanh người nói).

| Hàm | Luật (đã đọc) | Zone |
|---|---|---|
| **`0x081E3710(server, msg, ?, playerIdx, &out)`** — dòng kênh từ relay: `msg+8` dword id kênh, `+0xc` byte loại chi phí client khai, `+0xd` byte dài câu, câu từ `+0xf` | `Player+0x38c ≠ 0` (**`ForbitTalk`**) → bỏ; id kênh > 500 000 → `0x0804F6F0` (phòng chat / bang: bảng `0x9789f20`, gói 0xa4); **dài > 0x95 = 149 byte → bỏ**; tìm id trong bảng relay (không có → không xử lý, trả 0); **byte loại chi phí phải bằng `+4` của mục** (`0x081E390A`); `0x081E3250` (qua bản đồ phiên relay → `0x081E2550` → **`0x080502A0`**) trả 0 → bỏ; gói ra `{0x21, word size, dword −1, tên (`0x081E4790(player, 0x20)`), +0x27 id kênh, +0x2b dài, +0x2c câu}` gửi `0x080504E0(server, lớp, đích, gói, size)`: chữ **`T`** → lớp 0 (`tgtcls_team`), đích `Player+0x5998` (không đội `+0x5994 == 0` / id −1 → **bỏ**); **`S`** → lớp 3 (`tgtcls_scrn`), đích = chỉ số player; **`B`** → lớp 4 (`tgtcls_bc`), đích 0; chữ khác → chỉ khi `out` đã đặt (không) → bỏ | `KSubWorld::chat(sid, text, channel, target)` (`KSubWorldChat.cpp`): `forbid_talk`, `kChatSentenceMax 0x95`, `CH_SYSTEM`/`CH_TONG` từ chối, `CH_TEAM` = `KTeam::people()` (không đội → bỏ trước khi trả phí), `CH_NEARBY` = `watchers`, `CH_WHISPER` = người cùng tên trong bản đồ + mình (2003 `chat_someonechat`), `CH_WORLD/CITY/FACTION` → `KChatBroadcast` → `KEvChat` → `KGameServer::send_chat` (mọi phiên của zone; môn phái so `KPlayerFaction::current`; một `ZonePacket` mỗi gateway) |
| **`0x080502A0(server, playerIdx, loại, ?)`** — chi phí | chỉ số hợp lệ, `Player+0x3f4` (npc) > 0; **`Player+0x394 & 1` (`SetChatFlag`) → 0**; `[0x830ca4c] ≠ 0` (không nơi ghi = 0) → nhánh GM: `0x080A8080(player, 0) ≤ 0` và loại ∈ {2, 4, 8} → gửi `[0x978a64c]` rồi 0; **loại 0 → 1**; **loại 1** → `CheckTimeCount` (chỉ loại 2/4 mới gọi script) rồi `Pay([0x8badf14])` (không nơi ghi = 0 → không trả) + log `C_Chat` → 1; **loại 2..4**: mục `chatcost.ini[loại]` (`KNpcSet 0x8BACAC0 + 0x1440 + loại·16`: `Level +0, Money +4, ManaPercent +8, StaminaPercent +0xc`, nạp `0x080A0C40`): `npc.level < Level` → 0; `Money > tiền túi +0x508c` → 0; `nội cần = ManaPercent · nội tối đa (+0x11ac) / 100 > nội (+0x11a8)` → 0; `CheckTimeCount(loại)` (`\script\global\chat_timecount_limit.lua`, `0x080ACF90`, kết quả ở `Player+0x78ec`) → 0 → 0; `Pay(Money)` (> 0, log `C_Chat`); `nội −= cần`; `ConsumeTimeCount(loại)` (`0x080ACF40`); 1; **loại > 4 → 0**. `StaminaPercent` nạp mà không trừ | `chat_pay(e, type)`: `chat_flag`; loại ≤ 1 miễn phí; `KChatCostTable` (`chat_cost.json`, `jxassets export-chat-cost`); tiền `cost_money` + `send_money` + log `script money` reason `C_Chat`; nội `cur.mana −=` + `send_player_attrib`; script `chat_timecount_limit.lua` **không có trong cây** → không gọi |
| **`chatcost.ini` thật** | `[0]` GM 0/0/0; `[1]` Money 10; `[2]` thành: Level 20, ManaPercent 20; `[3]` bang/môn phái: ManaPercent 10; `[4]` thế giới: Level 30, ManaPercent 80 | `client/assets/chat_cost.json` (Go `player.ParseChatCost`) |
| kênh → loại chi phí | `relay_channcfg.ini`/`relay_channel.ini`: NEARBY 0, TEAM 0, CITY 2, FACTION 3, TONG 0, WORLD 4; someone chat 0 | `chat_cost_type()` (`KPlayerChat.cpp`) |
| **`ForbitTalk(n)` `0x0810CB70`** | `Player+0x38c = (n ≠ 0)` | `KPlayer::forbid_talk`, `l_ForbitTalk` |
| **`SetChatFlag(n)` `0x08111460`** | `n == 0` → `Player+0x394 &= ~1`, khác → `\|= 1` | `KPlayer::chat_flag`, `l_SetChatFlag` |
| `NW_ForbidChat(tên, phút)` `0x08128550` | cần `[0x830ca48] ≠ 0`; gửi relay gói `{0x10, 3, 0x10, 7, tên ≤ 0x1f, 0, phút}` — relay cấm | chưa (relay) |
| `IsDisabledChatWorld()` `0x081264B0` / `IsDisabledChatCity()` `0x0812EEE0` | giá trị nhiệm vụ 0x87 (`0x080A8C80`) bit 0x1 / bit 0x10 — script tự kiểm, game server không dùng | chưa (cần bảng giá trị nhiệm vụ, M13) |
| `RestoreMana()` `0x08112430` / `RestoreLife()` `0x08112480` | nội `+0x11a0 = max(+0x1a1c, +0x1a20)` / máu `+0x118c = max(+0x1a14, +0x1a18)` | `l_RestoreMana` / `l_RestoreLife` (+ `send_player_attrib`) |
| `GetMana(k)` `0x08112270` / `GetLife(k)` `0x081124D0` | `k == 0` → hiện tại (`+0x11a0` / `+0x118c`); `k == 1 \|\| 2` → **gốc** `+0x15b4` / `+0x15ac`; khác → `lua_error` | `l_GetMana` / `l_GetLife` |
| `OnChannelChat(loại kênh)` (`events.lua`, chuỗi `0x08269CD4` ở `0x081E3D5B`) | sự kiện script sau khi nói (`EventSys`) | chưa (eventsys) |

Client 2.0 (`CLIENT-2.0.md` §23): bảng kênh `\ui\ui3_1024\消息集合面板_左.ini` `[Channels] Channel0..14 = CH_NEARBY, CH_TEAM, CH_WORLD, CH_FACTION, CH_SYSTEM, CH_CITY, CH_TONG, CH_TONGUNION, CH_CHATROOM, …` (mỗi `[CH_*]`: `ShortName0/1`, `FormatName` = tên kênh relay, `TextColor`, `MenuText`, `SendMsgInterval`, `SendMsgNum`) → `pb::ChatChannel` cùng số thứ tự (0 NEARBY, 1 TEAM, 2 WORLD, 3 FACTION, 4 SYSTEM, 5 CITY, 6 TONG; 7 WHISPER thêm cho someone chat).

Zone: `KPlayerChat.h/.cpp`, `KSubWorldChat.cpp`, `KEvChat` (`KWorldCommand.h`) + `KGameServer::send_chat`; proto `ChatChannel`, `ChatReq.channel/target`, `ChatMsg.channel`; test `[chat]` 5 ca (`test_KPlayerChat.cpp`) + dòng thế giới qua server thật (`test_KGameServer.cpp`); e2e `AUTO_CHAT team=doi oi world=ca the gioi`. **Hạn chế**: đội chỉ tồn tại trong một bản đồ (`KTeamSet` của `KSubWorld`; `remove_player` rời đội) nên dòng đội không vượt bản đồ; `CH_CITY` = cả zone (relay thật gộp theo "thành" = máy chủ).

## 20. Hộp thoại npc — `KPlayer::DialogNpc 0x080B1300` (gói 0x6e), Lua `Say 0x08123C90` / `Talk 0x08116930` (gói 0x63 `PLAYER_SCRIPTACTION_SYNC`), trả lời gói 0x5f `0x080AC5D0`, `m_szTaskAnswerFun` `Player+0x5fa0` (M13 lát D1, đã kiểm từng dòng)

Mã 2003–2004 để đặt tên: `Core/Src/ScriptFuns.cpp` (`LuaSelectUI` = `Say`, `LuaTalkUI` = `Talk`, `MAX_ANSWERNUM 50`), `Core/Src/KPlayer.h/.cpp` (`DialogNpc`, `OnSelectFromUI`, `m_szTaskAnswerFun`, `m_nAvailableAnswerNum`, `m_bWaitingPlayerFeedBack`, `enum UIInfo {UI_SELECTDIALOG, UI_TRADEDIALOG, UI_TALKDIALOG, UI_NOTEINFO, UI_MSGINFO, UI_NEWSINFO, UI_PLAYMUSIC, UI_OPENTONGUI}`), `Headers/KProtocol.h` (`PLAYER_SCRIPTACTION_SYNC {byte, word len, byte m_nOperateType, m_bUIId, m_bOptionNum, m_bParam1, m_bParam2, int m_nParam, int m_nBufferLen, char content[300]}`, `PLAYER_SELECTUI_COMMAND {byte, int nSelectIndex}`), `Headers/KProtocolDef.h` (`c2s_dialognpc`, `c2s_playerselui`), `Core/Src/KNpc.h` (`m_DialogRadius` giữa `m_VisionRadius` và `m_ActiveRadius`).

| Hàm | Luật (đã đọc) | Zone |
|---|---|---|
| **`KPlayer::DialogNpc 0x080B1300(player, gói)`** — ô **0x6e** của bảng handler (`0x080DC4C0`), gói `{0x6e, dword id npc, dword mã đồng bộ}` | `+0x5700 == 3` hay `+0x5920` (đang giao dịch) → thôi; `0x080A79B0(player, 0x6e, mã)` (chống bot, không port); id ≥ 0; npc của mình `+0x3f4 > 0`, vùng ≥ 0; `idx = 0x080B12C0(player, id)` → `0x0807A1F0` (tìm npc theo id) > 0; `dist = 0x0809F370(idx, npc mình)`: khác subworld / vùng < 0 → −1; cùng vùng: `dx = Δô·[subworld+0x24] + (Δlẻ >> 10)` (pixel), `sqrt(dx²+dy²)`; **`npc+0x24 == 3`** (kind dialoger) hoặc **`0x0809EE50(g_NpcSet, npc mình, npc) == 1`** (quan hệ 1 — `KSubWorld::relation`); **`dist ≤ 2 · npc+0x1634`** (`m_DialogRadius`, `KNpc::Init 0x0807E02D` ghi **0x7c = 124**, không nơi nào ghi khác → 248 px); `0x08176160(Player+0x7d00) == 2` (chống nghiện: `+0x7d04` giây online ≥ `[0x82e6258]` = 0x4650 = 18 000 s, mốc 1 `[0x82e6254]` = 0x2a30 = 10 800 s) **và** `npc+0x18e4 & 1` → `0x080A8F30(player, [0x978a928])` = `L_PLAYER_TIRE_MSG11` rồi thôi (không port); **`npc+0x1538 == 0` → thôi** (không có script); `0x080AEBC0(player, 0xf)` sự kiện script 15 (eventsys, không port); `Player+8 = Player+0xc = idx`, `+0x52a4 = −1`; `0x080AD300(player, id script "\script\task\system\task_function.lua", "OnEventTalkNpc", 1, "d", idx)` ≠ 0 **và** `Player+0x78ec ≠ 0` → xong; không thì `0x080AD300(player, npc+0x1588 (id script của npc), "main", 0, "d", npc+0x158c)`; `Player+8 = −1` | `dialog_npc_request(sid, npc)` (`KSubWorldDialog.cpp`): đang giao dịch / không có npc / `npc_kind ≠ 3 && relation ≠ 1` / `dist > 2·dialog_radius` / `script` rỗng → từ chối (log `dialog refused` lý do); `KNpcAttrib::dialog_radius = 124`; `KNpc::script` = `KNpcPlacement.script` (Region_S.dat); `OnEventTalkNpc` gọi khi tệp nạp được (kết quả bỏ vì zone không đặt `+0x78ec`); `execute_script(script, "main", player, 0)` (`+0x158c` chưa xuất → 0) |
| **`0x080AD300` → `0x080ACBF0`** / **`0x080AC1A0(player, id script, tên hàm, nResults, "d", tham số)`** | lấy script theo id (`0x0805D080`), `npc+0x1588 = id`, đặt `PlayerIndex`/`PlayerId`/`SubWorld` (`0x08220930`), lưu đỉnh stack (`0x08220AB0`), `vtable+0x1c` = `KLuaScript::CallFunction(tên, nResults, "d", …)`; kết quả 1 khi có | `KScriptContext.script_path` (đường dẫn script đang chạy) — `Say`/`Talk` ghi vào `KPlayerDialog::script` để trả lời gọi về đúng script |
| **Lua `Say 0x08123C90`** (`LuaSelectUI`) — `Say(câu \| id, số, trả lời… \| {trả lời})` | `+0x78e8 = 0`; `+0x5f9c = lua_State` (`0x08220780`); `n ≠ 1 && type(2) == số` → số lựa chọn; `type(1)` số → **`m_bParam1 = 1`** (id chuỗi, gửi 4 byte) / chuỗi → câu (≤ 0x257) / khác → 0; `isstring(3)` → dạng vararg, `type(3) == bảng` → dạng bảng, không thì `số > 0` → 0; vararg: `số ≥ n−1 → n−2`; `min(số, 0x32 = 50)`; mỗi lựa chọn (bảng: `rawgeti(3, i+1)`; thiếu → chuỗi rỗng): `strstr(x, "/")` → phần sau `/` = **tên hàm** `strncpy(Player+0x5fa0 + i·0x80, …, 0x7f)`, không có `/` → `"main"`; **tổng câu + lựa chọn ≤ 0x384 byte** (`0x08123EC1`) — vượt → cắt số lựa chọn; `+0x78e4 = số`, `+0x78e8 = số ≠ 0`; gói: `+3 = 0`, `+4 = 0` (`UI_SELECTDIALOG`), `+5 = số`, `+6 = m_bParam1`, `+7 = 1`, `+9 = −1`, `+0xd = dài`, `+0x11` câu`\0`lựa chọn`\0`… → `0x080A8510` (`+7 == 1` → `[0] = 0x63`, `word[1] = dài + 0x10`, gửi `dài + 0x11`); câu dài hơn → chia gói `0x08124170` | `l_Say` (`ScriptFuns.cpp`) + `dialog_say`: `KPlayerDialog.answer_fun[50]`, `available_answers`, `waiting`; `G2C_SCRIPT_ACTION{operate 0, ui_id 0, text, text_id, interactive true, param −1, options}`; ngân sách 0x384 đếm trên byte gốc, chia gói không cần |
| **Lua `Talk 0x08116930`** (`LuaTalkUI`) — `Talk(số trang, "hàm gọi lại", trang…)` | `n ≤ 2` → 0; `+0x78e8 = 0`, `+0x5f9c`; `type(1)` phải số; `số ≥ n−1 → n−2`; `arg2` chuỗi = hàm; `type(3)` số → **trang là số** (`sprintf "%d"`, `+6 = 1`) / chuỗi; các trang nối **`"| |"`** (`0x08116B38`: `7c 20 7c`) ≤ 0x384 byte; hàm ≠ "" → `+0x5fa0 = hàm`, `+0x78e4 = 1`, `+0x78e8 = 1`, gói `+9 = 1` (client báo lại khi bấm trang cuối); không → 0/0/0; gói `+4 = 2` (`UI_TALKDIALOG`), `+5 = số trang` | `l_Talk` + `dialog_talk`: `options` = các trang, `param` = 1/0, `answer_fun[0]` = hàm |
| **trả lời gói 0x5f** — ô **0x5f** `0x080DC7A0` → **`0x080AC5D0(player, gói)`**, gói `{0x5f, int chỉ số, int loại, int, int}` 17 byte (client `KPlayer::OnSelectFromUI 0x005FC7D0`) | đang giao dịch → thôi; `+0x78e8 = 0`; **chỉ số < 0 → 0** (`0x080AC609`); `loại` (`+5`) `== 0`: `chỉ số < +0x78e4` và npc mình > 0 → `tên = Player+0x5fa0 + chỉ số·0x80`: `'#'` → **chạy phần sau như mã Lua** trong script `+0x5f9c` (`0x080AC6E6`: đặt `PlayerIndex/PlayerId/SubWorld`, `0x082222E0` LoadBuffer + `0x08222100` Execute); `\0` → thôi; khác → `strncpy` tên, **xoá cả 50 ô**, `+0x78e4 = 0`, `0x080AC1A0(player, +0x5f9c, tên, 0, "d", chỉ số)` = `tên(chỉ số)`; `loại == 1` → `0x081F68C0(0x97ac9e0, player, gói)` (giao diện chọn khác, không port) | `dialog_answer(sid, chỉ số, loại)`: `loại ≠ 0` bỏ; ngoài `available_answers` bỏ; `'#'` → `do_string`; khác → `call_number(tên, {chỉ số})` trong `KScriptCache::get(dialog.script)` với `KScriptContext` của người chơi |
| gói 0x89 `0x080DB680` → `0x080AC560` | trả lời giao diện chọn vật phẩm (`0x080AB980` kiểm `+0x52a4 == −3`, danh sách `+0x2b4/+0x314`, `0x080AC400` gọi `+0x60a0` với script npc / `0x080AC4B0` gọi `+0x5fa0`) — không thuộc hộp thoại này | chưa |
| **chuỗi script** (`Say`/`Talk`/`Msg2Player`) | script Việt hoá là **TCVN3** (một byte một chữ), script gốc/ghi chú là **GBK**; client 2.0 vẽ thẳng byte qua `\font\vn\gbk_fs*.fnt` | `KText.h/.cpp` `text::decode_mixed` (port `text.DecodeMixed` của jxassets: cắt ở `"` và `-`, mỗi đoạn TCVN3-lỏng hay GBK (cp936)) → UTF-8 cho client; `msg_to_player` cũng qua đó (+ `ChatMsg.channel = CH_SYSTEM`) |

Dữ liệu: bản đồ 1 có **65 npc kind 3 với script** (`Region_S.dat` → `map.json npcs[].script`); 52/56 tệp có trong `data/script/binserver` (thư mục tên GBK — `KLuaScript::os_path` đổi UTF-8 → cp936 nên `\script\西北南区\凤翔\…` tìm được), `data/script/server1` và `D:\ServerLinux\server1\script` chỉ có 1. `task_function.lua` nạp được nhưng thiếu `Include` (`treasure_head.lua`, `task_head.lua`, `tong_setting.lua`) và `FirstTask` (API nhiệm vụ, M13 D2) → `OnEventTalkNpc` lỗi mỗi lần gọi (log `call failed`), rồi `main` chạy như nhị phân.

Zone: `KPlayerDialog.h`, `KSubWorldDialog.cpp`, `KText.h/.cpp`; proto `C2G_NPC_DIALOG` 1121 (`NpcDialogReq`), `C2G_DIALOG_ANSWER` 1122 (`DialogAnswer`), `G2C_SCRIPT_ACTION` 2139 (`ScriptAction`), `EntityInfo.npc_kind` 31 (byte `+0x13` của gói 0x4c). Test `[dialog]` 5 ca 124 khẳng định (`test_KPlayerDialog.cpp`: quá xa / quái / không script / đang giao dịch từ chối; `Say` vararg, bảng, id chuỗi, không lựa chọn; trả lời đúng hàm với chỉ số, mã `#`, ngoài số bị bỏ, âm → 0; `Talk` trang + hàm; `[text]` TCVN3/GBK/hỗn hợp). e2e `AUTO_DIALOG npc=4294967977 name=Bành Tiểu đệ distance=247 ui=0 text_len=81 options=0 window=true answered=true` (`auto_dialog.png`: câu "Con gái của lão bà thật là đanh đá…" + "Kết thúc đối thoại").

## 21. Giá trị nhiệm vụ — `KPlayerTask` (`Player+0x809c`), `KPlayer::SetTaskValue 0x080A9190`, gói 0xa7 `0x080A8CC0` / 0xb5 `0x080A9550`, bảng `settings/task/player_task_def.txt` (`0x081C6E00`), Lua `GetTask/SetTask/GetTaskTemp/SetTaskTemp/SyncTaskValue/SyncTaskValueMore/GetBitTask/SetBitTask`, gói client 0xaa `0x080DB070`, lưu/nạp `0x080BF1C0/0x080C0050` (M13 lát D2, đã kiểm từng dòng)

Script nhớ gì về nhân vật: `data/script` có 1028 chỗ `GetTask(`, 833 `SetTask(`, 72 `GetTaskTemp(`, 81 `SetTaskTemp(`, 680 `AddNote(`, 12 `GetTaskMoney(`, 1 `SyncTaskValue(`.
Tên 2003: `Core/Src/KPlayerTask.h` (`nSave[MAX_TASK=256]`, `nClear[MAX_TEMP_TASK=2560]`, `GetSaveVal/SetSaveVal/GetClearVal/SetClearVal`, `Release`), `KPlayer::m_cTask`,
`KPlayerDBFuns.cpp` `LoadPlayerTaskList/SavePlayerTaskList`, `KTaskFuns.h` `TASKVALUE_SAVEWAYPOINT_BEGIN 201 (3)`, `TASKVALUE_SAVESTATION_BEGIN 210 (32)`, `TASKVALUE_TIMERTASK_* 220/221`,
`TASKVALUE_REPUTE 100`, `TASKVALUE_OFFER 101/102`. Bản Linux đảo cỡ: 256 ô tạm, giá trị lưu là `std::map` tới id 0x176f (6000 id).

| Hàm / cấu trúc | Đọc được | JX NEXT |
|---|---|---|
| **`KPlayerTask` tại `Player+0x809c`** | `+0`: `m_nTaskTemp[256]` — `GetClearVal 0x080CB5A0(task, id)` (`cmp 0xff / ja` → 0), `SetClearVal 0x080CB5C0` (ngoài 0..0xff bỏ). `+0x54c`: `std::map<int,int>` giá trị lưu (`_M_impl` `+0x54c`, đầu `+0x550`, gốc `+0x554`, nút: khoá `+0x10`, giá trị `+0x14`) — `GetSaveVal 0x080CB540` (`cmp 0x176f / ja` → 0; không có nút → 0), `SetSaveVal 0x080CB720` (ngoài 0..0x176f bỏ; **giá trị 0 → xoá nút** `0x080CB738 → 0x080CB7B8`; có nút → ghi `+0x14`; không → chèn `0x8056EC0`) | `KPlayerTask.h`: `temp[256]`, `saved` map không chứa 0, `get_save_val/set_save_val/get_temp/set_temp`; `KPlayer::task` |
| `GetBits 0x080CB5E0(task, id, đầu, số)` / `SetBits 0x080CB910(task, id, đầu, số, giá trị)` | điều kiện `id ≤ 0x176f`, `số > 0`, `đầu ≥ 0`, `đầu+số ≤ 32`; mặt nạ `(0xffffffff << đầu) & (0xffffffff >> (32−số−đầu))`; Get: không nút / nút 0 → 0, không thì `(v & mặt nạ) >> đầu` (dịch không dấu); Set: nút có → `(cũ & ~mặt nạ) \| ((giá trị << đầu) & mặt nạ)`, không nút → chỉ các bit; ghi qua `SetSaveVal` (**không báo client**); trả 1 / 0 khi điều kiện sai | `get_bits` (uint32) / `set_bits` / `bit_mask` |
| `ClearRange 0x080CBC40(task, đầu, số)` | `đầu ≤ 0x176f`; `cuối = min(đầu+số, 0x1770)`; `đầu < cuối` → xoá các nút `[đầu, cuối)` (chỉ `SavePlayerTaskList` gọi: 0xd2×10, 0xc9×3) | `clear_range` |
| `Serialize 0x080CB6A0(task, ra, tối đa)` | duyệt map theo khoá từ nút trái `+0x558`, **bỏ giá trị 0**, ghi `{int id, int giá trị}` tới `tối đa` cặp; trả số cặp | `serialize()` |
| **`KPlayer::SetTaskValue 0x080A9190(player, id, giá trị, bSync)`** | `cũ = GetSaveVal(Player+0x809c, id)`; **`cũ == giá trị` → về ngay** (`0x080A91C2`, không báo ai); `SetSaveVal`; tìm `id` trong map cờ `0x978bf7c` (so không dấu, nút `+0x10` khoá, `+0x14` byte cờ): có, `bit0` (SYNC_FLAG) và `bSync ≠ 0` → nhảy **`0x080A8CC0`** | `KSubWorld::task_set_value(e, id, giá trị, sync)` |
| `0x080A8CC0(player, id)` — gói **0xa7** 9 byte `{0xa7, int id, int giá trị}` | `id ≤ 0x176f` (không dấu, `0x080A8CCF`); giá trị = `GetSaveVal`; `0x080A8400(player, đệm, 9)`; trả 1 | `task_send_value` → `G2C_TASK_VALUE 2140 {id, value}` |
| `0x080A8CA0(player, id)` | `GetSaveVal(Player+0x809c, id)` — `0x080A8C80` đọc id 0x87 bit 0x400 (`DisabledTeam`) qua đây | `get_save_val` |
| **`SyncTaskValueMore 0x080A9550(player, từ, tới, chỉ khác 0)`** — gói **0xb5** 0x281 byte `{0xb5, {int id, int giá trị}[80]}` | `từ/tới` trong 0..0x176f và `từ ≤ tới` (sai → 0, `0x080A9562..0x080A957E`); đệm 0x281 byte xoá 0; duyệt `từ..tới`: cờ và giá trị 0 → bỏ (`0x080A9655`); ghi cặp; đủ 80 (`0x080A9632`) → gửi rồi xoá đệm; cuối còn cặp → gửi; **trả 1 kể cả không gửi gì**. Client 2.0 `0x00651350` chỉ đọc 79 cặp (`cmp edi, 0x4f`) và dừng ở id 0 | `task_sync_more` → `G2C_TASK_VALUES 2141` từng lô 80 (gói ta chỉ mang cặp thật) |
| **Bảng `\settings\task\player_task_def.txt`** — `0x081C70E0` nạp 4 bảng 0x40 byte: `0x978bf60` player, `0x978bfa0` `tong_task_def.txt`, `0x978bfe0` `workshop_task_def.txt`, `0x978c020` `member_task_def.txt`, mỗi bảng qua **`0x081C6E00(this, tệp)`** | `KTabFile` từ bộ đệm `0x97b25a0` (`0x081C7F10`), `GetHeight` (`vtable+0x1c`) phải > 2; **từ dòng 3** (dòng 1 tên cột, dòng 2 mô tả); cột **theo vị trí** qua `GetInteger` (`vtable+0x30`): 1 `TASK_ID_FIRST`, 2 `TASK_ID_LAST`, 4 `SYNC_FLAG`, 5 `CLIENT_FLAG` (mỗi cờ phải `== 1`); `first == 0` → bỏ dòng (`0x081C6EEC`); `last == 0 → first` (`0x081C6EF5`); SYNC → map khoảng `this+0x4` khoá `first` giá trị `{first, last}` (**insert-unique**, dòng đầu của cùng `first` thắng, `0x081C6F27..0x081C6FA4` + `0x081C72B0`); có SYNC hoặc CLIENT (`0x081C6E8D`) → mỗi `id ∈ [first..last]` (`0x081C6FD0..0x081C7042`) nút map cờ `this+0x1c` (= `0x978bf7c`, chèn `0x081C74B0`) rồi `+0x14 = bit0 SYNC \| bit1 CLIENT` (**ghi đè**, dòng sau thắng). Tệp máy chủ: 650 dòng, **68 dòng SYNC = 179 id**, CLIENT: 1276, 2881, 2882; tệp client 690 dòng (164 SYNC, 26 CLIENT) — zone theo tệp máy chủ | `KTaskDefTable::add` đúng luật (`sync_ranges()` map theo `first`, `flags(id)`, `synced/client_may_set`), `load()` từ `task_def.json` (`jxassets export-task-def`, `dev.py assets`); `KSubWorldConfig.task_def`; `main.cpp` `zone.task_def_file` → log `task def table loaded ids=179 ranges=68` |
| **Vào game** `0x080B9CF0` (máy trạng thái theo `[ebx]` 0..6, bảng `0x8256EC0`; nhánh `0x080B9D45`) | kiểm PK `0x080C30F0(Player+0x5a50)` (> 2 và `+0x507c/+0x5080` → `0x080C3740(…, 0, 1)`), gói **0xa4** 5 byte `{0xa4, int 0x080A8C80(player)}` (giá trị id 0x87, cờ tổ đội — M14 T); rồi duyệt map khoảng từ nút trái `0x978bf6c` tới đầu `0x978bf64`: mỗi khoảng đẩy mọi `id ∈ [first..last]` vào vector (`0x0805A180`); rồi **mỗi id một gói 0xa7** (`0x080A8CC0`, kể cả giá trị 0); rồi **`SyncTaskValueMore(player, 0x3e8=1000, 0x42e=1070, 1)`**; rồi `0x080A8A20(player)`; trả 1 | `task_login_sync(e)` trong `spawn_player` sau `send_skill_list` (0xa4 chưa; log `task values synced count=179`) |
| **Gói client 0xaa** — ô 0xaa `0x080DB070(?, chỉ số 1..0x4af, gói)` `{0xaa, int id (+1), int giá trị (+5)}` (bảng ô: `mov [edx + 0x18 + ô·8], hàm` tại `0x080DA640..`) | tìm `id` trong map cờ `0x978bf7c`; **bit1 (CLIENT_FLAG)** (`0x080DB0C4`) → `SetTaskValue(player, id, giá trị, bSync=0)` (`0x080DB0FB`); `id == 0xb41` (2881) → `0x080A9240(player, giá trị & 2)`: ghi `Player+0x7cfc`, đặt/xoá bit 2 của id 0xb41 (`SetTaskValue(…, 1)`) rồi `0x080A8DA0` (cờ 0) / `0x080A8D20` (cờ ≠ 0) — cờ tổ đội (`Player+0x460`, `0x830d300` bước 0x368), chưa port. Ô 0xa9 `0x080DAFF0` = bộ đăng ký `0x978bf20` (`0x081CE540(reg, [+1])` → đối tượng, gọi `vtable+8(obj, chỉ số, [+9]==1 ? 2 : [+9])`, các đối tượng tĩnh đăng ký ở `0x081BFE50..0x081C1731`); ô 0xa8 `0x080DB130` = "[ShutdownClient] Close connection" | `C2G_TASK_VALUE 1123 {id, value}` → `task_value_request(sid, id, value)` (chỉ CLIENT_FLAG; log `task value from client (refused)`) |
| **Lưu** `KPlayer::SavePlayerTaskList 0x080BF1C0(player, TRoleData*)` | `ClearRange(0xd2, 10)`, `ClearRange(0xc9, 3)`; danh sách trạm `m_PlayerStationList +0x2a0` (≤ 20 nút, `[nút+0x10]` ghi vào id `0xd2 + i/2` dịch `(i&1)·16` = `TASKVALUE_SAVESTATION_BEGIN 210`), điểm đường `m_PlayerWayPointList +0x294` (≤ 3, `[nút+0x10] ≠ 0` → id `0xc9 + n`); `0x080F8BB0(Player+0x10c, player)` (map `+0x10c` → 4 `SetTaskValue`), chống nghiện `0x08176690(Player+0x7d00)` (`\settings\player_limittime.ini`: `OnlineTiredTime 0x2a30`, `OnlineIllHealthTime 0x4650`, `TireMsgInterv1/2`, `TireExPointNum/Bit`…), `0x080F8E70(Player+0x120, player, [0x9777f00])` (10 bản ghi 0x18 byte × 5 giá trị từ id `0x64b` = 1611, bước 5); `[role+0x17a] = 0`; `Serialize(task, role + [role+0x17f] (dwTaskOffset), 0x1770)`; `[role+0x18b]` (dwItemOffset) = cuối; trả 1 | `save_task_values` → `RoleData.task_values` (`RoleTaskValue {id, value}`; các hệ trạm/điểm đường/`+0x10c`/chống nghiện/`+0x120` chưa) |
| **Nạp** `KPlayer::LoadPlayerTaskList 0x080C0050(player, role, &con trỏ, &đếm)` | xoá hai danh sách `+0x2a0/+0x294` (huỷ nút `0x08220510`); số cặp = `([role+0x18b] − [role+0x17f]) / 8`; đọc từng khối `đếm+10` cặp; mỗi cặp `id ≤ 0x176f` → `SetSaveVal` (`0x080C0187`, id lớn hơn bỏ); cập nhật con trỏ/đếm; trả 1 khi hết | `load_task_values` (`release()` rồi `set_save_val` từng cặp, id > 0x176f bỏ; log `task values loaded`) |
| Lua **`GetTask 0x08116890`** (2003 `{"GetTask", LuaGetTaskValue}`) | `0x08107860(L)` chỉ số ≤ 0 → `pushnil` (`0x08116910`); không thì `GetSaveVal(Player+0x809c, (int)arg1)` → `pushnumber` | `l_GetTask` |
| Lua **`SetTask 0x08116780`** | `SetTaskValue(player, (int)arg1, (int)arg2, 1)`; **`arg1 == 1`** → log `0x081E8DB0(L, 10, "TraceTaskValue", "Set\t%d\t%d\t%s\t%s", …)`; không trả gì | `l_SetTask` (+ log `trace task value`) |
| Lua **`GetTaskTemp 0x08123A20`** / **`SetTaskTemp 0x08123950`** (2003 `LuaGetTempTaskValue/LuaSetTempTaskValue`) | Get: id = **đối số cuối** (`gettop`); `> 0xff` (có dấu) → `nil`; chỉ số người chơi ≤ 0 → `nil`; `GetClearVal` (âm → 0). Set: id = đối số kề cuối, giá trị = đối số cuối; đọc toàn cục `PlayerIndex` > 0 → `SetClearVal` (0..0xff); không trả gì | `l_GetTaskTemp/l_SetTaskTemp` |
| Lua **`SyncTaskValue 0x0810E350`** / **`SyncTaskValueMore 0x0810E240`** | Sync: `gettop > 0` và chỉ số 1..0x4af → `0x080A8CC0(player, (int)arg1)`; không trả gì. More: `gettop ≤ 1` → không trả; chỉ số sai → trả 0; 2 đối số → `0x080A9550(p, a1, a2, 0)`; ≥ 3 → `(p, a1, a2, (int)a3)`; trả kết quả | `l_SyncTaskValue/l_SyncTaskValueMore` |
| Lua **`GetBitTask 0x081090A0`** / **`SetBitTask 0x08108F10`** | Get: chỉ số 1..0x4af và `gettop > 2` → `GetBits(task, a1, a2, a3)` đẩy số 64 bit không dấu; Set: `gettop > 3` → `SetBits(task, a1, a2, a3, a4)` → trả 1/0; `a1 == 1` → log `TraceTaskValue` | `l_GetBitTask/l_SetBitTask` |
| Hàm khác ghi/đọc giá trị (người gọi `0x080CB540`: 40 hàm; `0x080A9190`: 41 hàm) | `0x08117230/0x08117290` `GetRepute/AddRepute`, `0x08118620/0x081185B0` `PayRetScoreForGoldCoin/GetRetScoreForGoldCoin`, `0x081113C0/0x08117A10` `GetFightState/SetFightState`, `0x08114B10/0x08114B90` `Clear120SkillExpLimit/Set120SkillID`, `0x0810E3E0` `SetProtectTime`, `0x08108040/0x08107F40/0x08107E40/0x0812B6D0` `*ArenaCredits`, `0x081081A0/0x0812CFE0/0x081080B0` `*Respect`, `0x081143F0` `GetPartnerBagLevel`, `0x08111250` `SetPlayerLog`; `0x081D5840` (7 lần đọc), `0x0820DF90` (5), `0x0820F9C0` (14 đọc, 6 ghi), `0x081FF830` (4 ghi) — các hệ sau | từng lát sau |
| Client 2.0 (`CLIENT-2.0.md` §25) | 0xa7 `0x006512F0`, 0xb5 `0x00651350`, `KPlayer::SetTaskValue 0x00601ED0` (map `+0xa1a0`, gửi 0xa9 khi CLIENT_FLAG), bảng client `0x006CF940` | Godot `Game.task_values/task_value/set_task_value`, `task_value_changed`, `KPlayerTask.gd` |

Còn lại: **M13 D3** trạng thái nhiệm vụ (`FirstTask 0x08174E30`, `NextTask 0x08174D40`, `GetTaskStatus 0x08175090`, `SetTaskStatus 0x08174B70`, `GetEventTaskCount 0x08174430`,
`GetTaskEventID 0x08174230`; bit theo bảng `0x081E92B0/0x081E93C0` (đệ quy, `[nút] = vị trí bit`, `id = vị trí >> 5`, `bit = vị trí & 0x1f`, `[nút+4]` số bit, `[nút+8]` nút sau,
`[ctx+0xc]` mảng id) + `task_id.txt/task_type.txt/task_event.txt/missions.txt`), **`AddNote 0x08124DC0`** (680 chỗ; `PlayerIndex ≥ 0`, `gettop > 0`; đối số 1 số → `{int id, int tham số}` dài 8 với `+6 = 1`,
chuỗi → `strcpy` rồi `int tham số` ghi ngay sau chuỗi, dài `strlen+4`; gói 0x63 `+4 = 3` `UI_NOTEINFO`, `+3 = 0`, `+7 = 1` qua `0x080A8510`), `GetTaskMoney 0x08101270`, gói 0xa4, `0xb41 → +0x7cfc`,
trạm/điểm đường/chống nghiện/`+0x10c`/`+0x120` ghi vào giá trị, `SetProtectTime 0x0810E3E0`.

## 22. Hệ nhiệm vụ của script — thư viện `TASKSYS` (`IncludeLib`), bộ quản lý `0x9786620` (`Init 0x081725F0`: `task_id.txt/task_type.txt/task_event.txt` + condition/entity/award/talk mỗi loại, bộ nạp chung `0x08170990`), trạng thái 2 bit `0x0820E800/0x0820E720` (giá trị 2000+), giá trị tạm `0x0820DF90/0x0820E250` (2200..2299, băm `0x0821DF00`), `FirstTask/NextTask/StartTask/CloseTask/GetTmpValue/SetTmpValue/TaskXxx(Matrix)` (M13 lát D3, đã kiểm từng dòng)

Điểm vào của script: `script/task/system/task_function.lua` (`IncludeLib("TASKSYS")`; `OnEventTalkNpc(npc)` gọi `FirstTask` → `GetNpcName/NpcName2Replace/GetNpcPos/SubWorldName` → `TalkWithNpc` → `NextTask`;
`OnMenuTaskStart/Finish/Award(taskid)` → `TaskName`, `OnCheckTaskStart`, `StartTask`, `SetTaskStatus`, `CloseTask`), `task_main.lua` (2005, peres: `TaskConditionMatrix/TaskCondition`,
`TaskEntityMatrix/TaskEntity`, `GetTmpValue/SetTmpValue`, `GetTaskFinishConditionNum`…), `task_head.lua` (cột **đếm sau cột khoá**: `TaskID_Table.CanCancel=4, TaskText_Row=5`;
`Condition.TaskType_Row=1, TaskDesc_Row=2` + chuỗi TCVN3 `"Bắt đầu nhiệm vụ"`, `"đẳng cấp lớn"`…; `Entity.*_Row 1..16`; `Award.*_Row 1..18`; `TmpType.Collect/CollectGold/KillNpc/TalkNpc/ItemNpc/ItemRate/ItemSeed`;
`TASKSTAT_START 1, FINISH 2, AWARD 3`). Mã 2003 không có hệ này (VNG thêm 2005).

| Hàm / cấu trúc | Đọc được | JX NEXT |
|---|---|---|
| **Bộ quản lý `0x9786620`** (tĩnh: ctor `0x0816FFA0`, dtor `0x08171330`, đăng ký `0x081702C0`; `Init 0x081725F0`) | `+0x4` map tên → bản ghi (khoá `std::string` `+0x10`, **`+0x18` thứ tự dòng từ 0** = `0x08172820(mgr, tên)`, `+0x20` tên loại = `0x081726C0`); `+0x1c` map id → tên (`0x08170060/0x08170100(mgr, id)` trả `[nút+0x14]`); `+0x4c` map id sự kiện → vector tên (`0x081701A0` đếm, `0x08170200(mgr, sự kiện, i)` **i từ 0**, ngoài → 0); `+0x6c[3]` ba bảng chung từ cặp `0x82e6140`: `task_event.txt`, `task_id.txt`, `task_type.txt` (cột khoá 1) — `0x08170660(mgr, loại 0..2, khoá)` → ma trận; `+0x78 + chỉ số·12` danh sách đang duyệt của mỗi người chơi (`0x081702A0`, ≤ 0x4af); `+0x38b8` bảng loại (`0x08172030`: mỗi dòng `task_type.txt` = 4 bảng nạp bằng bộ nạp chung; `0x08172990` condition qua `0x08170630`, `0x08172E40` entity, `0x08172CB0` award, `0x08172B20` talk — mỗi hàm: tên → loại (`0x081726C0`) → bảng → ma trận của tên) | `KTaskManager` (`load` từ `task_tables.json`; `by_name/by_id/name_of/id_of/ordinal_of/event_of/event_task_count/event_task/id_matrix/event_matrix/type_of/condition/entity/award/talk`), `KSubWorldConfig.tasks`, `main.cpp` `zone.task_tables_file` → log `task tables loaded tasks=104 types=4 events=5` |
| **Bộ nạp chung `0x08170990(bảng, tệp, cột khoá)`** | `KTabFile` (`0x8228630` ctor, `0x82284E0` nạp); dòng ≤ 1 → không gì; **từ dòng 2** (dòng 1 tên cột; dòng mô tả của tệp cũng thành dòng dữ liệu); `GetString(dòng, cột khoá, "", đệm, 0xfff)` = khoá; các cột còn lại (theo độ rộng dòng 1) thành ô chuỗi thô GBK/TCVN3 (`new[]` mỗi dòng); nhóm theo khoá (map `+0x4`, nút `+0x14` → ma trận), thứ tự dòng giữ; ma trận `{vector dòng +0/+4/+8, số cột +0xc}`; **`0x08175BE0(ma trận, dòng, cột)`** = `[[đầu + dòng·4] + cột·4]`, `dòng ≥ số dòng` hay `cột ≥ số cột` → 0 | `KTaskTable::add/find`, `KTaskMatrix::cell` (0-based); Go `player.ParseTaskTable` (ô giữ **byte thô** dưới dạng rune Latin-1 trong JSON, C++ `latin1_bytes` trả lại byte — script so sánh bằng chính byte TCVN3 của nó) |
| `task_id.txt` (`0x08171390(mgr, tệp)`): `TaskID` int (cột 1), `TaskName` (2), `EventID` int (3), `TaskType` (4); `ebx = 2..số dòng`, thứ tự `= ebx − 2` | bản ghi chèn insert-unique vào map tên (`0x08173AB0`) và map id (`0x08173440`), vector theo sự kiện. Tệp máy chủ: 109 dòng = dòng mô tả + 107 nhiệm vụ ngẫu nhiên (`Ngẫu nhiên_Đối thoại/Thu thập/Sát quái…`); `task_type.txt` 5 loại → `settings/task/random/<talk|coll|kill|…>/{condition,entity,award,talk}.txt`; `task_event.txt` 5 sự kiện | `add_task` (thứ tự = vị trí trong danh sách xuất, dòng mô tả giữ như bản gốc) |
| **Trạng thái** `0x0820E800(iter, id, &word)` / `0x0820E720(iter, id, word)` | `tên = 0x08170100(mgr, id)` (không → 0); `thứ tự = 0x08172820(mgr, tên)`; `k = (thứ tự % 16)·2` (fpu, hướng 0); giá trị `0x7d0 + thứ tự/16` (`+15` khi âm); bit cao `1 << (31−k)` = 2, bit thấp `1 << (30−k)` = 1 → 0..3; ghi: `trạng thái & 2` đặt/xoá bit cao, `& 1` bit thấp, rồi `0x0820E1E0` | `task_status::value_id/hi_bit/lo_bit/status_of/status_value`; `KSubWorld::task_status/task_set_status` (log `task status changed`) |
| `0x0820E1E0(iter, player, id, giá trị)` | `GetSaveVal(id) ≠ giá trị` → `SetTaskValue(player, id, giá trị, 1)` **rồi luôn `0x080A8CC0`** (gói 0xa7 bất kể SYNC_FLAG; id đã SYNC → hai gói) | `task_set_value_synced` |
| **Giá trị tạm** — iterator `0x0820E170(iter, player, mgr)`: `+0x4` map id nhiệm vụ → map con (khoá băm → word), `+0x18` player, `+0x1c` mgr; dựng bằng **`0x0820DF90`**; huỷ `0x0820DD30` | `đếm = GetSaveVal(0x898)` (≤ 0 → xong); `vị trí = 0x899`; mỗi nhóm: `khoá = get(vị trí)`, `n = get(vị trí+1)`, `vị trí += 2`; **`vị trí + 2n + 2 > 0x8fb` → dừng, trả 0 (nhóm đã đọc giữ nguyên)**; `0x0820DE40(iter, khoá)` = `operator[]` (tạo nút, map con tạo khi cần `0x0820E130`); n cặp `(khoá băm, giá trị)` `0x080DF800` insert-unique; sang nhóm sau khi `vị trí ≤ 0x8f9` | `task_status::KTaskTemp::decode` |
| — ghi lại **`0x0820E250(iter, player)`** | duyệt nhóm theo khoá tăng: ghi `khoá`, `n = [map con+0x14]` (map con null → 0) rồi từng cặp theo khoá tăng, tất cả qua `0x0820E1E0`; rồi `0x898 = số nhóm`; rồi ghi 0 từ vị trí hiện tại tới `0x8fb` (0 = xoá nút, không gói) | `KTaskTemp::encode` (danh sách `(id, giá trị)` đúng thứ tự) → `task_write_temp` |
| — **băm khoá `0x0821DF00(chuỗi)`** | NULL → −1; `h = 0`; mỗi byte có dấu `c` ở vị trí `n` (từ 1): `x = c·n + h`; `x ≥ 0x8000000b` (không dấu) → `x −= 0x8000000b`; `h = x·(−17)`; trả `h ^ 0x12345678` (rỗng → `0x12345678`): `"TalkNpc" = 0xfa7e2d03`, `"KillNpc" = 0xc947adf0`, `"Collect" = 0xa26cb6dc`, `"ItemNpc" = 0x3fe53c11`, `"a" = 0xedcbaff7` | `task_status::key_hash`, Go `player.TaskKeyHash` (test hai bên) |
| `StartTask` → `0x0820E4E0(iter, id)` | nhóm đã có → 0; `0x0820DDF0` (2/nhóm + 2/cặp) **> 0x62 (98)** → 0; tạo nhóm rỗng, ghi lại → 1. (Đọc lại đòi dư 2 ô sau nhóm cuối `0x0820E00C`: cấu trúc đầy 98 ô đọc thiếu nhóm cuối — lỗi bản gốc, giữ nguyên) | `task_start` (log `task started`) |
| `CloseTask` → `0x0820E430(iter, id)` | không nhóm → 0; huỷ map con (`0x80567B0`), xoá nút, ghi lại → 1 | `task_close` (log `task closed`) |
| `SetTmpValue` → `0x0820E5C0(iter, id, khoá băm, word)` | `0x0820DE40` **tạo nhóm nếu chưa có** (không kiểm sức chứa); nút khoá có → `[+0x14] = giá trị`, không → chèn; ghi lại → 1 | `task_set_temp` |
| `GetTmpValue` → `0x0820DF10(iter, id, khoá băm, &giá trị)` | nhóm không có / map con null / khoá không có → 0 (nhóm tạm trong iterator không được ghi lại) | `task_temp` |
| `FirstTask 0x08174E30` / `NextTask 0x08174D40` | First: chỉ số 1..0x4af; `0x081702A0` danh sách; dựng iterator; xoá danh sách (`delete` từng nút); **`0x0820DCD0`** chép khoá các nhóm (id nhiệm vụ, theo khoá tăng) vào danh sách; con trỏ `+8` = nút đầu; có nút → `0x08170060(mgr, id)` → `pushstring` tên (không tên → `pushnil`), rỗng → nil; Next: con trỏ == đầu danh sách → xoá danh sách + nil; không thì tên nút kế (con trỏ tiến) | `task_first/task_next` với `KPlayer::task_list/task_cursor` |
| **Lua** — thư viện TASKSYS (`0x0814AD0F`, 30 hàm `0x08174230..0x08175B40`; số nguyên đẩy `pushnumber` — JX NEXT `pushinteger` để `tostring(102) = "102"` như Lua 4) | `TaskName(id)` (đúng 1 đối số, `0x08170060` → chuỗi/nil); `TaskNo(tên)` (`0x08170440` → số/nil); `GetTaskStatus(tên)` (chuỗi, chỉ số hợp lệ, id → `0x0820E800` → số/nil); `SetTaskStatus(tên, trạng thái)` (`0x82339B0` int → word → 1/0); `StartTask(tên)` (tên biết → gọi `0x0820E4E0` rồi **luôn đẩy 1** `0x081749B2`; không → 0); `CloseTask(tên)` (→ kết quả `0x0820E430`); `GetTmpValue(tên, khoá)` (2 chuỗi → số/nil); `SetTmpValue(tên, khoá, giá trị)` (→ 1/0); `TaskCondition/TaskEntity/TaskAward/TaskTalk/TaskId/TaskEvent(khoá, dòng, cột)` (3 đối số; `dòng ≤ 0` hay `cột ≤ 0` → nil; ma trận `0x08172990/0x08172E40/0x08172CB0/0x08172B20/0x08170800(loại 1)/0x081707D0(loại 0)`; ô `0x08175BE0(m, dòng−1, cột−1)` → chuỗi/nil; `TaskId(TaskNo(tên), 1, cột)` — khoá là id dạng chuỗi); `TaskXxxMatrix(khoá)` (→ số dòng, số cột; không có → không trả); `GetTaskEventID(tên)` (`0x08170830` → số/nil); `GetEventTaskCount(sự kiện)` (`0x081701A0`); `GetEventTask(sự kiện, i)` (`0x08170200`, i từ 0; script gọi `for i=1,n` → phần tử cuối nil như bản gốc); `SubWorldName(i)` (`i < [0x830ca50]` → tên subworld `[0x8fc81e0 + i·0x63fc8]`, không → nil); `SelectTaskStart/SelectTaskFinish/SelectTaskAward(id)` (`0x821DE70` lấy `task_function.lua`, `0x080AD300 CallFunction("OnMenuTaskStart/Finish/Award", 0, "d", id)`) | `l_TaskName … l_SelectTaskAward` (30 hàm, `task_text` viết số thành "102"); `SubWorldName` tạm trả id bản đồ (zone chưa có tên subworld); `task_select` → `execute_script(task_function.lua, fn, id)` |
| Chưa port (D4) | `NpcDialog 0x081744B0` (chạy `main` của script npc theo chỉ số npc, `0x080ABDF0`), `RemovePlayerEvent 0x0810C440` + sự kiện giết quái (`OnEventKillNpc`), `GetNpcName 0x08100040/GetNpcPos 0x081293F0/NpcName2Replace 0x081006D0` cho `OnEventTalkNpc` (hiện `FirstTask` nil → về 0 trước khi cần), tên subworld thật, `missions.txt` (`0x0805D6B5`, hệ chiến trường), `newtask/dailytask/tollgate/talk` (script + bảng riêng), cây bit `0x081E92B0/0x081E93C0` (hệ khác: gọi từ `0x081E9380/0x081EA2B0/0x081EA380/0x081EA440`) | |

## 23. Sự kiện trên người chơi và sự kiện giết quái — `KPlayerEvent` (`Player+0x8064`: thêm `0x081560C0`, xoá `0x08156050`, xoá hết `0x08155F60`), bảng `settings/npc/player/event_killnpc.txt` (`0x08156760` → `0x8bb2b54`), bắn khi quái chết `0x08083720 → 0x08155F80 → 0x08156330`, Lua `AddPlayerEvent/RemovePlayerEvent/RemoveAllPlayerEvent`, các hàm npc/người chơi `GetNpcName/GetNpcPos/NpcName2Replace/NpcDialog/GetLastDiagNpc/GetNpcSettingIdx/GetLevel/GetName/GetSex` (M13 lát D4, đã kiểm từng dòng)

`task_function.lua` gọi `OnEventKillNpc(player_index, taskno, mapid, npctemp, npccount, npcpower, npclevel)` — chuỗi này không có trong nhị phân: hàm được gọi là **cột 3 của
`event_killnpc.txt`** (428 dòng: `事件ID, 事件响应脚本, 事件响应函数, 任务ID, 只响应一次, 杀NPC总数, 地图ID, NPC模板ID, NPC能力, NPC级别, NPC名字, 说明`; ví dụ
`1  \script\task\newtask\branch\branch_killtimer.lua  killwolfone  199  1  49  90  7  1  -1`) với chữ ký `(nPlayerIdx, nTaskID, …)`.

| Hàm / cấu trúc | Đọc được | JX NEXT |
|---|---|---|
| **`KPlayerEvent` tại `Player+0x8064`** (vector `{word id, word đếm}`: đầu `+0x8068`, cuối `+0x806c`, sức chứa `+0x8070`) | `Add 0x081560C0(this, id, đếm)`: số phần tử **> 0x3f** → 0 (tối đa 64); id đã có → 1 (không đổi); không thì chèn `{id, đếm}` → 1. `Remove 0x08156050(this, id)`: tìm word id, `memmove` phần sau. `Clear 0x08155F60`: cuối = đầu, trả 1. Nạp từ bản ghi: khối phụ của `TRoleData` (`[+0x18f]` vị trí, `word [+0x17d]` số bản ghi 0x21 byte, thẻ byte 0..7 bảng `0x825706c`; thẻ sự kiện `0x080BEB60`: `word số` rồi cặp `{word đếm +4, word id +6}` → `Add(id, đếm)`) | `KPlayerEvent` (`add/remove/clear/find`, `kMax 63`), `KPlayer::events`; `RoleData.events {id, count}` (`load_player_events/save_player_events`) |
| **Bảng `\settings\npc\player\event_killnpc.txt`** — bộ nạp `0x08156760` (chuỗi tại `0x0825EED8`) | `KTabFile`; từ dòng 2; cột 1 int (mặc định −1) id; 2 chuỗi script → id script (`0x821DE70`); 3 chuỗi hàm; 4 int ô giá trị nhiệm vụ (**> 0x176f → bỏ dòng** `0x08156923`); 5..10 int mặc định −1: chỉ một lần, tổng, bản đồ, mẫu quái, sức mạnh (−1 bất kỳ / 0 người / 1 thường / 2 vàng / 3 boss), cấp; 11 tên; chèn map id → nút `{+0x14 bản đồ, +0x18 mẫu, +0x1c sức mạnh, +0x20 cấp, +0x2c script, +0x30 hàm, +0x34 ô giá trị, +0x38 một lần, +0x3c tổng, +0x40 cờ ghi log}` tại `0x8bb2b54` | `KKillEventTable` (`load` từ `kill_events.json` của `jxassets export-kill-events`, `add` bỏ ô > 0x176f, `find`), `KKillEventRow::matches`; `KSubWorldConfig.kill_events`; `main.cpp` `zone.kill_events_file` → log `kill events loaded rows=420` |
| **Khi quái chết** — `0x08083720` (cuối khung chết, cùng chỗ `RecoverBackData`) | `esi = [Npc[+0x1598] (kẻ đánh cuối) + 0x1908]` (chỉ số người chơi; 0 → không); quái không script (`+0x1538 == 0`, có script → nhánh `0x8083aad` gọi OnDeath rồi cũng bắn); **`0x08155F80(Player+0x8064, bản đồ = [subworld+0xc], mẫu = npc+0x1530, sức mạnh = 0x08079750(npc), cấp = npc+0x20)`** | `KSubWorld::fire_kill_events(dead)` trong `do_revive` (kẻ đánh cuối `last_damage_id` → `owner_of` → người chơi; `npc_power`: người 0 / `boss_flag` 3 / `gold.gold_kind() ≥ 1` 2 / khác 1) |
| `0x08155F80(events, bản đồ, mẫu, sức mạnh, cấp)` | duyệt cặp theo thứ tự (`[edi + i·4]`); mỗi id: tìm định nghĩa trong `0x8bb2b58` (không có → cặp sau); **`0x08156330(0x8bb2b54, [this+0xc0] chỉ số người chơi, id, bản đồ, mẫu, sức mạnh, cấp)`** trả ≠ 0 → **dừng** (chỉ một sự kiện đếm mỗi lần giết) | vòng lặp trong `fire_kill_events` |
| **`0x08156330`** | định nghĩa theo id; mỗi bộ lọc `bản đồ/mẫu/sức mạnh/cấp`: định nghĩa −1 hoặc tham số −1 hoặc bằng → qua, không → 0; script `0x0805D080(id)` không có → 0; `đếm = word đếm của cặp + 1` (`0x08156530`); `+0x40 ≠ 0` → ghi log `sprintf`/`0x81C9220`; **`tổng ≤ đếm`** (`0x0815645D`) → `0x080AD300(script, hàm, 0, "dddddddd", chỉ số người chơi, ô giá trị `+0x34`, id sự kiện, bản đồ, mẫu, đếm, sức mạnh, cấp)`; rồi `+0x38` một lần → `Remove` (`0x081566AB`), không thì đếm = 0 (`0x08156659`); ghi word đếm vào cặp (`0x08156680`); **`ô giá trị ≤ 0x176f` → `SetTaskValue(player, ô, đếm, 1)` (`0x080A93D0`) rồi luôn `0x080A8CC0`** (gói 0xa7); trả 1 | `fire_kill_events`: `execute_script_args(script, hàm, …8 số)`, `only_once` → `remove`, không → đếm 0; `task_set_value(…, true)` + `task_send_value` (log `kill counted`, `kill event fired`) |
| Lua `AddPlayerEvent(id) 0x0810C510` / `RemovePlayerEvent(id) 0x0810C440` / `RemoveAllPlayerEvent() 0x0810C3E0` | ≥ 1 đối số; chỉ số 1..0x4af (sai → 0); `Add(id word, 0)` / `Remove(id word)` / `Clear` → đẩy kết quả | `l_AddPlayerEvent/l_RemovePlayerEvent/l_RemoveAllPlayerEvent` → `player_event_add/remove`, `events.clear()` |
| Lua `GetNpcName(i) 0x08100040` | `0 < i < [0x830ca58]` → `pushstring(npc+0x1505)`, không → nil | `l_GetNpcName` (i = id thực thể; tên UTF-8 của zone — script so với byte TCVN3 của bảng chưa khớp) |
| Lua `GetNpcPos(i) 0x081293F0` | 1 đối số; `0x080EF710(subworld, vùng npc+0x1184, +0x1490/+0x148c/+0x1498/+0x149c)` → đẩy x, y rồi `npc+0x1180` (subworld) — 3 số; sai → 1 số | `l_GetNpcPos` → x/32, y/32 (ô, như `GetPos`; đơn vị gốc chưa kiểm), 0 |
| Lua `NpcName2Replace(tên) 0x081006D0` | chuỗi → `0x080A0420(tên)` (bảng thay tên bản Đài) → `pushstring` | `l_NpcName2Replace` trả nguyên (zone không có bảng) |
| Lua `NpcDialog() 0x081744B0` | chỉ số hợp lệ; `npc = Player+0xc` (0 < npc < số npc); `0x080ABDF0(player, npc+0x1588 script, "main", "d"?, npc+0x158c)` = chạy `main` script npc cho người chơi | `l_NpcDialog` → `execute_script(npc.script, "main", player, 0)` với `dialog.npc` |
| Lua `GetLastDiagNpc() 0x0810C5E0` | `Player+0xc` trong 1..số npc → đẩy chỉ số, không → 0 | `l_GetLastDiagNpc` (id thực thể, 0 khi không còn) |
| Lua `GetNpcSettingIdx(i) 0x080FDE50` / `GetLevel() 0x081111E0` / `GetName() 0x08111E70` / `GetSex() 0x08112020` | `npc+0x1530` mẫu (1 đối số, sai → đẩy 0); `npc+0x20` cấp; tên người chơi `pushstring`, không người chơi → nil; `npc+0x152c` giới | `l_GetNpcSettingIdx/l_GetLevel/l_GetName/l_GetSex` |
| Còn lại (rà `task_main.lua/task_head.lua`) | `TabFile_Load/…` (`class/ktabfile.lua` — `task/random/task_head.lua`, `tong_setting.lua` lỗi "attempt to call a nil value (global 'TabFile_Load')"), `Describe 0x081242A0`, `TaskTip 0x08122730`, `WriteLog 0x081237D0`, `GetAccount 0x0810F6A0`, `AddOwnExp`, vật phẩm (`AddItemEx/GetItemProp/GetItemStackCount/RemoveItemByIndex/SetItemMagicLevel/SyncItem/GetGiveItemUnit/GetGlodEqIndex/ITEM_GetItemRandSeed/GiveItemUI`), chuỗi (`AppendString/PushString/PopString/ReplaceString`), `PARTNER_*`, `AddNote 0x08124DC0`, tên subworld thật | lát sau |

## 24. Thư viện `TabFile_*` của script — `KTabFile` (2003 `Engine/Src/KTabFile.cpp`: `CreateTabOffset/GetValue/FindColumn`), bộ đệm `0x978262c` (đọc) / `0x9782644` (ghi), `TabFile_Load 0x0814AEF0`, `UnLoad 0x0814B040`, `GetRowCount 0x0814A690`, `GetColCount 0x0814A5E0`, `GetCell 0x0814A740`, `Search 0x0814A960` (`0x08227C90/0x08227BE0`), `SetCell 0x0814A420`, `Save 0x0814A3A0` (M13 lát D5, đã kiểm từng dòng)

Script dùng qua `script/class/ktabfile.lua` (`KTabFile` = `inherit(LError, …)`, `IncludeLib("FILESYS")`): `load(filename, key)` → `TabFile_Load(filename, key) == 0` → lỗi; `getCell(col, row)` →
`TabFile_GetCell(key, row+1, col)` (col là số hay tên cột); `getRow()` → `TabFile_GetRowCount(key) − 1`; `release()` → `TabFile_UnLoad(key)`. Đếm trong `data/script`: `TabFile_Load` 214
(142 tệp, hầu hết đường dẫn động), `GetCell` 849, `GetRowCount` 140, `UnLoad` 50, `Search` 17, `GetColCount` 2. Đường dẫn là `\settings\...` / `/settings/...` / `settings/...` của thư mục máy chủ cũ.

| Hàm / cấu trúc | Đọc được | JX NEXT |
|---|---|---|
| **`KTabFile::Load` / `CreateTabOffset`** (2003) | đọc cả tệp; **độ rộng = số tab ở dòng 1 + 1**; **mỗi dòng là một hàng** (dòng trống cũng tính; dòng cuối không có xuống dòng vẫn tính; xuống dòng cuối không thêm hàng); CRLF hoặc LF; mỗi hàng tối đa độ rộng ô (thiếu → rỗng, thừa → bỏ); không cắt khoảng trắng | `KTabFile::parse` |
| `GetValue(hàng−1, cột−1, đệm, cỡ)` → `GetString(hàng, cột, mặc định, đệm, cỡ)` | ngoài bảng hoặc **ô rỗng → FALSE** (đệm = mặc định); chép tối đa `cỡ` byte | `KTabFile::get_string(hàng, cột, ra, tối đa)` (1-based) |
| `FindColumn(tên)` (2003, `vtable+0x14`) | duyệt dòng 1: `GetValue(0, i, đệm, strlen(tên))` rồi so bằng → **ô cắt theo độ dài tên** ("Lev" tìm thấy "Level") → i+1, không → −1 | `find_column` (giữ đúng quirk) |
| `0x08227BE0(bảng, cột, giá trị)` (JX2 FindRow theo cột) / `0x08227C90(bảng, tên cột, giá trị)` | từng hàng: `GetValue(hàng, cột, đệm, 0x80)` so bằng giá trị → hàng (1-based), không → −1; bản tên cột: `FindColumn` trước | `find_row(cột, giá trị)` / `find_row(tên cột, giá trị)` |
| **Bộ đệm** `0x978262c` (đọc) / `0x9782644` (ghi): `std::map<khoá, {đường dẫn, KTabFile*}>` — `0x0814D1A0(bộ đệm, tệp, khoá)` | tệp/khoá rỗng → 0; khoá đã có: cùng đường dẫn → 1, khác → 0 (`compare` `0x0814D2C8`); `new KTabFile` + `Load` (`vtable+8`) thất bại → 0; cất → 1. `0x0814B600(bộ đệm, khoá)` tra bảng (không → 0); `0x0814DD50`/`0x0814B4A0` gỡ | `KTabFileCache` **toàn tiến trình** `g_TabFiles()` (như `0x978262c`; Include của script gọi `TabFile_Load` khi chưa có world/người chơi trên ngữ cảnh) + `g_TabFilesLock()`; `set_roots("a;b")` từ `zone.settings_root` (dev.py: thư mục máy chủ cũ của `config/oldgame.local.json`); `resolve` đổi `\`→`/`, bỏ `/` đầu, thử từng thư mục gốc rồi bản chữ thường |
| Lua `TabFile_Load(tệp, khoá[, ghi])` | ≤ 1 đối số → không trả; 2 đối số → bộ đệm đọc; 3 (đối số 3 int) → bộ đệm ghi `0x0814CDE0`; đẩy 1/0 | `l_TabFile_Load` (một bộ đệm, cờ `writable`) |
| `TabFile_UnLoad(khoá)` / `TabFile_GetRowCount(khoá)` / `TabFile_GetColCount(khoá)` | gỡ → kết quả; `GetHeight` (`vtable+0x1c`) / `GetWidth` (`vtable+0x18`) — không có bảng → 0 | `l_TabFile_UnLoad/GetRowCount/GetColCount` |
| **`TabFile_GetCell(khoá, hàng, cột)`** | > 2 đối số; cột số (`fistp`) hay **chuỗi = tên cột**; `GetString(hàng, cột, "", đệm, 0x400)` (`vtable+0x20`) → `pushstring` (rỗng khi ngoài/ô rỗng); không có bảng ở bộ đệm đọc → thử bộ đệm ghi `0x0814B4A0`, không → `pushstring("")` (`0x0814A8A0`) | `l_TabFile_GetCell` |
| `TabFile_Search(khoá, cột, giá trị)` | > 2 đối số; cột chuỗi → `0x08227C90`, số → `0x08227BE0`; đẩy hàng / −1 | `l_TabFile_Search` |
| `TabFile_SetCell(khoá, hàng, cột, giá trị)` / `TabFile_Save(khoá)` | bộ đệm ghi: `0x08229690(bảng, hàng, cột, giá trị)` → 1/0; `0x08228710(bảng)` ghi tệp | `l_TabFile_SetCell` (trong bộ nhớ), `l_TabFile_Save` → **0 + cảnh báo** (zone không ghi lại dữ liệu máy chủ cũ) |
| Sửa kèm | `log::write` `j.dump()` ném `type_error.316` khi trường mang byte GBK/TCVN3 (script `TabFile_Load` với đường dẫn/khoá GBK) → job `parallel_for` chết giữa tick, `main` npc không chạy (e2e `AUTO_DIALOG ui=-1`) | `dump(-1, ' ', false, error_handler_t::replace)` (byte lỗi → U+FFFD; test `[log]`), tên nhiệm vụ/khoá trong log qua `text::decode_mixed` |
| Dữ liệu tham chiếu | `settings/task/random/talk/entity.txt` chỉ 15 cột (thiếu `DropRate`, `TalkNpcMap` lệch) → `OnTaskNpcTalk` (`task_main.lua:734`) nối `nil` — lỗi cũng xảy ra ở máy chủ gốc; `OnEventTalkNpc` thất bại, `main` npc vẫn chạy (kết quả bị bỏ, §20) | như gốc (log `call failed`) |

## 25. Các hàm script còn lại của `task_main.lua` — `Describe 0x081242A0`, `TaskTip 0x08122730`, `WriteLog 0x081237D0`, `GetAccount 0x0810F6A0`, `AddOwnExp 0x081126C0 → 0x080AFEA0`, `AddRepute 0x08117290` / `GetRepute 0x08117230`, bộ đệm chuỗi `PushString 0x0812FDA0` / `AppendString 0x0812FCD0` / `ReplaceString 0x0812EB20` / `PopString 0x080FFB00` (M13 lát D6, đã kiểm từng dòng)

Đếm trong `data/script`: `WriteLog` 1045, `Describe` 912, `GetAccount` 494, `AddOwnExp` 490, `AddRepute` 53, `GetRepute` 36, `PushString`/`PopString` 12, `AppendString` 11,
`ReplaceString` 6, `TaskTip` 1 (`task/random/task_head.lua:95`). Mã 2003 chỉ có `LuaModifyRepute` (AddRepute), `LuaGetRepute`, `LuaAddOwnExp` (→ `KPlayer::DirectAddExp`);
các hàm còn lại là của JX2. Cách dùng: `lib/basic.lua:54..59` (`PushString(tb[1])`, `AppendString(sep)`, … `PopString()`), `lib/string.lua:49` `ReplaceString(pattern, s)`,
`task/system/task_string.lua:72` (`KEY_TASKSAY`), `activitysys/answer.lua:58` `Describe(question, getn(options), options)`, `activitysys/config/1023/extend.lua:53`
`Describe(strTittle, 2, format("…/#pActivity:NhanMocDiemLiXi(%d)", …), "Đóng/OnCancel")`, `activitysys/activity.lua:59` `WriteLog(format(…))`, `battles/battlehead.lua:1333` `AddRepute(repute)`.

| Hàm / cấu trúc | Đọc được | JX NEXT |
|---|---|---|
| **`Describe(câu \| id, số, trả lời… \| {trả lời})` `0x081242A0`** | ≥ 2 đối số (`0x081242B7`), chỉ số người chơi ≥ 0; `+0x78e8 = 0`, `+0x5f9c = lua_State` (`0x08220780`); **`type(2) ≠ số` → chỉ in `"Describe(%s,%s) by %s\n"` (`0x08124377`; tên npc `0x836eae0 + idx·0x1a4c + 0x1505`) và không gửi**; `type(1)` số → id chuỗi (`+6 = 1`, gửi 4 byte, `0x08124780`) / chuỗi → câu nguyên (không cắt 0x257 như Say) / khác → thôi; `type(3)` bảng → dạng bảng (`pushnumber(i+1)` + `gettable(3)`), `isstring(3)` → vararg (`số ≥ n−1 → n−2`, `0x08124828`), không có → `số > 0` → thôi (`0x08124460`); `min(số, 0x32)` (`0x08124478`); mỗi trả lời: `strstr(x, "/")` (`0x0805CF90`, `0x826f7d8`) → phần sau = tên hàm `strncpy(Player+0x5fa0 + i·0x80, …, 0x7f)`, không có → `"main"`, thiếu → chuỗi rỗng (`0x081246F0`); **chữ dài hơn 0xc8 → cắt 0xc8** (`0x0822A0A0(s, 200, 1)` → `0x0822AB40(bảng mã 0x97b2b68, s, 200, 1)` cắt theo ký tự: byte > `[+5]` tính `[+4]` byte); **ngân sách chỉ đếm các trả lời, không đếm câu**: `dùng + 3 + dài > 0x1f4 → dừng, bỏ phần sau` (`0x081245BB` → `0x08124700`); viết `"| |"` (`0x7c 0x20 0x7c`, `0x081246D4`) trước trả lời khi `i > 0` hoặc câu là id (`0x08124616`/`0x0812462C`) và `dùng += dài + 3`, không thì `dùng += dài`; `+0x78e4 = số giữ`, `+0x78e8 = số ≠ 0` (`0x08124726`/`0x0812472C`); gói `+3 = 0`, `+4 = 0xc`, `+5 = số`, `+6 = id?`, `+7 = 1`, `+9 = −1`, `+0xd = 0` → `0x08124170(Player, gói, câu, dài câu, đệm trả lời, dùng)` (câu > 0x384 chia gói) | `l_Describe` + `KSubWorld::dialog_describe` (`ui_describe_dialog = 12`, `kDescribeContentMax 0x1f4`, `kDescribeAnswerMax 0xc8` — cắt theo byte), log `dialog describe` / `describe without count`; `G2C_SCRIPT_ACTION{ui 12, text, text_id, options, param −1, interactive}`; trả lời về qua gói 0x5f kind 0 như Say (`m_szTaskAnswerFun` dùng chung) |
| **`TaskTip(chữ)` `0x08122730`** | ≥ 1 đối số, chỉ số 1..0x4af (`0x0812275B`), `tostring(1)` ≠ 0; đệm 0x40 byte về 0 (`rep stosd`), byte đầu `0x10`, `strcat(đệm+1, chữ)` (`0x081227A9`, **không giới hạn** — chữ dài tràn ngăn xếp máy chủ cũ), `0x08227030(đích, đệm, 0x40)` (= chép ≤ 0x3f byte + NUL), gói `{0xb6, đích[0x40]}` 0x41 byte qua `0x080A8400(Player, gói, 0x41)` (`+0x3f8` kết nối → `[0x82e8ca0]->vtable[1]`); **trả 1 mà không đẩy gì** (`0x081227F2`: Lua 4 trả lại giá trị trên đỉnh) | `l_TaskTip` → `KSubWorld::task_tip` (`kTaskTipMax 0x3e`), `G2C_TASK_TIP{text}` (2142), không trả gì |
| **`WriteLog(chữ)` `0x081237D0`** | ≥ 1 đối số, `tostring(1)` ≠ 0; `0x0821D790(0x977ff60, chữ)`: `0x0821D3A0` in `"%04d-%02d-%02d %02d:%02d:%02d\t"` (`0x826ca14`, `localtime`) rồi `0x0821D6F0` (`fwrite` qua `0x0821CF70`, `+0x4` = `FILE*`, `+0xc` = 0); rồi `0x0821D6F0(…, "\r\n")` (`0x826cca1`); tệp mở lúc khởi động `0x0805DF29`: `0x0821D160(0x977ff60, "Logs/KSG_ScriptLog", "txt", 1)`; cùng đối tượng nhận `"GetFeeType Error-…"` (`0x080A90D4`) | `l_WriteLog` → `log::info("zone.script", "script log", {text, script})` (chữ qua `decode_mixed`) |
| **`GetAccount()` `0x0810F6A0`** | chỉ số > 0 → `strcpy(đệm 0xd0, Player+0x264)` (tên tài khoản đăng nhập), không → `""`; `pushstring` | `l_GetAccount` ← `KPlayer::account` ← `KCmdSpawnPlayer.account` ← `SessionOpen.account = 4` (gateway `acc.Name`) |
| **`AddOwnExp(exp)` `0x081126C0`** (2003 `LuaAddOwnExp` → `DirectAddExp`) | ≥ 1 đối số, chỉ số > 0 (`0x081126E7`); `fistp` int64 cắt (`0x08112709`); **dword cao âm → thôi** (`0x08112717`); `0x080AFEA0(Player, int64)` | `l_AddOwnExp` → `KSubWorld::give_player_exp_direct` → `KPlayer::add_exp_direct` + `player_exp_changed` (log, lên cấp, đồng bộ thuộc tính, bị động mở ở cấp mới) |
| **`KPlayer 0x080AFEA0(this, int64 exp)`** — lõi cộng kinh nghiệm (cũng là đuôi của `AddExp 0x080B00C0` sau `CalcExp`/thưởng) | `exp > 0 && cấp (`npc+0x20`) > 0xc7` → thôi (`0x080AFEBE..0x080AFED6`); `cur = +0x595c`, `need = +0x5964` (int64); `cur ≥ 0`: `exp > need − cur → cur = need`, không → `cur += exp` (`0x080AFF22..0x080AFFFA`); `cur < 0` (mất do chết): `exp < −need − cur → cur = −need`, không → `cur += exp` (`0x080B0030..0x080B0079`); `cur < −need → cur = −need` (`0x080AFF6A`); `cur > 0 && cấp > 0xc7 → cur = 0` (`0x080AFF81..0x080AFFA9`); `cur < need` → gói `{0xc6, int64}` 9 byte (`0x080B0008`), không → `LevelUp(this, 1) 0x080AF800` (`0x080AFFBD`; phần dư mất) | `KPlayer::add_exp_direct(npc, gain, bảng, đồ)` (`add_exp` cũ gọi nó sau phần thưởng; đồng bộ thuộc tính thay gói 0xc6); test `[scriptfuns]` các kẹp |
| **`AddRepute(n)` `0x08117290`** (2003 `LuaModifyRepute`, `TASKVALUE_REPUTE = 100`) | ≥ 1 đối số, chỉ số ≥ 0 (`js` — chỉ số 0 vẫn qua ở máy chủ cũ); `n` = `fistp` cắt (`0x081172F4`); `v = 0x080CB540(Player+0x809c, 0x64) + n`; **`v < 0` → không đặt** (`0x08117315`); `SetTaskValue(Player, 0x64, v, 1)` (`0x0811732E` → gói 0xa7 nếu 100 có SYNC_FLAG); `sprintf` vào đệm cục bộ (`0x978a494` n ≥ 0 / `0x978a490` với −n) không dùng đến; không trả | `l_AddRepute` → `task_set_value(e, kTaskRepute, v, true)` + log `repute changed` |
| `GetRepute()` `0x08117230` | chỉ số < 0 → 0; `0x080CB540(…, 0x64)` → `pushnumber`; trả 1 | `l_GetRepute` |
| **Bộ đệm chuỗi** `0x9780d54` (con trỏ) / `0x9780d58` (dài) / `0x9780d5c` (sức chứa); bản sao `0x9780d60/0x9780d64/0x9780d68` | `PushString(s)` `0x0812FDA0`: ≥ 1 đối số; `tostring` 0 → `dài = 0` thôi (`0x0812FE70`); `dài = 0` (`0x0812FDDB`); `strlen = 0` → thôi; `n+1 > sức chứa → realloc` (`0x0812FE54`, thất bại → thôi); `memcpy` vào đầu, `dài = n`, NUL. `AppendString(s)` `0x0812FCD0`: như trên nhưng không đặt dài 0, `memcpy` sau `dài` (`0x0812FD4D`), `dài += n`. `ReplaceString(mẫu, s)` `0x0812EB20`: ≥ 2 đối số, hai chuỗi ≠ 0 (`0x0812EB60`/`0x0812EB6E`), `dài ≠ 0` (`0x0812EB7B`), `strlen(mẫu) ≠ 0` (`0x0812EB9B`); `dài sao = 0`; duyệt từ đầu: còn ≥ `strlen(mẫu)` và `strncmp` bằng (`0x0812EBE4`) → chép `s` vào bản sao (`realloc` `0x0812ED14`), nhảy qua mẫu; khác → chép 1 byte (`0x0812ECC8`); còn lại ngắn hơn mẫu → chép nguyên đuôi (`0x081394F0`); rồi **đổi hai bộ đệm** (`0x0812EC6C..0x0812ECAC`). `PopString()` `0x080FFB00`: `dài ≠ 0 → pushstring(đệm)`, không → `""` (`0x8250931`); **không xoá**; trả 1 | `script_string_buffer()` **`thread_local std::string`** (máy chủ cũ một luồng, một bộ đệm; zone chạy script trên nhiều luồng mô phỏng — script chỉ dùng trong một lần gọi: `lib/basic.lua` nối chuỗi), `l_PushString/l_AppendString/l_ReplaceString/l_PopString`; test `[scriptfuns]` (nối, thay hết, mẫu rỗng/dài hơn đệm, thay rỗng, không chồng `"aaa"→"ba"`) |

## 26. Hàm vật phẩm của script nhiệm vụ — `AddItemEx 0x08120470` (→ `Lua_NewItem 0x0811F230`), `GetItemProp 0x080FF260`, `SyncItem 0x08114EF0`, `RemoveItemByIndex 0x08114F80`, `GetItemStackCount 0x080FD250`, `GetGlodEqIndex 0x080FEF90`, `SetItemMagicLevel 0x080FD020`, `ITEM_GetItemRandSeed 0x081548E0`; đọc dở `GiveItemUI 0x0812BBA0` / `GetGiveItemUnit 0x08114E60` (M13 lát D7, đã kiểm từng dòng)

Đếm trong `data/script`: `GetItemProp` 346, `SyncItem` 285, `RemoveItemByIndex` 279, `GetGiveItemUnit` 240, `GiveItemUI` 158, `GetItemStackCount` 93, `AddItemEx` 58, `GetGlodEqIndex` 46,
`SetItemMagicLevel` 35, `ITEM_GetItemRandSeed` 16. "Chỉ số vật phẩm" của script là chỉ số vào bảng `Item[]` `0x830d300` (0x368 byte/ô, tối đa `[0x830ca5c]`); trong zone đó là `KItem::id`
của danh sách người chơi (mọi hàm script cùng dùng một không gian id nên script không phân biệt được).

| Hàm / cấu trúc | Đọc được | JX NEXT |
|---|---|---|
| **`Lua_NewItem 0x0811F230(L, chỉ số người chơi, thẻ)`** — 9 số đầu: `version, seed, quality, genre, detail, particular, level, series, luck`, rồi tới 6 cấp ma pháp (đối số 10..15; `top == 9` → cấp 0; `top > 17` → thêm hai word `+0x204/+0x206` `0x0811F9E0`) | `top ≤ 8` → 0; `version > 4` → 0 (`0x0811F3B9`); **`seed ≠ 0` (`0x0811F46C`) → `cũ = g_GetRandomSeed() 0x08226B00`, `g_SetRandomSeed(seed) 0x08226AC0`, sinh, rồi đặt lại `cũ`** (`0x0811F808..0x0811F8B7`); gọi `KItemSet::Add 0x0806E110(bộ 0x830d420, genre, quality, series, level, luck, detail, particular, &cấp[6], version, seed (0 khi không có), ngữ cảnh {người chơi, tệp}, cờ, 0, bNew 1)` (`0x0811F585`); chỉ số > 0 → đăng ký dấu vết `0x081E81F0/0x081E82D0(0x97ac340, item, người chơi, thẻ)` với `"Lua_NewItem File:[%s:%d]"` (`0x08234EB0` lấy tệp:dòng Lua) | `make_script_item(w, version, seed, quality, genre, detail, particular, level, series, luck, cấp)`: `seed ≠ 0 → gen.seed(seed)`; `KItemGenerator::equipment` ghi `rand_seed = seed()` (= `g_GetRandomSeed()` ở `0x0806B3A0`), `luck`, `magic_level` |
| **`AddItemEx([thẻ,] version, seed, quality, genre, detail, particular, level, series, luck [, m1..m6])` `0x08120470`** | `type(1) == chuỗi` → `tostring` rồi `lua_remove(L, 1)` (`0x081205B5`); `top ≤ 8` → in câu `[0x978a464]` (`0x0821CC00`) + đẩy 0; chỉ số người chơi > 0; `Lua_NewItem`; chỉ số > 0 → `KPlayer::AddItem 0x080B5180(player, idx, 1, 1, 0)` (túi, chồng) → được → **đẩy chỉ số** (`0x08120580`), không → `KItemSet::Remove 0x0806DB90(0x830d420, idx, 0, 0, 1)` giải phóng, đẩy 0. Script: `AddItemEx(4,15788,0, 0,3,0, 6,0,200, 6,6,6,6,6,6)` = phiên bản 4, seed 15788, quality 0, nhẫn (genre 0 detail 3) cấp 6 luck 200, sáu cấp 6 | `l_AddItemEx` (chỉ quality 0; `version ≤ 0` → bộ bảng hiện tại; bộ thiếu → mặc định; `give_item` → id hoặc 0), `l_AddItem` dùng chung `make_script_item` (seed 0) |
| **`GetItemProp(idx)` `0x080FF260`** | ≥ 1 đối số; `idx ≤ 0` hoặc `≥ [0x830ca5c]` → đẩy một số 0; không thì 6 số: `Item+0` genre, `+8` detail, `+0xc` particular, `+0x24` level, `+0x28` series, `+0x200` luck | `l_GetItemProp` (`KItem::luck` mới, +proto `luck = 24`) |
| `SyncItem(idx)` `0x08114EF0` | ≥ 1, chỉ số người chơi > 0, `idx > 0` → `KItemList::SyncItem 0x081FB9A0(Player+0x3fc, idx)`: tìm mục `+0x94 == idx` trong `list+0x4c6c`, gói `0x5f` `0x081F9430(&gói+1, item)` + `+0x30c` (word), `+0x308` (word), `+0x344/+0x345`, `+0x18/+0x1c`, `+0x350` (thời hạn khoá → `time()`…); không trả | `l_SyncItem` → `KSubWorld::sync_item` = `item_changed` (`G2C_ITEM_ADD` với vật như hiện tại) |
| **`RemoveItemByIndex(idx)` → 1/0** `0x08114F80` | ≥ 1, chỉ số > 0; `KItemList::Remove 0x082006B0(list, idx)` (§9: đang mặc → `0x081FFE90` tháo, phòng theo bảng `0x826B468`) → khác 0 → `KItemSet::Remove 0x0806DB90(bộ, idx, người chơi, lý do 0xd, 1)`; đẩy kết quả của `Remove` | `l_RemoveItemByIndex` → `take_item` (`G2C_ITEM_REMOVE`) |
| **`GetItemStackCount(idx)`** `0x080FD250` | `top ≠ 1` → −1; `idx` ngoài 1..max → −1; `Item+0x14` (chồng được) ≠ 0 **và** `0 < +0x308 ≤ max(+0x30c, 1)` → `+0x308`, khác → 1 | `l_GetItemStackCount` (`max_stack() > 0`, `count`) |
| `GetGlodEqIndex(idx)` `0x080FEF90` → `0x080FEEB0(L, 1)` | ≥ 1; idx trong 1..max; `Item+4 == 1` (hoàng kim, `Gen_GoldEquip` §9) → đẩy `Item+0x80 + 1` (dòng + 1), khác → 0 | `l_GetGlodEqIndex` (`ex_type == 1 → gen_param + 1`) |
| **`SetItemMagicLevel(idx, ô, cấp)`** `0x080FD020` | `top == 3` (`0x080FD03A`); `idx` không âm và ≤ max (`0x080FD100`, lệch một so với các hàm khác), `ô` 1..6 (`0x080FD10C`), `cấp` cắt; **`Item+0x1e4 + ô·4 = cấp`** (mảng sáu cấp ma pháp của lần sinh, sau seed `+0x1e0`); không đồng bộ, không trả. Script: `SetItemMagicLevel(nRandomItemIndex, 1, nRandomTaskID)` gắn id nhiệm vụ ngẫu nhiên vào vật | `l_SetItemMagicLevel` → `KItem::magic_level[ô−1]` (+proto `magic_level = 23`) |
| `ITEM_GetItemRandSeed(idx)` `0x081548E0` | `top ≠ 1` → −1; ngoài 1..max → −1; đẩy `Item+0x1e0` (không dấu, `fild qword`). Script: `nEquipRandSeed = ITEM_GetItemRandSeed(nItemIndex)` rồi `AddItemEx(version, seed, …)` sinh lại đúng vật | `l_ITEM_GetItemRandSeed` → `KItem::rand_seed` (+proto `rand_seed = 22`); test: hai `AddItemEx` cùng seed 12345 → cùng tiền tố |
| **Đọc dở — lát D8** `GiveItemUI(tiêu đề, nội dung \| id, hàmXácNhận, hàmHuỷ[, tham số])` `0x0812BBA0` | `top > 2`, chỉ số > 0; `+0x5f9c = lua_State`; gói ui **`0xb`** (`+4`, `[ebp-0x3a9]`), `+3 = 0`, `+6 = 1`, `+7 = 0`?; nội dung số → id 4 byte + tiêu đề (`dài + 5`), chuỗi → `strcpy` nội dung, tiêu đề sau NUL (`dài = l1 + l2 + 2`); **hàm xác nhận rỗng → `+0x78e4 = 0`, `+0x78e8 = 0`, thôi**; có → `+0x78e4 = 2`, `0x08227030(+0x5fa0, xác nhận, 0x80)`, `(+0x6020, huỷ, 0x80)`, `+0x78e8 = 1`; `top > 4` → tham số (`0x0812C0B0`); rồi **đặt lại khối giao dịch `Player+0x52a4..+0x5360` và 6 ô × 0x98 tại `+0x5364`** (cùng cấu trúc `KPlayerTrade`) — cửa sổ "đưa vật phẩm" của client dùng khung giao dịch | chưa port (cần cửa sổ client + gói đặt vật vào ô) |
| `GetGiveItemUnit(n)` `0x08114E60` | ≥ 1, chỉ số người chơi > 0, `n` 1..0x18 → đẩy `Player + 0x2ac + n·4 + 4` (24 ô chỉ số vật phẩm người chơi đã đặt vào cửa sổ đưa vật; ô trống 0) | chưa port (D8 cùng `GiveItemUI`) |
