// jx::core::EventQueue - facts leaving an owner (world -> network / persistence / scheduler).
// Same structure as the command queue, opposite direction; see CommandQueue.h for the rules.
#pragma once

#include "jx/core/CommandQueue.h"
