# JX NEXT build outputs (the legacy tree has its own rules in the repo root .gitignore)
build/
vcpkg_installed/
.vcpkg-bincache/
.cache/
CMakeUserPresets.json
*.log
*.crash.log
# runtime state of the dev servers
logs/
data/

# Python caches
__pycache__/
# machine-specific pointers to the reference game trees (docs/REFERENCES.md)
config/*.local.json

# thu nghiem 3D: tai san xuat tu bo tham khao (ban quyen game goc), sinh lai bang tools/scn3d/export_scene.py
client/assets3d/
